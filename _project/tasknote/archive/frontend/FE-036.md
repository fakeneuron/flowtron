---
title: tone down ✅ completed status-chip glyph
status: completed
tags: []
created: 2026-05-17
due:
related-tasks: [FE-006, FE-018, FE-023, FE-031, FE-033.2]
---

# FE-036 | tone down ✅ completed status-chip glyph

[← PLAN.md](../PLAN.md) · ✅ Completed

## 🎯 Goal

Replace the ✅ glyph in `STATUS_CHIP_LABEL.completed` with a subtler marker and run a coherence pass across the 5 chip glyphs (🌱 / ⚪ / 🟢 / ⏸ / ✅) so visual hierarchy matches semantic weight.

## ✅ Acceptance

- [ ] `STATUS_CHIP_LABEL.completed` in `viz/src/ui/constants.ts` swapped from `'✅'` to `'✓'` (U+2713)
- [ ] Other 4 chip glyphs (🌱 / ⚪ / 🟢 / ⏸) untouched (minimal-coherence-pass scope per user direction)
- [ ] SPEC.md §"Tasknote body shape" nav-header vocabulary untouched (viz-only scope per user direction); divergence is tolerated under SPEC's existing "chip is render-derived; markdown chip may lag" clause
- [ ] Visual confirmation in viz dev server (port 5174): completed-status rows render `✓` at chip caption size, inheriting the emerald `STATUS_BADGE` tint, without breaking row alignment / wrap
- [ ] `ShortcutsModal` Status legend (`viz/src/ui/ShortcutsModal.tsx:82`) also shows `✓` for Completed (it consumes the same `STATUS_CHIP_LABEL` map — no separate edit needed; verify rendering)
- [ ] Lint + type-check clean on changed files

## 🧩 Subtasks

- [ ] Edit `viz/src/ui/constants.ts:59` — change `completed: '✅'` to `completed: '✓'`
- [ ] Start viz dev server (port 5174) and ask user for visual confirmation on a Completed row and on the ShortcutsModal Status legend
- [ ] Run `npm --prefix viz run lint` and `npm --prefix viz run typecheck` (whichever scripts exist; see `viz/package.json`) on changed file
- [ ] Phase 4 closure: doc-drift sweep across AI-referenced docs; flip PLAN line; archive tasknote

## 🔗 Related

- [[FE-006]] — established `STATUS_LABEL` / `STATUS_BADGE` in `constants.ts`
- [[FE-018]] — viz audit; noted `STATUS_LABEL` ↔ `STATUS_CHIP_LABEL` divergence
- [[FE-023]] — viz-constants-consolidation; `STATUS_CHIP_LABEL` moved into `constants.ts`
- [[FE-031]] — viz-chip-system-trim; kept `STATUS_CHIP_LABEL` after removing other chip files
- [[FE-033.2]] — palette architecture; `STATUS_BADGE` (color) lives per-palette, `STATUS_CHIP_LABEL` (glyph) stays palette-agnostic in this task

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** PLAN brief is concrete: `STATUS_CHIP_LABEL.completed = '✅'` (`viz/src/ui/constants.ts:59`) reads as decorative compared to the other 4 monochrome / muted glyphs. Single-line constant change; user confirmed minimal scope (viz-only, swap-just-✅).

- [x] Read relevant source files
- [x] **Archive skim** — see Discovery Notes; load-bearing predecessors logged below
- [x] **Drift check** — see Discovery Notes; no drift, scope confirmed
- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

**Source files in scope:**

- `viz/src/ui/constants.ts:54-60` — `STATUS_CHIP_LABEL` map; the only literal-glyph site.
- `viz/src/ui/StatusChip.tsx:16` — single consumer (row-chip render).
- `viz/src/ui/ShortcutsModal.tsx:82` — second consumer (Status legend in `?` modal). No separate edit; picks up the new glyph automatically.

**Drift check (no drift):**

- `STATUS_CHIP_LABEL.completed = '✅'` still exists at `constants.ts:59`.
- The 5-glyph inventory cited in PLAN (🌱 / ⚪ / 🟢 / ⏸ / ✅) matches `constants.ts:55-59` exactly.

**Archive skim (load-bearing predecessors):**

- **[[FE-006]]** — established `STATUS_LABEL` / `STATUS_BADGE` in `constants.ts`. Did not introduce `STATUS_CHIP_LABEL` (that came later).
- **[[FE-018]]** — viz audit observed the divergence between text-form `STATUS_LABEL` and glyph-form `STATUS_CHIP_LABEL` and filed [[FE-023]] to co-locate them.
- **[[FE-023]]** — viz-constants-consolidation; moved `STATUS_CHIP_LABEL` from `StatusChip.tsx` into `constants.ts`. This is why the glyph map lives where it does today.
- **[[FE-031]]** — viz-chip-system-trim; removed several chip files but kept `STATUS_CHIP_LABEL` because row chips still need it. Doc-drift closure notes that SPEC.md §"Tasknote body shape" line 224 enumerates the same 5-glyph vocabulary as canonical for nav-header chips.
- **[[FE-033.2]]** — palette architecture. `STATUS_BADGE` colors are per-palette (`PaletteTokens`); `STATUS_CHIP_LABEL` glyphs are palette-agnostic. This task touches the palette-agnostic map only.

**Cross-surface coherence note (informational):**

- SPEC.md §"Tasknote body shape" (line 224) names `🟢 / ✅ / ⏸ / ⚪ / 🌱` as the canonical nav-header chip vocabulary. After this task, viz renders `✓` for completed while SPEC + 75 archived tasknote nav headers still say `✅`. User explicitly chose "viz row chips only" scope. SPEC's existing clause — *"the chip in the markdown body is hand-authored... [it is] not flipped at Phase 4 closure — visualizers compute the canonical chip from YAML `status:` at render time, so archived tasknotes may show a chip text that lags the YAML state"* — already allows this kind of viz↔markdown divergence. No SPEC edit required.

**Clarifying-questions outcome:** 3 questions surfaced via AskUserQuestion (scope, coherence-pass shape, glyph choice). User selected: viz-only scope · minimal pass (only swap ✅) · plain `✓` (U+2713).

**Why `✓` (U+2713):** monochrome (inherits chip text color from the emerald `STATUS_BADGE` tint), narrow (fits chip caption sizing without alignment churn), preserves "done" semantics, joins the other 4 muted glyphs in visual weight.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extending the existing `STATUS_CHIP_LABEL` map with a single value swap; no new shape. The map already mixes monochrome and color glyphs, so `'✓'` joins ⚪ / ⏸ as a third monochrome entry without introducing a new pattern.
- [x] Implemented the minimal solution
- [x] Updated/added tests for non-trivial behavior — none needed; no existing test asserts on the literal glyph value, and the swap is a 1-byte constant change with no behavior.

**Implementation Notes:**

Single edit at `viz/src/ui/constants.ts:59`: `completed: '✅'` → `completed: '✓'`. Both consumers (`StatusChip.tsx:16` row chip, `ShortcutsModal.tsx:82` Status legend) pick up the new glyph automatically through the shared map.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npm --prefix viz run test`: 8 files, 125/125 tests pass.
- [x] Ran lint/type-check on changed code — `npm --prefix viz run typecheck`: clean. (No `lint` script defined in `viz/package.json`.)
- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask) — user confirmed at dev server :5174.

**Testing Notes:**

Test surface: 125 tests across 8 files (`archiveCache`, `App.test.tsx`, `ProjectSelector.test.tsx`, `viewMode.test.ts`, others) — all green. No test broke on the glyph swap, which confirms no test was asserting on the literal `'✅'` (consistent with `STATUS_CHIP_LABEL` being a display-only constant). Visual confirmation: completed-row chip renders `✓` at chip caption size with emerald `STATUS_BADGE` tint, no alignment regression; ShortcutsModal Status legend updates in lockstep.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** across `_project/tasknote/README.md` §"AI-referenced docs":
  - `README.md` — **no change.** No chip-glyph or `STATUS_CHIP_LABEL` references.
  - `SPEC.md` — **no change.** Line 224 names `✅ Completed` as the **tasknote markdown nav-header** chip vocabulary; user explicitly scoped this task to viz row chips only, so the two surfaces (markdown nav-header `✅` ↔ viz row chip `✓`) intentionally diverge. SPEC's existing "chip is render-derived; markdown chip may lag" clause already covers this kind of divergence. SPEC does not currently document viz's row-chip glyph map separately, so no edit needed.
  - `docs/MIGRATION.md` — **no change.** No chip-glyph references.
  - `claude/CLAUDE-snippet.md` — **no change.** No chip-glyph references.
- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-05-17.` and tasknote moved to `_project/tasknote/archive/frontend/`
- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate)

**Final Summary:**

Replaced the `'✅'` glyph in `STATUS_CHIP_LABEL.completed` (`viz/src/ui/constants.ts:59`) with plain `'✓'` (U+2713). The sticker-emoji `✅` read as decorative against the other 4 muted glyphs (🌱 / ⚪ / 🟢 / ⏸); the plain checkmark is monochrome, narrow, inherits the emerald `STATUS_BADGE` tint, and preserves "done" semantics. Both the row chip (`StatusChip.tsx`) and the ShortcutsModal Status legend (`ShortcutsModal.tsx:82`) pick it up automatically through the shared map — no per-consumer edit needed. Scope kept minimal per user direction: viz only; SPEC.md §"Tasknote body shape" line 224 nav-header vocabulary and 75 archived tasknote nav headers stay on `✅` (SPEC's existing "markdown chip may lag" clause covers the divergence). 1-line diff; typecheck clean; 125/125 tests pass; visual confirmed at :5174.

**Archived:** 2026-05-17
