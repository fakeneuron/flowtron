---
title: asymmetric model-chip rendering
status: completed
tags: []
created: 2026-05-17
due:
related-tasks: [FE-031]
---

# FE-037 | asymmetric model-chip rendering

[← PLAN.md](../PLAN.md) · ✅ Completed

## 🎯 Goal

Make model-chip rendering asymmetric: Opus tasks show a subtle 🧠 icon chip; Sonnet tasks show nothing, reducing row noise for the majority-Sonnet default.

## ✅ Acceptance

- [ ] `ModelChip` returns `null` for `model !== 'opus'`; renders a bordered `🧠` icon chip (no text) for `[opus]`
- [ ] `showModel` in `TaskRowInner.tsx` updated to `rowChips.model && task.model === 'opus'` so `showOptionalChips` correctly excludes sonnet-only tasks from the optional-chip container
- [ ] Tests updated: 4 `'Opus'` text assertions swapped for `'🧠'` assertions in `App.test.tsx`
- [ ] `npm run typecheck` clean; `npm test` all green
- [ ] 👁️ Visual confirmation: Opus task rows show `🧠` chip; Sonnet rows show nothing for model; toggling "Model" in settings hides/reveals the 🧠 chip on Opus tasks

## 🧩 Subtasks

- [ ] Edit `viz/src/ui/ModelChip.tsx`: remove the `LABEL` map; add `if (model !== 'opus') return null;`; change chip content to `🧠`
- [ ] Edit `viz/src/ui/TaskRowInner.tsx:37`: `showModel = rowChips.model && task.model === 'opus'`
- [ ] Edit `viz/src/ui/App.test.tsx`: swap 4 `'Opus'` text assertions to `'🧠'`
- [ ] Run `npm --prefix viz run typecheck` and `npm --prefix viz run test`
- [ ] Start dev server (port 5174), ask for visual confirmation
- [ ] Phase 4 closure

## 🔗 Related

- [[FE-031]] — viz-chip-system-trim; removed ModelChip, then CORE-098.3 reintroduced it as an opt-in row chip behind `rowChips.model` toggle
- [[FE-036]] — immediate predecessor; same visual-noise reduction theme

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** PLAN description is concrete. `ModelChip.tsx` exists; renders both `[opus]` and `[sonnet]` as text pills. Implementation is asymmetric: return null for sonnet, render `🧠` chip for opus. Also need `showModel` guard update in `TaskRowInner.tsx` to keep `showOptionalChips` accurate, and 4 test assertions need updating.

- [x] Read relevant source files
- [x] **Archive skim** — see Discovery Notes
- [x] **Drift check** — see Discovery Notes; no drift, scope confirmed
- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

**Source files in scope:**

- `viz/src/ui/ModelChip.tsx` — renders `LABEL[model]` ("Opus" / "Sonnet") in a bordered pill. After change: `null` for sonnet, `🧠` for opus.
- `viz/src/ui/TaskRowInner.tsx:37` — `showModel = rowChips.model && !!task.model`. After change: `showModel = rowChips.model && task.model === 'opus'` so `showOptionalChips` stays accurate (no empty flex container for sonnet-only tasks).
- `viz/src/ui/App.test.tsx:344,352,387,393` — 4 assertions on `'Opus'` text. The test plan uses `- [ ] **CORE-100** [opus]` so CORE-100 is an opus task. Assertions swap to `'🧠'`.

**Archive skim:**

- **[[FE-031]]** — deleted `ModelChip.tsx` as part of chip-trim (strip all opt-in chips). Then **CORE-098.3** reintroduced it as an opt-in chip behind `rowChips.model` toggle in the settings modal. Current codebase reflects CORE-098.3's shape: ModelChip + RelatedChip + tags/due chips are all opt-in.
- No other tasknote touches ModelChip re: asymmetric behavior.

**Drift check (no drift):**

- `ModelChip.tsx` exists with `LABEL: Record<TaskModel, string> = { opus: 'Opus', sonnet: 'Sonnet' }` (confirmed current).
- `showModel = rowChips.model && !!task.model` is at `TaskRowInner.tsx:37` (confirmed).
- 4 `'Opus'` assertions in `App.test.tsx` at lines 344, 352, 387, 393 (confirmed).

**Assumptions (no clarifications needed):**

- Glyph: `🧠` as specified in PLAN.md.
- Chip styling for opus: retain the existing bordered pill shape (`border border-slate-300 ... font-mono text-xs text-slate-700`) — icon-only, no "Opus" text. Consistent with sibling chips; subtlety comes from asymmetry + no text.
- SettingsModal label stays "Model" — it controls model-chip visibility; semantically correct since only opus tasks have a model indicator.
- No `ShortcutsModal` update needed — model chip is not referenced in the shortcuts legend (confirmed by grep).

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — `ModelChip.tsx` already renders a `<span>` with chip styling; extended to an early-return null for sonnet + bare emoji span for opus. No new primitive needed. `TaskRowInner.tsx` uses the same `showX` guard pattern as tags/related/due for conditional rendering.
- [x] Implemented the minimal solution
- [x] Updated/added tests for non-trivial behavior — 4 `'Opus'` text assertions in `App.test.tsx` swapped for `'🧠'`; 2 `queryByText('Opus').not` assertions similarly updated.

**Implementation Notes:**

Three files changed:
- `viz/src/ui/ModelChip.tsx` — removed `LABEL` map, `DensityMode` import, `DENSITY_TOKENS` import, and `density` prop; added `if (model !== 'opus') return null;`; chip content changed from `LABEL[model]` to bare `🧠` in a `text-xs` span (no border, no padding per user direction, no density prop).
- `viz/src/ui/TaskRowInner.tsx` — updated `showModel` from `rowChips.model && !!task.model` to `rowChips.model && task.model === 'opus'`; removed `showModel` from `showOptionalChips` (middle column); removed `density={density}` from `<ModelChip>`; moved `<ModelChip>` render from middle-column optional-chips div into rightmost-column div (after `<StatusChip>`, before `extraRightSlot`) per user direction (🧠 rightmost, status chip to its left).
- `viz/src/ui/App.test.tsx` — 4 lines updated (`getByText('Opus')` → `getByText('🧠')`, `queryByText('Opus')` → `queryByText('🧠')`).

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npm --prefix viz run test`: 8 files, 125/125 pass.
- [x] Ran lint/type-check on changed code — `npm --prefix viz run typecheck`: clean.
- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask) — user confirmed at dev server :5174 (both bare-emoji and rightmost-position passes).

**Testing Notes:**

First two runs hit vitest worker timeout flakiness (186s / 154s duration, 7–14 failures) caused by the persistent dev server Monitor competing for resources. After stopping the Monitor, clean run: 125/125 in ~22s. Tests exercise `getByText('🧠')` and `queryByText('🧠')` against the rendered CORE-100 `[opus]` row; all pass.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** across `_project/tasknote/README.md` §"AI-referenced docs":
  - `README.md` — **no change.** No model-chip references.
  - `SPEC.md` — **no change.** No model-chip references.
  - `docs/MIGRATION.md` — **no change.** No model-chip references.
  - `claude/CLAUDE-snippet.md` — **no change.** No model-chip references.
- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-05-17.` and tasknote moved to `_project/tasknote/archive/frontend/`
- [x] Recap drafted

**Final Summary:**

Made model-chip rendering asymmetric: `ModelChip` now returns `null` for sonnet tasks and renders a bare `🧠` emoji (no border, no text) for opus tasks. Moved the chip to the rightmost position in the row (after the status chip) per user direction. `showModel` guard narrowed to `task.model === 'opus'` so sonnet-only rows don't hold an empty optional-chips slot. 4 test assertions updated from `'Opus'` text to `'🧠'`. `DensityMode`/`DENSITY_TOKENS` imports dropped from `ModelChip.tsx` as now unused. Typecheck clean; 125/125 tests pass; visual confirmed.

**Archived:** 2026-05-17
