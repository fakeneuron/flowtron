---
title: viz refactor
status: completed
priority: Medium
area: frontend
tags: []
created: 2026-05-04
due:
related-tasks: [FE-008, FE-009, FE-010, FE-011, FE-012, FE-014]
---

# FE-007 | viz refactor

[← PLAN.md](../PLAN.md) · ✅ Completed · 🔗 [[FE-008]] [[FE-009]] [[FE-010]] [[FE-011]] [[FE-012]] [[FE-014]]

## 🎯 Goal

Extract all 14 inline components from `viz/src/ui/App.tsx` into individual files under `viz/src/ui/`, leaving App.tsx with state and layout only — no behavior change.

## ✅ Acceptance

- [x] All named inline components extracted to their own files under `viz/src/ui/`
- [x] App.tsx contains only state management and top-level layout (no inline component definitions)
- [x] No behavior change: the viz renders identically before and after
- [x] No TypeScript errors; lint passes
- [x] Imports in App.tsx updated to reference the new module paths

## 🧩 Subtasks

- [x] Create `viz/src/ui/constants.ts` with shared constants (STATUS_BADGE, STATUS_LABEL)
- [x] Extract leaf components: Chevron, PhaseDots, SubtaskProgress, ModelChip, RelatedChip, BlockerChip, SubtaskRow
- [x] Extract WikilinkMarkdown (+ wikilinkifyMarkdown helper + WIKILINK_* constants)
- [x] Extract DetailSection (depends on WikilinkMarkdown)
- [x] Extract TaskDetail (depends on WikilinkMarkdown, DetailSection)
- [x] Extract TaskRowInner (depends on PhaseDots, SubtaskProgress, ModelChip, RelatedChip, BlockerChip; needs STATUS_BADGE/STATUS_LABEL/PRIORITY_BADGE)
- [x] Extract TaskRow (depends on TaskRowInner, TaskDetail)
- [x] Extract EpicRow (depends on Chevron, TaskRowInner, SubtaskRow, TaskDetail)
- [x] Extract PrioritySection (depends on Chevron, EpicRow, TaskRow; needs SECTION_TINT)
- [x] Update App.tsx: add imports, remove extracted definitions; keep SECTIONS, STATUS_FILTER_VALUES, HIGHLIGHT_MS, state, layout
- [x] Verify no TypeScript errors (`tsc --noEmit` or equivalent)
- [x] Lint pass on changed files
- [x] Request visual confirmation from user

## 🔗 Related

- [[FE-008]] — viz row-density redesign (dependent, builds on this)
- [[FE-009]] — viz dark mode (dependent, builds on this)
- [[FE-010]] — viz keyboard nav (dependent, builds on this)
- [[FE-011]] — viz auto-refresh on file change (dependent, builds on this)
- [[FE-012]] — viz inbound wikilink back-refs (dependent, builds on this)
- [[FE-014]] — viz UI component tests (dependent, builds on this)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** App.tsx is exactly 902 lines with all 14 named inline components present. The file structure and component list match the task description exactly. No drift.

- [x] Read relevant source files
- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code; flag any drift before re-interpreting the task
- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

All 14 components confirmed in App.tsx (lines 369–902): PrioritySection, EpicRow, TaskRow, TaskRowInner, SubtaskRow, WikilinkMarkdown, TaskDetail, DetailSection, Chevron, PhaseDots, SubtaskProgress, ModelChip, RelatedChip, BlockerChip.

Existing `viz/src/ui/` contains only `App.tsx` and `utils.ts` — clean slate for extraction.

**Constants handling (assumption, no clarification needed):**
- `STATUS_BADGE`, `STATUS_LABEL` — shared between App.tsx and TaskRowInner → extract to `viz/src/ui/constants.ts`
- `SECTION_TINT` → PrioritySection.tsx; `PRIORITY_BADGE` → TaskRowInner.tsx
- `WIKILINK_TEXT`, `WIKILINK_HREF_PREFIX`, `wikilinkifyMarkdown` → WikilinkMarkdown.tsx
- `SECTIONS`, `STATUS_FILTER_VALUES`, `HIGHLIGHT_MS` — App-only → stay in App.tsx

**Extraction order** (leaf → composite): Chevron, PhaseDots, SubtaskProgress, ModelChip, RelatedChip, BlockerChip, SubtaskRow, WikilinkMarkdown, DetailSection, TaskDetail, TaskRowInner, TaskRow, EpicRow, PrioritySection.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — looked at neighboring code for an existing pattern to extend; justified the new shape if none fits
- [x] Implemented the minimal solution
- [x] Updated/added tests for non-trivial behavior
- [x] Ran targeted tests on changed files

**Implementation Notes:**

Extracted all 14 components + shared constants to 15 new files under `viz/src/ui/`. Extraction order: leaf components first (Chevron, PhaseDots, SubtaskProgress, ModelChip, RelatedChip, BlockerChip, SubtaskRow, WikilinkMarkdown), then composites (DetailSection, TaskDetail, TaskRowInner, TaskRow, EpicRow, PrioritySection). Shared constants (STATUS_BADGE, STATUS_LABEL) went to `constants.ts`; component-only constants collocated with their component.

App.tsx reduced from 902 → 319 lines; contains only state, derived values, event handlers, and top-level layout.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code
- [x] Ran lint/type-check on changed code
- [x] (frontend) Asked the user for visual confirmation
- [x] Fixed all introduced issues

**Testing Notes:**

`tsc --noEmit`: 0 errors. `vitest run`: 53/53 passed. Visual confirmation from user: looks good.

## 🚀 Phase 4: Closure

- [x] Verified all prior phases complete
- [x] Updated docs/inventories affected by the change
- [x] Updated PLAN.md (status flipped to `Completed 2026-05-04`)
- [x] Updated nav header status icon to ✅ Completed
- [x] Moved this tasknote to `_project/tasknote/archive/frontend/`
- [x] Recapped changes with the user and got confirmation

**Final Summary:**

Extracted all 14 inline components from `viz/src/ui/App.tsx` into 15 new files. App.tsx shrunk from 902 → 319 lines. Zero TypeScript errors, 53/53 tests passing, visual parity confirmed. Unlocks FE-008 through FE-014.

**Archived:** 2026-05-04
