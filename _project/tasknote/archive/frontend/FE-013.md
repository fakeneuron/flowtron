---
title: viz polish bundle
status: completed
priority: Low
area: frontend
tags: []
created: 2026-05-04
due:
related-tasks: []
---

# FE-013 | viz polish bundle

[← PLAN.md](../PLAN.md) · ✅ Completed

## 🎯 Goal

Apply four small polish fixes to the viz UI: favicon, search autocomplete-off, relative due dates, and hiding empty status filter options.

## ✅ Acceptance

- [ ] `viz/index.html` has a favicon (no more blank browser tab icon)
- [ ] Search input has `autoComplete="off"` to suppress browser suggestions
- [ ] Due date chips show relative text ("in 6 days" / "today" / "overdue 3d") alongside or instead of ISO date
- [ ] Status filter hides options that have zero matching tasks

## 🧩 Subtasks

- [ ] Add favicon to `viz/index.html`
- [ ] Add `autoComplete="off"` to the search input element
- [ ] Implement relative date formatting for due chips
- [ ] Filter out zero-count status options in the status filter

## 🔗 Related

- (none)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Four independent small fixes, no design ambiguity, each scoped to a single file or component.

- [x] Read relevant source files
- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code; flag any drift before re-interpreting the task
- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- Favicon: `viz/index.html` has no `<link rel="icon">` and no `public/` dir. Will create `viz/public/favicon.svg` + add link tag.
- autoComplete: search input at `App.tsx:219` — missing attribute, trivial one-liner.
- Relative due dates: `TaskRowInner.tsx:91-95` renders raw `fm.due` ISO string. Will add `formatDue()` helper returning "in Nd" / "today" / "overdue Nd", with ISO as tooltip.
- Zero-count status filter: `App.tsx:264` iterates all `STATUS_FILTER_VALUES` unconditionally. Will derive `presentStatuses` set (tasks with ≥1 match per status) and filter the list before rendering.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — looked at neighboring code for an existing pattern to extend; justified the new shape if none fits
- [x] Implemented the minimal solution
- [x] Updated/added tests for non-trivial behavior
- [x] Ran targeted tests on changed files

**Implementation Notes:**

- `viz/public/favicon.svg` created (dark slate square, bold 'F'); `viz/index.html` gets `<link rel="icon" type="image/svg+xml" href="/favicon.svg" />`.
- `App.tsx:224` — added `autoComplete="off"` to search input.
- `TaskRowInner.tsx` — added `formatDue(iso)` helper (today/in Nd/overdue Nd); `dueLabel` memo replaces raw ISO. Chip colors: indigo (future), amber (today), red (overdue). ISO date surfaces as tooltip via `title`.
- `App.tsx` — added `presentStatuses` memo computing effective status for every task; status filter list filtered to `presentStatuses.has(s)` before render.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code
- [x] Ran lint/type-check on changed code
- [ ] (frontend) Asked the user for visual confirmation
- [x] Fixed all introduced issues

**Testing Notes:**

- `tsc --noEmit` passed clean. `vite build` succeeded (406 kB JS, no new warnings). Playwright browser unavailable — flagging for manual visual check.

## 🚀 Phase 4: Closure

- [x] Verified all prior phases complete
- [x] Updated docs/inventories affected by the change
- [x] Updated PLAN.md (status flipped to `Completed 2026-05-04`); FE-007 misfiling corrected (moved from Medium to Completed)
- [x] Updated nav header status icon to ✅ Completed
- [x] Moved this tasknote to `_project/tasknote/archive/frontend/`
- [x] Recapped changes with the user and got confirmation

**Final Summary:**

Four polish fixes shipped to viz: SVG favicon (`viz/public/favicon.svg` + HTML link tag), `autoComplete="off"` on search input, relative due-date chips with color coding (indigo/amber/red) and ISO tooltip, and `presentStatuses` memo that hides zero-count status filter options. Also corrected FE-007 misfiling in PLAN.md (was under Medium, now under Completed). TypeScript and build clean.

**Archived:** 2026-05-04
