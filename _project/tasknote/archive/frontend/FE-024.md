---
title: viz-app-decomposition
status: completed
tags: []
created: 2026-05-10
due:
related-tasks: [FE-018]
---

# FE-024 | viz-app-decomposition

[← PLAN.md](../../../PLAN.md) · ✅ Completed 2026-05-10 · 🔗 [[FE-018]]

## 🎯 Goal

Decompose `viz/App.tsx` by moving the localStorage active-project helpers into `projectStorage.ts` and replacing the four Set-toggle helpers with a `useToggleSet<T>()` hook, per [[FE-018]] audit.

## ✅ Acceptance

- [x] `viz/src/projectStorage.ts` exports `ACTIVE_PROJECT_KEY`, `readStoredProject`, `writeStoredProject`; App.tsx imports them instead of redefining
- [x] `viz/src/ui/useToggleSet.ts` exports a `useToggleSet<T>(initial?)` hook returning `[set, toggle, setSet]`
- [x] App.tsx's four `toggleX` helpers replaced by four `useToggleSet` call sites; `useKeyboardNav` and `handleSelectProject` reset/setter call sites continue to work
- [x] App.tsx LOC drops to ~440 (from 466) per FE-018 audit estimate — actual: **423 LOC, -43**
- [x] `npm test` passes (App.test.tsx integration tests untouched and green); `tsc` and lint clean on changed files

## 🧩 Subtasks

- [x] Create `viz/src/projectStorage.ts` exporting `ACTIVE_PROJECT_KEY`, `readStoredProject`, `writeStoredProject` (verbatim moves from App.tsx:37-53)
- [x] Update `viz/src/ui/App.tsx` to import the three symbols from `../projectStorage`; delete the inline definitions
- [x] Create `viz/src/ui/useToggleSet.ts` with hook signature `useToggleSet<T>(initial?: Set<T> | (() => Set<T>)): readonly [Set<T>, (item: T) => void, Dispatch<SetStateAction<Set<T>>>]`
- [x] Replace the four App.tsx state pairs (`tagFilter`/`toggleTag`, `statusFilter`/`toggleStatus`, `collapsedSections`/`toggleSection`, `expandedEpicIds`/`toggleEpic`) with `useToggleSet` calls; preserve initial value for `collapsedSections` (`new Set(['Completed'])`)
- [x] Verify `setTagFilter(new Set())` resets in `handleSelectProject` (App.tsx:285-286, 288) and `useKeyboardNav` (lines 61-62) still type-check and behave correctly
- [x] Verify prev-callback setters in `navigateToTask` (App.tsx:300-313) — `setCollapsedSections((prev) => …)` and `setExpandedEpicIds((prev) => …)` — still work via the exposed `setSet`
- [x] Run `npm test` in `viz/`; run `tsc --noEmit` and lint on changed files
- [x] Visual confirmation in dev (👁️ ask): tag/status/section/epic toggles and project-switch persistence all work end-to-end

## 🔗 Related

- [[FE-018]] — viz-code-token-audit (parent audit that surfaced this follow-up)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** FE-018 audit (lines 147-148, 173-174, 243, 300) explicitly framed this as a Medium-priority follow-up with concrete extraction targets and an estimated ~20 LOC + readability win. All four toggle helpers and all three localStorage helpers still exist and still match the audit's described shape. No interdependencies with other open follow-ups.

- [x] Read relevant source files
- [x] **Archive skim** — skim `_project/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope; log relevant findings in Discovery Notes before re-interpreting the task
- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code; flag any drift before re-interpreting the task
- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

**Source-file map (current state):**
- `viz/src/ui/App.tsx` (466 LOC; FE-018 audit cited 465). Two extraction surfaces:
  - **Lines 37-53** — `ACTIVE_PROJECT_KEY` + `readStoredProject` + `writeStoredProject`. Pure, no React deps. Callers: `useEffect` initial-load (line 110, `readStoredProject`) and active-project-change effect (line 124, `writeStoredProject`).
  - **Lines 253-279** — `toggleTag` / `toggleStatus` / `toggleSection` / `toggleEpic`. Identical `setX(prev => { const next = new Set(prev); next.has(v) ? next.delete(v) : next.add(v); return next; })` shape. Sibling state-pairs declared at lines 62-68.
- `viz/src/ui/useKeyboardNav.ts` — receives `setTagFilter`/`setStatusFilter`/`toggleEpic` as props (interface lines 4-21); calls `setTagFilter(new Set())` and `setStatusFilter(new Set())` on Esc (lines 61-62). The exposed `setSet` from `useToggleSet` must satisfy the existing `(s: Set<…>) => void` interface — `Dispatch<SetStateAction<Set<…>>>` is structurally assignable, no caller change needed.
- `viz/src/ui/App.test.tsx` (377 LOC) — uses literal `'flowtron-viz-active-project'` and `window.localStorage` directly (lines 258, 262, 279, 310). Integration-level coverage. Untouched by this refactor (key constant value is unchanged).

**Drift vs. FE-018 audit:**
- FE-018 cited App.tsx lines 28-45 (localStorage) and 245-271 (toggles). Current file: 37-53 and 253-279 — +9 line shift due to FE-021/22/23 imports (`STATUS_LABEL`, `STATUS_BADGE`, `PILL_ACTIVE`, `PILL_DEFAULT_SLATE`, etc.). Symbols, shapes, and call-sites are unchanged. Non-blocking.

**Archive skim (frontend/):**
- `grep -l "useToggleSet|toggleTag|projectStorage|ACTIVE_PROJECT_KEY|readStoredProject" archive/frontend/*.md` → only **FE-018.md**. The audit explicitly framed FE-024 with the exact symbol list, target file paths, and ~20 LOC estimate — no prior conflicting decisions.
- Broader grep for "App.tsx" hits FE-007 (split App.tsx into per-priority components — no overlap with toggle helpers or localStorage), FE-021/22/23 (recent extractions of constants and helpers — sets the precedent for sibling-extraction patterns this task continues).
- No prior tasknote has placed code at `viz/src/projectStorage.ts` or `viz/src/ui/useToggleSet.ts` (greenfield filenames).

**Design decisions confirmed (with user):**
1. `projectStorage.ts` lives at `viz/src/projectStorage.ts` (data-layer sibling of `parser.ts`/`tasknote.ts`/`workspace.ts`), not under `ui/`. localStorage is browser-API persistence, not React-specific.
2. `useToggleSet<T>()` returns the tuple `[set, toggle, setSet]` (not an object). `setSet` exposes the underlying `Dispatch<SetStateAction<Set<T>>>` so prev-callback updates (`navigateToTask`) and resets (`handleSelectProject`, `useKeyboardNav` Esc handler) continue to work.
3. Hook accepts `initial?: Set<T> | (() => Set<T>)` to preserve the `new Set(['Completed'])` initial state for `collapsedSections`.

**Out of scope (explicit):**
- App.test.tsx changes — tests use the literal key string and exercise integration behavior; rewriting them to import the constant is unrelated cleanup.
- A separate unit test for `useToggleSet` — sibling pattern is no per-hook test (cf. `useKeyboardNav.ts` has no test file); behavior is already covered transitively by App.test.tsx.
- Header/filter-rail extraction (`<Header>` ~90 LOC) — FE-018 listed it as a possibility but FE-024 scope is just localStorage helpers + toggle hook.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — looked at neighboring code for an existing pattern to extend; justified the new shape if none fits
- [x] Implemented the minimal solution
- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

**Pattern survey:**
- `viz/src/parser.ts`, `tasknote.ts`, `workspace.ts` all use module-level named exports with no React deps. `projectStorage.ts` mirrors the shape exactly.
- `viz/src/ui/useKeyboardNav.ts` is a named-export custom hook. `useToggleSet.ts` mirrors the named-export shape; differs in being generic and returning a tuple (vs. `useKeyboardNav`'s side-effect-only signature).

**Files created:**
- `viz/src/projectStorage.ts` (17 LOC) — exports `ACTIVE_PROJECT_KEY`, `readStoredProject`, `writeStoredProject` verbatim.
- `viz/src/ui/useToggleSet.ts` (15 LOC) — `useToggleSet<T>(initial?)` returning `[set, toggle, setSet] as const`. Toggle is wrapped in `useCallback([])` so the function identity is stable across renders (matches the previous inline-arrow behavior — those were redeclared each render but never reached a useEffect dep array, so no behavior change).

**Files changed:**
- `viz/src/ui/App.tsx` (466 → 423 LOC, **-43**, beats FE-018's ~440 estimate):
  - Added two imports (`useToggleSet`, `readStoredProject`/`writeStoredProject`).
  - Removed inline `ACTIVE_PROJECT_KEY` + `readStoredProject` + `writeStoredProject` (16 LOC).
  - Swapped 4× `useState<Set<…>>(…)` → `useToggleSet<…>(…)` (no LOC change at decl, but call sites now expose `toggleX` directly so the four toggle helpers below are gone).
  - Removed 4× inline `toggleX` helper definitions (27 LOC).

**Behavioral checks (verified by reading call sites):**
- `setTagFilter(new Set())` / `setStatusFilter(new Set())` resets in `handleSelectProject` (App.tsx:241-242) and `useKeyboardNav` Esc handler (lines 61-62) — `Dispatch<SetStateAction<Set<T>>>` is assignable to `(s: Set<T>) => void`. ✓
- `setExpandedEpicIds(new Set())` and `setCollapsedSections(new Set(['Completed']))` resets in `handleSelectProject` (App.tsx:244, 246). ✓
- `setCollapsedSections((prev) => …)` and `setExpandedEpicIds((prev) => …)` prev-callback updates in `navigateToTask` (App.tsx:257, 265). ✓
- `toggleEpic` passed to `useKeyboardNav` (App.tsx:294) — same `(id: string) => void` shape as before. ✓

**Tests:** No new unit test for `useToggleSet` (per Phase 1 scope decision — sibling pattern, integration-covered). App.test.tsx untouched.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code
- [x] Ran lint/type-check on changed code
- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:**

- `npm test` — **5 files, 73 tests passed**. App.test.tsx 8/8 (project-switching localStorage test green; filter intersection green; navigateToTask epic-expand green).
- `npm run build` — **tsc --noEmit clean + vite build clean** (406 modules transformed, 415kB bundle, no warnings beyond pre-existing gray-matter eval notice).
- No project lint config (no `.eslintrc*` or `eslint.config.*` at `viz/`); `tsc --noEmit` is the static-check gate per `package.json` scripts.
- Visual confirmation requested below the 📦 gate (👁️) — tag/status pill toggles, section collapse, epic expand, project-switch persistence.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `_project/tasknote/README.md` §"AI-referenced docs", state "no change" or the update
- [x] Closed — PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` (see §"`## Completed` archive convention") and tasknote moved to `_project/tasknote/archive/<area>/`
- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate)

**Doc-drift sweep results:**

| Doc | Verdict |
|---|---|
| `README.md` | no change — public-facing repo overview, no `viz/` internals |
| `SPEC.md` | no change — workflow contract, no React-layer references |
| `docs/MIGRATION.md` | no change — adoption procedures, no `viz/` internals |
| `claude/CLAUDE-snippet.md` | no change — adopter snippet, no `viz/` internals |

**Final Summary:**

Decomposed `viz/src/ui/App.tsx` per FE-018 audit: extracted three localStorage helpers into `viz/src/projectStorage.ts` (sibling of `parser.ts`/`tasknote.ts`/`workspace.ts`) and replaced four near-identical `Set`-toggle helpers with a generic `useToggleSet<T>()` custom hook at `viz/src/ui/useToggleSet.ts`. App.tsx 466→423 LOC (-43, beats audit's ~440 estimate); behavior unchanged; all 73 tests + tsc + vite build green.

**Archived:** 2026-05-10
