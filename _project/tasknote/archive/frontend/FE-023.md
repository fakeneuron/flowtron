---
title: viz-constants-consolidation
status: completed
tags: []
created: 2026-05-10
due:
related-tasks: [FE-018]
---

# FE-023 | viz-constants-consolidation

[← PLAN.md](../PLAN.md) · ✅ Completed · 🔗 [[FE-018]]

## 🎯 Goal

Consolidate viz/ status-label maps and active/default pill class strings into `constants.ts` so they live in one place per concern.

## ✅ Acceptance

- [x] `STATUS_CHIP_LABEL` lives in `viz/src/ui/constants.ts` alongside `STATUS_LABEL` (both maps preserved; same `Status` keys, divergent values per existing semantics)
- [x] `StatusChip.tsx` imports `STATUS_CHIP_LABEL` from `constants.ts`; no parallel definition remains in the chip file
- [x] `PILL_ACTIVE` + `PILL_DEFAULT_SLATE` exported from `constants.ts` and consumed by `App.tsx` (tag rail + status rail active state) and `ProjectSelector.tsx`
- [x] `viz` typecheck + lint clean; targeted tests still pass; visual confirmation that filter pills, status pills, project pills, and status chips all render identically pre/post

## 🧩 Subtasks

- [x] Move `STATUS_CHIP_LABEL` from `StatusChip.tsx` into `constants.ts` (export it; preserve key set + emoji values verbatim)
- [x] Update `StatusChip.tsx` to import `STATUS_CHIP_LABEL` from `./constants`
- [x] Add `PILL_ACTIVE` + `PILL_DEFAULT_SLATE` exports to `constants.ts`
- [x] Replace `PILL_ACTIVE` inline string at 3 sites: `App.tsx` tag rail (line 403), `App.tsx` status rail (line 425), `ProjectSelector.tsx` (line 25)
- [x] Replace `PILL_DEFAULT_SLATE` inline string at 2 sites: `App.tsx` tag rail (line 404), `ProjectSelector.tsx` (line 26) — status rail keeps its `STATUS_BADGE`-tinted default unchanged
- [x] Run `cd viz && npm test` (targeted) and `npm run typecheck` (no `lint` script exists)
- [x] Ask user for visual confirmation (filter pills, status pills, project pills, status chips render identically) — user: "looks good"

## 🔗 Related

- [[FE-018]] — viz/ code-token audit; filed this task as candidate C2 + C4 (`STATUS_LABEL` ↔ `STATUS_CHIP_LABEL` co-location + active/default pill pattern restated in 3 sites)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Brief is concrete and verbatim-derived from the [[FE-018]] audit candidate C2 + C4. Source files at HEAD show the cited duplications still hold: `STATUS_CHIP_LABEL` parallel to `STATUS_LABEL` at `StatusChip.tsx:5-11` (same key set, divergent emoji values), and the `PILL_ACTIVE` Tailwind string `'bg-slate-800 text-white dark:bg-slate-200 dark:text-slate-900'` appears verbatim at 3 sites + the slate default appears verbatim at 2 sites. Mechanical, low-risk; mirrors the [[FE-021]]/[[FE-022]] sibling shape from the same audit.

- [x] Read relevant source files
- [x] **Archive skim** — see Discovery Notes; load-bearing predecessors logged below
- [x] **Drift check** — see Discovery Notes; line numbers shifted ~2-8 lines vs FE-018's audit lines (FE-021/FE-022 commits since), patterns intact
- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

### Source file inventory at HEAD

- `viz/src/ui/constants.ts` (17 LOC) — `STATUS_LABEL` + `STATUS_BADGE`. Target home for the new exports.
- `viz/src/ui/StatusChip.tsx` (17 LOC) — local `STATUS_CHIP_LABEL` (lines 5-11), only consumer is `TaskDetail.tsx:39`.
- `viz/src/ui/App.tsx` (472 LOC) — `PILL_ACTIVE` at lines 403, 425; `PILL_DEFAULT_SLATE` at line 404. Already imports `STATUS_LABEL, STATUS_BADGE` from `./constants` (line 13).
- `viz/src/ui/ProjectSelector.tsx` (35 LOC) — `PILL_ACTIVE` at line 25; `PILL_DEFAULT_SLATE` at line 26. No existing import from `./constants`.

### Pattern map

- **`PILL_ACTIVE`** = `'bg-slate-800 text-white dark:bg-slate-200 dark:text-slate-900'` — 3 sites: `App.tsx:403` (tag rail), `App.tsx:425` (status rail), `ProjectSelector.tsx:25`. Verbatim identical.
- **`PILL_DEFAULT_SLATE`** = `'bg-slate-100 text-slate-700 hover:bg-slate-200 dark:bg-slate-800 dark:text-slate-300 dark:hover:bg-slate-700'` — 2 sites: `App.tsx:404` (tag rail), `ProjectSelector.tsx:26`. Verbatim identical.
- **Status rail default** (`App.tsx:426`) is `` `${STATUS_BADGE[s]} hover:opacity-80` `` — status-tinted, not slate. Out of scope for `PILL_DEFAULT_SLATE` extraction (FE-018's audit explicitly noted this asymmetry: "Status rail differs only in default classname"). PILL_ACTIVE still applies to status rail; PILL_DEFAULT_SLATE does not.
- **`STATUS_CHIP_LABEL`** vs **`STATUS_LABEL`** — same `TasknoteStatus` keys, divergent values (StatusChip prefixes 4/5 entries with status emojis; constants.ts uses no emojis except the lime starter sprout). Both maps are intentional — keep them distinct after co-location.

### Test impact

- 5 viz test files (`parser.test.ts`, `tasknote.test.ts`, `workspace.test.ts`, `App.test.tsx`, `ProjectSelector.test.tsx`).
- No test asserts on the literal `PILL_ACTIVE` / `PILL_DEFAULT_SLATE` class strings (verified via grep).
- No test imports `STATUS_CHIP_LABEL` (verified via grep). The `🟢 In progress` literal in `tasknote.test.ts:108` is inside a tasknote-header fixture, unrelated to `StatusChip`.
- Conclusion: pure constants extraction; no test updates expected. Run `App.test.tsx` + `ProjectSelector.test.tsx` to confirm.

### Archive skim — load-bearing predecessors

`ls archive/frontend/` enumerates 19 tasknotes. Grepping each for `constants.ts`, `StatusChip`, `STATUS_CHIP_LABEL`, `PILL_`, `ProjectSelector`, and `App.tsx` tag/status rails:

- **[[FE-006]]** — established `STATUS_LABEL` + `STATUS_BADGE` in `constants.ts`. Direct ancestor of this task. Did not introduce `STATUS_CHIP_LABEL`; that arrived later in `StatusChip.tsx`'s own creation.
- **[[FE-018]]** — the audit that filed this task. Candidate C2 + C4 in its Implementation Notes describe this exact dedup; its bundling decision was 4 individual Medium-priority tasks. ✓
- **[[FE-021]]** — extracted `rowOutlineClass` helper into `viz/src/ui/utils.ts` (sibling C5 from same audit). Establishes the **co-location pattern** for this audit family: helpers/constants belong with the maps they neighbour, not buried inside their primary consumer.
- **[[FE-022]]** — exported `isEpic`, `getSubtaskParentEpicId`, `WIKILINK_PATTERN` from `parser.ts` (sibling C3 from same audit). Same co-location lesson: the canonical home should expose what siblings need.

No file moves, hardlink notes, or regressions to flag. The [[FE-021]]/[[FE-022]] precedents validate exporting `STATUS_CHIP_LABEL` (rather than collapsing into `STATUS_LABEL`) — siblings of the same data with different shapes belong adjacent, not merged.

### Drift check

All paths and symbols cited in the PLAN.md description exist at HEAD:
- `STATUS_CHIP_LABEL` in `StatusChip.tsx:5-11` ✓ (FE-018 cited 5-11; unchanged)
- `STATUS_LABEL` in `constants.ts:3-9` ✓ (FE-018 cited 3-9; unchanged)
- `PILL_ACTIVE` string at `App.tsx:403, 425` (FE-018 cited 386-425 → drifted +17 / +0 lines from FE-021/FE-022 commits) and `ProjectSelector.tsx:25` (FE-018 cited 23-27; same span)
- `PILL_DEFAULT_SLATE` string at `App.tsx:404` and `ProjectSelector.tsx:26` ✓
- "tag/status rails and ProjectSelector" wording in PLAN.md is correct for `PILL_ACTIVE` (3 sites). For `PILL_DEFAULT_SLATE` it's actually 2 sites (tag rail + ProjectSelector); status rail uses a `STATUS_BADGE`-tinted default per FE-018's explicit note. Treating the wording as correct in spirit; will surface this asymmetry in the recap.

No drift material to scope.

### No clarifications needed

Assumptions:
1. `STATUS_CHIP_LABEL` is **exported** from `constants.ts` (parallel to `STATUS_LABEL`'s existing export shape) so `StatusChip.tsx` can import it. Both maps preserved as distinct exports (no value merge — divergent emoji values are intentional).
2. `PILL_ACTIVE` extraction targets all 3 sites (tag rail, status rail, ProjectSelector); `PILL_DEFAULT_SLATE` extraction targets only 2 sites (tag rail, ProjectSelector). Status rail's status-tinted default stays inline as `` `${STATUS_BADGE[s]} hover:opacity-80` `` per the asymmetry FE-018 flagged.
3. New constants live in `viz/src/ui/constants.ts` (the existing canonical home). No new file.
4. No prop/API surface change to `StatusChip` or `ProjectSelector` — internal refactor only.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — `STATUS_LABEL` / `STATUS_BADGE` already export from `constants.ts` as `Record<TasknoteStatus, string>` maps; `STATUS_CHIP_LABEL` mirrors that shape exactly. `PILL_*` are atomic strings — plain `export const` matches JS module convention. Existing canonical home; no new file shape.
- [x] Implemented the minimal solution
- [x] Updated/added tests for non-trivial behavior — n/a (mechanical constants extraction; no behavior change; existing tests cover the rendered output paths)

**Implementation Notes:**

- **`constants.ts`** (17 → 31 LOC, +14): added `STATUS_CHIP_LABEL` map (5 keys) between `STATUS_LABEL` and `STATUS_BADGE`; added `PILL_ACTIVE` + `PILL_DEFAULT_SLATE` atomic exports below the map block.
- **`StatusChip.tsx`** (17 → 10 LOC, −7): removed local `STATUS_CHIP_LABEL` const; added `STATUS_CHIP_LABEL` to existing import line.
- **`ProjectSelector.tsx`** (35 → 32 LOC, −3): added `PILL_ACTIVE`, `PILL_DEFAULT_SLATE` import; collapsed the 4-line ternary class string into a single-line ternary using the constants.
- **`App.tsx`** (472 → 466 LOC, −6): extended existing `./constants` import; replaced 2 multi-line ternaries (tag rail, status rail) with single-line constant references. Status rail keeps its asymmetric `${STATUS_BADGE[s]} hover:opacity-80` default — only `PILL_ACTIVE` swapped in there.
- **Net diff:** +14 / −16 = **net −2 LOC** across 4 files; 5 inline duplications removed; 0 prop/API surface changes.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `cd viz && npm test` → 5 files / 73 tests passed (incl. `App.test.tsx` and `ProjectSelector.test.tsx`)
- [x] Ran lint/type-check on changed code — `npm run typecheck` (`tsc --noEmit`) clean. `npm run lint` doesn't exist; tasknote README's "Viz lint" quick command is stale (drift surfaced for Phase 4 doc-sweep)
- [x] (frontend) Asked the user for visual confirmation — user: "looks good"

**Testing Notes:** Pure constants extraction; no rendered-output change expected. Existing tests cover the paths (filter rails, project selector, status chip via `App.test.tsx` + `ProjectSelector.test.tsx`); all green confirms class strings + label values are intact.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `_project/tasknote/README.md` §"AI-referenced docs", state "no change" or the update
  - `README.md` — **no change** from FE-023 itself (constants extraction is internal to `viz/src/ui/`).
  - `SPEC.md` — **no change.** Workflow contract untouched.
  - `docs/MIGRATION.md` — **no change** from FE-023 itself.
  - `claude/CLAUDE-snippet.md` — **no change** from FE-023 itself.

  *(Out-of-scope side commit in this session updates README.md / MIGRATION.md / CLAUDE-snippet.md for the unrelated dev-port collision fix — tracked separately, not part of FE-023's diff.)*
- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-05-10.` and tasknote moved to `_project/tasknote/archive/frontend/`
- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate)

**Final Summary:**

Co-located `STATUS_CHIP_LABEL` from `StatusChip.tsx` into `viz/src/ui/constants.ts` alongside `STATUS_LABEL` (both maps preserved as distinct exports; emoji-prefix divergence intentional). Extracted `PILL_ACTIVE` + `PILL_DEFAULT_SLATE` Tailwind constants and replaced 5 inline class-string sites:

- `PILL_ACTIVE` — 3 sites: `App.tsx` tag rail, `App.tsx` status rail, `ProjectSelector.tsx`
- `PILL_DEFAULT_SLATE` — 2 sites: `App.tsx` tag rail, `ProjectSelector.tsx` (status rail keeps its `${STATUS_BADGE[s]} hover:opacity-80` default — asymmetry per [[FE-018]] audit)

**Diff:** +14 / −16 = net **−2 LOC** across `constants.ts`, `StatusChip.tsx`, `App.tsx`, `ProjectSelector.tsx`. 5 inline duplications removed; 0 prop/API surface changes; visual confirmation passed (`looks good`).

**Tests:** 5 files / 73 tests pass; `tsc --noEmit` clean. No `lint` script in `viz/package.json` — tasknote-README's "Viz lint: cd viz && npm run lint" quick-command is stale (logged in SCRATCHPAD as a small follow-up — not in FE-023 scope).

**Bundle context:** third of [[FE-018]]'s four sibling follow-ups. Co-location precedent matches [[FE-021]] (helper into `utils.ts`) and [[FE-022]] (exports from `parser.ts`) — keep distinct exports adjacent rather than collapsing similar shapes. [[FE-024]] (App.tsx decomposition) remains.

**Archived:** 2026-05-10
