# Flowtron — PLAN.md

## Vision

A lightweight, versioned, project-agnostic tasknote system for solo
AI-assisted coding. One source of truth, consumed via git submodule by every
project under `~/code/`. Replaces the disjointed per-project workflow files
in fintown, InvisiPaw, and photard.

See [SPEC.md](../SPEC.md) for the canonical workflow contract.

## Critical

(none — bootstrap tasks complete; see Completed below)

## High

(none — see Completed below)

## Medium

- [ ] **CORE-EPIC-042** [opus] | workflow architecture rethink — Lazy-load SPEC into core + modules; question 4-phase rigidity (micro-tasknote shape + phase consolidation); audit frontmatter/status duplication. Filed prophylactically per [[CORE-037]] follow-on; rich context in [[CORE-042.1]] starter.
  - [x] **CORE-042.1** [opus] | discovery — Settled 8 starter open questions; filed children .2-.6 + renumbered audit to .7. Three thrusts confirmed: A (SPEC modularization), C (frontmatter `priority`/`area` drop + status simplification), B (`/micro-task` + phase rethink). v1.0 bump when A+C bundle. Completed 2026-05-06. Tasknote: `_project/tasknote/archive/core/CORE-042.1.md`.
  - [x] **CORE-042.2** [opus] | SPEC modularization — Thrust A: split `SPEC.md` (4,014w → 2,680w) + 5 lazy modules at `SPEC/<name>.md`; `/task` + `/starter-task` dispatch via directive Read on file-state branch + epic-ID prefix. ~10% per-`/task` saving on fresh-task branch (lower than predicted ~17% — SKILL-side savings deferred). Completed 2026-05-06. Tasknote: `_project/tasknote/archive/core/CORE-042.2.md`.
  - [x] **CORE-042.3** [opus] | frontmatter audit — Thrust C: dropped `priority:` + `area:` from frontmatter (both fully derivable). 12 files touched: viz parser/UI/tests, both templates + tasknote-README, SPEC §"Tasknote frontmatter", 3 skills, README + MIGRATION.md, 2 active tasknotes. New back-compat test mirrors [[CORE-023]]'s `model:` tolerance; legacy archives parse fine. SPEC held at v0.7.0; v0.8.0 deferred to [[CORE-042.4]] closure (covers all of Thrust C in one bump). Drive-by: also retired the stale `model` mention left in three doc field-lists (pre-existing CORE-023 cleanup gap). 54/54 viz tests pass, build clean. Completed 2026-05-06. Tasknote: `_project/tasknote/archive/core/CORE-042.3.md`.
  - [ ] **CORE-042.4** [opus] | status source-of-truth — Thrust C: nav header chip becomes render-derived from YAML `status:`; Phase 4 closure flips 2 places instead of 3. Update SPEC §"Tasknote body shape" + §"4-phase" + viz renderer + `/task` Step 4. Scope in [[CORE-042.1]] §A6.
  - [ ] **CORE-042.5** [opus] | micro-tasknote — Thrust B: new `/micro-task <ID>` skill + single-section template (do-the-work + recap; no Phase 3 boilerplate when no code changed; one closure step). SPEC §"When to use a tasknote" gains carve-out. Mirrors `/starter-task`.
  - [ ] **CORE-042.6** [opus] | phase rethink Discovery — Thrust B: produce side-by-side comparison of 4-phase status quo vs continuous-flow with named gates; decide ship/propose at filing. Non-negotiable contracts that must survive: relevance, drift, archive skim, pattern survey. [[CORE-042.1]] §A9 has short-form; this revisits post-A/post-C.
  - [ ] **CORE-042.7** [opus] | audit — Verify the new architecture: per-`/task` load actually shrunk (vs ~12,300w baseline); adopters can still adopt cleanly with new SPEC layout; contracts still discoverable from the always-loaded surface. File audit follow-ups as `.8+` if surfaced.
- [ ] **CORE-016** [opus] | InvisiPaw migration — Execute InvisiPaw migration per [[CORE-008]] playbook. Blocked by [[CORE-008]] — do not start until user signals InvisiPaw backlog is cleared. Scope: flowtron wiring + plan-file collapse + active-task-ID rename. Out of scope: renaming the 15 in-flight tasknote files (deferred further).
- [ ] **FE-009** [opus] | viz dark mode — `tailwind.config.ts` extends nothing; no `dark:` variants anywhere. Add a dark theme + toggle (system-preference default + manual override persisted to localStorage). Builds on [[FE-007]].
- [ ] **FE-010** [opus] | viz keyboard nav — Add shortcuts: `/` focuses search, `j/k` arrow-nav rows, `Enter` expands, `r` refreshes, `Esc` collapses detail / clears filters. Builds on [[FE-007]].
- [ ] **FE-012** [opus] | viz inbound wikilink back-refs — Surface inbound references: when a task's `related-tasks:` frontmatter or PLAN long-description points at another task, the target's row gets a "← referenced by N" chip. Build a `Map<id, Set<id>>` once at load (data is already parsed) and render in `TaskRowInner`. Builds on [[FE-007]].

## Low

- [ ] **CORE-EPIC-009** [opus] | fintown migration — Migrate **fintown** (epic; child tasks below). Demoted from Medium 2026-04-30 — user clearing fintown's pending tasks first + wants flowtron itself to mature before planning the integration. A [[CORE-008]]-style "draft fintown migration playbook" task will likely be filed before execution begins; not yet warranted.
  - [ ] **CORE-009.1** [sonnet] | submodule + CLAUDE.md — Add flowtron submodule + CLAUDE.md block
  - [ ] **CORE-009.2** [opus] | plan.json → PLAN.md — Convert `plan.json` → `PLAN.md` (preserve archive references)
  - [ ] **CORE-009.3** [sonnet] | retire shell scripts — Retire `create_tasknote.py`, `archive_tasknote.py`, `validate_plan.py`
  - [ ] **CORE-009.4** [opus] | reconcile in-flight tasknotes — Reconcile in-flight tasknotes (BE-128, BE-138, FE-145, FE-146)
  - [ ] **CORE-009.5** [sonnet] | update workflow docs — Update fintown's `WORKFLOW.md` and `TASKNOTE_QUICK_REFERENCE.md` to point at flowtron
- [ ] **FE-014** [opus] | viz UI component tests — Vitest + react-testing-library tests for App.tsx logic now that components are extracted: `matchesFilter` intersection, `navigateToTask` (epic auto-expand, scroll + highlight), expand-on-click toggling, status badge selection. Establish the testing pattern; subsequent component tests follow it. Builds on [[FE-007]].

## Future Opportunities

- [ ] **FE-002** [opus] | cross-project viz — Cross-project visualizer (extends [[FE-001]]) — Node-side filesystem scan over `~/code/*/_project/PLAN.md`, all projects in one Kanban, optional click-into tasknote detail view. Synergy: [[FE-004]] (frontmatter-aware cards/filters on the same viz codebase).
- [ ] **CORE-036** [opus] | PLAN.md `## Completed` archive strategy — explore rollup / stub-and-archive / age-truncation options for the unboundedly-growing Completed section (~78% of PLAN.md tokens per [[CORE-037]] audit). Spec in starter.
- [ ] **CORE-041** [opus] | dynamic-index direction — explore Dataview-style frontmatter queries / DB-backed index if flowtron scales beyond solo single-user planning. See [[CORE-037]] audit + [[CORE-040]] discipline conversation.

## Completed

- [x] **CORE-033** [sonnet] | extract legacy/ to a tag — Created annotated tag `legacy-pre-v0.1.0` at the last commit containing `legacy/`, removed the 71-file directory from main, updated README §"Repo layout" to reference the tag. Completed 2026-05-06. Tasknote: `_project/tasknote/archive/core/CORE-033.md`.

- [x] **CORE-032** [opus] | starter promotion verification — Added one **Fidelity check** sub-bullet to `claude/skills/task/SKILL.md` Step 3a Step 4 (line 100), parallel to the four existing spec-section bullets: _"verify the starter's `Solution shape` / `Decisions locked` / `Files to touch` are reflected in the synthesized Goal (and earmarked for Phase 1's Acceptance/Subtasks population); flag any dropped substance to the user."_ Closes a verification gap latent since [[CORE-027]] introduced starters: Step 3a Step 1's drift-check and the Phase 1 drift-check both target code-citation drift; neither catches content-fidelity loss when the AI re-synthesizes structured starter content (`### Solution shape` / `### Decisions locked in this conversation` / `### Files to touch`) into Goal/Acceptance/Subtasks. SPEC.md untouched — §"Starter tasknotes" Promotion §3 already encodes intent (`"The starter context informs the spec sections; it is not silently authoritative — Phase 1's drift check applies fully."`); skill addition operationalizes the missing half per [[CORE-038]] cite-don't-restate norm. Hardlink atomicity confirmed (both `.claude/` and `claude/` paths share inode post-edit; `diff -q` silent). Pre-flight: PLAN.md `[sonnet]` retagged to `[opus]` at Step 1.5 per user. 53/53 viz tests pass, tsc clean. Completed 2026-05-06. Tasknote: `_project/tasknote/archive/core/CORE-032.md`.

- [x] **CORE-031** [opus] | archive search in Phase 1 — Added a required **Archive skim** step to Phase 1 Discovery (between "Read relevant source files" and "Drift check"): SPEC.md §"📝 Phase 1: Discovery" gained a new checklist bullet + matching explanatory paragraph; `templates/tasknote-template.md` mirrored the bullet; `.claude/skills/task/SKILL.md` Step 4 added a thin operational imperative (`ls archive/<area>/` enumerate + per-source-path `grep -l`; log hits in Discovery Notes; empty/absent dir → log "no prior tasknotes" and tick). Cite-don't-restate shape per [[CORE-038]] — SPEC carries canonical intent, skill operationalizes mechanics. SKILL.md hardlink (CORE-038 finding) confirmed: edit landed in both `.claude/` and `claude/` paths atomically. SPEC v0.6.2 → **v0.7.0** (minor — adds a new template section per SPEC §"Versioning"; [[CORE-040]]'s patch-bump rationale explicitly excluded template-touching changes from patch). Self-dogfood ran during Phase 1 — the new skim surfaced three load-bearing findings ([[CORE-038]] hardlink note, cite-don't-restate norm, [[CORE-040]] patch-vs-minor precedent), empirical evidence the feature pays off in mature areas. Optional viz "archive search" view explicitly out of scope. 53/53 viz tests pass, tsc clean. Completed 2026-05-06. Tasknote: `_project/tasknote/archive/core/CORE-031.md`.
- [x] **CORE-039** [opus] | SPEC.md prose tightening sweep — Trimmed SPEC.md across nine sites in two rounds: round 1 = the six [[CORE-037]]-audit sites (§Epic lifecycle worked-example + Few/Many compression + apply/skip drop; §Long-description conventions wikilink-style framing drop; write-once-policy consolidation across §Tasknote frontmatter / §Tasknote body shape / §Model field; §Starter tasknotes filed-selectively duplicate; §Phase 1 prose; §Blocked tasks Viz interaction); round 2 = three structural cuts surfaced by user's mid-flight cold re-read (§Blocked tasks H3-subsection-to-inline-paragraph compression — 595w → ~340w; §Tasknote frontmatter YAML codeblock cite-and-trim against `templates/tasknote-template.md`; §Starter tasknotes YAML codeblock drop with diff-from-standard absorbed into prose). Final word count: **4,525w → 3,929w = 596w savings (~13.2% of file)** on every `/task` load. SPEC v0.6.1 → v0.6.2 (patch — clarification only; no field/parser/template change; adopters need no migration). Combined with [[CORE-038]]'s 503w skill-side trim earlier today: **1,099w cumulative savings** on every `/task` invocation, plus [[CORE-036]]'s archive strategy savings — [[CORE-037]]'s audit recommendations now fully landed. 53/53 viz tests pass (both rounds), tsc clean. Completed 2026-05-06. Tasknote: `_project/tasknote/archive/core/CORE-039.md`.

- [x] **CORE-038** [opus] | task SKILL.md cite-don't-restate — Trimmed `.claude/skills/task/SKILL.md` Step 3b (~440w → ~140w), Step 4 (~290w → ~95w), and Step 6 (~200w → ~135w) using bare-citation form against SPEC §"Tasknote frontmatter" / §"Tasknote body shape" / §"📝 Phase 1: Discovery" / §"Blocked tasks" / §"Post-closure protocol". Skill-specific value-add preserved verbatim: Step 1.5 model gate (intentionally untouched — hot path, cost of misfire high), Step 2 four-way file-state branch, Step 6 "one continuous flow" / "commit-go is the only gate" framing, recommended-model-per-option in the suggest step. Pattern survey: cite-don't-restate is already in active use throughout SKILL.md (Steps 1, 2, 3a, plus Step 5 / Notes pointers); this trim extends an existing pattern rather than introducing one. One regression caught in cold re-read: initial Step 3b draft hardcoded `templates/tasknote-template.md`, but Step 0 establishes the Template path as adopting-vs-self-hosted-varying — restored to `Copy the template (path resolved in Step 0)`. Final word count: 2,732w → 2,229w = **503w savings** (~18.4% of original); hits the ≥500w acceptance threshold. Hardlinked paths (`.claude/skills/task/SKILL.md` ↔ `claude/skills/task/SKILL.md`, inode 79872599) update atomically — single edit, both paths in sync. Filed by [[CORE-037]] audit. Sibling: [[CORE-039]] (parallel SPEC.md trim). Completed 2026-05-06. Tasknote: `_project/tasknote/archive/core/CORE-038.md`.

- [x] **CORE-040** [opus] | PLAN.md filing discipline — Codified the ≤50w target / 70w hard cap rule prescribed by [[CORE-037]]'s post-closure pass. SPEC.md §"When to use a tasknote (and when not to)" extended with explicit threshold language in both bullet blocks + new `### PLAN.md filing-discipline thresholds` sub-section (three-row table + `## Completed`-out-of-scope carve-out + skill pointers). `/starter-task` Step 5 gained a Threshold-check sub-block (word-count proposed long description; trim at 51-70w; >70w must trim or document override rationale in the starter body). `/task` Step 1 gained an advisory ⚠️ one-liner when the located task line's long description >70w (non-blocking — task is already filed). Sweep finding: zero existing active long-descriptions exceed the 70w cap; longest is CORE-031 at 50w (the recent CORE-037 dogfood pass already brought CORE-036/038/039 into compliance, so no PLAN.md outlier edits required). SPEC v0.6.0 → v0.6.1 (patch — clarification only; no parser/schema/template change). Release tag deferred to follow-up. 53/53 viz tests pass, tsc clean. Completed 2026-05-05. Tasknote: `_project/tasknote/archive/core/CORE-040.md`.

- [x] **CORE-037** [opus] | workflow token-cost audit — Measured all six surfaces (PLAN.md 4,459w, SPEC.md 4,319w, task SKILL.md 2,653w, global+workspace CLAUDE.md 674w combined, template 338w, tasknote bodies variable). Big three load ~11,400w on every `/task` invocation. Filed [[CORE-038]] (skill cite-don't-restate) + [[CORE-039]] (SPEC prose sweep). User flagged a structural gap mid-closure: active PLAN.md long descriptions ARE trim-able by routing rich context into starter tasknotes (existing v0.4.0 mechanism, qualitatively required by SPEC §"When to use a tasknote" but not enforced). Filed [[CORE-040]] to sharpen the threshold (≤50w target / 70w cap → starter) + update skills + sweep. Filed [[CORE-041]] as a Future-Opportunities marker for the dynamic-index direction (Dataview / DB-backed). Dogfooded by converting [[CORE-036]] / [[CORE-038]] / [[CORE-039]] from inline-prose to starter tasknotes in this same closure commit. Estimated total savings: ~1,100-1,300w from CORE-038/039 (≈10-12% of per-`/task` load), independent of CORE-036's archive-strategy savings. Completed 2026-05-05. Tasknote: `_project/tasknote/archive/core/CORE-037.md`.

- [x] **FE-011** [opus] | viz auto-refresh on file change — Replaced manual Refresh button with chokidar-watched SSE auto-refresh. New `/api/events` endpoint in `viz/vite.config.ts`'s `flowtronApi` plugin watches `_project/PLAN.md` + top-level `_project/tasknote/*.md` (200ms debounce, 30s heartbeat); client opens an `EventSource` from `App.tsx` and calls existing `load()` on each `change` event. Refresh button kept as fallback (unchanged). Bug surfaced in Phase 3: chokidar's file-level `fs.watch` on macOS doesn't survive `sed -i ''`-style atomic-save edits (inode swap goes unnoticed); fixed with `usePolling: true, interval: 200` — trivial CPU cost for our file count. SSE setup guarded behind `server.httpServer` so vitest's middleware-mode runs don't hang. Verified end-to-end via Playwright: live PLAN.md edit → row text updates without page reload, expanded detail panel survives the refresh, 0 console errors. 53/53 tests pass, tsc clean. Builds on [[FE-007]]. Completed 2026-05-05. Tasknote: `_project/tasknote/archive/frontend/FE-011.md`.
- [x] **FE-013** [sonnet] | viz polish bundle — Four small fixes: favicon in `viz/index.html`; `autoComplete="off"` on search input; relative due-date chips ("in Nd" / "today" / "overdue Nd") with ISO tooltip; status filter hides options with zero matching tasks. Completed 2026-05-04. Tasknote: `_project/tasknote/archive/frontend/FE-013.md`.
- [x] **FE-007** [sonnet] | viz refactor — split App.tsx — Mechanical refactor: `viz/src/ui/App.tsx` is 902 lines with 12+ inline components (`PrioritySection`, `EpicRow`, `TaskRow`, `TaskRowInner`, `SubtaskRow`, `WikilinkMarkdown`, `TaskDetail`, `DetailSection`, `Chevron`, `PhaseDots`, `SubtaskProgress`, `ModelChip`, `RelatedChip`, `BlockerChip`). Extract each to its own file under `viz/src/ui/`; App.tsx keeps state + layout only. No behavior change. Foundation for [[FE-008]], [[FE-009]], [[FE-010]], [[FE-011]], [[FE-012]], [[FE-014]]. Completed 2026-05-04. Tasknote: `_project/tasknote/archive/frontend/FE-007.md`.
- [x] **FE-015** [sonnet] | filter chips include no-tasknote rows — One-line fix in `matchesFilter` (`viz/src/ui/App.tsx:93-95`): replaced `if (!fm) return false` with an `effectiveStatus` derivation (`fm?.status ?? (task.completed ? 'completed' : 'not-started')`). Clicking "Not started" now includes uncompleted PLAN.md rows without a tasknote; "Completed" includes completed rows without a tasknote. Frontmatter wins when present. 53/53 tests pass, tsc clean. Completed 2026-05-04. Tasknote: `_project/tasknote/archive/frontend/FE-015.md`.
- [x] **FE-008** [opus] | viz row-density redesign — Redesigned `TaskRowInner`'s right-gutter chip strip from a single 480px-min flex into a two-cluster CSS grid with `gap-x-4`: signal cluster (`PhaseDots`/`SubtaskProgress`/`BlockerChip`) gets a Tier-1 size bump (dot 1.5→2, bar 1.5×12→2×16, blocker 10→11px font-medium); metadata cluster (`ModelChip`/tags/`RelatedChip`/due/status) stays at `text-[10px]`. Three chips moved into a thin metadata header strip atop `TaskDetail`: priority badge (redundant with section header), VS Code link, completed-date — each conditional on data. Hardcoded `min-w-[30rem]` removed; titles claim all unused width. Five files touched (`TaskRowInner.tsx`, `TaskDetail.tsx`, `PhaseDots.tsx`, `SubtaskProgress.tsx`, `BlockerChip.tsx`); `PRIORITY_BADGE` constant migrated `TaskRowInner`→`TaskDetail`. No new tests (no UI test surface yet — [[FE-014]] will scaffold). Visual confirmation captured at 1280×900 + 900×700 plus expanded-row variants for in-progress (FE-008) and completed (FE-007) rows. 53/53 viz tests pass, tsc clean. Completed 2026-05-04. Tasknote: `_project/tasknote/archive/frontend/FE-008.md`.
- [x] **CORE-030** [opus] | "blocked" workflow phase — Codified the `status: blocked` lifecycle in `SPEC.md` (new top-level §"Blocked tasks" between §"The 4-phase workflow" and §"Post-closure protocol"): two-signals table (PLAN-line `Blocked by [[ID]]` = filed/not-yet-started; tasknote `status: blocked` = started/parked) defined as **independent layers**; entry paths (Phase 1 Re-scope adds `Blocked by [[ID]]` and deletes the just-scaffolded tasknote — no 4th verdict; mid-Phase-2 parking flips frontmatter + nav header); parked-state semantics (no Phase 4 while blocked; tasknote stays in `_project/tasknote/`); resume protocol (drift-check, status flip, nav-header swap, continue at Phase 2); viz interaction note (independent rendering, no enforcement). Phase 1 Discovery + Phase 2 Execution body extended to cite the new section. `claude/skills/task/SKILL.md` extended: Step 2 three-way → four-way file-state branch (adds `status: blocked` → Step 3c); new Step 3c "Resume a blocked tasknote" mirroring Step 3a's shape; Step 4 item 2 gains `Re-scope (blocked subset)` bullet; Step 5 Phase 2 line gains mid-execution parking instruction. User clarified via AskUserQuestion: Phase 1 entry = Re-scope path (no 4th verdict), mid-flow lifecycle = park-and-resume, signal relation = independent layers. SPEC bumped v0.5.0 → v0.6.0 (additive minor — new optional convention; no parser/schema/template change). `viz/` unaffected — 53/53 tests pass, tsc clean (existing rendering already matches the independent-layers design). Release tag deferred to follow-up. Completed 2026-05-04. Tasknote: `_project/tasknote/archive/core/CORE-030.md`.
- [x] **CORE-029** [opus] | epic lifecycle — Codified the epic open-with-Discovery + close-with-Audit pattern in `SPEC.md` (new top-level §"Epic lifecycle" between §"Task ID convention" and §"Task-line format"): opening definition + epic-vs-Phase-1 Discovery disambiguation, `<AREA>-<N>.1` Discovery + final-`.N` Audit numbering with worked example, 5-step lifecycle (file → run Discovery → run children → run Audit → handle follow-ups: few → additional children; many → fresh Audit subtask), qualitative threshold (code sweeps + multi-child features only; user judgment), forward-looking note (existing in-flight epics like [[CORE-EPIC-009]] need no migration). Parallel one-liner added to `templates/tasknote-README.md` Layout. `claude/skills/task/SKILL.md` Notes "Sub-tasks of an epic" bullet extended to flag Discovery + Audit roles. User clarified via AskUserQuestion: `Discovery`/`Audit` naming, numbered subtasks (no `.0` slot), new top-level SPEC section, qualitative threshold. SPEC bumped v0.4.0 → v0.5.0 (additive minor — new optional convention; no parser/schema change). `viz/` unaffected — 53/53 tests pass, tsc clean. Release tag deferred to follow-up. Completed 2026-05-04. Tasknote: `_project/tasknote/archive/core/CORE-029.md`.
- [x] **CORE-035** [sonnet] | retire "Last updated:" line — Removed `**Last updated:** ...` from `_project/PLAN.md` and `templates/PLAN.md`. The line was hand-maintained and drift-prone; `git log -1 _project/PLAN.md` is authoritative. Completed 2026-05-04. No tasknote (doc patch — under SPEC skip threshold).
- [x] **CORE-034** [sonnet] | priority-name reconciliation — `SPEC.md` §"Priority levels" listed `Backlog` while `templates/PLAN.md`, `_project/PLAN.md`, and `viz/src/parser.ts` all use `Future Opportunities`. Replaced `Backlog` → `Future Opportunities` in the bullet line and the selection rule. No SPEC version bump (clarification only). Completed 2026-05-04. No tasknote (doc patch — under SPEC skip threshold).
- [x] **CORE-028** [sonnet] | release v0.4.0 — Tagged and pushed annotated `v0.4.0` bundling [[CORE-027]] (starter tasknotes + SPEC v0.4.0), [[FE-006]] (viz/ 🌱 chip), and [[CORE-026]] (doc fix). Additive minor release — no migration required; `starter` is a new optional status value only. Completed 2026-05-03. Tasknote: `_project/tasknote/archive/core/CORE-028.md`.
- [x] **FE-006** [opus] | viz/ starter chip — Shipped viz/ rendering for `status: starter` tasknotes per [[CORE-027]]: row-level 🌱 STARTER lime badge (replacing the standard status badge), `PhaseDots` / `SubtaskProgress` correctly hidden for starters, header filter rail gains 🌱 Starter chip, header subtitle extended to `N tasks · M in progress · K starter(s)` (with proper pluralization, segment hidden when K=0), click-to-expand renders the `## 🌱 Starter context` body via the existing `DetailSection` + `WikilinkMarkdown` pipeline (clickable wikilinks). **Load-bearing drift finding (Phase 1):** `STATUS_VALUES` in `viz/src/tasknote.ts` did not include `'starter'` — so before this task, starter frontmatter was rejected (`parseFrontmatter` returned `null`) and starters rendered as "no tasknote" rows. Adding `'starter'` to `TasknoteStatus` + `STATUS_VALUES` was the load-bearing first edit; UI work built on it. `Tasknote` interface gained `starterContext: string` (extracted via the existing `extractSection(body, 'Starter context')` pattern). Visual confirmation via Playwright against a temporary CORE-028 starter scaffold (deleted at closure). Tests: 51 → 53 (+2 in `tasknote.test.ts`); `tsc --noEmit` clean. Three files touched: `viz/src/tasknote.ts` (+5), `viz/src/ui/App.tsx` (+18 / -6), `viz/src/tasknote.test.ts` (+50). Unblocks [[CORE-028]] (release v0.4.0) — can now bundle the complete starter feature. Completed 2026-05-03. Tasknote: `_project/tasknote/archive/frontend/FE-006.md`.
- [x] **CORE-027** [opus] | starter tasknotes — Shipped a `status: starter` lightweight tasknote shape (frontmatter + `## 🌱 Starter context` only) for capturing rich AI-discovered context at task-filing time without bloating PLAN.md. New `templates/tasknote-starter-template.md`; new `/starter-task <ID>` skill (`claude/skills/starter-task/SKILL.md` + `claude/commands/starter-task.md`); `claude/skills/task/SKILL.md` Step 2 restructured into a 3-way file-state branch (`status: starter` → Step 3a Promote / other → stop / absent → Step 3b Scaffold); `SPEC.md` v0.3.0 → v0.4.0 with new §"Starter tasknotes" section between frontmatter and body-shape sections + body-shape carve-out + parallel "When to file a starter" paragraph in §"When to use a tasknote" + `starter` added to status enum; `templates/tasknote-README.md` parallel bullet. Dogfooded — CORE-027 itself was filed as a starter and promoted via the new flow during the same conversation; the manual promotion (since the skill didn't yet know about starters) validated the Step 3a sequence end-to-end. Drift finding (Phase 1): starter's MIGRATION.md deliverable conflicted with CORE-013/CORE-015 convention (per-version notes belong in tag message + release tasknote, not MIGRATION.md); dropped from Acceptance. Follow-ups: [[FE-006]] (viz/ 🌱 chip rendering), [[CORE-028]] (release v0.4.0). Tests: 51/51 viz pass; tsc clean. Completed 2026-05-03. Tasknote: `_project/tasknote/archive/core/CORE-027.md`.
- [x] **CORE-026** [sonnet] | remove "Pinned to:" version line — Dropped the redundant `**Pinned to:** vX.Y.Z` line from `templates/tasknote-README.md`; removed matching instructions from `docs/MIGRATION.md` §1.5 + §"Pinning and bumping" (step 3 + intro sentence); collapsed the two-bullet Step 6 in `/new-project` skill. Submodule SHA is the authoritative version pin — the text line was a drift-prone duplicate. No SPEC change. Completed 2026-05-02. No tasknote (doc patch — under SPEC skip threshold).
- [x] **CORE-025** [sonnet] | release v0.3.0 — Tagged annotated `v0.3.0` summarizing CORE-017 / CORE-018 / CORE-020 / CORE-022 / FE-001 / FE-004 / FE-005 / CORE-023 / CORE-024 / FE-003; pushed main + tag. First viz/ release. SPEC contract at v0.3.0 since CORE-024. No migration required for existing tasknotes (write-once; new fields additive). Completed 2026-05-02. No tasknote (release-only — under SPEC skip threshold).
- [x] **FE-003** [opus] | wikilink resolution — Extended [[CORE-024]]'s wikilink-resolution behavior from the no-tasknote branch of `TaskDetail` to the tasknote branch's per-section markdown render. Hoisted `wikilinkifyMarkdown` + the `components.a` wikilink-button map into a new `WikilinkMarkdown` component at module scope; both branches of `TaskDetail` and `DetailSection` (Goal / Acceptance / Subtasks) now compose this single source of truth. `[[TASK-ID]]` references inside expanded tasknote body text now render as clickable monospace buttons that invoke `navigateToTask(id)` (auto-expand collapsed section + parent epic, scroll-into-view, amber highlight). Code-span exclusion preserved (wikilinks inside backticks render as literal code); unresolved IDs render identically and silently no-op. Self-dogfooded mid-Phase-3 via FE-003's own Acceptance `[[CORE-024]]` reference. Single-file diff (+56 / −35 in `viz/src/ui/App.tsx`); 51 tests remain green; `tsc --noEmit` clean. Drift captured: PLAN.md description's "Kanban card" phrase is stale (FE-005 replaced the Kanban with a vertical list) — semantic intent unchanged. Completed 2026-05-02. Tasknote: `_project/tasknote/archive/frontend/FE-003.md`.
- [x] **CORE-024** [opus] | PLAN.md signal extraction — Extended PLAN.md long-description prose with two parseable conventions: `[[TASK-ID]]` → `Task.relatedTasks`, `Blocked by [[ID]], [[ID]]` → `Task.blockedBy` (wikilink-only; bare-ID forms rejected; wikilinks inside backtick code spans treated as literal). viz/'s `parser.ts` got `extractBlockedBy` / `extractRelatedTasks` / `stripCodeSpans` helpers; `App.tsx` `TaskRowInner` now renders related-task chips for rows without tasknotes (frontmatter still wins when present) and a new rose-tinted `BlockerChip` (⛔ glyph) always; `TaskDetail` no-tasknote branch wikilinkifies the description into clickable buttons via `react-markdown`'s `components.a` map. Four active PLAN.md descriptions migrated to wikilinks (CORE-016 + Blocked-by line, FE-003 → FE-001/FE-004/CORE-018, FE-002 → FE-001/FE-004, CORE-EPIC-009 → CORE-008). SPEC.md bumped v0.2.0 → v0.3.0 with new §"Long-description conventions" subsection (table + examples + code-span exclusion clause). Mid-Phase-3 hardening (parser + UI now skip code-span content) discovered via FE-003's `[[FE-042]]` placeholder appearing as a spurious chip — same dogfood pattern that filed CORE-024 itself. Tests: 41 → 51 (+10 parser). Completed 2026-05-02. Tasknote: `_project/tasknote/archive/core/CORE-024.md`.
- [x] **CORE-023** [opus] | task-line grammar — Extended PLAN.md task-line grammar with optional `[model]` + `| shortname` segments (legacy minimal form preserved); retired the YAML frontmatter `model:` field — PLAN.md is now the single source of truth for the model assignment. `/task` skill gained Step 1.5 model-gate (block + offer override on mismatch; ask user on legacy lines). SPEC.md bumped v0.1.1 → v0.2.0 with new §"Task-line format" + rewritten §"Model field" + slimmed §"Tasknote frontmatter". viz/'s `parser.ts` regex + `Task` interface + `ui/App.tsx` row-title preference + `ModelChip` source updated; ModelChip now renders for tasks without tasknotes too. 5 active PLAN.md entries + 5 epic children migrated to new grammar. Re-scoped 2026-05-02 mid-Phase-1 from "shortname only" after surfacing the parallel `/task`-decided-too-late workflow defect — one grammar revision covers both. CORE-024 filed mid-Phase-3 from visual review (description-prose signal extraction). Tests: 32 → 41 (+8 parser, +1 tasknote back-compat). Completed 2026-05-02. Tasknote: `_project/tasknote/archive/core/CORE-023.md`.
- [x] **FE-005** — Rebuilt `viz/` layout: replaced the 6-column Kanban with a priority-grouped vertical list (Critical → Future → Completed); section headers chevron-toggle (Completed default-collapsed); epic parents (`<AREA>-EPIC-<N>`) detected by ID convention and rendered with `N/M done` chip + nested collapsible subtask block (subtasks minimal — ID + description + completion checkmark). Top-level rows that have a tasknote show the new signal set: 4-dot phase indicator (earliest-incomplete = active), inline subtask progress bar (counted from the tasknote's 🧩 Subtasks checklist), text-only `opus`/`sonnet` model chip, and clickable related-tasks chips that auto-expand the target's collapsed section + scroll-into-view + briefly outline in amber. Tasks without a tasknote get a uniform "no tasknote" pill in the reserved right gutter, click-to-expand renders the full PLAN.md description below. Old Kanban code path fully removed. Added: `tasknote.ts` `countChecklist` + `activePhaseIndex` + `phases` / `subtasksProgress` on `Tasknote`; `parser.ts` `groupTasks` epic-detection helper. Tests: 18 → 32 (3 new in `countChecklist`, 4 in `activePhaseIndex`, 5 in `groupTasks`, 2 in `parseTasknote` for phase + subtask-progress extraction). Filed CORE-023 follow-up for SPEC-level shortname convention. Completed 2026-05-02. Tasknote: `_project/tasknote/archive/frontend/FE-005.md`.
- [x] **FE-004** — Enhance `viz/` to consume the new frontmatter: richer cards (priority badge, tag chips, due-date pill, frontmatter-derived status), inline expand-on-click rendering Goal/Acceptance/Subtasks via react-markdown + remark-gfm, "Open in VS Code" link via `vscode://file/<path>`, tag + status multi-select filter chips in header, quick-search extended to match across tags + status + frontmatter title. Server-side gray-matter parse in the Vite dev plugin (`/api/active` now returns parsed `Tasknote[]`); new `src/tasknote.ts` (parseFrontmatter + extractSection + parseTasknote) with 12 unit tests. Backwards-compat preserved — archived tasknotes (no frontmatter) keep FE-001's minimal card layout. Re-scoped from CORE-019 → FE-004 mid-Phase-1 (scope was entirely viz/ frontend, mirroring FE-001/FE-002/FE-003). Bugs surfaced + fixed during execution: gray-matter parses YAML dates as JS Dates (coerced to ISO strings); react-markdown doesn't render task-list checkboxes by default (added remark-gfm). Completed 2026-05-01. Tasknote: `_project/tasknote/archive/frontend/FE-004.md`.
- [x] **CORE-020** — Update `/new-project` skill and `docs/MIGRATION.md` for the new template format; optionally mention "use natabula for a full opinionated setup." Completed 2026-05-01. Tasknote: `_project/tasknote/archive/core/CORE-020.md`.
- [x] **CORE-001** — Hard reset flowtron repo (moved old contents to `legacy/`, scaffolded new dirs). Completed 2026-04-28. Bootstrap task — no tasknote (flowtron not yet self-hosting at the time).
- [x] **CORE-002** — Drafted `SPEC.md` (4-phase + Relevance Gate + post-closure protocol; tightened with drift check, pattern survey, model-field rule, and recap shape after cross-repo audit). Completed 2026-04-28. Bootstrap task — no tasknote.
- [x] **CORE-003** — Built `templates/` (tasknote-template.md, PLAN.md, tasknote-README.md). Completed 2026-04-28. Bootstrap task — no tasknote (flowtron not yet self-hosting; CORE-004 wires up the `/task` flow).
- [x] **CORE-004** — Built `claude/` (CLAUDE-snippet.md, `/task` command, `/task` skill); flowtron is now self-hosting via symlinks. Completed 2026-04-28. Tasknote: `_project/tasknote/archive/core/CORE-004.md`.
- [x] **CORE-005** — Wrote `docs/PHILOSOPHY.md` (narrative "why" — drift across prior projects, principles that stuck) and `docs/MIGRATION.md` (procedural guide for fresh adoption + migrating from a prior workflow system). Completed 2026-04-28. Tasknote: `_project/tasknote/archive/core/CORE-005.md`.
- [x] **CORE-006** — Released v0.1.0 to private GitHub: bumped `SPEC.md` to `v0.1.0` / `Stable`, rewrote §Versioning major-bump bullet (tasknote + annotated tag message in place of `CHANGELOG.md`), added repo-root `README.md`, tagged and pushed. Re-scoped to drop `CHANGELOG.md` (tasknotes + git history + tag messages are the changelog). Completed 2026-04-28. Tasknote: `_project/tasknote/archive/core/CORE-006.md`.
- [x] **CORE-007** — Migrated **photard** to flowtron — first real cross-repo adoption. Added `_project/flowtron/` submodule pinned to `v0.1.0`; symlinked `.claude/commands/task.md` + `.claude/skills/task` from the submodule; collapsed photard's four root plan files (PLAN/ROADMAP/PLAN_ARCHIVE/FUTURE_OPPORTUNITIES) into one `_project/PLAN.md` and renamed active task IDs to canonical (`FE-/BE-/OCR-`); rewrote `_project/tasknote/README.md` from flowtron's template (declared `OCR-` prefix; added concrete "Bumping the pinned flowtron version" procedure to replace a ghost CHANGELOG.md reference); patched root `CLAUDE.md` Workflow block. Surfaced CORE-013 follow-up. Completed 2026-04-30. Tasknote: `_project/tasknote/archive/core/CORE-007.md`.
- [x] **CORE-013** — Fixed ghost `CHANGELOG.md` references in `claude/CLAUDE-snippet.md` and `SPEC.md` §Versioning (both stale, left over from before CORE-006's re-scope dropped CHANGELOG.md). Replaced with pointers to the annotated tag message (`git show vX.Y.Z` in the flowtron submodule) + per-release archived tasknote, mirroring the language CORE-007 added to photard's `_project/tasknote/README.md`. Re-scoped from snippet-only after photard adoption review surfaced the SPEC.md instance. Completed 2026-04-30. Doc patch — no tasknote (under SPEC §"When to use a tasknote (and when not to)" skip threshold).
- [x] **CORE-014** — Released v0.1.1 to private GitHub: tagged annotated `v0.1.1` summarizing CORE-007 + CORE-013 + task-skill tweak; pushed tag. Release-only action — no content changes (three commits since v0.1.0 already on main). Migration: none (doc-only patch). Completed 2026-04-30. No tasknote (release-only — under SPEC skip threshold).
- [x] **CORE-015** — Doc cleanup sweep: finished CHANGELOG.md ghost removal in `templates/tasknote-README.md` and `docs/MIGRATION.md` §"Pinning and bumping" (CORE-013 missed both); bumped `SPEC.md` `Version` header to `v0.1.1` (stale since CORE-013 patched §Versioning); leaned out version sprawl by replacing hardcoded `v0.1.0` examples with `vX.Y.Z` placeholders in `templates/tasknote-README.md` + `docs/MIGRATION.md` and dropping the duplicated version pointer in `README.md`. `SPEC.md` `Version` is now the single source of truth, bumped on release. Completed 2026-04-30. Doc patch — no tasknote (under SPEC §"When to use a tasknote (and when not to)" skip threshold).
- [x] **CORE-010** — Reference flowtron in `~/Code/CLAUDE.md` as a suggested workflow option for new repos under `~/code/`. Re-scoped from "as canonical workflow" framing (some `~/code/` repos still on legacy systems; mandate-style language would imply non-compliance). Single bullet appended to `~/Code/CLAUDE.md` "Shared Workflow Philosophy" with pointers to `SPEC.md` + `docs/MIGRATION.md`. Completed 2026-04-30. Tasknote: `_project/tasknote/archive/core/CORE-010.md`.
- [x] **CORE-011** — Fold or delete `~/code/TasknoteSystem/` (the older predecessor folder). Verdict: delete outright — TasknoteSystem (5 small files dated 2025-04-02) is strictly superseded by flowtron; nothing to fold. Patched `docs/PHILOSOPHY.md:17` and `:47` to past-tense and dropped the `~/code/` absolute-path prefix so the historical narrative no longer points at a defunct location. Completed 2026-04-30. Tasknote: `_project/tasknote/archive/core/CORE-011.md`.
- [x] **CORE-008** — Re-scoped from "Migrate InvisiPaw (single task)" to "Draft InvisiPaw migration playbook" after discovery surfaced 15 in-flight tasknotes + dirty working tree + extra root files; user wants execution deferred until InvisiPaw backlog is cleared. Deliverable: ready-to-execute playbook captured in archived tasknote (preconditions, 9 execution steps, explicit out-of-scope items, ID-rename rule that handles whichever in-flight pile survives the backlog drain, version pin resolved at execution time). Filed CORE-016 as the blocked follow-up. Completed 2026-04-30. Tasknote: `_project/tasknote/archive/core/CORE-008.md`.
- [x] **CORE-012** — Re-scoped from "cookiecutter-style scaffolder" to "build `/new-project` skill" after Relevance Assessment surfaced collision with SPEC §"What flowtron does NOT provide" (no CLI tool); promoted Future Opportunities → Medium and shipped immediately. Deliverable: `claude/skills/new-project/SKILL.md` (8-step skill mirroring `/task` shape — preconditions, inputs, submodule add, symlink wire, CLAUDE.md append, PLAN.md + tasknote README from templates, stage + commit-go, verify), `claude/commands/new-project.md` (thin pointer), `docs/MIGRATION.md` §1.0 "Quick path" with one-time global-install symlink one-liner (§1.1–1.7 retained as manual fallback), README.md "Bootstrapping a new project" section. Live dry-run deferred (user opted out at Phase 1 checkpoint). Completed 2026-04-30. Tasknote: `_project/tasknote/archive/core/CORE-012.md`.
- [x] **CORE-017** — Added YAML frontmatter to the tasknote template (fields: `title`, `status`, `priority`, `area`, `model`, `tags`, `created`, `due`, `related-tasks`) so adopting projects' tools (visualizers, dashboards) can parse tasknote metadata without scraping. Replaced the body `**Priority:** **Area:** **Model:** **Status:**` lines with a YAML block above the H1; kept `**Goal:**` as prose. Propagated through `claude/skills/task/SKILL.md` Step 3 (frontmatter-filling instructions + `created:` auto-fill), `templates/tasknote-README.md` (Layout bullet), and `SPEC.md` (new §"Tasknote frontmatter" between Task ID convention and 4-phase workflow). Archived tasknotes left as-is — write-once historical records. Scope kept tight per CORE-018 separation. SPEC version bump (v0.1.1 → v0.2.0) and release tag deferred to a follow-up release task. Completed 2026-05-01. Tasknote: `_project/tasknote/archive/core/CORE-017.md`.
- [x] **CORE-018** — Polished the tasknote body to a **spec-on-top + log-below** shape so every tasknote reads like a small, polished spec rather than a pure execution log. Body now opens with a single-line nav header (`[← PLAN.md] · 🟢 In progress · 🔗 [[RELATED]]`), four spec sections (`🎯 Goal` / `✅ Acceptance` / `🧩 Subtasks` / `🔗 Related`), then a `---` divider, then the four phase sections with consistent emoji prefixes (`📝` / `🛠️` / `🧪` / `🚀`). Cross-references use Obsidian `[[TASK-ID]]` wikilinks. The actionable plan moved from Phase-1-internal `**Execution Steps:**` up to the top-level `## 🧩 Subtasks` section (Phase 1 still defines them; they live in the spec layer). Phase 4 gained a "flip nav header status icon to ✅" closure step to keep the duplicated status chip in sync with YAML. Propagated through `templates/tasknote-template.md` (full body rewrite), `.claude/skills/task/SKILL.md` Step 3 (Body field-filling instructions for all new sections), `SPEC.md` (new §"Tasknote body shape" with fenced layout diagram + emoji-prefixed phase subheadings + Phase 1/4 checklist text synced), and `templates/tasknote-README.md` (Layout bullet extended). Dogfooded mid-Phase-2 to validate the shape on a real, populated tasknote. Archived tasknotes left as-is per CORE-017 backwards-compat policy. SPEC version bump (v0.1.1 → v0.2.0) and release tag still deferred — CORE-017 + CORE-018 form a natural release boundary. Completed 2026-05-01. Tasknote: `_project/tasknote/archive/core/CORE-018.md`.
- [x] **FE-001** — Shipped flowtron's first frontend artifact: `viz/`, a single-project read-only Kanban visualizer (Vite 5 + React 18 + TS 5 + Tailwind 3) with six priority columns sourced from `_project/PLAN.md`, "In progress" badge driven by tasknote-file presence in `_project/tasknote/`. Re-scoped 2026-04-30 from cross-project view after discovery surfaced legacy/'s old JSON data model required a full data-layer rewrite; cross-project + tasknote drill-in filed as FE-002. 6 parser tests green, build clean. Run via `cd viz && npm install && npm run dev`. Completed 2026-04-30. Tasknote: `_project/tasknote/archive/frontend/FE-001.md`.
- [x] **CORE-021** — De-scoped 2026-05-01. Filed earlier the same day as a natabula migration task, but Phase 1 discovery confirmed natabula was already greenfield-bootstrapped onto flowtron via NAT-002 (2026-04-30): `_project/flowtron/` submodule pinned to `v0.1.1`, `.claude/` symlinks, `_project/PLAN.md` with `NAT-*` IDs, tasknote dir, CLAUDE.md Workflow block — both task-description paths (fresh `/new-project` adoption / `docs/MIGRATION.md` §2 prior-tooling migration) were N/A. Pinned-version drift (v0.1.1 predates CORE-012 + CORE-017) is a normal submodule bump and is already covered by natabula's own NAT-003 / NAT-005 / NAT-010 / NAT-011 backlog. No code changes. Tasknote: `_project/tasknote/archive/core/CORE-021.md`.
- [x] **CORE-022** — Re-scoped 2026-05-01 from "Write `docs/OBSIDIAN.md` (~250-line guide)" to a 17-line "Working in Obsidian" section in `README.md` after Phase 1 surfaced no current adopters use Obsidian + a maintained external-tooling doc would fail FUTURE.md's lightweight-in-six-months filter. Shipped: new section between "Visualizer" and "Repo layout" — positioning paragraph (editor-agnostic framing), bullets naming the two flowtron features that map natively to Obsidian (`[[TASK-ID]]` wikilinks from CORE-018; YAML frontmatter from CORE-017 as Dataview's query target), and one verified Dataview snippet wrapped in a quadruple-backtick outer fence so it renders as literal text on GitHub and doesn't auto-execute when README.md is read inside an Obsidian vault. Opt-in framing throughout. Saved a project memory recording flowtron's Obsidian-friendliness as positioning for prospective adopters, not user's daily workflow. Completed 2026-05-01. Tasknote: `_project/tasknote/archive/core/CORE-022.md`.
