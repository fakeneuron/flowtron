---
title: viz-polish
status: in-progress
tags: []
created: 2026-06-01
due:
related-tasks: []
---

# FE-051 | viz-polish

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Guard `setLoading(false)` against stale project loads in `useProjectData.ts`, and capitalize the sentence-fragment starting "filters" at `README.md:66`.

## ⚡ Notes

**Relevance:** Proceed — two independent low-severity fixes, both clear-diff, no design tradeoffs.

**Drift check:** `useProjectData.ts:46` is `setLoading(false)` inside `finally` — confirmed. The stale-project guard at lines 36-38 correctly short-circuits state updates but `finally` always fires, so `setLoading(false)` still runs for a superseded load. `README.md:66` is `filters and scroll position reset on switch.` — lowercase sentence start confirmed.

**Archive skim:** FE-049 (test deflake in App.test.tsx) and FE-050 (originGuard referer path) — no overlap with useProjectData or README Visualizer section.

**Pattern survey:** The existing stale guard at line 36 (`if (activeProjectRef.current !== project) return;`) is the established pattern for preventing stale writes. Adding the same check to the `finally` block is consistent — `activeProjectRef.current === project` before calling `setLoading(false)`.

**Implementation:** Added `if (activeProjectRef.current === project)` guard around `setLoading(false)` in the `finally` block (`useProjectData.ts:46`). Capitalized `filters` → `Filters` at `README.md:66`.

**Docs touched:** README.md — cosmetic fix only (capitalization), no content change to the AI-referenced doc set.

## ✅ Recap

Fixed two Low findings from the 2026-06-01 audit: (1) `setLoading(false)` in the `finally` block of `useProjectData.ts` now only fires when the load is still current (`activeProjectRef.current === project`), preventing a stale response from prematurely clearing the loading spinner for a newer project that is still fetching; (2) `README.md:66` "filters" capitalized to "Filters" to close the sentence-fragment gap.

**Archived:** 2026-06-01
