---
title: subtask-row-a11y-completion
status: completed
tags: []
created: 2026-06-14
due:
related-tasks: []
---

# FE-060 | subtask-row-a11y-completion

[← PLAN.md](../../PLAN.md) · ✅ Completed

## 🎯 Goal

Add completion state to the `SubtaskRow` button's accessible name so screen-reader users can distinguish done from not-done subtasks.

## ✅ Acceptance

- [x] Screen-reader users hear "Completed" or "Incomplete" as part of the button's accessible name
- [x] Visual rendering is unchanged
- [x] Lint and type-check pass

## 🧩 Subtasks

- [x] Survey existing a11y patterns in the codebase (sr-only vs aria-label)
- [x] Add completion-state text to the button's accessible name in `SubtaskRow.tsx`
- [x] Lint/type-check on changed file

## 🔗 Related

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** `SubtaskRow.tsx` line 49 has `aria-hidden` on the visual ✓ glyph; the button at line 59 renders only `task.id` + `task.description` as its accessible text — completion state is absent. Exactly what the task describes.

- [x] Read relevant source files

- [x] **Archive skim** — checked `.flowtron/tasknote/archive/FE/` (9 files). FE-058 references `aria-hidden="true"` on the App.tsx logo — unrelated. No prior work on SubtaskRow a11y.

- [x] **Drift check** — `viz/src/ui/SubtaskRow.tsx` matches the description exactly: `aria-hidden` span at line 50, button content at lines 65-70 shows only id + description spans, no completion signal in accessible name. No drift.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  No clarifications needed. Explicit assumptions: (1) Prefer `aria-label` over a `sr-only` span — consistent with codebase pattern (`TaskRowInner.tsx`, `SubtaskProgress.tsx`); (2) label format `"${task.completed ? 'Completed' : 'Incomplete'}: ${task.id} ${task.description}"`.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

`SubtaskRow.tsx:59` — button has no `aria-label`; accessible name computed from two `<span>` children. `task.completed` drives only the visual glyph (aria-hidden) and CSS.

Pattern survey: `aria-label` is the established codebase pattern (9 hits across `TaskRowInner.tsx`, `SubtaskProgress.tsx`, `ThemeToggle.tsx`, etc.); no `sr-only` usage anywhere in `viz/src/`.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — `aria-label` is the established pattern; extends `TaskRowInner.tsx:53`'s shape (`${task.id}: ${task.shortname}`)

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — no tests warranted (attribute-only change with no logic)

**Implementation Notes:**

Added one line to `viz/src/ui/SubtaskRow.tsx:63`:
```tsx
aria-label={`${task.completed ? 'Completed' : 'Incomplete'}: ${task.id} ${task.description}`}
```
Visual rendering unchanged.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — no test file for SubtaskRow; change is attribute-only

- [x] Ran lint/type-check on changed code — `npm run typecheck` + `npm run lint` both pass clean

- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:**

tsc and eslint pass. aria-label is a string expression on a typed `Task` — no type issues.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep**
  - README.md: no change
  - SPEC.md: no change
  - docs/MIGRATION.md: no change
  - claude/AGENTS-snippet.md: no change
  - docs/CONVENTIONS.md: no change
  - CONTRIBUTING.md: no change
  - SECURITY.md: no change
  - docs/AGENT-NEUTRALITY.md: no change
  - docs/PLATFORMS.md: no change
  - claude/CAPABILITIES.md: no change
  - docs/AGENT-COMPAT.md: no change

- [x] Closed — PLAN.md line flipped to stub form; tasknote moved to `.flowtron/tasknote/archive/FE/FE-060.md`

- [x] Recap drafted

**Final Summary:**

Added `aria-label` to the `SubtaskRow` button in `viz/src/ui/SubtaskRow.tsx` (+1 line) so screen-reader users hear "Completed: …" or "Incomplete: …" before the task id and description. Follows the codebase's established `aria-label` pattern; tsc and eslint pass clean.

**Archived:** 2026-06-14
