---
title: unparsed-line-diagnostics
status: completed
tags: []
created: 2026-07-02
due:
related-tasks: [FE-EPIC-063]
---

# FE-063.2 | unparsed-line-diagnostics

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-063]]

## 🎯 Goal

Stop the visualizer silently dropping `- [ ]` lines that fail the `TASK_LINE` regex — collect them in the parser and surface an "N unparsed" badge with the offending line text in the UI.

## ✅ Acceptance

- [x] Checkbox-bullet lines inside a recognized priority section that fail `TASK_LINE` are collected (line number + raw text), not silently dropped
- [x] `parsePlan(md): Task[]` keeps its existing signature and behavior (31 test call sites untouched)
- [x] UI surfaces an "N unparsed" badge when the count is > 0, with the offending line text visible
- [x] Parser tests cover: malformed checkbox line collected; valid lines not collected; lines outside known sections not collected; prose bullets not collected
- [x] UI test covers the badge + line-text rendering
- [x] `npm --prefix viz` test / lint / typecheck pass

## 🧩 Subtasks

- [x] Add `UnparsedLine` type + diagnostics-returning parse function in `viz/src/parser.ts`; `parsePlan` becomes a thin wrapper
- [x] Switch `viz/src/ui/useProjectData.ts` to the diagnostics variant; expose `unparsed`
- [x] Render "N unparsed" badge + offending-line surface in `viz/src/ui/App.tsx` (per user's presentation choice)
- [x] Add parser tests (diagnostics describe block)
- [x] Add App test for the badge
- [x] Run viz test / lint / typecheck

## 🔗 Related

- [[FE-EPIC-063]] — parent epic: viz-plan-grammar-resilience (audit-repo 2026-07-02)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Parser still silently drops malformed `- [ ]` lines at `parser.ts:171`; the epic's premise (hand-authored PLAN.md input losing tasks invisibly) holds at HEAD.

- [x] Read relevant source files

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope; log relevant findings in Discovery Notes before re-interpreting the task

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code; flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions — asked one: badge UX. User chose **badge + always-on amber strip** (header stats badge; always-visible strip below header mirroring the red error strip, listing line number + raw text per offending line).

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- Drift check: clean. `viz/src/parser.ts:171-172` is `const m = TASK_LINE.exec(line); if (!m) continue;` — exactly the silent-drop point the task cites.
- Data flow: `useProjectData.ts:39` calls `parsePlan(md)` on `/api/plan` text; App.tsx header subtitle renders count stats (`N tasks · N in progress …`) — natural badge home. Existing red error strip at `App.tsx:354` is the pattern to mirror for showing offending lines.
- Back-compat constraint: `parsePlan` has 31 call sites in `parser.test.ts` all expecting `Task[]`. Plan: internal parse returns `{ tasks, unparsed }`; export a diagnostics function; keep `parsePlan` as a thin wrapper.
- Collection rule: only lines inside a recognized priority section (after the `!currentPriority` guard — preserves the deliberate skip of task-shaped lines under unknown headings, covered by an existing test) that match a loose checkbox-bullet prefix (`^\s*-\s+\[[ xX]\]`) but fail `TASK_LINE`. Prose bullets and `(none)` placeholders stay ignored.
- Archive skim: no fe archive tasknote grep-hits `parser.ts`. FE-058 (logo optimize) and FE-060 (subtask-row a11y) hit on `App.tsx`/incidental mentions only — nothing load-bearing for the parser surface.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — looked at neighboring code for an existing pattern to extend; justified the new shape if none fits

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

- `viz/src/parser.ts`: added `UnparsedLine` (`{ line, text }`, 1-based) + `PlanParseResult` types and `parsePlanWithDiagnostics()`. The main loop is now indexed; a checkbox-bullet line (`^\s*-\s+\[[ xX]\]`) inside a recognized section that fails `TASK_LINE` is collected instead of dropped. `parsePlan()` kept as a thin `.tasks` wrapper — all 31 existing test call sites untouched.
- `viz/src/ui/useProjectData.ts`: switched to `parsePlanWithDiagnostics`; new `unparsed` state, exposed in the hook return, cleared on `reset()`.
- `viz/src/ui/App.tsx`: amber "⚠ N unparsed" badge appended to the header stats line (amber chip classes match the `in-progress` StatusChip precedent in `constants.ts:84`); always-on amber strip below the header (mirrors the red error strip) listing `L<n>: <raw text>` per offending line in monospace. Gated on `!loading` to avoid a flash alongside the skeleton.
- Pattern survey: reused the existing error-strip placement/styling shape and the stats-line chip idiom; amber = attention per the CORE-098.2 palette invariant noted in `constants.ts:30`.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:**

- `npm --prefix viz run test -- run src/parser.test.ts src/ui/App.test.tsx` — 70/70 pass (4 new parser diagnostics tests, 2 new App badge/strip tests).
- `npm --prefix viz run lint` + `typecheck` — clean.
- 👁️ ask surfaced at the 📦 gate (flowtron's own PLAN.md is currently fully parseable, so seeing the badge needs a deliberately malformed line).

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

  README.md · SPEC.md · docs/MIGRATION.md · claude/AGENTS-snippet.md · docs/CONVENTIONS.md · CONTRIBUTING.md · SECURITY.md · docs/AGENT-NEUTRALITY.md · docs/PLATFORMS.md · claude/CAPABILITIES.md · docs/AGENT-COMPAT.md — **no change** (all 11). Change is viz-internal and additive; SPEC §"Task-line format"'s "visualizers parse the line per `viz/src/parser.ts` (canonical reference)" still holds and the grammar itself is untouched.

- [x] Closed — PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` (see SPEC/tasknote-selection.md §"`## Completed` archive convention") and tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

The visualizer no longer silently loses hand-authored `- [ ]` lines that fail the task-line grammar. `parsePlanWithDiagnostics()` collects such lines (1-based line number + raw text) when they sit inside a recognized priority section; `parsePlan()` remains a back-compat wrapper. The UI surfaces an amber "⚠ N unparsed" badge in the header stats line plus an always-visible amber strip below the header listing each offending line (`L<n>: <text>`, monospace) — user-chosen presentation. 4 new parser tests + 2 new App tests; 70/70 pass, lint + typecheck clean. Files: `viz/src/parser.ts`, `viz/src/ui/useProjectData.ts`, `viz/src/ui/App.tsx`, `viz/src/parser.test.ts`, `viz/src/ui/App.test.tsx` (~+120 LOC).

**Archived:** 2026-07-02
