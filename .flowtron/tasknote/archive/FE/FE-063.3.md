---
title: search-reaches-subtasks
status: completed
tags: []
created: 2026-07-02
due:
related-tasks: [FE-EPIC-063, FE-063.2, FE-063.4]
---

# FE-063.3 | search-reaches-subtasks

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-063]] [[FE-063.2]] [[FE-063.4]]

## 🎯 Goal

Make the visualizer's search filter reach epic subtasks: `filteredNodes` in `viz/src/ui/App.tsx` keeps an epic visible when any of its children matches the query, so searching a subtask ID (e.g. `FE-063.3`) surfaces it instead of silently dropping the whole group.

## ✅ Acceptance

- [ ] Searching a subtask-only match (e.g. a subtask ID) keeps the parent epic node in `filteredNodes` — the epic group is no longer silently dropped
- [ ] An epic whose own line matches keeps working as before; non-matching plain tasks and non-matching epics (no matching children) are still filtered out
- [ ] Fix reaches both list and board views (both derive from `filteredNodes` via `bySection`)
- [ ] App test covers subtask-ID search surfacing the parent epic
- [ ] `npm --prefix viz` test / lint / typecheck pass

## 🧩 Subtasks

- [ ] Extend the `filteredNodes` predicate in `viz/src/ui/App.tsx` to `matchesFilter(n.task) || n.children.some(matchesFilter)`
- [ ] Add App test: query = subtask ID → epic row visible; control: unrelated query still hides it
- [ ] Run viz test / lint / typecheck

## 🔗 Related

- [[FE-EPIC-063]] — parent epic: viz-plan-grammar-resilience
- [[FE-063.2]] — predecessor: unparsed-line-diagnostics (closed 2026-07-02)
- [[FE-063.4]] — sibling: order-independent-grouping (also touches groupTasks/filter surface)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** `filteredNodes` at `App.tsx:136-139` still filters on `matchesFilter(n.task)` only — children are never consulted, so a query matching only a subtask drops the whole epic group. The epic's premise holds at HEAD.

- [x] Read relevant source files

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope; log relevant findings in Discovery Notes before re-interpreting the task

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code; flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- Drift check: near-clean. Task cites `App.tsx:135`; after FE-063.2's edits the `filteredNodes` memo sits at `App.tsx:136-139`. Same code, one line shifted — root-cause hypothesis exact.
- `matchesFilter` (`App.tsx:109-132`) is a per-`Task` predicate covering status filter + query over id/shortname/description/frontmatter — reusable as-is for children; no new matching logic needed.
- Reach: `bySection` (both list view and BoardView) and `filteredCount` all derive from `filteredNodes`, so the one-predicate fix covers both views and the header count.
- Archive skim: FE-058 (logo) and FE-060 (subtask-row a11y) grep-hit `App.tsx` incidentally; [[FE-063.2]] (unparsed-line diagnostics) touched the same file but a disjoint surface (badge/strip). Nothing load-bearing for the filter.
- No clarifications needed. Explicit assumptions: (1) minimal predicate fix only — a kept epic is **not** auto-expanded to reveal the matching child (out of the filed scope; SubtaskRow already highlight-matches once expanded; auto-expand-on-search is a possible follow-up); (2) children of a kept node are not pruned to matching ones — the whole group renders, consistent with today's behavior when the epic itself matches; (3) `filteredCount` semantics unchanged (counts kept node + all its children, as today).

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — looked at neighboring code for an existing pattern to extend; justified the new shape if none fits

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

- `viz/src/ui/App.tsx:137`: filter predicate extended to `matchesFilter(n.task) || n.children.some(matchesFilter)` — one line. Pattern survey: reused the existing `matchesFilter` per-`Task` callback unchanged (it already covers id/shortname/description/frontmatter + status filter); `visibleIds`/`navigateToTask` already treat children as first-class, so consulting `n.children` in the filter extends the established shape rather than inventing one.
- `viz/src/ui/App.test.tsx`: new `App — search reaches subtasks` describe block, following the existing `renderApp({ plan })` + `getByRole('searchbox')` fixtures: (1) subtask-ID query keeps the parent epic visible while an unrelated task filters out; (2) control — an epic with no matching children still filters out.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:**

- `npm --prefix viz run test -- run src/ui/App.test.tsx` — 34/34 pass (2 new).
- `npm --prefix viz run lint` + `typecheck` — clean.
- 👁️ ask surfaced at the 📦 gate (search for a subtask ID, e.g. `FE-063.4`, at http://localhost:5120 — the parent epic should stay visible).

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

  README.md · SPEC.md · docs/MIGRATION.md · claude/AGENTS-snippet.md · docs/CONVENTIONS.md · CONTRIBUTING.md · SECURITY.md · docs/AGENT-NEUTRALITY.md · docs/PLATFORMS.md · claude/CAPABILITIES.md · docs/AGENT-COMPAT.md — **no change** (all 11). Viz-internal filter fix; no grammar, contract, or doc surface touched.

- [x] Closed — PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` (see SPEC/tasknote-selection.md §"`## Completed` archive convention") and tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Searching the visualizer now reaches epic subtasks: the `filteredNodes` predicate in `viz/src/ui/App.tsx` keeps an epic when the epic itself **or any of its children** matches the query/status filter, so a subtask-ID search (e.g. `FE-063.3`) surfaces the parent group instead of silently dropping it. One-line predicate change (`|| n.children.some(matchesFilter)`); reaches both list and board views plus the header count via `bySection`/`filteredCount`. Two new App tests (subtask-ID query keeps the epic; non-matching epic still filters out); 34/34 App tests, lint + typecheck clean. Files: `viz/src/ui/App.tsx` (1 line), `viz/src/ui/App.test.tsx` (+32 LOC). Deliberately out of scope: auto-expanding a kept epic to reveal the matching child (follow-up candidate).

**Archived:** 2026-07-02
