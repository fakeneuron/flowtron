---
title: node-engines-bump-20
status: completed
tags: []
created: 2026-05-18
due:
related-tasks: []
---

# CORE-119 | node-engines-bump-20

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Bump `viz/package.json` engines.node from `>=18` to `>=20` (current LTS) and verify npm install, tests, and build still pass.

## ✅ Acceptance

- [ ] `viz/package.json` engines.node reads `>=20`
- [ ] `npm install` completes cleanly under Node 20.x
- [ ] `npm test` passes (all tests green)
- [ ] `npm run build` (tsc + vite) passes cleanly
- [ ] README.md and MIGRATION.md have no stale Node 18 references

## 🧩 Subtasks

- [ ] Edit `viz/package.json` engines.node `>=18` → `>=20`
- [ ] Run `npm install --prefix viz` to regenerate lock file
- [ ] Run `npm test --prefix viz` — verify all tests pass
- [ ] Run `npm run build --prefix viz` — verify clean build
- [ ] Confirm README.md / MIGRATION.md need no updates (no Node version strings present)
- [ ] Phase 4 closure

## 🔗 Related

- [[CORE-114]] — prior viz task (Vite v5→v6 bump); touched viz/package.json but left engines field at >=18

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** `viz/package.json` engines.node is confirmed at `>=18`. Node 18 reached EOL 2025-04-30; machine runs Node v20.18.0 (current LTS). Change is correct and low-risk.

- [x] Read relevant source files
- [x] **Archive skim** — grepped archive/core/ for package.json / engines / node-18 hits. CORE-114 touched viz/package.json for Vite v6 + Vitest v4 deps but left engines.node at `>=18`. No other prior tasknote is load-bearing here.
- [x] **Drift check** — `viz/package.json` engines.node is `>=18` (confirmed by file read). README.md and MIGRATION.md contain no explicit Node version strings (only generic `npm install` / `npm run dev` commands). No `.nvmrc` / `.node-version` / CI config found. Task description is accurate.
- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- `viz/package.json` engines.node: `>=18` → needs to be `>=20`
- `viz/package-lock.json` line 35 echoes the project engines field; will be regenerated by `npm install`
- README.md: no Node version string (lines 49–50 are `npm install` / `npm run dev` only) — no update needed
- MIGRATION.md: no Node version string — no update needed
- Machine Node: v20.18.0, npm: 11.1.0 — tests/build will run natively on Node 20
- CORE-114 left engines at `>=18`; this task closes that gap

No clarifications needed. Assumptions: `>=20` (not `^20` or `20.x`) to match the existing open-range style and allow future LTS upgrades without another bump.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — looked at neighboring code for an existing pattern to extend; justified the new shape if none fits
- [x] Implemented the minimal solution
- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

One-line edit: `viz/package.json` engines.node `>=18` → `>=20`. `npm install` regenerated the lock file. No new tests needed — the engine field is declarative metadata, not behavior.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code
- [x] Ran lint/type-check on changed code
- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:**

125/125 vitest tests pass. `tsc --noEmit` clean. `vite build` clean (311 modules, 0 errors). `npm audit` reports 0 vulnerabilities. No visual surface changed.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `_project/tasknote/README.md` §"AI-referenced docs", state "no change" or the update
- [x] Closed — PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` (see §"`## Completed` archive convention") and tasknote moved to `_project/tasknote/archive/<area>/`
- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Bumped `viz/package.json` engines.node from `>=18` to `>=20` (current LTS). `npm install` regenerated the lock file. All 125 tests pass, tsc and vite build clean under Node v20.18.0. README.md and MIGRATION.md had no Node version strings — no doc updates needed. **Files:** 1 modified (`viz/package.json`), 1 regenerated (`viz/package-lock.json`), 1 tasknote (this file).

Doc-drift sweep verdicts: README.md — no change; SPEC.md — no change; docs/MIGRATION.md — no change; claude/CLAUDE-snippet.md — no change; docs/CONVENTIONS.md — no change; CONTRIBUTING.md — no change.

**Archived:** 2026-05-18
