---
title: section-citation-resolver
status: completed
tags: []
created: 2026-09-20
due:
related-tasks: [CORE-EPIC-622, CORE-609, CORE-546, CORE-543, CORE-492]
touches:
  - .github/workflows/ci.yml
  - claude/skills/ft-release/step-7.1-mirror-pairs.md
  - claude/skills/ft-release/SKILL.md
  - docs/CONVENTIONS.md
  - .flowtron/tasknote/README.md
  - docs/GLOSSARY.md
  - SPEC/tasknote-inserts.md
  - claude/skills/ft-task/SKILL.md
  - claude/skills/ft-epic-discovery/SKILL.md
---

# CORE-622.3 | section-citation-resolver

[← PLAN.md](../PLAN.md) · ✅ Completed

## 🎯 Goal

Add a CI `drift` step and a §7.1 pair that resolves every path-bearing `` `<file>.md` §"<Section>" `` citation in live markdown (archives excluded) against a heading or a bold-lead in the target, so Pairs N and O become instances of one general resolver.

## ⚡ Notes

**Relevance:** Proceed — after an operator gate. The archive skim found this exact guard declined twice (CORE-492 2026-08-29, CORE-543 2026-09-09) with `.flowtron/tasknote/README.md` recording "not re-filed a third time"; the audit-repo filing cited neither. Surfaced with a fresh measurement (below) and the operator chose Proceed over De-scope. What changed since the declinations: CORE-620 rotated the self-quoting `## Completed` rows into `PLAN-ARCHIVE.md` (excludable), CORE-609 made bold-leads a valid target, and the placeholder false positives skip on `<`/`…`/`\`.
**Best Practices Review:** The check is shell lifted into CI in the established §7.1 → `drift` shape, bound by Pair L like the other eleven. Pairs N and O carried one-file instances of the same resolution; keeping both a general resolver and two special cases would be the duplication class this fragment exists to catch, so their `STALE LABEL` / `STALE CITATION` halves retired into Q and each keeps only the presence halves Q cannot derive (literal emitted, mirror labeled, `auto-commit` post-stage read, path-bearing citation present). One accepted loosening: O's `^## Filing commits` anchor becomes Q's prefix/substring match — recorded in O's notes. Skill-local path variables (`<SPEC_DIR>`, `<SKILL_DIR>`, `<FT>`, `<tasknote dir>`) are substituted so 16 skill→module citations are read instead of skipped; `passes/<domain>.md` (a glob) stays skipped.
**Drift check:** PLAN.md line holds — `.github/workflows/ci.yml` `drift` job and `step-7.1-mirror-pairs.md` are where it says; Pairs N/O exist as described; CORE-609's rule is `docs/CONVENTIONS.md` line 95 (heading renamed here from `` `§"Title"` citations … `` to `Section citations …` because a heading containing `"` cannot itself be cited in the `§"…"` shape). One PLAN claim was off: "the 2026-09-20 sweep found zero real breaks" — the resolver finds five `§"Fan-out."` citers (period inside the quotes; target is `## Fan-out (optional)`) plus one genuinely stale `SPEC.md` §"Procedure SOPs (`SPEC/procedures/`)" in `PLAN-ARCHIVE.md` (moved to `SPEC/layout.md` — historical, excluded). The `ft-release/SKILL.md` fragment list said "Pair A–N" (already stale at P); now A–Q. Contract contradiction: `docs/CONVENTIONS.md` §"Section citations …" said "The gap is accepted, not closed with a new detector" — rewritten, since this task is that detector.
**Archive skim:** `ls archive/core/` (828 notes); `grep -l` on `ci.yml` / `step-7.1-mirror-pairs.md` / `CONVENTIONS.md`. Load-bearing: CORE-609 (bold-lead rule, chose "record convention" over promoting four bold-leads to headings; named the K/N/O heading-only idiom), CORE-543 (re-scoped the same guard into Pair L; measured 62 `SPEC.md` citations, 3 false positives — the ticket's own line and two `## Completed` rows quoting drift; "1 of 3 defects reached"), CORE-492 (first declination, 5 artifacts / 63 citations resolved), CORE-546 (the CI-red-through-a-cut precedent), CORE-622.2 (sibling: sized the `ft-release/**` 125,000 directory budget at 117,971 + ~1.5 working units, naming "a new mirror pair or CI binding" as the unit).
**Declared scope:** YAML `touches:` above — nine files: CI workflow, §7.1 fragment, `ft-release/SKILL.md` (A–N → A–Q), CONVENTIONS, tasknote README, and the four citer files carrying `§"Fan-out."`.
**Pattern survey:** Pair P is the newest lifted pair and the shape followed: bold-lead paragraph naming the drift class and its precedent, scope paragraph, ```sh fence, "Must print nothing" + finding glossary + fix direction, then bullets. CI twin is the same block with `printf` findings and `bad=` → `exit 1`, appended after Pair P. Pair L mapping row added and its "eleven lifted checks" count bumped; `docs/CONVENTIONS.md` §"GitHub Actions CI" roster gains Q. N and O edits mirror between §7.1 and CI byte-for-byte on the reading lines so Pair L's path sets stay equal.
**Implementation:** (1) `ci.yml`: N loses the `labels=`/`STALE LABEL` loop (presence via `grep -qE`), O loses `cites=`/`STALE CITATION` loop (presence via `grep -qE`), Pair Q step appended. (2) `step-7.1-mirror-pairs.md`: same two trims plus prose (N gains "Whether the label still resolves is Pair Q's half"; O's "Three citation shapes resolve" → "count", "anchored" and `2>/dev/null` bullets replaced by "`STALE CITATION` retired into Pair Q"); Pair L row + count + roster; Pair Q appended (+4,169 chars; directory 122,140 / 125,000). (3) `docs/CONVENTIONS.md`: roster; §"Section citations …" heading renamed, `file.md` example → `<file>.md`, "gap accepted" bullet replaced by the Pair Q bullet + a citer's-obligations bullet. (4) `.flowtron/tasknote/README.md`: `path.md` → `<path>.md`; new "Landed at CORE-622.3" paragraph after the two declinations (kept as record). (5) Five `§"Fan-out."` → `§"Fan-out".` citer fixes. Resolver design: three shapes (backticked, `](path)`, bare), `.flowtron/core/` prefix stripped, `./`/`../` resolve from the citing dir, others root-then-citing-dir; skip on `<`/`…`/`\`; `MISSING FILE` and `STALE SECTION` findings; `grep -qF -e "# $sec" -e "**$sec"` (prefix, level-blind, emoji must be cited); `|| true` per extraction pipeline so `pipefail` cannot truncate a file's shapes (found: GLOSSARY's finding vanished under `-eo pipefail` before the guard).
**Verification receipt:** all 12 `drift` steps extracted from `ci.yml` via pyyaml and run under `bash -e` → exit 0 each (Q included; YAML parses) · §7.1 fences for Pairs L, N, O, Q extracted with Pair L's own awk and run → exit 0 each (L confirms CI ↔ §7.1 path sets equal for every mapped step, Q's row included) · Pair Q under `bash -e` and `bash -eo pipefail` → identical output (md5 match), 611 citations read across 132 live files, 26 placeholder skips listed by hand · mutation proofs (each restored with `git checkout --`): heading suffix `## Conditional skip rule XX` → still resolves (exit 0); heading rename → 25 `STALE SECTION` lines; bold-lead rename `**Deferred hand-off filing.**` → 3 `STALE SECTION`; `<SPEC_DIR>/model.md` target rename → 3 `STALE SECTION` (variable substitution proven); path typo `SPEC/gate.md` → 2 `MISSING FILE` · `find claude/skills/ft-release -type f -exec cat {} + | wc -c` → 122,140 ≤ 125,000 · `git status --short` clean of mutation residue. Structural assertions: no duplication (N/O resolution halves retired rather than triplicated), no dead code (obsolete N/O bullets removed), code-facing docs updated (CONVENTIONS, README, SKILL.md fragment list). Viz/updater suites not run — no code under `viz/` or `tools/` touched.
**Docs touched:** `README.md` no change · `AGENTS.md` no change · `SPEC.md` no change · `docs/MIGRATION.md` no change · `claude/AGENTS-snippet.md` / `codex/` / `cursor/` / `grok/` no change · `docs/CONVENTIONS.md` **updated** (§"GitHub Actions CI" roster; §"Section citations may target a heading or a bold-lead paragraph" rewritten) · `CONTRIBUTING.md` no change · `SECURITY.md` no change · `docs/AGENT-NEUTRALITY.md` no change · `docs/PLATFORMS.md` no change · `claude/CAPABILITIES.md` no change · `docs/AGENT-COMPAT.md` no change · `docs/EXTERNAL-AGENTS.md` no change · `docs/WORKTREES.md` no change · `docs/VISION.md` no change (§"Schema validators" still holds — Q is a pointer check, not a prose validator) · `docs/GLOSSARY.md` citer fix only · `.flowtron/tasknote/README.md` **updated** (declination record).

## ✅ Recap

Landed §7.1 **Pair Q** and its CI `drift` twin: a resolver for every path-bearing `§"Section"` citation in live markdown (611 at HEAD, three shapes, archives excluded, placeholders skipped, skill-local path variables substituted) against a `# <Title>` heading at any level or a `**<Title>` bold-lead. Pairs N and O retired their one-file resolution halves into it and keep their presence halves; Pair L binds the new step (twelve lifted checks). The task re-filed a guard declined twice (CORE-492, CORE-543) — surfaced to the operator, who chose Proceed on the changed evidence; `.flowtron/tasknote/README.md` now records why the third filing landed, and `docs/CONVENTIONS.md` no longer says the gap is accepted. Five real `§"Fan-out."` citer breaks fixed (period inside the quotes), contradicting the PLAN line's "zero real breaks". Budget: `ft-release/**` at 122,140 of 125,000 — one working unit of headroom left, so the next pair should consider a fragment split. `touches:` reconciliation: `git diff --name-only` = exactly the nine declared paths, nothing undeclared. Maintainability: the CORE-535.3 class (lazy split strands `§` pointers) now fails CI on the commit that lands it across ~130 files instead of three.

**Archived:** 2026-09-20
