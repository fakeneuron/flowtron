---
title: viz-dedup-orphan-cleanup
status: completed
tags: []
created: 2026-07-02
due:
related-tasks: []
---

# FE-065 | viz-dedup-orphan-cleanup

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[RELATED]]

## 🎯 Goal

Collapse the duplicate `PrioritySection` prop-spreads and starter-count pluralization in `App.tsx`, and delete the orphan `parsePlan` export from `parser.ts`.

## ✅ Acceptance

- [x] `App.tsx`'s board-view and list-view `PrioritySection` renders share one `renderSection` closure
- [x] `App.tsx`'s starter-count pluralization suffix is computed once, not duplicated across the two header-text branches
- [x] `parsePlan` is no longer exported from `parser.ts`; the test suite still passes without importing it from production code

## 🧩 Subtasks

- [x] Extract `renderSection(p: Priority)` closure in `App.tsx` and use it for both `BELOW_BOARD_SECTIONS.map(...)` (board view) and `SECTIONS.filter(...).map(...)` (list view)
- [x] Extract a single `starterSuffix` computation and reuse it in both header-text template branches (App.tsx:281-284)
- [x] Remove the `export` keyword from `parsePlan` in `parser.ts:240`; add a local test-only helper in `parser.test.ts` so the ~35 existing `parsePlan(...)` call sites keep working without importing a public API
- [x] Rename/repoint the one test explicitly asserting back-compat ("parsePlan stays back-compat...") to call `parsePlanWithDiagnostics(md).tasks` directly, since that's no longer a public-API back-compat guarantee
- [x] Run viz test suite + lint/typecheck on changed files

## 🔗 Related

- [[TASK-ID]] — short context (predecessor / follow-up / parent epic)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Small, well-scoped cleanup surfaced by the 2026-07-02 audit (Finding #4 Medium, #5 Low). Confirmed both duplication sites and the orphan export still match current code.

- [x] Read relevant source files

- [x] **Archive skim** — `grep -l "App.tsx\|parser.ts" .flowtron/tasknote/archive/FE/*.md` hit FE-058, FE-060, FE-063.2/.3/.4/.5, FE-066. These touched `App.tsx`/`parser.ts` for grammar-tolerance and render-resilience work, not the two prop-spread/pluralization sites or `parsePlan`. No load-bearing prior context for this specific cleanup.

- [x] **Drift check** — All three cited locations still match: `App.tsx:409-430` (board `PrioritySection` map) and `App.tsx:435-457` (list-view `PrioritySection` map) are near-identical prop spreads; `App.tsx:281-283` duplicates the `starterCount > 0 ? ... 'starter'/'starters' ...` ternary in both header-text branches; `parser.ts:240` (`export function parsePlan`) is unused in production code (only `App.tsx` imports from `parser.ts`, and it doesn't import `parsePlan`) — confirmed via repo-wide grep. However, `parsePlan` is used ~35 times across `parser.test.ts`, not just in the single test literally named "back-compat" (line 387-394). The task description's phrase "repoint its back-compat test" undersells this: deleting the export naively would break the other ~34 call sites too.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  No clarifications needed. Assumption: the intent is to remove `parsePlan` from `parser.ts`'s *public* export surface (the orphan the audit flagged), not to force `parsePlanWithDiagnostics(md).tasks` at all 35 test call sites. Minimal-diff approach: drop `export` from `parsePlan` in `parser.ts`, add a local (non-exported) `parsePlan` test helper inside `parser.test.ts` that wraps `parsePlanWithDiagnostics(md).tasks`, and specifically repoint/rename the one test whose description explicitly claims a back-compat *public-API* guarantee (since that guarantee no longer applies) to assert against `parsePlanWithDiagnostics` directly.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

Three independent cleanup sites, all in `viz/src/`:
1. `ui/App.tsx:409-430` vs `435-457` — two `PrioritySection` renders differing only in which array they map over (`BELOW_BOARD_SECTIONS` vs filtered `SECTIONS`); all 12 props identical.
2. `ui/App.tsx:281-284` — the starter-count suffix ternary (`` `${starterCount} ${starterCount === 1 ? 'starter' : 'starters'}` ``) is byte-identical in both branches of the `filteredCount === total ? ... : ...` conditional.
3. `parser.ts:240` — `export function parsePlan(markdown) { return parsePlanWithDiagnostics(markdown).tasks; }` has zero production callers (confirmed via repo-wide grep on `viz/src`); only `parser.test.ts` imports it, extensively (~35 call sites), as a convenience shorthand predating `parsePlanWithDiagnostics`.

Discovery surfaced no significant scope deviation (the "back-compat test" phrasing needed disambiguation, but the resolution stays inside this task's boundaries) → skip 🛠️.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — `renderSection` follows the existing local-closure pattern already used elsewhere in `App.tsx` (e.g. `handleSelectProject`, `navigateToTask`) for component-scoped helpers that close over hook state; no new shape introduced.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — repointed the one back-compat-named test to assert `parsePlanWithDiagnostics(md).tasks` directly; no new test needed for the pure App.tsx refactor (behavior-preserving).

**Implementation Notes:**

- `App.tsx`: extracted `starterSuffix` const (computed once, next to `starterCount`) and reused in both header-text branches; extracted `renderSection(p: Priority)` closure (defined after `navigateToTask`, before `useKeyboardNav`) and used it via `.map(renderSection)` at both the board-view (`BELOW_BOARD_SECTIONS`) and list-view (`SECTIONS.filter(...)`) call sites, replacing ~44 lines of duplicated JSX with 2 one-liners.
- `parser.ts`: deleted the `export function parsePlan(...)` wrapper (dead in production code).
- `parser.test.ts`: added a local (non-exported) `parsePlan` helper wrapping `parsePlanWithDiagnostics(...).tasks` so the ~35 existing call sites keep working unchanged; renamed/repointed the one test that explicitly asserted public-API back-compat ("parsePlan stays back-compat, returning tasks only" → "parsePlanWithDiagnostics tasks omit malformed lines") to call `parsePlanWithDiagnostics` directly, since that guarantee is no longer about a public export.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npx vitest run` (full viz suite): 212/212 passed, 16 test files.

- [x] Ran lint/type-check on changed code — `eslint src/ui/App.tsx src/parser.ts src/parser.test.ts` and `tsc --noEmit`: both clean.

- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:**

Screenshotted the running viz app (existing dev server on :5120) against the `flowtron` project in both List and Board view — both render the `Low` section (FE-065 itself) and the collapsed `Completed`/`Future Opportunities` sections correctly via the new shared `renderSection` closure. Screenshots: `/tmp/playwright-mcp/fe-065-flowtron-list-view.png`, `/tmp/playwright-mcp/fe-065-flowtron-board-view.png`.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — internal `viz/` refactor (JSX dedup + orphan export removal), no behavior or contract change. All entries in `.flowtron/tasknote/README.md` §"AI-referenced docs": no change.

- [x] Closed — PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` (see SPEC/tasknote-selection.md §"`## Completed` archive convention") and tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Collapsed two `App.tsx` duplication sites (board/list `PrioritySection` prop-spreads into a shared `renderSection` closure; starter-count pluralization into a single `starterSuffix` const) and removed the orphan `parsePlan` export from `parser.ts`, moving it to a local test-only helper in `parser.test.ts`. Behavior-preserving: full viz suite (212 tests) + lint + typecheck all pass; visually confirmed List and Board views render identically via Playwright screenshots.

**Archived:** 2026-07-02
