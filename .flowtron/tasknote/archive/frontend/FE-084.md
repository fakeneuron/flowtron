---
title: task-line-regex-compose
status: completed
tags: []
created: 2026-08-04
due:
related-tasks: [FE-066]
---

# FE-084 | task-line-regex-compose

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-066]]

## 🎯 Goal

Compose `TASK_LINE` (`viz/src/parser.ts:59-60`) from named fragments joined via `new RegExp` so each FE-066 tolerance is independently readable and diffable, with no change in parsing behavior.

## ✅ Acceptance

- [x] `TASK_LINE` is built from named fragment constants (e.g. `GLYPH`, `MODEL_TOKEN`, …) joined via `new RegExp`/string composition, replacing the single ~380-char literal.
- [x] Each FE-066 tolerance (leading status glyph, stacked `[model]` tokens, trailing suggestion glyph) maps to its own independently named fragment.
- [x] Capture-group order/count is unchanged (`[, mark, id, criticalRaw, modelRaw, shortnameRaw, longRaw]` destructure — now at parser.ts:276, shifted from :252 by the added lines — still works untouched).
- [x] Full `viz` parser test suite passes unmodified (no behavior change) — 57/57.
- [x] Real `.flowtron/PLAN.md` still parses with 0 unparsed rows (matches FE-066 baseline; 654 tasks now vs. 513 at FE-066 time).
- [x] Header comment above `TASK_LINE` (parser.ts:42-64) updated to match the fragment-based structure.

## 🧩 Subtasks

- [x] Design named fragment constants for each regex segment (status glyph, id, critical, model token, stacked-model tolerance, suggestion glyph, shortname, long description).
- [x] Compose fragments into `TASK_LINE` via `new RegExp(...)`, preserving capture-group order and count exactly.
- [x] Run the `viz` parser test suite — confirm zero regressions.
- [x] Sanity-check real `.flowtron/PLAN.md` still parses with 0 unparsed rows.
- [x] Update the header comment above `TASK_LINE` to reflect the composed structure.
- [x] Doc-drift sweep across README §"AI-referenced docs".

## 🔗 Related

- [[FE-066]] — predecessor; introduced the three non-capturing tolerances this task composes into named, independently diffable fragments.

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** `TASK_LINE` at parser.ts:59-60 is confirmed as a single ~380-char regex literal with 6 capture groups and three non-capturing FE-066 tolerances, exactly as described. The file is under active edit (FE-082/FE-083 landed today) so readability now has compounding value. Pure internal refactor, well-scoped.

- [x] Read relevant source files

- [x] **Best Practices Review** — touched responsibility is confined to the `TASK_LINE` regex literal and its header comment; no dependency-direction change. Existing abstraction is a single inline `RegExp` literal; the new shape is named fragment constants composed via `new RegExp`. No nearby duplication to address. No refactor beyond the regex composition itself is in scope — the destructure, `Task` interface, and call site (parser.ts:237-270) stay untouched.

- [x] **Archive skim** — `.flowtron/tasknote/archive/frontend/` skimmed for tasknotes touching `parser.ts`. FE-066 is the direct predecessor: added the three non-capturing tolerances (leading status glyph, stacked model tokens, trailing suggestion glyph) as `(?:...)?` groups layered onto the existing optional-group idiom (`[!critical]`, `[model]`, `| shortname`), explicitly preserving the capture-group destructure. FE-063.2/.3/.4/.5 (epic predecessors) built the diagnostics/unparsed-row machinery that depends on `TASK_LINE` failing cleanly on non-matching lines — the compose refactor must preserve that fail-closed behavior. No prior tasknote attempted fragment decomposition; this is a fresh direction, not a redo.

- [x] **Drift check** — `TASK_LINE` confirmed at parser.ts:59-60 (matches task description exactly). Capture groups confirmed as 6: mark, id, criticalRaw, modelRaw, shortnameRaw, longRaw (destructured at parser.ts:252). Three non-capturing tolerances confirmed present (status glyph, stacked models, suggestion glyph) per the header comment at parser.ts:48-58. No SPEC contract governs the regex's *internal* representation (SPEC §"Task-line format" documents the grammar, not the implementation), so this refactor doesn't touch SPEC. No divergence from the PLAN.md line.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  **No clarifications needed.** Assumptions:
  - "Named fragments... joined via `new RegExp`" (PLAN.md wording) means: define each segment as its own string constant (or small template literal) with a descriptive name, then concatenate and pass to `new RegExp(...)` — not a regex-builder library dependency (none exists in `viz`'s package.json; adding one would be out of scope for a readability-only task).
  - This is a pure refactor: zero behavior change, zero new capture groups, zero new fixtures required. Existing test suite is the correctness oracle.
  - The header comment (parser.ts:42-58) should be trimmed/adjusted to match the new self-documenting fragment names, but the grammar summary (`- [ ] **TASK-ID** [!critical] [model] | shortname — long description`) stays since it documents the *grammar*, not the implementation.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- `TASK_LINE` regex segments (left to right): leading whitespace/checkbox `^\s*-\s+\[([ xX])\]\s+`, leading status glyph tolerance `(?:(?:🟢|⏸|✅|⚪|🌱)️?\s+)?`, bold ID `\*\*([A-Z]+(?:-EPIC)?-\d+(?:\.(?:\d+|N))?)\*\*`, critical flag `(?:\s+\[(!critical)\])?`, model token `(?:\s+\[([a-z][\w.-]*)\])?`, stacked-model tolerance `(?:\s+\[[a-z][\w.-]*\])*`, suggestion glyph tolerance `(?:\s*(?:🧠|🔧|🧩)️?)?`, shortname `(?:\s+\|\s+([^\n]+?))?`, long description `(?:\s+[—-]\s+([^\n]+?))?\s*$`.
- `TASK_LINE` is not exported — covered only indirectly via `parsePlan`/`parsePlanWithDiagnostics` behavior in `parser.test.ts` (623 lines, no fixture references the regex object directly), so a compose refactor is safe as long as `.exec()` output shape is preserved.
- Sibling regexes in the same file (`COMPLETED_DATE`, `HEADING_LINE`, `WIKILINK_PATTERN`, `BLOCKED_BY_BLOCK`, `CODE_SPAN`) are all short single-purpose literals — no existing fragment-composition pattern to extend in this file; this task establishes the pattern for `TASK_LINE` specifically since it's the only regex complex enough to warrant it.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — no existing fragment-composition idiom in `viz/src`; the file's other regexes (`COMPLETED_DATE`, `HEADING_LINE`, `WIKILINK_PATTERN`, `BLOCKED_BY_BLOCK`, `CODE_SPAN`) are all short single-purpose literals with no decomposition need. `TASK_LINE` is the only regex complex enough to warrant fragments, so a new shape (named `String.raw` fragment constants joined via `new RegExp`) is justified rather than extending a pattern that doesn't exist. No dependency-boundary or SRP concerns — this is a same-function literal-to-composed-constants change.

- [x] **Minimal refactor gate** — refactor scope is exactly the `TASK_LINE` declaration and its header comment (parser.ts:42-83); the destructure, `Task` interface, and call site are untouched. No unrelated cleanup performed.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — N/A: pure refactor, zero behavior change, existing 57-test suite is the correctness oracle (see Testing Notes).

**Implementation Notes:**

- Split `TASK_LINE` into 9 named `String.raw` fragment constants (`BULLET_CHECKBOX`, `STATUS_GLYPH`, `TASK_ID`, `CRITICAL_FLAG`, `MODEL_TOKEN`, `STACKED_MODEL_TOKENS`, `SUGGESTION_GLYPH`, `SHORTNAME`, `LONG_DESCRIPTION`), concatenated in match order and passed to `new RegExp(...)`. `String.raw` was chosen over a plain string literal so the pattern text (backslash escapes like `\s`, `️`) doesn't need double-escaping.
- Capture-group order/count is byte-identical to the original: the concatenated fragment string reconstructs the exact same pattern that was previously a single literal (verified by diff against the pre-edit regex source via a Python script, then confirmed by the full test suite passing unmodified).
- Header comment above the fragments (parser.ts:42-64) rewritten to name each FE-066 tolerance's owning fragment constant, replacing the old prose-only numbered list.
- Files touched: `viz/src/parser.ts` only (regex declaration + header comment, ~23 lines net added for readability). No test file changes — behavior is unchanged.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Quality assertions** — no avoidable duplication (each fragment is used exactly once); no dead code; complexity reduced (9 named, independently-readable fragments vs. one 380-char literal); no public-surface growth (`TASK_LINE` stays module-private, fragment constants are also module-private); code-facing documentation (header comment) updated to match.

- [ ] (frontend) N/A — this is a parser-internals refactor (`viz/src/parser.ts`) with no visual/UI surface; there is nothing to visually confirm.

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

- `viz` parser suite: `npx vitest run src/parser.test.ts` — 57/57 passed, unmodified from pre-change baseline (no new fixtures needed; behavior is unchanged).
- `npx tsc --noEmit` — clean, no errors.
- `npx eslint src/parser.ts` — clean, no warnings.
- Real `.flowtron/PLAN.md` end-to-end via `parsePlanWithDiagnostics`: 654 tasks parsed, **0** unparsed rows (matches the FE-066 zero-unparsed baseline; task count is higher than FE-066's 513 because the board has grown since 2026-07-02).

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — all 12 AI-referenced docs: no change. `SPEC.md` §"Task-line format" (the one doc governing this surface) documents the PLAN.md task-line *grammar*, not the regex's internal representation — confirmed unaffected during Phase 1 Discovery drift check and reconfirmed here. This was a pure internal refactor (literal regex → composed fragments) with zero behavior or grammar change.

- [x] Closed — every `## ✅ Acceptance` criterion ticked, YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed 2026-08-04.` and placed at the top of `## Completed` (standalone task), then tasknote moved to `.flowtron/tasknote/archive/frontend/`

- [x] **Evidence-based recap** drafted (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Composed `TASK_LINE` in `viz/src/parser.ts` from 9 named `String.raw` fragment constants (`BULLET_CHECKBOX`, `STATUS_GLYPH`, `TASK_ID`, `CRITICAL_FLAG`, `MODEL_TOKEN`, `STACKED_MODEL_TOKENS`, `SUGGESTION_GLYPH`, `SHORTNAME`, `LONG_DESCRIPTION`) joined via `new RegExp(...)`, replacing the single ~380-char literal — each FE-066 tolerance now has its own named, independently-diffable fragment. Pure refactor: zero behavior change, capture-group order/count identical. Verification: `viz` parser suite 57/57 passed unmodified, `tsc --noEmit` clean, `eslint` clean, real `.flowtron/PLAN.md` parses with 0 unparsed rows (654 tasks). Header comment above the fragments rewritten to name each tolerance's owning constant. Files: `viz/src/parser.ts` only (~23 net lines added for readability, no test changes). Doc-drift: no change (SPEC documents the grammar, not the implementation).

**Archived:** 2026-08-04
