---
title: picker UI
status: in-progress
tags: []
created: 2026-05-17
due:
related-tasks: [FE-EPIC-033, FE-033.3, FE-033.4, FE-033.6]
---

# FE-033.5 | picker UI

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-033]]

## 🎯 Goal

Add a Palette radio-group fieldset to `SettingsModal` (between Density and Detail panel), extend `visibilityPrefs` with a `palette` field (`'default' | 'linear' | 'github'`), bump the schema to `version: 2`, and cover both in `visibilityPrefs.test.ts` + `App.test.tsx`.

## ✅ Acceptance

- [ ] Palette fieldset (radio group, 3 options: Default / Linear / GitHub) appears in `SettingsModal` between Density and Detail panel
- [ ] `visibilityPrefs.ts` schema bumped to `version: 2`; parser accepts v1 and migrates to v2 on read
- [ ] `visibilityPrefs.test.ts`: round-trip test uses `version: 2`; migration test (v1 stored → reads back as v2) passes
- [ ] `App.test.tsx`: palette-modes describe block covers 3 radios render, selection, reset, and per-project reload

## 🧩 Subtasks

- [ ] `visibilityPrefs.ts`: bump `version: 1 → 2` in interface, DEFAULT_PREFS, and return value; update parser to accept `d.version === 1 || d.version === 2`
- [ ] `visibilityPrefs.test.ts`: update round-trip `next: VisibilityPrefs` to `version: 2`; add "migrates stored v1 data to v2 on read" test
- [ ] `SettingsModal.tsx`: import `PaletteName`; add `PALETTE_LABEL` + `PALETTE_KEYS` + `setPalette`; insert Palette fieldset after Density fieldset
- [ ] `App.test.tsx`: add `describe('App — palette modes', ...)` mirroring density-modes pattern (3 radios + default, selection, reset, per-project)
- [ ] `npm test --prefix viz` + `npm run typecheck --prefix viz`; request visual confirmation

## 🔗 Related

- [[FE-EPIC-033]] — parent epic: theme-system
- [[FE-033.3]] — predecessor: linear palette (curation)
- [[FE-033.4]] — predecessor: github palette (curation)
- [[FE-033.6]] — follow-up: audit subtask

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** FE-033.2 (palette architecture) did the heavy lifting — `PaletteName`, `palette` field in `VisibilityPrefs`, `isPalette` guard, parser back-fill, `usePalette()` hook, and palette tests are all in place. This task adds the picker UI and the explicit schema version bump that FE-033.2 deferred.

- [x] Read relevant source files (`visibilityPrefs.ts`, `visibilityPrefs.test.ts`, `SettingsModal.tsx`, `App.test.tsx`, `VisibilityContext.tsx`)
- [x] **Archive skim** — `archive/frontend/FE-033.2.md` reviewed; load-bearing finding in Discovery Notes
- [x] **Drift check** — see Discovery Notes
- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

### Archive skim

`archive/frontend/FE-033.2.md` (palette architecture, 2026-05-17) — direct predecessor. Acceptance criterion explicitly states: "version stays 1 (`.5` bumps to `2`)"; Implementation Notes confirm: "Schema stays `version: 1` per the locked decision — `.5` bumps to `2` when picker UI lands." The migration strategy is also locked: parser accepts both v1 and v2, always returns version: 2 (same as the existing density back-fill pattern). No other FE archive tasknotes touch these paths.

### Source files reviewed

- `viz/src/visibilityPrefs.ts` — `palette: PaletteName` field and `isPalette` guard already present; `version: 1` in interface, `DEFAULT_PREFS`, and `parsePrefs` return — all three locations need updating to `version: 2`.
- `viz/src/visibilityPrefs.test.ts` — 3 palette-specific tests already exist (lines 180–216); round-trip test at line 20 uses typed `VisibilityPrefs` with `version: 1` → must change to `version: 2`; all other `version: 1` occurrences are raw JSON strings inside `JSON.stringify` (migration payload tests) → stay as `version: 1`.
- `viz/src/ui/SettingsModal.tsx` — Density fieldset (lines 117–135) is the direct template for the Palette fieldset. Current layout: Row chips → Density → Detail panel → Starter context → buttons. Palette slots between Density and Detail panel.
- `viz/src/ui/App.test.tsx` — `describe('App — density modes', ...)` (lines 417–530) is the direct template for the palette-modes describe block. Palette tests will follow the same structure.
- `viz/src/ui/VisibilityContext.tsx` — `usePalette()` already reads `useContext(VisibilityContext).palette`. Picker UI just needs to update the context value via the existing `setVisibilityPrefs` setter in App.tsx.

### Drift check

PLAN.md says "extend `visibilityPrefs.palette`" — **done by FE-033.2**, no action here. PLAN.md says "cover in `visibilityPrefs.test.ts`" — **partially done by FE-033.2** (palette tests exist; round-trip needs version bump). "bump schema to `version: 2`" and "Add Palette fieldset to SettingsModal" and "`App.test.tsx` coverage" — **all deferred to this task**, confirmed by archive.

### No clarifications needed

FE-033.2 archive resolves all ambiguities: migration strategy (accept v1+v2, return v2), palette fieldset position (between Density and Detail panel per task description), radio group pattern (mirrors Density fieldset exactly), `PALETTE_LABEL` values (`{ default: 'Default', linear: 'Linear', github: 'GitHub' }`).

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — Density fieldset in `SettingsModal.tsx` (lines 117–135) and `describe('App — density modes', ...)` in `App.test.tsx` (lines 417–530) are the exact twins for Palette. `isDensity`/`isPalette` guard pattern in `visibilityPrefs.ts` is the model for the migration path.
- [x] Implemented the minimal solution (4 files changed: `visibilityPrefs.ts`, `visibilityPrefs.test.ts`, `SettingsModal.tsx`, `App.test.tsx`)
- [x] Updated/added tests for non-trivial behavior — migration test added to `visibilityPrefs.test.ts`; 4-test palette-modes describe added to `App.test.tsx`

**Implementation Notes:**

- **`visibilityPrefs.ts`**: `version: 1` → `version: 2` in interface, `DEFAULT_PREFS`, and `parsePrefs` return; parser now accepts `d.version === 1 || d.version === 2` (v1 migrates to v2 on read).
- **`visibilityPrefs.test.ts`**: round-trip `next: VisibilityPrefs` updated to `version: 2`; added "migrates stored v1 data to v2 on read" test; all existing v1 JSON payload tests remain as migration tests.
- **`SettingsModal.tsx`**: imported `PaletteName`; added `PALETTE_LABEL` record + `PALETTE_KEYS` array + `setPalette` handler; inserted Palette fieldset between Density and Detail panel — exact structural mirror of the Density fieldset (radio group, `name="palette-mode"`).
- **`App.test.tsx`**: added `describe('App — palette modes', ...)` with 4 tests (3 radios render + Default checked, Linear selection, Reset restores Default, per-project reload); scoped "Default" radio queries to `within(screen.getByRole('group', { name: 'Palette' }))` to avoid ambiguity with Density's "Default" radio; also fixed the density "renders three Density radios" test's "Default" query the same way.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npm test --prefix viz` → 125/125 pass (8 test files)
- [x] Ran lint/type-check on changed code — `npm run typecheck --prefix viz` → `tsc --noEmit` clean, zero diagnostics
- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:**

- visibilityPrefs.test.ts: 17 tests (was 16; +1 migration test). All pass.
- App.test.tsx: 24 tests (was 20; +4 palette-mode tests). All pass.
- Two transient test failures (navigateToTask timeout, expand-on-click text) appeared on the first run and cleared on the second — same intermittent timing issue noted in FE-033.2's Testing Notes.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — `README.md`: no change · `SPEC.md`: no change · `docs/MIGRATION.md`: no change · `claude/CLAUDE-snippet.md`: no change. Pre-existing drift in tasknote README "Viz lint" → deferred to FE-033.6.
- [x] Closed — PLAN.md line flipped to stub form and tasknote moved to `_project/tasknote/archive/frontend/FE-033.5.md`
- [x] Recap drafted

**Final Summary:**

Added a Palette fieldset (radio group: Default / Linear / GitHub) to `SettingsModal.tsx` between Density and Detail panel, mirroring the Density fieldset's structure. Bumped `visibilityPrefs.ts` schema from `version: 1` to `version: 2`; parser now accepts and migrates v1 stored data on read (no settings loss). `visibilityPrefs.test.ts` updated (round-trip uses v2; migration test added). `App.test.tsx` gained a 4-test palette-modes describe block; density "Default" radio query hardened with `within(group)` scoping to avoid ambiguity with the new Palette "Default" option. 125/125 tests pass; `tsc --noEmit` clean. Palette selection is live and persisted per-project; visual differentiation lands at FE-033.3 (Linear) and FE-033.4 (GitHub) — stubs alias Default for now.

**Archived:** 2026-05-17
