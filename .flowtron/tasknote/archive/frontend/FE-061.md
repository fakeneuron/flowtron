---
title: workspace-semver-filter
status: completed
tags: []
created: 2026-06-14
due:
related-tasks: []
---

# FE-061 | workspace-semver-filter

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Add a semver-validity filter to `viz/src/workspace.ts:latestReleaseTag()` so non-semver tags (e.g. `legacy-v0`, `tmp-test`) can't sort above real release tags, matching the guard already present in `tools/update-adopters.mjs`.

## ⚡ Notes

**Relevance:** Proceed — single-function patch with a clear correctness gap to close.
**Drift check:** `latestReleaseTag` at line 54 of workspace.ts matched description exactly; no drift.
**Archive skim:** No prior FE tasknotes touch workspace.ts semver handling.
**Pattern survey:** `update-adopters.mjs:parseSemverTag` (line 72) uses `/^v(\d+)\.(\d+)\.(\d+)$/`; mirrored as a boolean regex test in the TypeScript `.find()` predicate.
**Implementation:** Changed `.find((line) => line.length > 0)` to `.find((line) => /^v\d+\.\d+\.\d+$/.test(line))` in `workspace.ts:latestReleaseTag()`; added "ignores non-semver tags" test in `workspace.test.ts`. 190/190 tests pass, typecheck and lint clean.
**Docs touched:** no change — this is an internal correctness guard with no user-facing API change.

## ✅ Recap

Added `/^v\d+\.\d+\.\d+$/` filter to `workspace.ts:latestReleaseTag()` so non-semver tags (e.g. `legacy-v0`, `tmp-test`) cannot sort above real release tags. Mirrors the guard already in `tools/update-adopters.mjs`. One new test case added and all 190 tests pass.

**Archived:** 2026-06-14
