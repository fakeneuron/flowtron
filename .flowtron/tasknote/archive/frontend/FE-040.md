---
title: task-row-expand-affordance
status: in-progress
tags: []
created: 2026-05-19
due:
related-tasks: []
---

# FE-040 | task-row-expand-affordance

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Add a persistent visual affordance to expanded list-view task rows — chevron rotation or a left-border accent — so a scrolled reader can always tell which row is currently expanded.

## ✅ Acceptance

- [ ] `<Chevron expanded={isExpandedDetail} />` renders inside the toggle button in `TaskRowInner.tsx`
- [ ] Chevron rotates 90° when expanded, returns to default when collapsed
- [ ] No regression in keyboard nav, existing click-to-expand behaviour, or other list-view features
- [ ] Passes lint/type-check and existing test suite

## 🧩 Subtasks

- [ ] Import `Chevron` in `TaskRowInner.tsx`
- [ ] Render `<Chevron expanded={isExpandedDetail} />` as first child of toggle button (before task ID span)
- [ ] Run `npm --prefix viz run lint && npm --prefix viz run typecheck`
- [ ] Run `npm --prefix viz run test`
- [ ] Start dev server, 👁️ visual confirmation

## 🔗 Related

(none)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** `Chevron.tsx` exists and is already used in EpicRow and PrioritySection for the same expand/collapse pattern. Only `TaskRowInner.tsx` needs the import + render — no new component needed.

- [x] Read relevant source files
- [x] **Archive skim** — `archive/fe/` absent; no prior FE tasknotes.
- [x] **Drift check** — all identifiers confirmed: `expandedId` in `TaskRow.tsx:49`, `isExpandedDetail` in `TaskRowInner.tsx:29`, `Chevron.tsx:3`. Description is accurate.
- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**
- Using chevron (not left-rule): `Chevron.tsx` already handles rotation; left-rule would need new CSS.
- `Chevron` goes inside the existing `<button>` in `TaskRowInner.tsx` (before task ID span) — same click target, no new click target needed.
- `aria-expanded={isExpandedDetail}` already on the button; `Chevron` is `aria-hidden` so no a11y change.
- Existing tests use `getByRole('button', { expanded: true/false })` — unaffected by visual-only Chevron addition.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — `Chevron.tsx` is already used in `EpicRow.tsx` and `PrioritySection.tsx` for the same expand/collapse pattern; extended it.
- [x] Implemented the minimal solution
- [x] Updated/added tests for non-trivial behavior — `Chevron` is `aria-hidden`; no behaviour change, no new test needed; existing 154 tests pass.

**Implementation Notes:**
- Added `import { Chevron } from './Chevron';` to `TaskRowInner.tsx`
- Added `<Chevron expanded={isExpandedDetail} />` as first child of toggle `<button>`, before task ID span
- 2 lines added, 0 removed

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — 154/154 passed
- [x] Ran lint/type-check on changed code — clean
- [x] (frontend) Asked the user for visual confirmation — chevron `▶` rotates to `▼` on expand; confirmed via Playwright screenshot

**Testing Notes:**
- Switched to List view, clicked FE-040, chevron rotated as expected
- FE-041–043 remained `▶` (collapsed) — no bleed

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — README.md: no change · SPEC.md: no change · docs/MIGRATION.md: no change · claude/CLAUDE-snippet.md: no change · docs/CONVENTIONS.md: no change · CONTRIBUTING.md: no change
- [x] Closed — PLAN.md line flipped to stub form and tasknote moved to `_project/tasknote/archive/frontend/`
- [x] Recap drafted

**Final Summary:**
Wired the existing `Chevron` component into `TaskRowInner.tsx`'s toggle button so it rotates 90° (`▶` → `▼`) when a row is expanded. 2-line change; no new components; all 154 tests pass.

**Archived:** 2026-05-19
