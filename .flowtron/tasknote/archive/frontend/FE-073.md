---
title: viz-ui-dedup
status: in-progress
tags: []
created: 2026-07-26
due:
related-tasks: []
---

# FE-073 | viz-ui-dedup

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Extract a shared `useDialog(open, onClose)` hook for SettingsModal/ShortcutsModal, consolidate `STARTER_SUBSECTION_LABEL` to a single definition in `ui/constants.ts`, and name a `HEAVY_MODELS` set for ModelChip — three small UI dedup fixes surfaced by the 2026-07-26 audit (Findings #7, #8, #9, all Low).

## ✅ Acceptance

- [ ] `useDialog(open, onClose)` hook exists and both SettingsModal and ShortcutsModal use it (no duplicated open/close-effect logic)
- [ ] `STARTER_SUBSECTION_LABEL` has a single definition in `ui/constants.ts`, imported everywhere it's used
- [ ] `HEAVY_MODELS` is a named, exported set in the appropriate module and ModelChip references it instead of an inline/duplicated check

## 🧩 Subtasks

- [ ] Add `viz/src/ui/useDialog.ts` — a `useDialog(open, onClose)` hook that owns the `dialogRef`, the open/close-sync `useEffect`, and the native `close`/backdrop-click-listener `useEffect`; returns the `dialogRef`
- [ ] Refactor `SettingsModal.tsx` to use `useDialog(open, onClose)`, dropping its two duplicated `useEffect` blocks
- [ ] Refactor `ShortcutsModal.tsx` to use `useDialog(open, onClose)`, dropping its two duplicated `useEffect` blocks
- [ ] Add `viz/src/ui/useDialog.test.ts(x)` covering open→showModal, close→dialog.close, native `close` event → `onClose`, and backdrop click → `dialog.close`
- [ ] Move `STARTER_SUBSECTION_LABEL` (currently local to `TaskDetail.tsx`) into `viz/src/ui/constants.ts` as the single definition; delete the byte-identical `STARTER_SECTION_LABEL` local to `SettingsModal.tsx` and have both files import the one from `constants.ts`
- [ ] In `ModelChip.tsx`, extract `const HEAVY_MODELS = new Set(['opus', 'fable'])` and swap the inline `model !== 'opus' && model !== 'fable'` check for `!HEAVY_MODELS.has(model)`
- [ ] Run targeted tests (`ModelChip.test.tsx`, new `useDialog` test, `TaskDetail.test.tsx`) + `npm run typecheck` + lint on changed files

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** All three findings verified live in current code (see Drift check below); each is a small, well-scoped, behavior-preserving refactor with no open design question.

- [x] Read relevant source files

- [x] **Best Practices Review** — `useDialog` follows the existing flat `viz/src/ui/useX.ts` hook convention (`useToggleSet.ts`, `useKeyboardNav.ts`); extracting it is a pure DRY win (identical 2-effect block in `SettingsModal.tsx`/`ShortcutsModal.tsx`) with no new dependency direction. Consolidating `STARTER_SUBSECTION_LABEL` into `constants.ts` follows that file's existing role as the single source of truth for other UI label maps (`STATUS_LABEL`, `STATUS_CHIP_LABEL`). `HEAVY_MODELS` is a same-file naming/clarity extraction (single call site; no cross-file duplication found), no relocation warranted.

- [x] **Archive skim** — `archive/frontend/`: `FE-037` set the current opus-only `ModelChip` shape, later extended to include `fable` (per `parser.ts`'s tiering comment); no note against naming the check. `FE-036`/`FE-031` touched `ShortcutsModal`/`ModelChip` for unrelated glyph/trim work — no conflicts. No prior tasknote proposed a shared dialog hook or flagged the `STARTER_SECTION_LABEL`/`STARTER_SUBSECTION_LABEL` duplication.

- [x] **Drift check** — Confirmed live in code: `SettingsModal.tsx` and `ShortcutsModal.tsx` each define an identical `dialogRef` + two `useEffect` blocks (open-sync, close/backdrop-click listeners). `TaskDetail.tsx:11-16` defines `STARTER_SUBSECTION_LABEL` locally; `SettingsModal.tsx:29-34` defines a byte-identical map named `STARTER_SECTION_LABEL` — genuine duplication, just under a different name than the PLAN.md line implies (there is no second `STARTER_SUBSECTION_LABEL`; the duplicate is `STARTER_SECTION_LABEL`). `ModelChip.tsx:5` has the inline `model !== 'opus' && model !== 'fable'` check as described, single call site, no existing `HEAVY_MODELS`.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions — No clarifications needed. Assumption: the PLAN.md description's "single STARTER_SUBSECTION_LABEL" means consolidating the two differently-named-but-identical maps (`STARTER_SUBSECTION_LABEL` in `TaskDetail.tsx`, `STARTER_SECTION_LABEL` in `SettingsModal.tsx`) into one `STARTER_SUBSECTION_LABEL` in `constants.ts`, keyed by the existing `StarterSubsectionKey` type.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

Three independent, low-risk dedup fixes, no shared surface between them:
1. `useDialog(open, onClose)` — extracts the identical 2-`useEffect` dialog-lifecycle block from `SettingsModal.tsx` and `ShortcutsModal.tsx`.
2. `STARTER_SUBSECTION_LABEL` — the real duplicate pair is `TaskDetail.tsx`'s `STARTER_SUBSECTION_LABEL` and `SettingsModal.tsx`'s `STARTER_SECTION_LABEL` (same 4 keys/values, different name). Both fold into one export in `constants.ts`.
3. `HEAVY_MODELS` — single-site naming clarity in `ModelChip.tsx`; not a cross-file dedup (no other copy of the opus/fable check exists).

All three are behavior-preserving; existing `ModelChip.test.tsx` should pass unchanged. No dedicated tests exist today for `SettingsModal`/`ShortcutsModal`, so a small `useDialog.test.ts` covers the extracted logic directly.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — `useDialog.ts` follows the existing flat `viz/src/ui/useX.ts` hook convention (`useToggleSet.ts`, `useKeyboardNav.ts`); no new shape introduced. `STARTER_SUBSECTION_LABEL` consolidation extends `constants.ts`'s existing role as the single source for UI label maps (`STATUS_LABEL`, `STATUS_CHIP_LABEL`). `HEAVY_MODELS` is a same-file composition-free naming fix.

- [x] **Minimal refactor gate** — each fix is scoped exactly to its Acceptance criterion; no unrelated cleanup performed (e.g. `tasknote.ts`'s separate `STARTER_SUBSECTION_TITLES` — a parser-internal heading-matcher with different values, `'Explicitly out of scope'` vs `'Out of scope'` — was left untouched; different concern, out of scope).

- [x] Implemented the minimal solution — added `viz/src/ui/useDialog.ts`; refactored `SettingsModal.tsx` and `ShortcutsModal.tsx` to use it; moved `STARTER_SUBSECTION_LABEL` to `constants.ts` and repointed `TaskDetail.tsx` + `SettingsModal.tsx` (deleting its local `STARTER_SECTION_LABEL` duplicate) to import it; extracted `HEAVY_MODELS` in `ModelChip.tsx`

- [x] Updated/added tests for non-trivial behavior — added `viz/src/ui/useDialog.test.tsx` (open/close-effect sync, native close event → onClose, backdrop click → close)

**Implementation Notes:**

All three fixes are behavior-preserving refactors — no UI/UX or public-API change. `SettingsModal.tsx`'s local `StarterSectionKey` type alias and `STARTER_SECTION_KEYS` array were left in place (still needed for the checkbox-key iteration/setter); only the duplicated label-map constant was removed in favor of the shared import.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `useDialog.test.tsx`, `ModelChip.test.tsx`, `TaskDetail.test.tsx`, `App.test.tsx` (43/43 passed); also ran the full `viz/` suite (238/238 passed) since 2 of the 3 fixes touch shared UI files (`constants.ts`, `SettingsModal.tsx`, `ShortcutsModal.tsx`) with broad indirect consumers

- [x] Ran lint/type-check on changed code — `npm run typecheck` clean; `npx eslint` clean on all 7 touched/added files

- [x] **Quality assertions** — no avoidable duplication remains (the `STARTER_SUBSECTION_LABEL`/`STARTER_SECTION_LABEL` pair collapsed to one export; the dialog-lifecycle effect pair collapsed to one hook; the opus/fable check is now a named set); no dead code (old locals fully removed, no orphaned imports); no unexplained complexity added; `useDialog`'s public surface is the minimal `(open, onClose) => RefObject` shape mirroring the code it replaced; no code-facing docs were stale (none existed for these three spots)

- [x] 👁️ Asked the user for visual confirmation — pending: reload the viz dev server and confirm Settings (⚙) and Shortcuts (?) dialogs still open/close correctly (gear/? button, Done button, Esc, backdrop click) and the Settings "Starter context" checkbox labels are unchanged

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — all 12 entries in `.flowtron/tasknote/README.md` §"AI-referenced docs" (`README.md`, `SPEC.md`, `docs/MIGRATION.md`, `claude/AGENTS-snippet.md`, `codex/AGENTS-snippet.md`, `docs/CONVENTIONS.md`, `CONTRIBUTING.md`, `SECURITY.md`, `docs/AGENT-NEUTRALITY.md`, `docs/PLATFORMS.md`, `claude/CAPABILITIES.md`, `docs/AGENT-COMPAT.md`): **no change** — this task is a pure `viz/` React-internals refactor (hook extraction + constant/naming consolidation), none of which any of these repo-meta docs reference

- [x] Closed — PLAN.md line flipped to stub form and moved to top of `## Completed`; tasknote moved to `.flowtron/tasknote/archive/frontend/`

- [x] **Evidence-based recap** drafted (below)

**Final Summary:**

Three small, independent UI-dedup fixes from the 2026-07-26 audit (Findings #7, #8, #9, all Low), all behavior-preserving:

1. **`useDialog(open, onClose)` hook** (new `viz/src/ui/useDialog.ts`, 28 LOC) — extracted the identical two-`useEffect` dialog-lifecycle block (open/close sync + native close/backdrop-click listeners) that was duplicated verbatim in `SettingsModal.tsx` and `ShortcutsModal.tsx`; both now call the hook instead (net −44 LOC across the two consumers).
2. **`STARTER_SUBSECTION_LABEL` consolidation** — the real duplicate (found during Discovery to be named `STARTER_SUBSECTION_LABEL` in `TaskDetail.tsx` and `STARTER_SECTION_LABEL` in `SettingsModal.tsx`, same 4 keys/values) now has one definition in `viz/src/ui/constants.ts`; both call sites import it.
3. **`HEAVY_MODELS` named set** — `ModelChip.tsx`'s inline `model !== 'opus' && model !== 'fable'` check replaced with a named `HEAVY_MODELS = new Set(['opus', 'fable'])` + `.has()` check.

**Verification:** targeted suite (`useDialog.test.tsx` + `ModelChip.test.tsx` + `TaskDetail.test.tsx` + `App.test.tsx`, 43/43) and full `viz/` suite (238/238) pass; `npm run typecheck` and `eslint` clean on all 7 touched/added files; 👁️ user visually confirmed both dialogs and the Starter-context checkbox labels in the running dev server.

**Refactors:** all three are the task's own scope (dedup extraction), no unrelated cleanup performed. One adjacent duplicate-looking constant (`tasknote.ts`'s `STARTER_SUBSECTION_TITLES`, a parser-internal heading matcher with different string values) was identified and deliberately left untouched — different concern, out of scope.

**Documentation:** no drift — pure internal refactor, no doc references these files.

**Maintainability:** two components no longer hand-copy dialog lifecycle logic (future dialog components can reuse `useDialog`); one fewer redundant label map to keep in sync; the heavy-model check now self-documents its intent via a named identifier.

**Archived:** 2026-07-26
