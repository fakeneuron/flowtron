---
title: palette picker shows unimplemented options
status: in-progress
tags: []
created: 2026-05-17
due:
related-tasks: [FE-EPIC-033, FE-033.4, FE-033.5]
---

# FE-035 | palette picker shows unimplemented options

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-033]] · [[FE-033.4]] · [[FE-033.5]]

## 🎯 Goal

Remove the unimplemented `github` radio from the Settings palette picker so the picker offers only options that produce visible differentiation; the `'github'` value stays in the `PaletteName` type and parser so FE-033.4 can re-enable the radio by adding one array element back.

## ✅ Acceptance

- [ ] `PALETTE_KEYS` in `SettingsModal.tsx` no longer lists `'github'`; the Palette fieldset renders only Default + Linear
- [ ] One-line WHY comment above `PALETTE_KEYS` points at FE-033.4 as the re-enable trigger
- [ ] `PaletteName` type, `isPalette` guard, `PALETTES['github']` entry, and `PALETTE_LABEL['github']` all stay (no breaking change for users with `palette: 'github'` already in localStorage)
- [ ] `App.test.tsx` palette-modes describe updated: "renders three Palette radios" → "renders two Palette radios" (drop GitHub lookup + assertion); per-project test's stored payload switches from `palette: 'github'` to `palette: 'linear'` so it asserts a still-rendered radio
- [ ] `npm test --prefix viz` 125/125 pass; `npm run typecheck --prefix viz` clean
- [ ] User 👁️ visually confirms the picker now shows two radios (Default / Linear)

## 🧩 Subtasks

- [ ] `viz/src/ui/SettingsModal.tsx`: change `PALETTE_KEYS: PaletteName[] = ['default', 'linear', 'github']` → `['default', 'linear']`; add one-line comment above pointing at FE-033.4 as the re-enable trigger
- [ ] `viz/src/ui/App.test.tsx`: rename the first palette-modes test from "renders three Palette radios" → "renders two Palette radios"; drop the `getByRole('radio', { name: 'GitHub' })` lookup and the `expect(github).not.toBeChecked()` assertion
- [ ] `viz/src/ui/App.test.tsx`: in the "per-project: switching projects reloads palette" test, change the stored `palette: 'github'` to `palette: 'linear'` and the final assertion `getByRole('radio', { name: 'GitHub' })` to `getByRole('radio', { name: 'Linear' })`
- [ ] `npm run typecheck --prefix viz` + `npm test --prefix viz` — both must be clean / 125 pass
- [ ] Visual confirmation: bring up viz, open Settings, confirm Palette fieldset shows two radios (Default / Linear), no GitHub
- [ ] Phase 4 closure: doc-drift sweep (4-entry walk; all expected "no change"), flip PLAN.md FE-035 line to stub, move tasknote to `_project/tasknote/archive/frontend/`, recap for 📦 gate

## 🔗 Related

- [[FE-EPIC-033]] — parent epic (theme-system)
- [[FE-033.4]] — re-enable trigger: when GitHub curation ships, add `'github'` back to `PALETTE_KEYS` (one-line revert)
- [[FE-033.5]] — picker UI shipped (surfaced this issue at FE-033.3 visual review)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** PLAN.md frames this as "Discovery scopes the call" between two alternatives (hide unimplemented vs. accelerate FE-033.4). The hide path is a 4-line diff and a one-line revert when FE-033.4 ships; the accelerate path is a real opus-tier curation already filed as FE-033.4. Hide wins on minimal-mechanism + reversibility grounds (locked via clarifying question below).

- [x] Read relevant source files (`viz/src/ui/SettingsModal.tsx`, `viz/src/ui/constants.ts`, `viz/src/visibilityPrefs.ts`, `viz/src/ui/App.test.tsx`)
- [x] **Archive skim** — see Discovery Notes
- [x] **Drift check** — see Discovery Notes (no drift; PLAN.md description matches current code exactly)
- [x] Asked clarifying questions — 1 surfaced via AskUserQuestion; user picked the recommended option (hide)
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

### Source files reviewed

- `viz/src/ui/SettingsModal.tsx` — the picker source. `PALETTE_KEYS: PaletteName[] = ['default', 'linear', 'github']` at line 57; fieldset rendered at lines 146-164 maps over PALETTE_KEYS into 3 radios. `PALETTE_LABEL` at lines 42-46 includes all three labels.
- `viz/src/ui/constants.ts` — `PALETTES.github = DEFAULT_PALETTE` at line 160 (alias; confirms the "3 options, 2 distinct visuals" framing). `LINEAR_PALETTE` is a real curated literal at lines 116-155; `DEFAULT_PALETTE` at 75-114.
- `viz/src/visibilityPrefs.ts` — `PaletteName = 'default' | 'linear' | 'github'` (line 2); `isPalette` guard accepts all three (lines 51-52); parser accepts and persists all three. Stored prefs with `palette: 'github'` survive the hide because the type + guard + storage path stay intact.
- `viz/src/ui/App.test.tsx` — palette-modes describe at lines 534-639. Four tests: "renders three Palette radios" (asserts 3 radios incl. GitHub), "selecting Linear" (unchanged), "Reset to defaults restores Default" (uses Linear as the stored selection, unchanged), "per-project: switching projects reloads palette" (uses `palette: 'github'` to verify per-project isolation; needs the stored value swapped to `'linear'` since GitHub radio no longer renders).

### Archive skim — load-bearing precedents (frontend/)

- **`FE-033.5`** (picker UI, 2026-05-17) — direct predecessor. Shipped the SettingsModal Palette fieldset that surfaces this issue; closure summary explicitly notes "stubs alias Default for now" (Linear and GitHub both, at the time). Linear has since been curated by FE-033.3, leaving GitHub as the only alias.
- **`FE-033.3`** (linear palette, 2026-05-17) — filed FE-035 in its closure ("filed three out-of-scope follow-ups surfaced during review: FE-035..."). Acceptance line cites the user verdict that surfaced this. Confirms this task's framing matches the original observation.
- **`FE-033.2`** (palette architecture, 2026-05-17) — established the `PALETTES` record + `PaletteTokens` shape that makes hiding trivial. `PALETTES.github = DEFAULT_PALETTE` was the stub pattern locked here.

No other FE archive tasknotes touch `SettingsModal.tsx` palette code.

### Drift check

PLAN.md description cites: "picker lists 3 palettes today" (✓ confirmed: PALETTE_KEYS = ['default', 'linear', 'github']); "`github` aliases `default` until FE-033.4 ships" (✓ confirmed: `PALETTES.github = DEFAULT_PALETTE` at constants.ts:160); "3 options, 2 distinct visuals" (✓ confirmed: Default + Linear are distinct, GitHub renders as Default). No drift.

### Clarifying-question outcome (resolved 2026-05-17)

Surfaced 1 question via AskUserQuestion with three options: (A) Hide unimplemented; (B) Disable + "(coming soon)" label; (C) Accelerate FE-033.4 in this task. User picked **A (hide)**.

**Rationale for A over B over C:**
- **A vs B:** B previews the roadmap but adds a new per-palette `implemented: boolean` mechanism (or equivalent) for a problem with one offender today. A is a one-line diff with a one-line revert when FE-033.4 ships. Avoids the "design for hypothetical future requirements" anti-pattern.
- **A vs C:** C is real opus-tier design+impl work already filed as its own task (FE-033.4). Doing it here would conflate the alias-leak UX fix with the GitHub palette curation. Also doesn't *prevent* the alias-leak pattern from recurring if a 4th palette were added.
- **Why hide is non-lossy:** stored prefs with `palette: 'github'` keep parsing successfully (type, guard, PALETTES record entry all stay); the picker just stops *offering* GitHub as a new selection. Reset to defaults already lands on `palette: 'default'`. No data migration required.

### Expected closure-diff signal profile (informs Step 6 📦 evaluation)

- Frontend files: **yes** (2 files: `viz/src/ui/SettingsModal.tsx` + `viz/src/ui/App.test.tsx`)
- Privileged-ops paths: no
- Perf-narrative: no (bundle delta near-zero — one string drops out of an array literal)
- → **📦 fire branch** — frontend signal hits. Closure surfaces the bundled ready-to-commit gate with the visual-confirmation request inside.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — `PALETTE_KEYS` mirrors the sibling `DENSITY_KEYS` (same `Type[] = [...]` shape sourced from a string-union); no existing precedent for "hide a member of a string-union from a UI list" — minimal-mechanism wins (drop the entry from the array literal; the WHY-comment carries the dependency cite)
- [x] Implemented the minimal solution — 2 files changed: `SettingsModal.tsx` (1 array literal + 1 comment line) and `App.test.tsx` (2 tests updated)
- [x] Updated/added tests for non-trivial behavior — no new tests; existing palette-modes describe block updated to match the 2-radio reality

**Implementation Notes:**

- **`viz/src/ui/SettingsModal.tsx`** (line 57-58): `PALETTE_KEYS: PaletteName[] = ['default', 'linear', 'github']` → `['default', 'linear']`; added one-line WHY-comment above pointing at FE-033.4 as the re-enable trigger. Re-enabling on .4's ship is a one-element array push + comment delete.
- **`viz/src/ui/App.test.tsx`** test 1 (line ~556): "renders three Palette radios" → "renders two Palette radios"; dropped the `getByRole('radio', { name: 'GitHub' })` lookup and the corresponding `not.toBeChecked()` assertion.
- **`viz/src/ui/App.test.tsx`** test 4 (line ~620, per-project isolation): stored payload's `palette: 'github'` → `palette: 'linear'`; final assertion `getByRole('radio', { name: 'GitHub' })` → `getByRole('radio', { name: 'Linear' })`. Test purpose (per-project palette persistence) is hue-agnostic; swapping to Linear keeps the assertion targeting a still-rendered radio.
- **What stayed**: `PaletteName` type, `isPalette` guard, `PALETTES.github` entry, `PALETTE_LABEL.github` entry. Stored prefs with `palette: 'github'` keep parsing through `parsePrefs`; the picker just stops *offering* GitHub as a new selection. No data migration.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npm test --prefix viz` → **125/125 pass** (8 test files)
- [x] Ran lint/type-check on changed code — `npm run typecheck --prefix viz` → `tsc --noEmit` clean, zero diagnostics
- [x] (frontend) Asked the user for visual confirmation — user 👁️ confirmed picker shows two radios (Default / Linear), no GitHub

**Testing Notes:**

- App.test.tsx palette-modes describe: still 4 tests, all pass (renames + assertion swaps only; no test added/removed).
- visibilityPrefs.test.ts: 17 tests, all pass (no changes — still exercises `palette: 'github'` round-trip / fallback at the storage layer, which is intentional since the type + parser still accept it).
- Two stderr lines from `archiveCache.test.ts` are pre-existing fixture noise (deliberately malformed YAML to test error handling); not introduced by this change.
- Dev server started on port 5120 (vite strictPort); visual confirmation completed against the live picker.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — `README.md` no change · `SPEC.md` no change · `docs/MIGRATION.md` no change · `claude/CLAUDE-snippet.md` no change. (Closure diff is a 2-file viz-only UX paper-cut fix; none of the 4 AI-referenced docs reference palette-picker mechanics or palette enumeration.)
- [x] Closed — PLAN.md line flipped to stub `[x] **FE-035** [opus] | palette picker shows unimplemented options — Completed 2026-05-17.` and moved to `## Completed`; tasknote moved to `_project/tasknote/archive/frontend/FE-035.md`
- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate below)

**Final Summary:**

Dropped `'github'` from the Settings palette picker so users no longer see a third radio that resolves to a visual duplicate of Default — the picker now offers only Default + Linear, the two palettes with curated tokens today. `PaletteName` type, `isPalette` guard, and the `PALETTES.github` alias all stay intact so stored prefs with `palette: 'github'` keep parsing; the change is picker-only. When FE-033.4 ships the curated GitHub palette tokens, re-enabling is a one-element push back into `PALETTE_KEYS` plus deleting the inline WHY-comment. Decision (hide vs. accelerate FE-033.4 vs. disable + "coming soon" label) locked via clarifying question — hide won on minimal-mechanism + reversibility grounds.

**Files touched:** `viz/src/ui/SettingsModal.tsx` (−1 array element, +1 comment line) · `viz/src/ui/App.test.tsx` (2 tests updated: 3-radios→2-radios assertion; per-project test stored palette swapped `'github'`→`'linear'`) · `_project/PLAN.md` (FE-035 line flipped to stub, moved to `## Completed`) · tasknote moved from `_project/tasknote/FE-035.md` to `_project/tasknote/archive/frontend/FE-035.md`.

**Verification gates:** `npm run typecheck --prefix viz` → 0 diagnostics · `npm test --prefix viz` → 125/125 pass · user 👁️ visual confirmation pass.

**Archived:** 2026-05-17
