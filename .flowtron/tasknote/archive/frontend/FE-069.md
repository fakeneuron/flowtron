---
title: viz-deexport-internal-types
status: in-progress
tags: []
created: 2026-07-25
due:
related-tasks: []
---

# FE-069 | viz-deexport-internal-types

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Drop `export` on five internal-only viz types (`VersionCurrency`, `DensityTokens`, `PlanParseResult`, `ProjectInfo`, `ProjectsResponse`) that are never imported outside their defining file, keeping their paired exported values (functions/consts) exported.

## ⚡ Notes

**Relevance:** Proceed — mechanical de-export, exactly as scoped by the audit finding; no design tradeoffs.
**Best Practices Review:** N/A — pure visibility narrowing, no responsibility or dependency-direction change; no refactor needed.
**Drift check:** All five paths/names still match current code: `viz/src/parser.ts:172` (`PlanParseResult`), `viz/src/ui/utils.ts:31` (`VersionCurrency`), `viz/src/ui/constants.ts:213` (`DensityTokens`), `viz/src/ui/useProjects.ts:4,9` (`ProjectInfo`, `ProjectsResponse`).
**Archive skim:** `archive/frontend/` — no prior tasknotes touch these five type declarations directly (FE-063.2/FE-067 touched adjacent parser.ts logic, not this type's visibility).
**Pattern survey:** N/A — one-line visibility change per type, no new shape introduced.
**Implementation:** Confirmed via grep that none of the five types are referenced outside their own file. Removed the `export` keyword from each of the five interface/type declarations. Their paired exported values (`parsePlanWithDiagnostics`, `DENSITY_TOKENS`, `useProjects`) remain exported unchanged.
**Docs touched:** no change — internal type visibility, not AI-referenced doc surface.

## ✅ Recap

Dropped `export` on 5 internal-only viz types across 4 files (`viz/src/parser.ts`, `viz/src/ui/utils.ts`, `viz/src/ui/constants.ts`, `viz/src/ui/useProjects.ts`), confirmed via grep each type has zero cross-file references before editing. Paired exported values (`parsePlanWithDiagnostics`, `DENSITY_TOKENS`, `useProjects`) untouched. `tsc --noEmit` passes clean. No behavior change — visibility-only cleanup from ft-audit Finding #3.

**Archived:** 2026-07-25
