---
title: sse-heartbeat-extract
status: completed
tags: [viz, sse]
created: 2026-09-13
due:
related-tasks: []
touches:
  - viz/vite.config.ts
  - viz/src/flowtronWatch.ts
  - viz/src/flowtronWatch.test.ts
---

# FE-120 | sse-heartbeat-extract

[← PLAN.md](../PLAN.md) · ✅ Completed

## 🎯 Goal

Extract the inline SSE heartbeat (`SSE_HEARTBEAT_MS`, the `setInterval`/`: ping` writer, and its `close` teardown) out of `viz/vite.config.ts` into a `createHeartbeat` helper beside `createChangeBroadcaster` in `flowtronWatch.ts`, with fake-timers test coverage.

## ⚡ Notes

**Relevance:** Proceed — task line still matches current code exactly (line numbers, names, file); no re-scope needed.
**Best Practices Review:** `vite.config.ts` is the one Node-tier file outside the vitest suite (per audit finding); its sibling SSE constants/broadcaster already live in `flowtronWatch.ts` and are unit-tested there with fake timers. Extracting the heartbeat closes that testability gap and keeps `flowtronApi()`'s closure to wiring only. Direct precedent: FE-028 extracted `archiveCache` out of the same plugin closure for the identical reason (module-scope state → separate testable module).
**Drift check:** no drift — `SSE_HEARTBEAT_MS` (line 34), the `setInterval` (lines 103-105), and the `close` teardown (lines 107-115) in `viz/vite.config.ts` match the PLAN.md line verbatim.
**Archive skim:** FE-011 introduced the heartbeat (30s `: ping`, cleanup on `httpServer.close`) alongside the original SSE/watcher pattern. FE-028 established the extraction precedent this task follows (`archiveCache.ts`, same closure). No prior tasknote attempted the heartbeat extraction itself.
**Declared scope:** see YAML `touches:` above.
**Pattern survey:** Extended `createChangeBroadcaster`'s shape exactly — a `create*` factory taking `{ sseClients, ms }`-style opts and returning an object with lifecycle methods (`start`/`stop`), tested with `vi.useFakeTimers()` / `vi.advanceTimersByTime` matching the existing `describe('createChangeBroadcaster ...')` block.
**Implementation:** Added `createHeartbeat(sseClients, ms)` to `flowtronWatch.ts`, returning `{ start(), stop() }`; `start()` is idempotent (no-op if already running) and `stop()` clears the interval and is safe to call when not running. Moved `SSE_HEARTBEAT_MS` from `vite.config.ts` into `flowtronWatch.ts` (single definition, re-exported — not duplicated) and wired the plugin in `vite.config.ts` to call `heartbeat.start()` / `heartbeat.stop()` instead of managing the interval inline. Added a `describe('createHeartbeat (FE-120)')` block to `flowtronWatch.test.ts` (4 tests, using the existing fake-timers setup): writes `: ping\n\n` to every client on each tick, `stop()` clears the interval, `start()` is idempotent, `stop()` before `start()` is a safe no-op. Verification receipt: `npm --prefix viz test` → 0 (556 passed, incl. the 4 new); `npm --prefix viz run typecheck` → 0; `npm --prefix viz run lint` → 0.
**Docs touched:** no change — `viz/README.md`'s module-tier table already groups `vite.config.ts` with `src/*.ts` under "Node-only dev API" and still holds true after this extraction; it describes plugin wiring generally, not this specific heartbeat detail.

## ✅ Recap

Extracted the SSE heartbeat out of `viz/vite.config.ts`'s `flowtronApi()` closure into `createHeartbeat(sseClients, ms)` in `viz/src/flowtronWatch.ts`, mirroring the `createChangeBroadcaster` factory shape. `SSE_HEARTBEAT_MS` moved with it (no longer duplicated). The plugin now calls `heartbeat.start()` in `configureServer` and `heartbeat.stop()` in the `httpServer.on('close', ...)` teardown, replacing the inline `setInterval`/`clearInterval` pair. Added 4 fake-timers tests to `flowtronWatch.test.ts` covering tick behavior, `stop()`, and `start()` idempotency — `vite.config.ts` no longer carries any untested Node logic of its own for this piece.

`touches:` reconciliation — `git diff --name-only` matches the declared scope exactly (`viz/vite.config.ts`, `viz/src/flowtronWatch.ts`, `viz/src/flowtronWatch.test.ts`); no undeclared paths touched.

Verification: `npm --prefix viz test` (556 passed), `npm --prefix viz run typecheck` (clean), `npm --prefix viz run lint` (clean).

**Archived:** 2026-09-13
