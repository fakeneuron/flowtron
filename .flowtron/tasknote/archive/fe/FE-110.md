---
title: sse-head-and-archive-prefix
status: completed
tags: []
created: 2026-09-12
due:
related-tasks: []
touches:
  - viz/src/devApi.ts
  - viz/src/devApi.test.ts
  - viz/src/archiveCache.ts
  - viz/src/archiveCache.test.ts
  - viz/src/watchSet.ts
  - viz/src/watchSet.test.ts
---

# FE-110 | sse-head-and-archive-prefix

[← PLAN.md](../PLAN.md) · ✅ Completed

## 🎯 Goal

HEAD `/api/events` ends the response without registering an SSE client, and
`archiveCache.invalidate` / `watchSet.projectForPath` match archive paths on a
directory boundary (`archiveDir + sep`) instead of a raw string prefix.

## ✅ Acceptance

- [x] A HEAD request to `/api/events` gets a response and does not occupy an SSE client slot — `npm --prefix viz test -- devApi.test.ts`
- [x] `archiveCache.invalidate` no longer false-matches a path that merely shares `archiveDir` as a string prefix without being inside it — `npm --prefix viz test -- archiveCache.test.ts`
- [x] `watchSet.projectForPath` no longer false-matches a path that merely shares `archiveDir` as a string prefix without being inside it — `npm --prefix viz test -- watchSet.test.ts`
- [x] Typecheck and lint pass on changed files — `npm --prefix viz run typecheck` / `npm --prefix viz run lint`

## 🧩 Subtasks

- [x] In `createEventsHandler` (`viz/src/devApi.ts`), branch on `req.method === 'HEAD'`: set the same SSE headers, `res.end()`, and return before `sseClients.add` / preamble write (capacity check stays first, unaffected)
- [x] Add a devApi test: HEAD `/api/events` ends the response and `sseClients.size` stays 0
- [x] In `archiveCache.ts` `invalidate`, import `sep` from `node:path` and match `filepath.startsWith(p.archiveDir + sep)`
- [x] In `watchSet.ts` `projectForPath`, same `archiveDir + sep` fix
- [x] Add a regression test per file: a sibling path that string-prefixes `archiveDir` but sits outside it (e.g. `archiveDir + '-other/x'`) must not match

## 🔗 Related

(none)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** PLAN.md line matches current code exactly — `createEventsHandler` in `viz/src/devApi.ts` registers HEAD requests as SSE clients identically to GET, and both `archiveCache.invalidate` (`viz/src/archiveCache.ts`) and `watchSet.projectForPath` (`viz/src/watchSet.ts`) use a raw `filepath.startsWith(p.archiveDir)` with no separator boundary.

- [x] Read relevant source files — `viz/src/devApi.ts` (`createEventsHandler`, `guarded`, `methodGuard`), `viz/src/archiveCache.ts` (`invalidate`), `viz/src/watchSet.ts` (`projectForPath`), plus their test files for existing helper/fixture conventions.

- [x] **Best Practices Review** — both prefix-match sites are the same latent bug shape: `String.startsWith` on a directory path with no trailing separator lets a sibling directory whose name extends `archiveDir` (e.g. `archive-other`) false-match. The fix is a one-token boundary (`archiveDir + sep`), matching the existing containment-safety lineage in these files (FE-088.2's realpath containment, FE-101.5's cache bound) rather than introducing a new pattern. No broader refactor warranted — both sites stay independent (`archiveCache.ts` has no dependency on `watchSet.ts` or vice versa).

- [x] **Archive skim** — `viz/src/devApi.ts`, `viz/src/archiveCache.ts`, and `viz/src/watchSet.ts` each have a long prior-tasknote history (20+ hits) dominated by unrelated feature work; the load-bearing precedents are already cited inline in the source comments read during Discovery (FE-088.2 realpath containment, FE-101.5 MAX_CACHED_PROJECTS bound, FE-047 error-detail/log split). No prior tasknote specifically addressed a `startsWith` directory-boundary bug in either file. `<area>` = `fe` confirmed against the README table (not derived).

- [x] **Drift check** — line numbers and function names in the PLAN.md description match current code exactly (`createEventsHandler` devApi.ts:218, `invalidate` archiveCache.ts:59, `projectForPath` watchSet.ts:49). No SPEC contract or PLAN.md line divergence.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  No clarifications needed (--fast). Assumption: a HEAD response should carry the same headers a GET would set (Content-Type/Cache-Control/Connection) so a capability probe reading headers gets accurate values, but with no body and no client-slot registration — matching plain HTTP HEAD semantics.

- [x] Subtasks above populated with concrete, ordered steps, and YAML `touches:` declared with the paths this task expects to edit

**Discovery Notes:**

Both fixes are mechanical, clear-diff, single-responsibility edits confined to
the three named functions. No shared abstraction needed between the two
`startsWith` sites — they're unrelated modules with the same bug shape,
independently precedented. Judgment: Discovery surfaced no significant
deviation → skip 🛠️.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended the existing `archiveDir` containment-boundary pattern already used in these files (FE-088.2's realpath containment lineage) with a `sep`-joined prefix; no new abstraction introduced.

- [x] **Minimal refactor gate** — no refactor beyond the three targeted fixes; unrelated code in each file left untouched.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

- `viz/src/devApi.ts` `createEventsHandler`: moved the SSE headers above the capacity check (unconditional for both GET and HEAD), then branch on `req.method === 'HEAD'` to `res.end()` immediately — before `flushHeaders`/preamble write/`sseClients.add`. The 503-capacity branch still runs first and is unaffected (its `endPlain` overwrites Content-Type as before).
- `viz/src/archiveCache.ts` `invalidate`: imported `sep` from `node:path`; changed `filepath.startsWith(p.archiveDir)` to `filepath.startsWith(p.archiveDir + sep)`.
- `viz/src/watchSet.ts` `projectForPath`: same fix — imported `sep`, changed the archive-scope `startsWith` check to include the boundary separator.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Verification receipt** — see Testing Notes below. No duplication, dead code, unexplained complexity, or public-surface growth introduced; no code-facing documentation went stale.

- [x] (frontend) Asked the user for visual confirmation — N/A: dev-server API/watch-set logic with no rendered UI surface.

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**

- `npm --prefix viz test -- devApi.test.ts archiveCache.test.ts watchSet.test.ts` → 0 (3 files, 65 tests passed)
- `npm --prefix viz run typecheck` → 0
- `npm --prefix viz run lint` → 0

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — no change to any entry in `.flowtron/tasknote/README.md` §"AI-referenced docs". This is an internal fix to `viz/src` implementation details (an HTTP method branch and a path-boundary check); none of the swept docs describe this code at that level of detail.

- [x] Closed — every Acceptance criterion ticked; `status:` flipped to `completed`; PLAN.md line flipped to stub form; tasknote moved to `.flowtron/tasknote/archive/fe/`.

- [x] **Evidence-based recap** drafted (below).

**Final Summary:**

Fixed three latent bugs surfaced by the 2026-09-12 audit, all sharing a
boundary-safety shape:

- `viz/src/devApi.ts` (`createEventsHandler`, +7/-3 lines): a HEAD request to
  `/api/events` now gets its response headers and ends immediately, instead of
  being treated as a live SSE subscriber (occupying a `MAX_SSE_CLIENTS` slot
  it would never release until the client disconnected).
- `viz/src/archiveCache.ts` (`invalidate`, +1/-1 plus import): matches
  `archiveDir + sep` instead of a raw string prefix, closing a false-positive
  window where a sibling directory extending `archiveDir`'s name (e.g.
  `archive-other`) would incorrectly trigger a cache invalidation.
- `viz/src/watchSet.ts` (`projectForPath`, +1/-1 plus import): the same
  `archiveDir + sep` fix, closing the same false-positive window for file-watch
  routing.

No refactor beyond the three targeted diffs — each site already followed an
established containment-boundary lineage (FE-088.2), so the fix extends that
pattern rather than introducing a new one.

**Verification:** `npm --prefix viz test -- devApi.test.ts archiveCache.test.ts watchSet.test.ts` → 0 (65 passed); `npm --prefix viz run typecheck` → 0; `npm --prefix viz run lint` → 0.

**Documentation:** no drift — see doc-drift sweep above.

**`touches:` reconciliation:** declared `viz/src/devApi.ts`, `viz/src/devApi.test.ts`, `viz/src/archiveCache.ts`, `viz/src/archiveCache.test.ts`, `viz/src/watchSet.ts`, `viz/src/watchSet.test.ts` — matches `git diff --name-only` exactly (plus this tasknote and the PLAN.md line, which are workflow artifacts, not code deliverables).

**Maintainability effect:** closes two directory-prefix false-positive windows (cache/watch misrouting between projects or areas whose paths happen to share a string prefix) and stops a HEAD probe from silently leaking an SSE client slot — small, low-risk, test-covered fixes with no new public surface.

**Archived:** 2026-09-12
