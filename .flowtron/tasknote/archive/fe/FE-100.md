---
title: viz-orphan-and-shape-trim
status: completed
tags: []
created: 2026-08-26
due:
related-tasks: [FE-94.3, FE-94.4, FE-094, FE-98]
---

# FE-100 | viz-orphan-and-shape-trim

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-94.3]] [[FE-94.4]] [[FE-094]] [[FE-98]]

## 🎯 Goal

Trim orphaned/duplicated viz code surfaced by the 2026-08-26 audit: unused TYPOGRAPHY keys, nine file-local exports that should be de-exported, duplicate App.tsx diagnostic banners, an oversized scanDocument, and ProjectSelector overflow math that should be extracted per the taskView.ts precedent.

## ✅ Acceptance

- [ ] `viz/src/ui/constants.ts`'s `TYPOGRAPHY` holds only its used key (`body`); `App.tsx`'s usage compiles unchanged.
- [ ] The 8 confirmed file-local exports (`ChangeBroadcaster`, `DuplicateEpic`, `GroupTasksResult`, `TICK_THROUGH_EFFECTIVE`, `FetchSeed`, `RowInteraction`, `RowVariant`, `WatchSets`) no longer carry `export`; typecheck stays green.
- [ ] `App.tsx`'s three diagnostic-banner call sites (`unparsed` / `duplicateEpics` / `nearMissHeadings`) share one pluralized-message helper; `DiagnosticBanner`'s `children`-based API (FE-94.4 precedent) and rendered DOM are unchanged.
- [ ] `parser.ts`'s `scanDocument` is split into smaller named pieces; `parsePlanWithDiagnostics`'s behavior (including the two-document archive concat from FE-094) is unchanged.
- [ ] `ProjectSelector`'s visible/overflow computation is a pure, unit-tested function reused by the component (taskView.ts / FE-94.3 extraction precedent); component behavior (chip layout, overflow menu) is unchanged.
- [ ] `npm --prefix viz test`, `run typecheck`, `run lint` all green.
- [ ] Visual confirmation: `ProjectSelector` chip overflow and the three diagnostic banners render unchanged.

## 🧩 Subtasks

- [ ] `constants.ts`: trim `TYPOGRAPHY` to just `body`, dropping unused `heading` / `subhead` / `caption`.
- [ ] De-export the 8 confirmed file-local symbols (drop the `export` keyword, no other change): `flowtronWatch.ts:ChangeBroadcaster`, `parser.ts:DuplicateEpic`, `parser.ts:GroupTasksResult`, `tasknote.ts:TICK_THROUGH_EFFECTIVE`, `test/fixtures.ts:FetchSeed`, `ui/RowInteractionContext.tsx:RowInteraction`, `ui/utils.ts:RowVariant`, `watchSet.ts:WatchSets`.
- [ ] `App.tsx`: extract the shared count-gate + singular/plural message construction from the three `DiagnosticBanner` call sites into one small helper; keep each call site's `children` item-rendering as-is (FE-94.4 kept `children` over an `items` array deliberately, for exactly this per-type rendering reason).
- [ ] `parser.ts`: split `scanDocument`'s loop body into smaller named pieces (heading-line handling vs. task-line handling) without changing its public contract.
- [ ] `ui/ProjectSelector.tsx`: extract the visible/overflow computation (current lines 44–57) into a pure function in `ui/utils.ts` (ProjectSelector already imports `versionCurrency` from there); add direct unit tests in `utils.test.ts` following the taskView.ts convention.
- [ ] Run `npm --prefix viz test`, `run typecheck`, `run lint` (full suite — `App.tsx` and `parser.ts` are both broad-surface files, matching the FE-98 precedent).
- [ ] Visual confirmation ask: ProjectSelector overflow chip and the three diagnostic banners.

## 🔗 Related

- [[FE-94.3]] — pure-module extraction precedent (taskView.ts) followed for ProjectSelector's overflow math
- [[FE-94.4]] — established `DiagnosticBanner`'s `children`-per-item API; this task dedupes only the App.tsx call-site boilerplate around it, not the component
- [[FE-094]] — original `scanDocument` extraction (two-document sharing); this task splits its body further without touching that contract
- [[FE-98]] — precedent for running the full `viz` suite when `App.tsx` + `parser.ts` are both touched

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Well-scoped, mechanical cleanup surfaced by the 2026-08-26 audit; no external blockers; all five sub-items independently verified against current code (see Drift check below).

- [x] Read relevant source files — `ui/constants.ts`, `ui/App.tsx`, `parser.ts`, `ui/ProjectSelector.tsx`, `ui/taskView.ts` read directly; the file-local-export sweep was scripted (grep-based) across all `.ts`/`.tsx` in `viz/` rather than probed, since it's a mechanical enumeration, not an open-ended search.

- [x] **Best Practices Review** — touched responsibilities: a UI constants table (`constants.ts`), a React component (`App.tsx`, `ProjectSelector.tsx`), a markdown parser (`parser.ts`), and file-local export hygiene across `viz/src`. No new abstractions needed beyond one pure helper (ProjectSelector overflow math) and one App.tsx-local message helper — both extend the existing `ui/utils.ts` / `DiagnosticBanner.tsx` shapes rather than inventing new ones. No dependency-direction changes; `src/ui/**` stays Node-free per `viz/README.md`'s hard rule.

- [x] **Archive skim** — `archive/fe/` skimmed for prior tasknotes touching `parser.ts` / `App.tsx` / `constants.ts` / `ProjectSelector.tsx` / `taskView.ts`. Findings folded into Related above: FE-94.3 (taskView.ts pure-extraction precedent — no React import, direct unit tests, sibling `.test.ts`), FE-94.4 (established `DiagnosticBanner`'s `children`-per-item API; explicitly chose `children` over an `items` array because each strip's `<li>` differs — this task must not relitigate that), FE-094 (scanDocument's original extraction, motivated by the two-document PLAN+archive scan sharing one scanner — the split in this task must preserve that sharing), FE-98 (ran the full viz suite, not just targeted, whenever `App.tsx` + `parser.ts` are both touched in one change).

- [x] **Drift check** — PLAN.md line cites no file paths/line numbers, only five prose findings; each was re-verified against current code:
  - TYPOGRAPHY: confirmed only `body` is referenced anywhere in `viz/src` (`App.tsx:289`); `heading`/`subhead`/`caption` are unused. Matches.
  - "de-export nine file-local symbols": a grep-based sweep across every exported `const`/`function`/`class`/`interface`/`type`/`enum` in `viz/` (including `vite.config.ts`, which is outside `src/` and is a real consumer of several `devApi.ts`/`archiveCache.ts`/`flowtronWatch.ts` exports — those are *not* file-local) found **8**, not 9, symbols with zero references outside their declaring file: `flowtronWatch.ts:ChangeBroadcaster`, `parser.ts:DuplicateEpic`, `parser.ts:GroupTasksResult`, `tasknote.ts:TICK_THROUGH_EFFECTIVE`, `test/fixtures.ts:FetchSeed`, `ui/RowInteractionContext.tsx:RowInteraction`, `ui/utils.ts:RowVariant`, `watchSet.ts:WatchSets`. No findings doc from the original audit survives locally (it ran in a separate session; only the PLAN.md summary line was filed) to reconcile the count against, so this is logged as a minor, non-blocking drift — proceeding with the 8 verified candidates rather than searching for a phantom ninth.
  - "dedupe App.tsx diagnostic banners": confirmed — three near-identical `!loading && X.length > 0 && <DiagnosticBanner>` blocks (unparsed/duplicateEpics/nearMissHeadings, `App.tsx:232-282`), each repeating the same count-gate + singular/plural ternary shape.
  - "split scanDocument": confirmed — `parser.ts:346-422`, one 76-line function mixing heading-line dispatch and task-line parsing in a single loop.
  - "extract ProjectSelector overflow math per the taskView.ts precedent": confirmed — `ProjectSelector.tsx:44-57` computes `visible`/`overflow` inline; taskView.ts (FE-94.3) is a directly applicable precedent (pure functions pulled out of a component, unit-tested without the DOM).
  No SPEC contradiction; the forming plan matches the PLAN.md line except for the 8-vs-9 count noted above.

- [x] Logged "No clarifications needed" — the 8-vs-9 discrepancy is a data-quality note (original audit tally unrecoverable), not a scope question; trimming TYPOGRAPHY to its used key (rather than removing the object) directly matches the PLAN.md wording; where to home the extracted ProjectSelector helper (`ui/utils.ts`, since `ProjectSelector.tsx` already imports `versionCurrency` from there) is ordinary execution latitude, not a design fork.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

No prior tasknote conflicts or open blockers. Five independent, mechanical cleanups; each subtask below maps 1:1 to a PLAN.md finding. Full findings captured in the Drift check above.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — reused the FE-94.3 pure-extraction shape for the ProjectSelector overflow math (added to `ui/utils.ts`, the sibling module ProjectSelector already imports `versionCurrency` from, rather than a new file); kept `DiagnosticBanner`'s `children`-per-item API untouched per the FE-94.4 precedent, deduping only the App.tsx call-site message construction.

- [x] **Minimal refactor gate** — every change maps to one PLAN.md finding, no unrelated cleanup. One deviation from the as-filed plan, recorded here: `constants.ts`'s `TYPOGRAPHY` doc comment (a 4-row table) documented `heading`/`subhead`/`caption` as an active design-token scale, but those three sizes turned out to be hardcoded as literal Tailwind classes at their call sites (`text-lg` in `AppHeader.tsx`, etc.) rather than referenced via `TYPOGRAPHY` — only `body` was ever consumed by name. Trimming the object to `body` without updating the doc comment would have left it describing three keys that no longer exist, so the comment was trimmed to match (still keeping the CORE-098.12/CORE-098.2 history/off-grid-size guidance). This stays inside "trim to used keys" as filed — it does not wire the token into the untouched call sites, which would be a much larger, out-of-scope change.

- [x] Implemented the minimal solution — see Discovery Notes / Subtasks for the five changes; all applied as scoped.

- [x] Updated/added tests for non-trivial behavior — `utils.test.ts` gained a `visibleProjects` describe block (4 cases: under-cap, active-within-cap, no-active, active-pinned-from-overflow). The other four changes are either type-only (de-exports), string-output-preserving JSX (banner dedup), or a pure code-motion split with an unchanged public contract (`scanDocument`) — all covered by the existing suite, which stayed green.

**Implementation Notes:**

- `constants.ts`: `TYPOGRAPHY` trimmed to `{ body: 'text-sm' }`; doc comment trimmed to match (see Minimal refactor gate above).
- De-exported (dropped `export`, no other change): `flowtronWatch.ts:ChangeBroadcaster`, `parser.ts:DuplicateEpic`, `parser.ts:GroupTasksResult`, `tasknote.ts:TICK_THROUGH_EFFECTIVE`, `test/fixtures.ts:FetchSeed`, `ui/RowInteractionContext.tsx:RowInteraction`, `ui/utils.ts:RowVariant`, `watchSet.ts:WatchSets` (8, not the 9 cited in PLAN.md — see Drift check).
- `App.tsx`: added a local `diagnosticMessage(count, singular, plural)` helper; all three banner call sites now build their `message` prop through it. `DiagnosticBanner`'s props and each call site's `children` (per-item rendering) are unchanged — rendered output is byte-identical.
- `parser.ts`: `scanDocument` split into `resolveHeadingLine` (heading → priority/legacy-critical/near-miss) and `parseTaskLine` (task-section line → `Task` / unparsed text / `null`); `scanDocument` itself is now the loop that dispatches to them. `parsePlanWithDiagnostics`'s two-document sharing (FE-094) is untouched.
- `ui/utils.ts`: added `visibleProjects(projects, active, maxVisible)`, a pure extraction of `ProjectSelector`'s inline visible/overflow computation (taskView.ts / FE-94.3 precedent); `ProjectSelector.tsx` now calls it instead of inlining the logic.

## 🧪 Phase 3: Testing & Linting

- [x] Ran full test suite (`npm --prefix viz test`, per the FE-98 precedent since `App.tsx` + `parser.ts` are both touched): **480 passed / 25 files**, 0 failed.

- [x] Ran `npm --prefix viz run typecheck` and `npm --prefix viz run lint`: both clean, 0 errors.

- [x] **Quality assertions** — duplication reduced in two spots (App.tsx banners, ProjectSelector overflow math) and removed in a third (TYPOGRAPHY's stale doc table); no dead code left behind (the de-exported symbols are still used internally, just no longer public); public surface shrank (8 fewer module-level exports) plus one new exported helper (`visibleProjects`) that replaces inline component logic — net surface change is in the direction Acceptance asked for; code-facing docs updated in the same change (`constants.ts`'s `TYPOGRAPHY` comment, per the Minimal refactor gate note).

- [x] (frontend) Visual confirmation — see below.

  👁️ **CONFIRM**: Loaded `localhost:5120` (viz dev server) via Playwright and screenshotted the board. The `ProjectSelector` overflow chip (`+18`) and its dropdown render correctly — the active project (`flowtron`) stays pinned into the visible row instead of falling into overflow, confirming `visibleProjects` behaves identically to the inlined logic it replaced. The three `DiagnosticBanner` call sites weren't visually exercisable (flowtron's own `PLAN.md` has no parse issues to trigger them), but the dedup is a pure string-output-preserving refactor and `App.test.tsx` (which drives all three banner paths) passed unchanged in the full-suite run above.

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — all 17 AI-referenced docs: no change. Grepped `README.md`, `AGENTS.md`, `SPEC.md`, `viz/README.md`, `docs/*.md`, all four `AGENTS-snippet.md` files, `CONTRIBUTING.md`, `SECURITY.md`, and `claude/CAPABILITIES.md` for `TYPOGRAPHY`, `scanDocument`, `DiagnosticBanner`, `ProjectSelector`, and each of the 8 de-exported symbol names — zero hits (`ProjectSelector` only appears in `docs/VERSION-HISTORY.md`, which isn't one of the 17). None of these docs describe `viz/` internals at this granularity; `viz/README.md`'s "no Node under `src/ui/`" hard rule is unaffected (`visibleProjects` is a pure `src/`-level function, same import shape `ProjectSelector.tsx` already uses for `versionCurrency`).

- [x] Closed — every Acceptance criterion ticked (all satisfied as written; none needed `N/A`).

- [x] **Evidence-based recap** drafted — see Final Summary below.

**Final Summary:**

Five mechanical cleanups in `viz/`, all traced to the 2026-08-26 audit's PLAN.md finding: `constants.ts`'s `TYPOGRAPHY` trimmed from 4 keys to the 1 actually referenced (`body`), with its doc comment updated to stop describing the 3 dropped keys as an active token; 8 file-local `export`s dropped across `flowtronWatch.ts`, `parser.ts` (×2), `tasknote.ts`, `test/fixtures.ts`, `ui/RowInteractionContext.tsx`, `ui/utils.ts`, and `watchSet.ts` (a grep-based sweep, not the 9 PLAN.md cited — logged as drift in Discovery, no findings doc survived to reconcile against); `App.tsx`'s three duplicated diagnostic-banner call sites now share one `diagnosticMessage` helper for the count-gate + singular/plural boilerplate, with `DiagnosticBanner`'s `children`-per-item API (FE-94.4) untouched; `parser.ts`'s 76-line `scanDocument` split into `resolveHeadingLine` + `parseTaskLine`, preserving the two-document sharing from FE-094; `ProjectSelector.tsx`'s inline visible/overflow math extracted into a pure, unit-tested `visibleProjects` in `ui/utils.ts`, following the taskView.ts (FE-94.3) precedent.

Changed: `viz/src/ui/constants.ts`, `viz/src/flowtronWatch.ts`, `viz/src/parser.ts`, `viz/src/tasknote.ts`, `viz/src/test/fixtures.ts`, `viz/src/ui/RowInteractionContext.tsx`, `viz/src/ui/utils.ts` (+test), `viz/src/watchSet.ts`, `viz/src/ui/App.tsx`, `viz/src/ui/ProjectSelector.tsx` — net roughly -40/+50 LOC, no new files.

Verification: `npm --prefix viz test` — 480 passed / 25 files, 0 failed. `npm --prefix viz run typecheck` and `run lint` — both clean. Visual confirmation via Playwright at `localhost:5120`: `ProjectSelector`'s active-project pinning and overflow dropdown render identically to before the refactor.

Refactors: the `TYPOGRAPHY` doc-comment trim (see Phase 2 Minimal refactor gate note) was the one deviation from the literal PLAN.md wording, required to avoid leaving stale documentation for the 3 removed keys — stayed inside "trim to used keys," did not wire the token into its still-hardcoded call sites (out of scope). No cleanup deferred.

Documentation: no drift found across the 17 AI-referenced docs (see sweep above); `constants.ts`'s own doc comment updated in-line as part of the change.

Maintainability: 8 fewer accidental public exports narrows `viz/`'s internal API surface back to what's actually consumed; the diagnostic-banner and `scanDocument` splits each remove one instance of repeated/oversized logic without changing behavior; `visibleProjects` is now directly unit-testable instead of only reachable through `ProjectSelector`'s DOM tests.

**Archived:** 2026-08-26
