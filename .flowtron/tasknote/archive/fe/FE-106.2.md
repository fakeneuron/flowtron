---
title: eslint-node-tier-list
status: completed
tags: []
created: 2026-09-09
due:
related-tasks: [FE-EPIC-106, FE-97, FE-104]
touches:
  - viz/eslint.config.js
---

# FE-106.2 | eslint-node-tier-list

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-106]] [[FE-97]] [[FE-104]]

## 🎯 Goal

Close the ui-tier eslint denylist so it names every Node-only module in `viz/README.md` (including `tasknoteRead`) and matches both extensionless and `.ts`-suffixed import forms.

## ✅ Acceptance

- [x] `viz/eslint.config.js` `no-restricted-imports` patterns include `tasknoteRead` plus every other README-named Node-only module
- [x] Each of those modules is restricted in both extensionless (`**/devApi`) and `.ts`-suffixed (`**/devApi.ts`) forms
- [x] `npm --prefix viz run lint` stays clean on the existing tree
- [x] A probe import of `../tasknoteRead` and `../tasknoteRead.ts` from `src/ui/` both fire `no-restricted-imports`

## 🧩 Subtasks

- [x] Extend the ui-tier `no-restricted-imports` group: add `tasknoteRead` and a `.ts` glob for each of the nine Node-only modules
- [x] Confirm `npm --prefix viz run lint` is still clean
- [x] Probe both import forms for `tasknoteRead` (and one existing module `.ts` form), then delete the probe

## 🔗 Related

- [[FE-EPIC-106]] — parent epic (viz-tier-guardrail)
- [[FE-97]] — original ui-tier `no-restricted-imports` rule; scoped to eight extensionless names
- [[FE-104]] — added `tasknoteRead` and updated the README module list; did not update eslint
- [[FE-106.3]] — sibling (js-yaml bump); independent
- [[FE-106.4]] — sibling (`@types/node` engines); independent
- [[FE-106.N]] — epic audit

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** README names nine Node-only modules including `tasknoteRead`; eslint still lists FE-97's eight extensionless names, so the hard rule is not fully enforced.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; when the grep returns more than a handful of notes (~3 is a fair line), prefer handing the reading to a **probe**; log relevant findings in Discovery Notes before re-interpreting the task

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- PLAN line (Medium, `[light]`): add `tasknoteRead` plus `.ts`-suffixed globs for every Node-only module to `viz/eslint.config.js`. Matches current code: `viz/eslint.config.js:45-54` lists eight names, missing `tasknoteRead`; README §"Hard rule" (lines 45-48) lists nine, including `tasknoteRead`.
- No `.1` Discovery tasknote and no `## 🌳 Fan-out` — epic Discovery was supplied by audit-repo; omit `blocked-by` / `parallel-safe-with`.
- FE-97 (2026-08-26) added the rule and deliberately skipped `.ts` suffixes because `src/ui/` used extensionless imports. Node-tier sources now import with `.ts` (`./tasknoteRead.ts`, etc.); a UI file doing the same would bypass the current patterns.
- FE-104 (2026-09-08) added `viz/src/tasknoteRead.ts` and updated the README list with the claim that eslint must catch a `tasknoteRead` import; it did not edit `eslint.config.js`. That is the gap this child closes — not a superseded-claim rewrite of FE-104.
- UI still imports shared modules extensionless (`../parser`, `../tasknote`, …). Both forms must stay restricted.
- Best Practices: extend the existing `no-restricted-imports` group in the `src/ui/**` block (FE-97 shape). No new rule, no README edit (list already names `tasknoteRead`). Deferred: a permanent lint-rule unit test — FE-97 verified with a temp probe; this task does the same.
- Assumptions: restrict only the nine README-named `src/*.ts` Node-only modules (not `vite.config.ts`); cover extensionless + `.ts` only, not `.js`/`.tsx`. No clarifications needed.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

- Extended FE-97's existing `src/ui/**` `no-restricted-imports` group. Added `**/tasknoteRead` / `**/tasknoteRead.ts` and a `.ts` twin for each of the other eight names, in README order. No new block, no README edit (list already names `tasknoteRead`).
- Minimal refactor gate: N/A — additive glob list only. Deferred: a permanent lint-rule unit test (same as FE-97).

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Quality assertions** — for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

- Targeted tests: N/A — lint-config only; no test file references `eslint.config.js`.
- `npm --prefix viz run lint` with a temp `src/ui/__lint_probe.tsx` importing `../tasknoteRead`, `../tasknoteRead.ts`, and `../apiResponse.ts` → 3 `no-restricted-imports` errors, expected messages. Probe deleted; `git status --porcelain` showed only `viz/eslint.config.js` + this tasknote.
- `npm --prefix viz run lint` after probe removal → exit 0. `npm --prefix viz run typecheck` → exit 0.
- Quality: no duplication (same group, paired globs); README already listed `tasknoteRead`.
- 👁️ **CONFIRM**: N/A — no UI/visual surface.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

  - `README.md`, `AGENTS.md`, `SPEC.md`, `docs/MIGRATION.md`, `claude/AGENTS-snippet.md`, `codex/AGENTS-snippet.md`, `cursor/AGENTS-snippet.md`, `grok/AGENTS-snippet.md`, `docs/CONVENTIONS.md`, `CONTRIBUTING.md`, `SECURITY.md`, `docs/AGENT-NEUTRALITY.md`, `docs/PLATFORMS.md`, `claude/CAPABILITIES.md`, `docs/AGENT-COMPAT.md`, `docs/EXTERNAL-AGENTS.md`, `docs/WORKTREES.md`, `docs/VISION.md` — no change. Grep of the sweep set for `no-restricted-imports` / `tasknoteRead` / `no Node imports` hit only `SECURITY.md` (containment helper, not the eslint denylist).
  - `viz/README.md` (not in the sweep set) — already lists `tasknoteRead`; no edit.

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed per SPEC/tasknote-selection.md §"`## Completed` archive convention" (standalone → top of `## Completed`; epic child → kept nested beneath its active parent), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Closed the ui-tier eslint denylist so it matches the README hard-rule module list: `tasknoteRead` plus `.ts` twins for all nine Node-only modules. A `src/ui/` import of `../tasknoteRead` or `../tasknoteRead.ts` now fails lint the same way the original eight extensionless names did.

- **Files changed:** `viz/eslint.config.js` (8 → 18 restricted module patterns in the existing `src/ui/**` block).
- **Verification:** probe file fired 3 `no-restricted-imports` errors; probe removed; `npm --prefix viz run lint` and `run typecheck` exit 0.
- **Refactors:** none. Deferred: permanent lint-rule unit test.
- **Documentation:** sweep-set no change; `viz/README.md` already listed `tasknoteRead`.
- **Maintainability:** FE-104's new Node-tier helper can no longer be imported from `src/ui/` via either import form.

**Archived:** 2026-09-09
