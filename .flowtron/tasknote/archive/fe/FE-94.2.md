---
title: Pin the App seams the suite misses before any restructuring
status: completed
tags: [viz, tests]
created: 2026-08-23
due:
related-tasks: [FE-EPIC-94, FE-94.3]
touches:
  - viz/src/ui/App.test.tsx
---

# FE-94.2 | Pin the App seams the suite misses before any restructuring

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-94]] [[FE-94.3]]

## 🎯 Goal

Gap-fill `App.test.tsx` to pin the five App-level seams the ~70-test suite doesn't yet cover — view-mode toggle, `listViewEmptySections` copy, "No matches" empty state, header starter suffix, and the error banner — before any FE-94.x extraction moves that code.

## ✅ Acceptance

- [x] New tests for the five gaps (view-mode toggle, `listViewEmptySections` copy, "No matches" empty state, header starter suffix, error banner) written and green before any restructuring child (FE-94.3+) starts
- [x] No source file under `viz/src/` changes in this child — tests only
- [x] `npm --prefix viz test` and `npm --prefix viz run typecheck` green

## 🧩 Subtasks

- [x] Pin view-mode toggle: `role="group"`/`aria-label="View mode"`, correct `aria-pressed`, clicking `Board` switches the rendered view, `writeStoredViewMode` persists to `localStorage['flowtron-viz-view']`
- [x] Pin `listViewEmptySections` copy: "No tasks in this project" (all sections empty) vs "No tasks in: <a · b>" (some empty)
- [x] Pin "No matches. Press Esc to clear filters." empty state (query/status filter active, `filteredCount === 0`)
- [x] Pin header starter suffix: "· N starter"/"· N starters" from `frontmatter.status === 'starter'`, absent at zero
- [x] Pin error banner: `projectsError ?? dataError` renders the red strip, via `renderApp`'s `perProject.fail` seam
- [x] Run `npm --prefix viz test` and `npm --prefix viz run typecheck`

## 🔗 Related

- [[FE-EPIC-94]] — parent epic
- [[FE-94.3]] — next in sequence; consumes this pinning

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Starter context drift-checked clean against current code (see Drift check below); the task is a well-scoped, tests-only gap-fill with no blocking dependency and no design ambiguity beyond two minor implementation leans already resolved by the starter's own recommendations.

- [x] Read relevant source files — read `App.tsx` (hooks/derived state, header markup, view-mode toggle, empty states, error banner, ~lines 47-217, 340-540), `viewMode.ts`, `App.test.tsx` board tests (~lines 900-925) and the real-timers convention comment (~lines 24-31), and `test/fixtures.ts` (`renderApp`/`seedFetch`/`ProjectFetchOverride` shape). Read set was bounded (one component + one helper file), so no probe needed.

- [x] **Best Practices Review** — N/A (tests-only task; touches a single existing test file, no new module/abstraction). Convention observed: each `describe('App — <topic> (<TASK-ID>)', ...)` block gets its own scoped `beforeEach` (e.g. `window.localStorage.clear()`) rather than a global one — new blocks should follow this shape, not add global hooks.

- [x] **Archive skim** — see Discovery Notes below.

- [x] **Drift check** — no drift. All cited seams confirmed exact in current code: `writeStoredViewMode`/`updateViewMode` (App.tsx:100-103), `role="group"`/`aria-label="View mode"`/`aria-pressed` toggle (App.tsx:363-386), `listViewEmptySections` copy (App.tsx:519-525, exact strings match), "No matches. Press Esc to clear filters." (App.tsx:501), header starter suffix (App.tsx:208-214, exact strings match), error banner `projectsError ?? dataError` (App.tsx:55,63,69,430-434). Board tests seeding `localStorage['flowtron-viz-view']='board'` are at App.test.tsx:904/1187 (cited as :903/:1186 in the starter — 1-line drift from file churn since filing, not load-bearing). `App.test.tsx` is 1236 lines as cited. No contradiction with the FE-EPIC-94/FE-94.2 PLAN.md line or SPEC.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions — see below.

  **No clarifications needed.** The starter's two "Open at promotion" questions carry clear recommended leans that this task adopts as explicit assumptions:
  - View-mode toggle test asserts **both** the re-render (List/Board content swap, `aria-pressed`) **and** the `localStorage['flowtron-viz-view']` read-back after click, since persistence is exactly the behavior `FE-94.6` will later move into `useViewPrefs`.
  - Empty-state tests use **real timers**, per the suite-wide convention documented at `App.test.tsx:24-31` (FE-045/FE-089.2) — no `vi.useFakeTimers()` in these new tests.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

**Starter context (preserved verbatim from filing, 2026-08-23):**

> Captured during the `/ft-refactor viz/src/ui/App.tsx` depth survey.
>
> **Why this exists**
>
> First step in the sequence: every later child moves code that `App.test.tsx`
> must already pin, or the "behavior-preserving" claim is unverifiable.
>
> The survey found coverage unusually good — `App.test.tsx` (1236 lines, ~70
> tests) drives the real `App` through `renderApp()` against a seeded `fetch`,
> already pinning navigation, filtering/pruning + counts, project switching,
> prefs, all three diagnostics families, SSE disconnect, and completed-bucket
> grouping. So this step is a **gap-fill, not a build-out** — a handful of tests,
> not a suite.
>
> The gaps are exactly the surfaces FE-94.4/.5/.6 will move.
>
> **Explicitly out of scope**
>
> - Any change to `viz/src/ui/App.tsx` — this child is tests only.
> - The extractions themselves — FE-94.3 (selectors), FE-94.4 (diagnostics),
>   FE-94.5 (header), FE-94.6 (state hooks).
> - Broadening coverage beyond the seams the sequence moves; the existing ~70
>   tests already carry the rest.
>
> **Decisions locked in this conversation**
>
> | Decision | Choice | Rationale |
> |---|---|---|
> | Characterization level | App-level via `renderApp()` | Matches the existing suite's seam; survives every later extraction unchanged, which is the point |
> | Scope of new tests | Only the five unpinned seams | Coverage is already deep; make-work tests dilute the signal |
> | Position in sequence | First, blocking everything | Pin before move — the epic's behavior-preservation contract rests on it |
>
> **Open at promotion (Phase 1 should resolve)**
>
> - Does asserting the view-mode toggle need a `localStorage` read-back, or is
>   asserting the re-render enough? Lean: assert both — persistence is what
>   FE-94.6 moves into `useViewPrefs`.
> - Real vs fake timers for the empty-state tests. Lean: real timers, per the
>   suite-wide convention documented at `App.test.tsx:23-30` (FE-045/FE-089.2).

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended the existing `describe('App — <topic> (<ID>)', ...)` + scoped `beforeEach(() => window.localStorage.clear())` shape used throughout `App.test.tsx` (e.g. FE-044, FE-086); reused `renderApp`/`makeTasknote`/`perProject.fail` from `test/fixtures.ts` and the established `role`/`aria-pressed` query convention (FE-014/FE-032/FE-068). No new shape introduced.

- [x] **Minimal refactor gate** — N/A. Pure test addition; nothing in `App.tsx` or the fixture helpers needed to change to express these five seams.

- [x] Implemented the minimal solution — added one new `describe('App — App-level characterization gaps (FE-94.2)', ...)` block at the end of `App.test.tsx` with 8 `it`s covering the five seams (view-mode toggle gets 1, empty-section copy gets 2 for the two message shapes, "No matches" gets 1, starter suffix gets 3 for singular/plural/absent, error banner gets 1).

- [x] Updated/added tests for non-trivial behavior — this task's entire deliverable is the 8 new tests (see above); no other tests changed.

**Implementation Notes:**

Appended the new `describe` block after the existing terminal block (`App — completed-bucket grouping (FE-086)`, previously ending the file at line 1236). Notable implementation decisions:
- View-mode test asserts `aria-pressed` on both buttons before/after, board-container presence (`.overflow-x-auto`, matching the FE-044/FE-086 board-test convention), and the `localStorage['flowtron-viz-view']` write-back in one test (per the Phase 1 assumption).
- Empty-section copy split into two tests: `## High\n` with zero bullets for the all-empty "No tasks in this project" case, and a High+Medium-populated plan for the partial "No tasks in: Low · Future Opportunities · Completed" case (exercises `SECTIONS` order preservation).
- Starter suffix split into three tests (singular/plural/absent) rather than one, since each is a distinct pluralization branch worth pinning independently — still within the "handful, not a suite" framing from the starter context.
- Error banner test seeds `perProject: { flowtron: { fail: { plan: 500 } } }` (single-project failure on initial load) rather than FE-030's project-switch scenario — simpler and sufficient to pin `errorMessage = projectsError ?? dataError` rendering; asserts on the exact thrown message (`PLAN.md fetch failed: HTTP 500` from `useProjectData.ts`).

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npm --prefix viz test -- --run src/ui/App.test.tsx`: **62/62 passed** (54 pre-existing + 8 new)

- [x] Ran lint/type-check on changed code — `npm --prefix viz run typecheck` clean; `npm --prefix viz run lint` clean

- [x] **Quality assertions** — no duplication beyond the established per-describe-block shape every other block in this file already uses; no dead code; no new public surface (test-only file); no stale docs (nothing in `.flowtron/tasknote/README.md` describes `App.test.tsx` internals)

- [x] (frontend) Visual confirmation — **N/A**: test-only change, no `viz/src/` (non-test) file touched and no UI surface altered, same rationale as the FE-053/FE-089.2 precedent for test-infra-only tasknotes

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

Also ran the full viz suite (`npm --prefix viz test`, no filter) as a broader-change sanity check since this is App-level test infra: **402/402 passed** across all 22 test files — no regression in any pre-existing test from the new `describe` block. `git status --porcelain` after implementation confirms only `viz/src/ui/App.test.tsx` (source) and `.flowtron/tasknote/FE-94.2.md` changed — no file under `viz/src/` outside the test file was touched, satisfying the tests-only Acceptance criterion.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — all 15 AI-referenced docs: **no change**. This task added test cases to `viz/src/ui/App.test.tsx` only; none of the 15 docs (README, AGENTS, SPEC, MIGRATION, the four agent-snippet wiring docs, CONVENTIONS, CONTRIBUTING, SECURITY, AGENT-NEUTRALITY, PLATFORMS, CAPABILITIES, AGENT-COMPAT, EXTERNAL-AGENTS, WORKTREES) describe viz/App.tsx internals or test conventions.

- [x] Closed — all three `## ✅ Acceptance` criteria ticked (tests written and green, no non-test source touched, `npm --prefix viz test` + `typecheck` green). YAML `status:` flipped to `completed`. PLAN.md line flipped to stub form and kept nested beneath the active `FE-EPIC-94` parent (epic child — not moved to top-level `## Completed`, per SPEC/tasknote-selection.md §"`## Completed` archive convention"). Tasknote moved to `.flowtron/tasknote/archive/fe/`.

- [x] **Evidence-based recap** drafted — see Final Summary below.

**Final Summary:**

Added 8 new characterization tests to `viz/src/ui/App.test.tsx` (+~160 lines, one new `describe` block, one file) pinning the 5 App-level seams the ~70-test suite didn't yet cover: the view-mode toggle (aria-pressed + localStorage persistence), `listViewEmptySections` copy (both the all-empty and partial-empty message shapes), the "No matches" empty state, the header starter-count suffix (singular/plural/absent), and the error banner (`projectsError ?? dataError`). No source file under `viz/src/` changed — this was a tests-only gap-fill, as scoped.

Verification: targeted run 62/62 green (54 pre-existing + 8 new); full suite 402/402 green across 22 files; `typecheck` and `lint` both clean; `git status --porcelain` confirms only the test file + this tasknote changed.

Refactors: none — Minimal refactor gate found nothing to refactor (pure test addition, extending an established per-block pattern already used ~20 times in this file).

Documentation: no drift across all 15 AI-referenced docs (see doc-drift sweep above).

Maintainability effect: `App.tsx`'s five most recently-added/least-covered seams are now pinned at the App level via `renderApp()`, the same seam every later FE-94.x extraction child renders through — so FE-94.3 (selectors), FE-94.4 (diagnostics), FE-94.5 (header), and FE-94.6 (state hooks) can each move this code with a concrete regression signal instead of relying on manual re-verification.

**Archived:** 2026-08-23
