---
title: origin-guard-same-site-reject
status: completed
tags: []
created: 2026-09-13
due:
related-tasks: [FE-102]
touches:
  - viz/src/originGuard.ts
  - viz/src/originGuard.test.ts
  - SECURITY.md
---

# FE-119 | origin-guard-same-site-reject

[← PLAN.md](../PLAN.md) · ✅ Completed · 🔗 [[FE-102]]

## 🎯 Goal

Reject `Sec-Fetch-Site: same-site` in `originGuard`, alongside the existing `cross-site` rejection, closing the sibling-loopback-port SSE-slot vector.

## ⚡ Notes

**Relevance:** Proceed — the loophole and fix are exactly as described on the PLAN.md line; no re-scope needed.
**Best Practices Review:** Extends the existing `Sec-Fetch-Site` early-rejection branch added by FE-102 rather than introducing a new check path; reused the same `endPlain(res, 403, 'Forbidden: cross-site request')` call and message since `same-site` is conceptually the same "not from our own origin" rejection, avoiding a new message string with no behavioral distinction. No dependency-direction or duplication concerns.
**Drift check:** `originGuard.ts` and `originGuard.test.ts` match the PLAN.md line's description exactly — the `cross-site`-only check, the `it.each(['same-origin', 'same-site', 'none'])` allow-row. No contradiction with SPEC.
**Archive skim:** `FE-102` (`archive/fe/FE-102.md`) added the original `Sec-Fetch-Site: cross-site` rejection this task extends — its Acceptance explicitly listed `same-site` as a passing value at the time, which this task now reverses for the reason described on the PLAN.md line. No other `archive/fe/` tasknote touches `originGuard.ts`.
**Declared scope:** see YAML `touches:` above.
**Pattern survey:** Extended the existing single-branch `if (fetchSite === 'cross-site')` check to also match `same-site`, rather than adding a second branch — same shape, same error path.
**Implementation:** `originGuard.ts` — the early `Sec-Fetch-Site` check now rejects both `cross-site` and `same-site` with the existing `Forbidden: cross-site request` 403; updated the preceding comment block to explain the sibling-loopback-port vector and drop `same-site` from the "falls through" list. `originGuard.test.ts` — removed `'same-site'` from the `it.each` allow-row (now `['same-origin', 'none']`) and added a dedicated `'blocks a Sec-Fetch-Site: same-site request with 403'` test mirroring the existing `cross-site` block test. `SECURITY.md` §"Visualizer (`viz/`) dev-server scope" — updated the `Sec-Fetch-Site` bullet to say both `cross-site` and `same-site` are rejected, with the sibling-loopback-port rationale, and narrowed the "falls through" list to `none` / `same-origin`.
Verification receipt:
- `npm --prefix viz test -- originGuard` → 0 (15 passed)
- `npm --prefix viz run typecheck` → 0
- `npm --prefix viz run lint` → 0
**Docs touched:** `SECURITY.md` — updated per above (the doc named directly on the PLAN.md line).

## ✅ Recap

Extended `originGuard`'s `Sec-Fetch-Site` check to reject `same-site` alongside `cross-site`, since a page served from a sibling loopback port can send a header-less `no-cors`/`no-referrer` GET that arrives as `same-site` and would otherwise hold `/api/events` SSE slots up to `MAX_SSE_CLIENTS` — the same class of gap FE-102 closed for `cross-site`. Changed `viz/src/originGuard.ts` (check + comment), `viz/src/originGuard.test.ts` (flipped the `it.each` allow-row, added a dedicated same-site block test), and `SECURITY.md`'s `Sec-Fetch-Site` bullet — all three match the declared `touches:`. Tests (15/15), typecheck, and lint all clean.

**Archived:** 2026-09-13
