---
title: shared-fencemask
status: completed
tags: []
created: 2026-08-26
due:
related-tasks: ['CORE-421.2', 'CORE-423']
touches:
  - viz/src/parser.ts
  - viz/src/tasknote.ts
# Optional planning keys — omit when absent (SPEC.md §Tasknote frontmatter).
# Omitted means undeclared, not "touches nothing" / "safe with everyone".
# blocked-by:
#   - TASK-ID
# parallel-safe-with:
#   - TASK-ID
# supersedes:
#   - TASK-ID
---

# FE-99 | shared-fencemask

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[CORE-421.2]] [[CORE-423]]

## 🎯 Goal

Extract the verbatim-duplicated `fenceMask`/`FENCE_DELIMITER` (parser.ts:121 / tasknote.ts:139) into one shared-pure module imported by both.

## ✅ Acceptance

- [ ] N/A — de-scoped in Phase 1 Discovery before an acceptance criterion was needed; see Relevance Assessment for rationale

## 🧩 Subtasks

N/A — de-scoped in Phase 1 Discovery; no execution steps were planned.

## 🔗 Related

- [[CORE-421.2]] — introduced `fenceMask`/`FENCE_DELIMITER` in `tasknote.ts`; deliberately kept unexported ("consumed only inside this module" — public-surface-growth cost)
- [[CORE-423]] — ported the same helper into `parser.ts` by duplication, explicitly *not* as a shared import, to avoid creating a `parser.ts`↔`tasknote.ts` coupling that didn't exist before

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** De-scope
  **Rationale:** The proposed extraction directly reverses a documented, twice-made architectural decision — CORE-421.2 kept `fenceMask`/`FENCE_DELIMITER` unexported specifically to avoid public-surface growth, and CORE-423 explicitly duplicated the helper into `parser.ts` rather than sharing it, specifically to avoid creating a `parser.ts`↔`tasknote.ts` coupling that doesn't otherwise exist. Neither the PLAN.md line nor the 2026-08-26 audit finding that filed this task engaged with that precedent. Surfaced to the operator via AskUserQuestion (proceed / de-scope / re-scope-to-hardened-comments); operator selected De-scope.

- [x] Read relevant source files — `viz/src/parser.ts` (fence helper + surrounding context, L95-160), `viz/src/tasknote.ts` (fence helper + surrounding context, L125-165). Read set was narrow and known — no probe needed.

- [x] **Best Practices Review** — `fenceMask`/`FENCE_DELIMITER` are byte-identical between the two modules (confirmed by direct read). The audit finding treats this as ordinary duplication warranting extraction. But the duplication is not accidental — see Archive skim.

- [x] **Archive skim** — `grep -rl "fenceMask\|FENCE_DELIMITER" .flowtron/tasknote/archive/` → `CORE-421.2.md`, `CORE-421.N.md`, `CORE-423.md`, `FE-98.md`. Load-bearing findings:
  - [[CORE-421.2]] introduced the helper in `tasknote.ts` and its Implementation Notes state: *"Deliberately not done. No export of either helper — they are consumed only inside this module... [[CORE-416.3]]'s quality assertion treats public-surface growth as a cost."*
  - [[CORE-423]] ported the helper into `parser.ts` and its Best Practices Review states: *"CORE-421.2 deliberately did not export `fenceMask`/`FENCE_DELIMITER`... The established shape... is pure predicate + module-level regex constant, private to the consuming module — not a shared cross-module import. Porting means duplicating that same shape into `parser.ts`... not importing from `tasknote.ts`. `parser.ts` has no existing import from/to `tasknote.ts` today (checked both directions), so this preserves that boundary."* Its Quality Assertions explicitly reaffirm: *"the two per-module `fenceMask` copies are the established, deliberate pattern — `.2` chose module-private over shared-export."*
  - This is a decision made **twice**, independently, with a stated architectural reason (avoid growing public surface; avoid a `parser.ts`↔`tasknote.ts` coupling that doesn't otherwise exist), not an oversight.

- [x] **Drift check** — Code matches the PLAN.md line exactly: verbatim-duplicated `FENCE_DELIMITER`/`fenceMask` at `parser.ts:121` (comment)/`~124` (regex) and `tasknote.ts:139` (comment)/`~142` (regex) — line numbers close enough to the cited ones, no material drift there. **Contract drift found**: the task's premise ("extract into one shared-pure module") is not a neutral cleanup — it directly reverses a documented, twice-made architectural decision (CORE-421.2, reaffirmed by CORE-423) to keep this helper module-private and per-module rather than shared. Neither the `PLAN.md` line nor the 2026-08-26 audit finding cites or engages with that precedent, which suggests the audit sweep flagged the duplication as a code smell without checking archive history — exactly the failure mode the Archive skim step exists to catch. Note: a *third shared module* (as FE-99 proposes) is a different shape than what CORE-423 rejected (it doesn't create a `parser.ts`↔`tasknote.ts` import edge) — but it still requires exporting the helper from somewhere, which is the specific cost CORE-421.2 named.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions — asked via AskUserQuestion: proceed with extraction anyway / de-scope / re-scope to hardened comments. Operator chose De-scope.

- [x] Subtasks above populated with concrete, ordered steps — N/A. De-scope verdict jumps directly to Phase 4 closure; there is no execution plan to populate.

**Discovery Notes:**

Surfaced to the operator via AskUserQuestion before setting the Relevance Assessment verdict — this is a real fork (override a twice-documented precedent vs. respect it) rather than a judgment call for the agent to make unilaterally. Operator selected De-scope: the duplication stays as-is, precedent stands unchallenged, no code changes.

## 🛠️ Phase 2: Execution

- [ ] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [ ] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [ ] Implemented the minimal solution

- [ ] Updated/added tests for non-trivial behavior

**Implementation Notes:**

## 🧪 Phase 3: Testing & Linting

- [ ] Ran targeted test suite for changed code

- [ ] Ran lint/type-check on changed code

- [ ] **Quality assertions** — for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [ ] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — 17/17 no change. No code was touched (de-scoped in Discovery), and none of `README.md` / `AGENTS.md` / `SPEC.md` / `docs/MIGRATION.md` / `claude/AGENTS-snippet.md` / `codex/AGENTS-snippet.md` / `cursor/AGENTS-snippet.md` / `grok/AGENTS-snippet.md` / `docs/CONVENTIONS.md` / `CONTRIBUTING.md` / `SECURITY.md` / `docs/AGENT-NEUTRALITY.md` / `docs/PLATFORMS.md` / `claude/CAPABILITIES.md` / `docs/AGENT-COMPAT.md` / `docs/EXTERNAL-AGENTS.md` / `docs/WORKTREES.md` describes `viz/src/parser.ts` or `viz/src/tasknote.ts` internals.

- [x] Closed — the single Acceptance box is annotated `N/A — de-scoped`; YAML `status:` flipped to `completed` below; PLAN.md line to be flipped to the stub form and moved to `## Completed` in the same commit; tasknote to be moved to `.flowtron/tasknote/archive/fe/`.

- [x] **Evidence-based recap** drafted — see Final Summary below.

**Final Summary:**

**De-scoped — no code changed.** FE-99 asked to extract the verbatim-duplicated
`fenceMask`/`FENCE_DELIMITER` helper (`parser.ts:121` / `tasknote.ts:139`) into
one shared module. Archive skim found the duplication is a deliberate,
twice-made architectural decision, not an oversight: [[CORE-421.2]] introduced
the helper in `tasknote.ts` and explicitly kept it unexported ("consumed only
inside this module... public-surface growth is a cost"); [[CORE-423]] later
ported the same helper into `parser.ts` **by duplication, not import**,
specifically to preserve the fact that `parser.ts` and `tasknote.ts` have zero
import edges between them in either direction. Both notes name the same
reasoning independently.

**Changed files.** None — 0 LOC. The tasknote itself is the only artifact
this task produces.

**Verification.** N/A — no code changed, nothing to test or lint.

**Refactors.** None made; the one under consideration (extract to a shared
module) was explicitly declined per the Relevance Assessment.

**Documentation verdict.** 17/17 no change.

**Maintainability effect.** None directly, but this closure record itself
has value: it re-affirms the CORE-421.2/CORE-423 precedent a third time and
gives the next audit sweep (or the next `/ft-task FE-99`-shaped filing) a
tasknote to find via archive skim before re-flagging the same duplication.
The 2026-08-26 audit finding that filed this task did not surface or engage
with the existing precedent — a pattern worth naming for future audit runs,
though no action is taken here beyond this record.

**Archived:** 2026-08-26
