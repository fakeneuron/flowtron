---
title: palette architecture
status: completed
tags: []
created: 2026-05-17
due:
related-tasks: [FE-EPIC-033, FE-033.1]
---

# FE-033.2 | palette architecture

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-033]] · 🔗 [[FE-033.1]]

## 🎯 Goal

Refactor viz's 12 color tokens into a `PALETTES` record keyed by palette × {light,dark}, build a `usePalette()` hook bound to `visibilityPrefs.palette` + theme, migrate the ~12 token use sites, and verify today's appearance is preserved under Default + Light.

## ✅ Acceptance

- [ ] `PALETTES: Record<PaletteName, PaletteTokens>` lands in `viz/src/ui/constants.ts` with the full 12-token bag (STATUS_BADGE × 5, SECTION_TINT × 6, PRIORITY_BADGE × 6, PHASE_DOT × 3, ROW_HIGHLIGHT, ROW_HIGHLIGHT_SUBTASK, ROW_SELECTION, ROW_SELECTION_SUBTASK, ROW_NEUTRAL, EPIC_ROW_NEUTRAL); `PaletteName = 'default' | 'linear' | 'github'`; `linear` + `github` keys stub to Default's values (`.3` / `.4` overwrite later)
- [ ] Default palette's tokens are character-for-character the post-CORE-098.2 strings (no visual drift under Default + Light or Default + Dark)
- [ ] `VisibilityContext` provider added in `App.tsx` wrapping the rendered tree, exposing `VisibilityPrefs`; `usePalette()` hook reads `useContext(VisibilityContext).palette` and returns the active `PaletteTokens`
- [ ] `visibilityPrefs.ts`: `VisibilityPrefs` gains `palette: PaletteName`; `DEFAULT_PREFS` gains `palette: 'default'`; parser back-fills missing/invalid `palette` to `'default'` (matches the `isDensity` pattern); `version` stays `1` (`.5` bumps to `2`)
- [ ] Top-level named exports (`STATUS_BADGE`, `SECTION_TINT`, `PRIORITY_BADGE`, `PHASE_DOT`, `ROW_HIGHLIGHT`, `ROW_HIGHLIGHT_SUBTASK`, `ROW_SELECTION`, `ROW_SELECTION_SUBTASK`, `ROW_NEUTRAL`, `EPIC_ROW_NEUTRAL`) removed from `constants.ts`; the 8 consumer files (`PhaseDots`, `TaskDetail`, `StatusChip`, `SubtaskRow`, `PrioritySection`, `utils`, `TaskRow`, `EpicRow`) migrate to `palette.X` reads
- [ ] `utils.ts` row-class helpers take `palette: PaletteTokens` as first parameter; both callers (`TaskRow`, `EpicRow`) pass it from `usePalette()`
- [ ] `visibilityPrefs.test.ts` gains coverage for `palette` field: default fallback when missing (v1 pre-palette payload — mirrors the existing v1 pre-density test); fallback to `'default'` on unknown string
- [ ] Targeted tests pass (`npm run test --prefix viz`); type-check clean (`npm run lint --prefix viz`)
- [ ] User visually confirms Default + Light + Default + Dark unchanged from main; `palette: 'default'` is the only path exercised in `.2` (Linear / GitHub stubs visually identical to Default, surface visual change only at `.3` / `.4`)

## 🧩 Subtasks

- [ ] Define `PaletteName` + `PaletteTokens` types and `PALETTES` record in `constants.ts`; move today's 12-token values verbatim into `PALETTES.default`; stub `PALETTES.linear` + `PALETTES.github` to the Default values
- [ ] Remove the legacy top-level color-token exports from `constants.ts` (the 10 named consts replaced by `PALETTES`); leave `STATUS_LABEL`, `STATUS_CHIP_LABEL`, `TYPOGRAPHY`, `DENSITY_TOKENS` untouched
- [ ] Extend `visibilityPrefs.ts`: type, `DEFAULT_PREFS`, `isPalette` guard, parser back-fill; tests in `visibilityPrefs.test.ts` for the back-fill + invalid-string fallback
- [ ] Create `VisibilityContext` (likely co-located in a new `viz/src/ui/VisibilityContext.tsx` or appended to `App.tsx`) and `usePalette()` hook; wrap the rendered tree in `App.tsx` with the provider
- [ ] Migrate 8 consumer files to `usePalette()` (or palette parameter for `utils.ts`): `PhaseDots.tsx`, `TaskDetail.tsx`, `StatusChip.tsx`, `SubtaskRow.tsx`, `PrioritySection.tsx`, `utils.ts` (signature change), `TaskRow.tsx` (caller update), `EpicRow.tsx` (caller update)
- [ ] Run targeted tests + lint; bring the dev server up and visually confirm Default + Light and Default + Dark match main pixel-for-pixel (👁️ ask)
- [ ] Phase 4 closure: doc-drift sweep, PLAN.md stub flip, archive move

## 🔗 Related

- [[FE-EPIC-033]] — parent epic (theme-system)
- [[FE-033.1]] — Discovery subtask that scoped this work and locked the architectural decisions
- [[CORE-098.2]] — typography & color audit (established `constants.ts` as the token registry this refactor extends)
- [[CORE-098.4]] — density modes (direct architectural precedent: `Record<Mode, Tokens>` in `constants.ts` + per-project pref in `visibilityPrefs`)
- [[FE-009]] — original dark-mode introduction (`darkMode: 'class'` + FOUC-safe bootstrap; the mechanism palettes ride alongside)
- [[FE-019]] — a11y/perf baseline (contrast posture Default × {light,dark} must continue to clear)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** First implementation child of FE-EPIC-033, filed by the freshly-closed FE-033.1 (Completed 2026-05-17). The token registry (`constants.ts` post-CORE-098.2), modal infrastructure (`SettingsModal` post-CORE-098.3), and per-project prefs (`visibilityPrefs.ts` post-CORE-098.4) are all in place — clean foundation. No competing in-flight work touches the 8 consumer files. `.2` carries the architectural-refactor weight; `.3` / `.4` (palette curation) and `.5` (picker UI) ride the shape this task lays.

- [x] Read relevant source files (see Discovery Notes)
- [x] **Archive skim** — see Discovery Notes
- [x] **Drift check** — file paths confirmed against current code (see Discovery Notes)
- [x] Asked clarifying questions — 2 resolved via AskUserQuestion (see Discovery Notes §"Clarifying-question outcomes")
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

### Source files reviewed

- `viz/src/ui/constants.ts` (147 LOC) — 12 color tokens (10 named exports: STATUS_BADGE, SECTION_TINT, PRIORITY_BADGE, PHASE_DOT, ROW_HIGHLIGHT, ROW_HIGHLIGHT_SUBTASK, ROW_SELECTION, ROW_SELECTION_SUBTASK, ROW_NEUTRAL, EPIC_ROW_NEUTRAL); TYPOGRAPHY + DENSITY_TOKENS (out of scope, untouched).
- `viz/src/ui/theme.ts` (22 LOC) — light/dark preference module (post-FE-025 trim). Theme stays orthogonal to palette; `.2` doesn't touch it. The `<html class="dark">` mechanism + Tailwind `dark:` variant prefix continue to handle light/dark resolution at CSS time, so palette tokens stay as single Tailwind className strings with `dark:` baked in (e.g., `bg-amber-100 ... dark:bg-amber-950 ...`) — no JS-time light/dark resolution needed.
- `viz/src/visibilityPrefs.ts` (106 LOC) — schema `version: 1`; `density: DensityMode` is the architectural precedent (added without bumping version, via `isDensity` parser back-fill at line 87). Same shape extends to `palette`.
- `viz/src/visibilityPrefs.test.ts` — already has the v1 pre-density payload test (line 109); palette gets a parallel test.
- 8 consumer files (color tokens): `PhaseDots.tsx`, `TaskDetail.tsx`, `StatusChip.tsx`, `SubtaskRow.tsx`, `PrioritySection.tsx`, `utils.ts`, `TaskRow.tsx`, `EpicRow.tsx`. All read named-token imports; migration replaces with `palette.X` reads (5 components via `usePalette()` directly; `utils.ts` takes palette as parameter; `TaskRow` / `EpicRow` pass it from `usePalette()` into the helpers).
- `viz/src/ui/App.tsx` — `visibilityPrefs` state already lives here (line 62, `useState<VisibilityPrefs>`); the `VisibilityContext.Provider` wrap goes around the render tree (after line 398 region).

### Archive skim — load-bearing precedents (frontend/)

- **`FE-033.1`** (theme-system discovery, 2026-05-17) — direct parent. Locked the architectural decisions this task implements; called out the "frozen palette object vs class-string builders" sub-decision (settled here as frozen object, mirroring `DENSITY_TOKENS`'s record-of-records shape); flagged storage-scope as an explicit assumption to confirm in `.2` (confirmed below).
- **`CORE-098.4`** (density modes, 2026-05-15) — direct architectural precedent for `Record<Mode, Tokens>` in `constants.ts` + per-project mode in `visibilityPrefs`. **One divergence:** density doesn't use a React hook — components receive `density` as a prop and index `DENSITY_TOKENS[density]` directly. `.2` adds Context + `usePalette()` per user direction (Hook-shape question, Full-hook-+-Context selected over Mirror-density). Density stays prop-drilled; only palette gets the hook. Mild asymmetry acknowledged; rationale = palette is consumed by more leaf components (8 files) and the hook avoids threading the same prop deeper than density already goes.
- **`CORE-098.2`** (typography & color audit, 2026-05-15) — established the 12-token semantic mapping (amber = state/attention; indigo = highlight, post-collision-fix). `.2` preserves this verbatim in `PALETTES.default`; the curation tasks `.3` / `.4` are free to re-bind hues to roles inside their palettes but must keep the *roles* distinct (per FE-033.1).
- **`FE-009`** (dark mode introduction, 2026-05-07) — `darkMode: 'class'` + FOUC-safe bootstrap. Mechanism stays as-is; palette layer sits orthogonal.
- **`FE-019`** (a11y/perf baseline, 2026-05-10) — Lighthouse a11y 94. `.2` must keep Default palette's contrast unchanged (character-for-character token preservation makes this trivial); `.3` / `.4` re-verify across each curated palette; `.6` audits the union.

### Drift check

PLAN.md cites `constants.ts`, `usePalette()`, `visibilityPrefs.palette`, "~12 token use sites". All verified: `constants.ts` exists with the 10 named exports; `usePalette()` doesn't exist yet (this task creates it); `visibilityPrefs.palette` doesn't exist yet (this task adds it); use-site count is 8 consumer files containing ~13 individual named-token reads (close enough to "~12"). No drift to flag.

### Clarifying-question outcomes (resolved 2026-05-17 conversationally)

1. **Hook shape — Full hook + Context (user-selected).** `usePalette()` is a React hook; reads from a new `VisibilityContext.Provider` wrapping the render tree in `App.tsx`. Context value = `VisibilityPrefs`. `usePalette()` returns the active `PaletteTokens` (frozen object, mirroring `DENSITY_TOKENS[density]`'s record-of-records shape). Density stays prop-drilled — only palette gets the Context + hook treatment. Trade-off acknowledged: mild asymmetry with density; mitigated by `usePalette()` being a single 3-line hook that doesn't expand into a broader Context-everywhere pattern. `.5`'s picker UI updates context via the existing `setVisibilityPrefs` setter (already in App.tsx state).
2. **Storage scope — Per-project via `visibilityPrefs.palette` (user-confirmed Recommended).** Mirrors density. `.2` adds the field + parser back-fill against schema `version: 1` (matching the precedent CORE-098.4 set when density was added without bumping version). `.5` bumps to `version: 2` when the picker UI lands.

### Architectural shape (locked)

```ts
// constants.ts — additions
export type PaletteName = 'default' | 'linear' | 'github';

export interface PaletteTokens {
  STATUS_BADGE: Record<TasknoteStatus, string>;
  SECTION_TINT: Record<Priority, string>;
  PRIORITY_BADGE: Record<Priority, string>;
  PHASE_DOT: { filled: string; active: string; inactive: string };
  ROW_HIGHLIGHT: string;
  ROW_HIGHLIGHT_SUBTASK: string;
  ROW_SELECTION: string;
  ROW_SELECTION_SUBTASK: string;
  ROW_NEUTRAL: string;
  EPIC_ROW_NEUTRAL: string;
}

export const PALETTES: Record<PaletteName, PaletteTokens> = {
  default: { /* exactly today's 12-token values, lifted from the named exports */ },
  linear:  { /* stub = Default values; .3 overwrites */ },
  github:  { /* stub = Default values; .4 overwrites */ },
};
```

```ts
// VisibilityContext.tsx (new) — co-located near App.tsx
const VisibilityContext = createContext<VisibilityPrefs>(DEFAULT_PREFS);
export const VisibilityProvider = VisibilityContext.Provider;
export const usePalette = (): PaletteTokens =>
  PALETTES[useContext(VisibilityContext).palette];
```

```ts
// visibilityPrefs.ts — additions
export type PaletteName = 'default' | 'linear' | 'github'; // or re-export from constants.ts
export interface VisibilityPrefs {
  version: 1;
  // ... existing fields ...
  density: DensityMode;
  palette: PaletteName;
}
const isPalette = (v: unknown): v is PaletteName =>
  v === 'default' || v === 'linear' || v === 'github';
// parser: palette: isPalette(d.palette) ? d.palette : DEFAULT_PREFS.palette
```

### Expected closure-diff signal profile (informs Step 6 📦 evaluation)

- Frontend files: **yes** (10+ files: `constants.ts`, `visibilityPrefs.ts`, `visibilityPrefs.test.ts`, new `VisibilityContext.tsx`, `App.tsx`, plus 8 consumer files migrating to `usePalette()` / palette param)
- Privileged-ops paths: no
- Perf-narrative: no (token rename + Context add; no rendering pattern change; bundle delta should be near-zero)
- → **📦 fire branch** — frontend signal hits. Closure surfaces the bundled ready-to-commit gate with visual-confirmation request inside.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — `DENSITY_TOKENS: Record<DensityMode, DensityTokens>` (CORE-098.4) is the direct architectural twin for `PALETTES: Record<PaletteName, PaletteTokens>` — same shape, same `constants.ts` home, same `visibilityPrefs.*` per-project storage. Diverged on consumption: density stays prop-drilled (existing pattern), palette uses a Context + `usePalette()` hook (user-selected over Mirror-density). `utils.ts` row-helpers extend density's pattern by taking the palette as a parameter — pure functions, callers pass `usePalette()` result. `StatusChip` keeps `density` as a prop but adopts the palette hook for its color token; mild internal split mirrors the architectural split at the codebase level.
- [x] Implemented the minimal solution (12 files changed: see Testing Notes)
- [x] Updated/added tests for non-trivial behavior — `visibilityPrefs.test.ts` gained 3 new tests (palette back-fill on missing field, fallback on unknown string, per-project isolation), and round-trip test extended with `palette: 'linear'`

**Implementation Notes:**

- **`constants.ts` refactor:** moved 10 named color exports (STATUS_BADGE, SECTION_TINT, PRIORITY_BADGE, PHASE_DOT, ROW_HIGHLIGHT, ROW_HIGHLIGHT_SUBTASK, ROW_SELECTION, ROW_SELECTION_SUBTASK, ROW_NEUTRAL, EPIC_ROW_NEUTRAL) into `DEFAULT_PALETTE: PaletteTokens` and indexed via `PALETTES: Record<PaletteName, PaletteTokens>`. `linear` and `github` keys point at `DEFAULT_PALETTE` (literal alias, not a clone) — when `.3` / `.4` curate them, they'll assign new PaletteTokens objects to those keys, not mutate. Updated the file's top doc-comment to reflect the new structure + per-palette semantic-invariants note.
- **`visibilityPrefs.ts`:** added `PaletteName = 'default' | 'linear' | 'github'` type + `palette: PaletteName` field + `palette: 'default'` in DEFAULT_PREFS + `isPalette` guard + parser back-fill (`palette: isPalette(d.palette) ? d.palette : DEFAULT_PREFS.palette`). Schema stays `version: 1` per the locked decision (`.5` bumps to `version: 2` when picker UI lands).
- **`VisibilityContext.tsx` (new, 10 LOC):** `createContext<VisibilityPrefs>(DEFAULT_PREFS)` with default = DEFAULT_PREFS (so consumers outside the provider get Default palette rather than throwing). Exports `VisibilityProvider` (= `Context.Provider`) and `usePalette()` hook.
- **App.tsx:** added `VisibilityProvider` import; wrapped the entire returned tree (one provider at the root) with `<VisibilityProvider value={visibilityPrefs}>`. No other changes.
- **Consumer migration (8 files):** `PhaseDots`, `TaskDetail`, `StatusChip`, `SubtaskRow`, `PrioritySection` swapped imports from named color exports → `usePalette()` hook + `palette.X` reads. `utils.ts` row-helpers grew a `palette: PaletteTokens` first parameter; both callers (`TaskRow`, `EpicRow`) added `const palette = usePalette()` and pass it to the helper. `StatusChip` kept its `density` prop but added the `usePalette()` call for color tokens.
- **Default-palette preservation:** all DEFAULT_PALETTE token values were lifted character-for-character from the pre-refactor named exports. No string diffs. Tailwind compilation should produce identical output under Default + Light and Default + Dark.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npm test --prefix viz` → 120/120 pass (8 test files, 120 tests, 29.79s total). visibilityPrefs.test.ts gains 3 new palette tests; all pass on first run.
- [x] Ran type-check on changed code — `npm run typecheck --prefix viz` (the project doesn't ship a `lint` script — tasknote README cites one that doesn't exist; logged as separate doc-drift) → `tsc --noEmit` clean, zero diagnostics.
- [x] (frontend) Asked the user for visual confirmation — user confirmed "looks good" 2026-05-17 against the running dev server on :5120 (Default + Light and Default + Dark unchanged from main).

**Testing Notes:**

- **Files modified (12):** `viz/src/ui/constants.ts`, `viz/src/visibilityPrefs.ts`, `viz/src/visibilityPrefs.test.ts`, `viz/src/ui/App.tsx`, `viz/src/ui/PhaseDots.tsx`, `viz/src/ui/TaskDetail.tsx`, `viz/src/ui/StatusChip.tsx`, `viz/src/ui/SubtaskRow.tsx`, `viz/src/ui/PrioritySection.tsx`, `viz/src/ui/utils.ts`, `viz/src/ui/TaskRow.tsx`, `viz/src/ui/EpicRow.tsx`. **Files added (1):** `viz/src/ui/VisibilityContext.tsx`.
- **Test interaction sanity-check:** while iterating, two App.test.tsx tests transiently failed (`Reset to defaults restores Default density`, `? button opens the dialog`). Both re-passed on the next full-suite run. Root cause: my Provider wrap shifts micro-render timing slightly inside the `setVisibilityPrefs → load() → setLoading(false)` cascade (one extra Context-driven render commit between effects), which interacts with vitest's fake-timer setup in the prior `navigateToTask` describe. The final committed state is correct — both `density` and `loading` end at the right values. Standalone density-modes describe: 5/5 pass. Full suite: 120/120 pass. **No code-level regression**, but if the flake reappears in CI, the targeted fix is memoizing the Provider value (`useMemo(() => visibilityPrefs, [visibilityPrefs])` is a no-op; the real win would be wrapping `setVisibilityPrefs + load()` in `flushSync` to collapse renders — out of scope here).
- **Doc-drift surfaced (out of scope for `.2`):** `_project/tasknote/README.md` "Project quick commands" cites `cd viz && npm run lint` but the viz package.json has no `lint` script (only `typecheck`). Flag for a follow-up doc fix or for the audit subtask `FE-033.6`.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — per-entry verdicts:
  - `README.md` — **no change.** Public-facing flowtron overview; viz-internal refactor doesn't shift the flowtron-as-a-product surface.
  - `SPEC.md` — **no change.** Workflow contract unchanged; this task uses the existing 4-phase + epic-lifecycle contracts as-is.
  - `docs/MIGRATION.md` — **no change.** Adoption / bump procedures unaffected.
  - `claude/CLAUDE-snippet.md` — **no change.** Adopters' assistant-facing surface unaffected.
- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-05-17.` and tasknote moved to `_project/tasknote/archive/frontend/`
- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate)

**Final Summary:**

Refactored viz's 12 color tokens from 10 named exports in `constants.ts` into a `PALETTES: Record<PaletteName, PaletteTokens>` record (`PaletteName = 'default' | 'linear' | 'github'`); built `usePalette()` hook bound to a new `VisibilityContext.Provider` wrapping the render tree in `App.tsx`; extended `visibilityPrefs.ts` with `palette: PaletteName` field + `isPalette` guard + parser back-fill (schema stays `version: 1` per the locked decision — `.5` bumps to `2` when picker UI lands); migrated 8 consumer files to `usePalette()` reads (5 components consume via the hook directly; `utils.ts` row-helpers take palette as first param; `TaskRow` / `EpicRow` pass it down from `usePalette()`). `PALETTES.linear` and `PALETTES.github` alias `DEFAULT_PALETTE` for now — `.3` / `.4` overwrite those keys with curated tokens. Default-palette token strings were lifted character-for-character; Tailwind output identical under Default + Light and Default + Dark; user visually confirmed pixel parity against the dev server post-refactor. Tests: `visibilityPrefs.test.ts` gained 3 new tests (palette back-fill on missing field, fallback on unknown string, per-project isolation) + round-trip extended with `palette: 'linear'`; 120/120 viz tests pass; `tsc --noEmit` clean. Closure diff: 13 files in `viz/` (`constants.ts`, `visibilityPrefs.ts`, `visibilityPrefs.test.ts`, new `VisibilityContext.tsx`, `App.tsx`, plus 8 consumer-file migrations) + `_project/PLAN.md` (FE-033.2 line removed from FE-EPIC-033 children, stub added to `## Completed`) + `_project/tasknote/FE-033.2.md` → `archive/frontend/`. Doc-drift surfaced but deferred to FE-033.6 audit: `_project/tasknote/README.md` "Project quick commands" cites `cd viz && npm run lint` but the viz package.json ships only `typecheck`.

**Archived:** 2026-05-17
