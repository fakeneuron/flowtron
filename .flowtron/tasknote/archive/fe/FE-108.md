---
title: model-chip-xheavy-glyph
status: completed
tags: [viz]
created: 2026-09-12
due:
related-tasks: []
touches:
  - viz/src/ui/ModelChip.tsx
  - viz/src/ui/ModelChip.test.tsx
---

# FE-108 | model-chip-xheavy-glyph

[← PLAN.md](../PLAN.md) · ✅ Completed

## 🎯 Goal

`ModelChip` renders 🔭 for the `[xheavy]` category token (the same asymmetric-chip design FE-078 used for `[heavy]`→🧠), with its SPEC citation corrected and test coverage added.

## ✅ Acceptance

- [x] `ModelChip` renders 🔭 for the `xheavy` token — `npm --prefix viz test -- ModelChip`
- [x] `ModelChip` still renders 🧠 for heavy-tier tokens and null for medium/light tokens (no regression) — same command
- [x] `xheavy` and `heavy` glyphs are mutually exclusive (no token renders both) — same command
- [x] `ModelChip.tsx`'s SPEC citation points at the section that actually defines the glyph mapping — `judgment`: reviewed against `SPEC/model.md` §"Tier ladder vs. the next-move suggestion glyph"
- [x] Type-check and lint clean — `npm --prefix viz run typecheck` · `npm --prefix viz run lint`

## 🧩 Subtasks

- [x] Add an `XHEAVY_MODELS` set (`'xheavy'` only — no concrete token buckets to xheavy per SPEC/model.md) and branch on it before the existing `HEAVY_MODELS` check
- [x] Correct the `HEAVY_MODELS` comment's SPEC citation from §"Category-vs-concrete matching" to §"Tier ladder vs. the next-move suggestion glyph" (the section that actually defines tier→glyph, which is what this component implements)
- [x] Extend `ModelChip.test.tsx`: positive case for `xheavy`→🔭, plus mutual-exclusivity guards (`xheavy` doesn't render 🧠; `heavy` doesn't render 🔭)
- [x] Run targeted tests, typecheck, lint

## 🔗 Related

- [[FE-078]] — predecessor; established the asymmetric chip design (bare glyph for heavy-tier only, null for medium/light) and added the `'heavy'` category token this task's `XHEAVY_MODELS` mirrors

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** `ModelChip.tsx` still only handles the heavy tier (`HEAVY_MODELS`) and renders null for everything else, including `xheavy` — the finding reproduces exactly as filed. The PLAN line's cited section ("SPEC.md §Task-line format") doesn't itself carry a glyph table; the canonical tier→glyph mapping (`[light]`→🔧, `[medium]`→🧩, `[heavy]`→🧠, `[xheavy]`→🔭, concrete tokens bucket to their inherent tier, none bucket to 🔭) lives in `SPEC/model.md` §"Tier ladder vs. the next-move suggestion glyph" — confirmed by reading it. Scope matches the PLAN line.

- [x] Read relevant source files — `viz/src/ui/ModelChip.tsx` (11 LOC), `viz/src/ui/ModelChip.test.tsx` (9 tests), `viz/src/ui/TaskRowInner.tsx` (sole call site), and grepped all of `viz/src` for other 🧠/🔭 usage — none found outside `ModelChip`, so no legend or second call site needs updating.

- [x] **Best Practices Review** — single-responsibility change; extends `ModelChip`'s existing `Set`-per-tier shape (matches FE-078's pattern) rather than introducing a new abstraction. No refactor needed; nothing deferred.

- [x] **Archive skim** — `archive/fe/` confirmed against the README table. `FE-078.md` (model-chip-category-tokens) is the direct predecessor: it added the `'heavy'` category token and explicitly documents the asymmetric design ("bare 🧠 for opus-tier only... medium/light stay glyph-free"), which this task extends one rung further rather than overturning. `FE-037.md` (cited by FE-078) originated that asymmetric design. No other archive hit touches `ModelChip.tsx`.

- [x] **Drift check** — PLAN line's file/behavior claims (`ModelChip`, `[xheavy]`→🔭, `ModelChip.test.tsx`) all match current code. The PLAN line's own SPEC citation is imprecise (see Relevance Assessment) — that imprecision is exactly what "refresh its SPEC citation" asks to fix, not a blocker.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  No clarifications needed (--fast). Assumptions: "refresh its SPEC citation" means correcting `ModelChip.tsx`'s own inline comment citation (currently §"Category-vs-concrete matching", about the Step 1.5 model gate, not glyph rendering) to the section that actually defines the glyph table; the asymmetric design (no chip for medium/light) stays unchanged — only the heavy/xheavy split is added.

- [x] Subtasks above populated with concrete, ordered steps, and YAML `touches:` declared

**Discovery Notes:** `parser.ts`'s `TaskModel` doc comment (touched by FE-078) doesn't mention `xheavy` either, but the PLAN line's `touches` scope is `ModelChip` + its test only — left untouched as out of scope.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:** Pattern survey — added a second `Set` literal (`XHEAVY_MODELS`) alongside the existing `HEAVY_MODELS`, matching FE-078's established shape; no new abstraction. Minimal refactor gate — nothing else touched. `ModelChip.tsx`: added `XHEAVY_MODELS = new Set(['xheavy'])`, branched on it before the `HEAVY_MODELS` check (both return early, so no token can match both), and corrected the `HEAVY_MODELS` comment's citation to §"Tier ladder vs. the next-move suggestion glyph". `ModelChip.test.tsx`: added a `describe('ModelChip — 🔭 xheavy-model glyph', …)` block with the positive case plus two mutual-exclusivity guards, following the existing file's `describe`/`it` structure.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Verification receipt** — recorded each Acceptance verify command in Testing Notes as `command → exit code`, with the first failure line when non-zero; and, for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation — suppressed (`--fast` implied by `[unattended]`); operator owns visual confirmation. Change is a one-glyph addition to an existing chip, covered by RTL assertions.

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**
- `npm --prefix viz test -- --run ModelChip` → 0 (12 passed — 9 existing + 3 new)
- `npm --prefix viz run typecheck` → 0
- `npm --prefix viz run lint` → 0
- Structural: no duplication (second `Set` mirrors the first's shape by design, per FE-078 precedent), no dead code, no public-surface growth (component signature unchanged), no code-facing doc references the pre-fix glyph mapping.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — no change on all `.flowtron/tasknote/README.md` §"AI-referenced docs" entries; this task edits only inline code comments and a test file, none of which any AI-referenced doc mirrors.

- [x] Closed — every `## ✅ Acceptance` criterion ticked; `status:` flipped to `completed`; PLAN.md line flipped to stub form under `## Completed`; tasknote moved to `archive/fe/`.

- [x] **Evidence-based recap** drafted (below).

**Final Summary:**
`viz/src/ui/ModelChip.tsx` (+7/−2): added an `XHEAVY_MODELS = new Set(['xheavy'])` branch rendering 🔭, checked before the existing `HEAVY_MODELS`→🧠 branch (mutually exclusive by construction), and corrected the `HEAVY_MODELS` comment's SPEC citation from §"Category-vs-concrete matching" to §"Tier ladder vs. the next-move suggestion glyph" — the section that actually defines the tier→glyph table this component implements. `viz/src/ui/ModelChip.test.tsx` (+17): new `describe` block covers the positive `xheavy`→🔭 case and two mutual-exclusivity guards. Verification: targeted tests 12/12 (9 existing unchanged + 3 new), typecheck 0, lint 0. Doc-drift sweep: no change. `touches:` reconciliation: `git diff --name-only` = the two declared paths exactly. Maintainability effect: the asymmetric heavy-tier chip design (FE-078/FE-037) now extends cleanly to the manual-only `xheavy` rung, and the inline comment points a future reader at the section that actually governs the behavior.

**Archived:** 2026-09-12
