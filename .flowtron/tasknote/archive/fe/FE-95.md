---
title: viz-suite-flake-structural
status: completed
tags: []
created: 2026-08-26
due:
related-tasks: [FE-045, FE-053, FE-089.2]
touches:
  - viz/vite.config.ts
  - viz/src/ui/App.test.tsx
---

# FE-95 | viz-suite-flake-structural

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-089.2]]

## 🎯 Goal

Fix the App.test.tsx full-suite flake structurally — cap vitest worker parallelism and/or split App.test.tsx per feature — so repeated clean full-suite runs succeed under load with no further timeout bumps.

## ✅ Acceptance

- [x] Repeated clean full-suite runs under load (multiple consecutive `npm test` full-suite passes, including with concurrent CPU load) — 4/4 green post-change, incl. one run under 6 CPU burners
- [x] No further per-test/per-hook timeout bumps introduced (the FE-045/FE-053/FE-089.2 approach is off the table) — no timeout value touched
- [x] Structural change in place: vitest worker parallelism capped and/or App.test.tsx split per feature — `maxWorkers: '50%'` landed; split deferred by operator choice (Discovery ask)

## 🧩 Subtasks

- [ ] Add `maxWorkers: '50%'` (with rationale comment) to the `test` block in `viz/vite.config.ts`; confirm the percentage form is honored by vitest 4.1.6, else use an explicit number
- [ ] Run repeated full-suite runs (`npm test`) under load — target ≥3 consecutive green
- [ ] `typecheck` + `lint` clean
- [ ] Doc-drift sweep + closure

## 🔗 Related

- [[FE-045]] — prior timeout-bump attempt (exhausted approach)
- [[FE-053]] — prior timeout-bump attempt (exhausted approach)
- [[FE-089.2]] — deflake wikilink-nav waits (release-gate flake hardening epic; exhausted approach)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Flake reproduced on the first baseline run today: 1/470 fail at the FE-089.2 wikilink `findByRole` site (`App.test.tsx:44`), suite 101.7s under load (vs 12–14s cold in FE-089.2) — the 5000ms `asyncUtilTimeout` expires under CPU contention even after FE-053/FE-089.2's shared-config bumps. Structural lever confirmed empirically: `--maxWorkers=4` (vs default 8 = `os.availableParallelism()` on this 8-CPU machine) ran 470/470 green twice consecutively under the same load (64.6s, 31.9s), with `environment` cost halved (253.8s → 127.4s → 47.2s).

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; log relevant findings in Discovery Notes before re-interpreting the task

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions — asked the lever choice (PLAN's "and/or"); operator chose **cap workers only**, split deferred (optionally filed as follow-up for maintainability)

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- Read: `viz/vite.config.ts` (test block: `testTimeout: 15_000` from FE-053; no worker config — vitest 4.1.6 defaults to forks pool with `maxWorkers = os.availableParallelism()` = 8 here), `viz/src/test/setup.ts` (`asyncUtilTimeout: 5000`, FE-053), `viz/src/ui/App.test.tsx` (1389 lines, 19 `describe` blocks, zero local timing overrides since FE-089.2), `viz/package.json` (vitest ^4.1.6, 25 test files / 470 tests total).
- Archive skim: [[FE-053]] (raised `testTimeout` 15s + `asyncUtilTimeout` 5s; characterized flake as load-driven), [[FE-089.2]] (removed all five per-call/per-test overrides; its Testing Notes flag "if the gate flakes again, escalate the global" — that escalation path is now declared exhausted by this task's PLAN line), FE-052 (Node-26 slowness root cause). FE-045 is cited in-file (fake-timers→real-timers) but has no FE-area tasknote — predates the tasknote or lives elsewhere; not load-bearing here.
- Drift check: PLAN line says `App.test.tsx` — actual path `viz/src/ui/App.test.tsx` (same minor path drift FE-053 noted; no impact). No worker-parallelism config exists anywhere yet (grep `maxWorkers|poolOptions|fileParallelism` over viz/ config: none). Baseline evidence above confirms the audit's root-cause hypothesis (parallel contention, not per-test slowness).
- Mechanism note against split-only: `environment` (jsdom per-file setup) dominates suite cost; splitting App.test.tsx into N files *adds* N−1 fresh jsdom environments while doing nothing about worker contention. The cap is the lever that attacks the contention directly.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

- One config key: `maxWorkers: '50%'` added to the `test` block in `viz/vite.config.ts`, with a rationale comment in the established FE-053 comment style (each timing/parallelism key carries its why + task ID). Percentage form verified against vitest 4.1.6's type surface (`maxWorkers?: number | string` — "number or percentage of workers"); `'50%'` scales with core count instead of hardcoding this machine's 8.
- Pattern survey: extends the existing shared-config shape (all suite timing/parallelism knobs live in `vite.config.ts` `test` + `setup.ts`; FE-053/FE-089.2 invariant of zero per-test knobs preserved). No new tests — the change is test infra; the 470-test suite is the coverage. Minimal refactor gate: nothing touched beyond the one key + comment; App.test.tsx split deliberately deferred per operator choice.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Quality assertions** — for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation — N/A: test-infra config only, no UI surface changed (same rationale as FE-053/FE-089.2)

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

- Baseline (default 8 workers, organic machine load): 1/470 fail at `App.test.tsx:44` (wikilink `findByRole` expiry — the exact FE-089.2 site), 101.7s.
- Pre-change probe: `--maxWorkers=4` green twice under the same load (64.6s, 31.9s).
- Post-change (`maxWorkers: '50%'` in config): **4 consecutive full-suite runs, 470/470 green every time** — 21.3s, 36.9s, 36.6s, and 45.9s with **6 concurrent `yes` CPU-burner processes** pinned for the whole run (synthetic worst-case load; burners verified killed afterwards).
- `typecheck` + `lint` clean.
- Quality assertions: one config key + comment; no duplication, no dead code, no public-surface growth; comment matches the file's established FE-053 style. Honest bound (same as FE-089.2's): this removes the suite's self-contention and measured green under heavy synthetic load, but no configuration can prove zero flake probability under arbitrary external load. If it ever recurs, the remaining lever is the deferred per-feature split of App.test.tsx.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — all 17 AI-referenced docs: **no change**. Grep for worker/parallelism/vitest mentions across the full roster (and viz/README.md beyond it) returned zero hits; no doc describes the viz test-runner parallelism config.

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed per SPEC/tasknote-selection.md §"`## Completed` archive convention" (standalone → top of `## Completed`; epic child → kept nested beneath its active parent), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Capped viz vitest fork-pool parallelism at half the cores (`maxWorkers: '50%'`, `viz/vite.config.ts` `test` block, +9 lines incl. rationale comment) — the structural fix for the App.test.tsx full-suite flake after FE-053/FE-089.2 exhausted the timeout-bump lever. Root cause confirmed empirically: with the default (all 8 cores) the suite starves its own userEvent waits under machine load (baseline: 1/470 fail, 101.7s); capped, it ran 470/470 green four consecutive times including once under 6 synthetic CPU burners (21–46s), with jsdom environment cost cut by more than half. No timeout values touched; App.test.tsx per-feature split deferred by operator choice at the Discovery ask. Typecheck + lint clean; 17/17 AI-referenced docs unaffected. Maintainability: parallelism now lives beside the FE-053 timing knobs in shared config, and the deferred split remains the documented next lever if flake ever recurs.

**Archived:** 2026-08-26
