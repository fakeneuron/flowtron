---
title: unattended-chip
status: completed
tags: []
created: 2026-09-13
due:
related-tasks: [CORE-494]
touches:
  - viz/src/ui/UnattendedChip.tsx
  - viz/src/ui/TaskRowInner.tsx
  - viz/src/ui/TaskRow.test.tsx
---

# FE-118 | unattended-chip

[← PLAN.md](../PLAN.md) · ✅ Completed · 🔗 [[CORE-494]]

## 🎯 Goal

Render the already-parsed `Task.unattended` flag as an `[unattended]` chip beside the model chip on task rows, so rows an autonomous runner may dispatch are visible on the board.

## ✅ Acceptance

- [x] A task row with `unattended: true` renders an unattended chip immediately after the model chip in the right-hand chip cluster; a row with `unattended: false` renders none — `npm --prefix viz test -- TaskRow`
- [x] The chip carries a `title` / `aria-label` naming the marker so the glyph is legible on hover and to assistive tech — `grep -q 'aria-label' viz/src/ui/UnattendedChip.tsx`
- [x] `npm --prefix viz test`, `run typecheck`, and `run lint` all green
- [ ] Chip reads correctly on the live board in both themes — `👁️` (visual; no command decides glyph legibility) — **not-met at closure:** the ask is suppressed under the implied `--fast`; the operator confirms on their live board.

## 🧩 Subtasks

- [x] Add `viz/src/ui/UnattendedChip.tsx` — glyph-only chip with `title` + `aria-label`, same shape as `ClosureDriftChip`
- [x] Render `{task.unattended && <UnattendedChip />}` in `TaskRowInner.tsx` right after the `ModelChip` line
- [x] Add a `TaskRow.test.tsx` describe with the present / absent cases
- [x] Run targeted test, typecheck, lint

## 🔗 Related

- [[CORE-494]] — predecessor: parsed `Task.unattended` in `viz/src/parser.ts` and deferred the render to this task

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** The gap is live at HEAD — `Task.unattended` is set at `viz/src/parser.ts:405` and no file under `viz/src/ui/` reads it. The board therefore hides the one row-level marker an operator-less runner keys on; a chip is the smallest render that closes that.

- [x] Read relevant source files — `viz/src/ui/TaskRowInner.tsx` (full), `ModelChip.tsx`, `CriticalChip.tsx`, `ClosureDriftChip.tsx`, `TaskRow.test.tsx`, `ModelChip.test.tsx`, `viz/src/parser.ts` (marker block + `Task` type), `viz/src/visibilityPrefs.ts` (`rowChips` keys), `SPEC/cue-vocabulary.md` §"Glyph layers and reuse" + §"Non-cue glyphs". Narrow, known read set — no probe.

- [x] **Best Practices Review** — one new presentational component + one render line; no dependency-direction shift (chip depends on nothing; `TaskRowInner` already imports sibling chips). Existing abstraction: the right-hand chip cluster at `TaskRowInner.tsx:95-101`, where each chip is one file. No nearby duplication to fold; no refactor needed; no cleanup deferred.

- [x] **Archive skim** — 22 archived notes touch the paths in scope, most about row layout/plumbing, not chip shape. Load-bearing: **FE-037 → FE-078 → FE-108** established and extended the *asymmetric glyph-only chip* design for `ModelChip` (bare glyph, null when nothing to say); **FE-101.4** memoized `TaskRow` and wrote the only two `TaskRow.test.tsx` cases (render-count spies) — the new case lives beside them; **CORE-494** (Related) parsed the field and *deliberately* shipped no renderer, offering this task as the follow-up — so the chip is the intended completion, not a reversal.

- [x] **Drift check** — `viz/src/parser.ts:143` is exactly the `UNATTENDED_MARKER` line the PLAN cites; `TaskRowInner.tsx` / `ModelChip.tsx` exist as named; `TaskRow.test.tsx` exists with a `Task` fixture already carrying `unattended: false`. Plan matches the PLAN line and contradicts no SPEC contract — the viz chip is a layer outside the cue table (`SPEC/cue-vocabulary.md` §"Glyph layers and reuse"), and the chosen glyph collides with nothing in that table or its non-cue residual.

- [x] No clarifications needed (--fast). Explicit assumptions: (1) **Glyph-only chip, 🤖**, with `title` + `aria-label` "Unattended — may be dispatched with no operator present", matching the FE-037 asymmetric shape and `ClosureDriftChip`'s labelling — not a padded text pill, which would widen the tight right cluster. 🤖 appears nowhere in the cue table or its non-cue residual; ⚡ was rejected because it already means `--fast active` and the marker only *implies* `--fast`. (2) **Ungated** — rendered whenever `task.unattended`, like `CriticalChip` / `ClosureDriftChip`; no new `rowChips` key, since the PLAN line asks for visibility, not a toggle. (3) New file `UnattendedChip.tsx` rather than a branch inside `ModelChip.tsx` — one chip per file is the cluster's convention, and `ModelChip` maps model → glyph, a different responsibility. (4) `EpicRow` is out of scope: the PLAN line names task rows only.

- [x] Subtasks above populated with concrete, ordered steps, and YAML `touches:` declared with the paths this task expects to edit (omit only on a task with no file deliverable)

**Discovery Notes:**

- Right-cluster order at `TaskRowInner.tsx:96-100`: `CriticalChip` → `StatusChip` → `ClosureDriftChip` → `ModelChip` → `extraRightSlot`. The new chip slots between `ModelChip` and `extraRightSlot` ("beside the model chip").
- `TaskRow.test.tsx` uses `render` from RTL but not `screen`; the new case adds `screen` and queries by `aria-label`.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended the one-chip-per-file right-cluster pattern (`ClosureDriftChip` is the closest sibling: glyph + `title` + `aria-label`, no props); composed into `TaskRowInner` with one conditional render line, no new context or pref.

- [x] **Minimal refactor gate** — no refactor; nothing in the touched path was duplicated or mis-bounded.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — two `TaskRow.test.tsx` cases (marker present → chip with 🤖; absent → no chip), queried by `aria-label`.

**Implementation Notes:**

- `viz/src/ui/UnattendedChip.tsx` (new, 12 lines): propless `React.FC`, `🤖`, label "Unattended — may be dispatched with no operator present".
- `viz/src/ui/TaskRowInner.tsx` (+2): import + `{task.unattended && <UnattendedChip />}` directly after the `ModelChip` line.
- `viz/src/ui/TaskRow.test.tsx` (+24): `screen` import + a new describe block.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npm --prefix viz test -- --run TaskRow`: 4 passed (2 existing memo cases + 2 new)

- [x] Ran lint/type-check on changed code — `tsc --noEmit` and `eslint src` clean

- [x] **Verification receipt** — see Testing Notes. Changed code: no duplication (one new component, one call site), no dead code, no public-surface growth beyond the one export the row consumes, and the component's header comment cites the SPEC section that owns the marker.

- [x] (frontend) Suppressed — `--fast` implied by the row's own `[unattended]` marker; operator owns the visual confirmation.

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**

- `npm --prefix viz test -- --run TaskRow` → 0 (4 passed)
- `npm --prefix viz test` → 0 (29 files, 552 passed)
- `npm --prefix viz run typecheck` → 0
- `npm --prefix viz run lint` → 0
- `grep -q 'aria-label' viz/src/ui/UnattendedChip.tsx` → 0

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — every entry in `.flowtron/tasknote/README.md` §"AI-referenced docs": **no change**. The change is a viz-only render of an already-documented grammar segment; none of the swept docs enumerate board chips (`viz/README.md`, outside the sweep set, does not either).

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed per SPEC/tasknote-selection.md §"`## Completed` archive convention" (standalone → top of `## Completed`; epic child → kept nested beneath its active parent), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — see Final Summary.

**Final Summary:**

- **Changed:** `viz/src/ui/UnattendedChip.tsx` (new, 12 lines), `viz/src/ui/TaskRowInner.tsx` (+2), `viz/src/ui/TaskRow.test.tsx` (+24).
- **Verified:** full viz suite 552/552, typecheck, lint — all exit 0 (receipt above).
- **Refactors:** none made, none deferred.
- **Docs:** no change (sweep above).
- **`touches:` reconciliation:** `git diff --name-only` matches the three declared paths exactly; no undeclared paths.
- **Maintainability effect:** the parsed `Task.unattended` field now has a consumer on flowtron's own board, so a marker seeded by an operator is visible at a glance; the chip follows the existing one-file-per-chip shape, so a future toggle or glyph change is a one-file edit.

**Archived:** 2026-09-13
