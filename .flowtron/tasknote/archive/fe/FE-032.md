---
title: projectselector-restyle
status: completed
tags: []
created: 2026-05-16
due:
related-tasks: [FE-EPIC-033]
---

# FE-032 | projectselector-restyle

[← PLAN.md](../../PLAN.md) · ✅ Completed · 🔗 [[FE-EPIC-033]]

## 🎯 Goal

Restyle the ProjectSelector chips in viz's header rail so per-chip visual weight, hover/active states, and focus rings feel intentional instead of default-flat.

## ✅ Acceptance

- [x] `ProjectSelector` renders as a segmented control matching the View-mode toggle's visual language (`App.tsx:325-348`) — outer `inline-flex rounded border + shadow`, per-segment `rounded-l` / `rounded-r` / no-rounding, internal dividers between middle segments
- [x] Active state uses `bg-slate-800 text-white dark:bg-slate-200 dark:text-slate-900` (matches View-mode active fill)
- [x] Hover/inactive state matches View-mode: `hover:bg-slate-50 dark:hover:bg-slate-800` on the inactive segments
- [x] Focus ring matches the rest of the header cluster: `focus:outline-none focus:ring-2 focus:ring-slate-400 dark:focus:ring-slate-500`
- [x] "Project:" label preserved (kept outside the segmented group, same visual position)
- [x] `aria-pressed` + `aria-label="Project: <name>"` semantics preserved (existing `ProjectSelector.test.tsx` passes unchanged)
- [x] `PILL_ACTIVE` / `PILL_DEFAULT_SLATE` removed from `constants.ts` (also dropped `PILL_FOCUS_RING` — only used to compose the other two, transitively unused)
- [x] `npm test` + `npm run typecheck` clean in `viz/`
- [x] Visual confirmation accepted by user (👁️)

## 🧩 Subtasks

- [x] Rewrite `viz/src/ui/ProjectSelector.tsx` to the segmented-control shape with inline classes matching the View-mode toggle pattern; preserve aria semantics + the "Project:" label outside the group; add internal dividers between middle segments
- [x] Grep for remaining consumers of `PILL_ACTIVE` / `PILL_DEFAULT_SLATE`; deleted all three pill tokens from `constants.ts` (zero consumers post-rewrite)
- [x] Run `npm test` in `viz/` — 117/117 pass (no ProjectSelector test breakage)
- [x] Run `npm run typecheck` in `viz/` — clean
- [x] Request visual confirmation (👁️ prefix) — user replied "go"

## 🔗 Related

- [[FE-EPIC-033]] — theme-system epic; may subsume this restyle if a palette picker lands first

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** PLAN intent is sound and well-localized — `ProjectSelector` is a 32-line component with one call site (`App.tsx:381`) and tokens used only here post-FE-031. User confirmed the complaint covers shape + visual weight + interaction states (all three), and wants alignment with the View-mode segmented toggle so header controls share one visual system. FE-EPIC-033 subsumption flag from the PLAN line declined — this is a small, low-risk win now; a theme-system epic later would still benefit from a unified header shape.

- [x] Read relevant source files
- [x] **Archive skim** — see Discovery Notes
- [x] **Drift check** — see Discovery Notes (clean — all paths/symbols match current code)
- [x] Asked clarifying questions (2 questions; user picked "all three" complaint + "unify with View-mode")
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- **Current shape (`ProjectSelector.tsx`).** 32 lines, one component, one call site at `App.tsx:381`. Renders `<span>Project:</span>` followed by one `<button>` per project, each `rounded-full px-2 py-0.5` with `PILL_ACTIVE` (active) or `PILL_DEFAULT_SLATE` (inactive). Both pill tokens defined at `constants.ts:99-100` and (per FE-031 closure notes) used **only** here post-trim. Aria contract: `aria-pressed={on}` + `aria-label={`Project: ${name}`}`.
- **Header-control neighbors.** `App.tsx:325-348` defines a View-mode segmented toggle (List / Board) with inline classes: outer `inline-flex rounded border border-slate-300 bg-white shadow-sm dark:border-slate-700 dark:bg-slate-900`, per-button `rounded-l`/`rounded-r` + `px-3 py-1.5 text-base focus:outline-none focus:ring-2 focus:ring-slate-400 dark:focus:ring-slate-500`, active `bg-slate-800 text-white dark:bg-slate-200 dark:text-slate-900`, inactive `hover:bg-slate-50 dark:text-slate-200 dark:hover:bg-slate-800`. **This is the existing pattern the restyle extends** (pattern-survey verdict for Phase 2).
- **Project count.** Variable N from filesystem scan (`workspace.ts:39-55`); typical solo-dev range is 1-5. Segmented control handles that range cleanly; truncation/scroll is a non-issue at these counts.
- **Archive skim findings:**
  - `FE-031` (viz-chip-system-trim, 2026-05-14) — explicit drift note: *"ProjectSelector in the PLAN line is a misnomer — ProjectSelector is the header-level project switcher, not a per-row chip; left untouched."* FE-032 is the deferred half. Also notes `PILL_DEFAULT_SLATE` was dropped from App.tsx during FE-031 because the Tags-row removal left ProjectSelector as its only consumer — so the tokens are already nearly-private to this component.
  - `FE-023` (viz-constants-consolidation) — created the `PILL_*` tokens; expected to be used cross-cutting but FE-031 collapsed the surface.
  - `FE-002` / `FE-005` / `FE-007` / `FE-008` — original ProjectSelector / row-density / App-split provenance. No load-bearing decisions for the restyle beyond confirming the component is stable single-purpose.
- **Drift check.** `viz/src/ui/ProjectSelector.tsx` exists ✓; `App.tsx:381` call site present ✓; `PILL_ACTIVE` / `PILL_DEFAULT_SLATE` at `constants.ts:99-100` ✓. PLAN line cites no specific line numbers — nothing else to drift-check.
- **Scope discipline (explicit non-goals).** View-mode toggle's inline classes are **not** extracted into shared `SEGMENTED_*` tokens — that's a DRY refactor on adjacent code the task doesn't call for. ProjectSelector gets matching inline classes; later if the user wants the extraction, that's a separate task. `tags:` frontmatter field, header search input, ⓘ button, ThemeToggle, ⚙️ button — all out of scope.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — View-mode segmented toggle at `App.tsx:325-348` is the existing pattern. ProjectSelector extends it directly (inline classes mirroring outer + per-button shape, active fill, hover, focus ring); the only addition is internal dividers between middle segments since ProjectSelector has variable N ≥ 2 buttons where View-mode has exactly 2.
- [x] Implemented the minimal solution
- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

- **`ProjectSelector.tsx`** rewritten end-to-end (~32 → 45 LOC). Removed `PILL_ACTIVE` / `PILL_DEFAULT_SLATE` import. Outer group: `role="group" aria-label="Project"` with `inline-flex rounded border border-slate-300 bg-white shadow-sm dark:border-slate-700 dark:bg-slate-900` matching the View-mode toggle exactly. Per-segment classes computed inline: `corner` = `rounded-l` / `rounded-r` / `''` based on index; `divider` = `border-l border-slate-300 dark:border-slate-700` on all but the first segment. Per-segment base: `px-3 py-1.5 text-base focus:outline-none focus:ring-2 focus:ring-slate-400 dark:focus:ring-slate-500`. Active: `bg-slate-800 text-white dark:bg-slate-200 dark:text-slate-900`; inactive: `hover:bg-slate-50 dark:text-slate-200 dark:hover:bg-slate-800`. "Project:" label kept outside the group as a sibling `<span>` — same visual position, just bumped wrapper gap from `gap-1.5` → `gap-2` to match the breathing room around the View-mode toggle.
- **`constants.ts`** trimmed: deleted `PILL_FOCUS_RING`, `PILL_ACTIVE`, `PILL_DEFAULT_SLATE` (lines 97-100). Grep confirmed zero remaining consumers across `viz/src/`.
- **Scope discipline.** View-mode toggle classes left inline (not extracted into shared `SEGMENTED_*` tokens) — out of scope for this task; would be a separate DRY refactor touching unrelated code.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npm test` in `viz/`: **117/117 pass** (including all 3 existing `ProjectSelector.test.tsx` cases, unchanged — they check aria semantics, not classes)
- [x] Ran lint/type-check on changed code — `npm run typecheck` (tsc --noEmit) clean; no `lint` script in `viz/`
- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask) — user replied "go"

**Testing Notes:**

- ProjectSelector tests passed without modification; the rewrite preserved the aria contract (`aria-pressed`, `aria-label="Project: <name>"`, button per project, empty-list returns null).
- Typecheck clean confirms the `constants.ts` deletions left no dangling imports anywhere in `viz/src/`.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — per-entry verdicts:
  - `README.md` — **update.** Line 31 said "project-selector chip group in the header rail" — "chip group" was already loose phrasing for the pill row, and is wrong for the new segmented control. Rewrote to "The header-rail project selector swaps the active project" (matches the wording already in `claude/CLAUDE-snippet.md`).
  - `SPEC.md` — **no change.** "chip" references in lines 206-215 describe the tasknote-body nav-header status chip glyph (🟢 / ✅ / ⏸ / ⚪ / 🌱), unrelated to ProjectSelector.
  - `docs/MIGRATION.md` — **update.** Line 346 "the header-rail chip group swaps the active project" → "the header-rail project selector swaps the active project". Same rationale as README.
  - `claude/CLAUDE-snippet.md` — **no change.** Already used implementation-neutral "header-rail project selector" wording (which the README/MIGRATION updates now align with).
- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-05-16.` and tasknote moved to `_project/tasknote/archive/frontend/`
- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate)

**Final Summary:**

Restyled `ProjectSelector` from a row of round pill buttons to a segmented-button control matching the View-mode (List / Board) toggle's visual language — outer rounded border + shadow, dark-slate active fill, matching hover/focus states, internal dividers between middle segments for the variable-N case. Header controls now share one visual system. Files: `viz/src/ui/ProjectSelector.tsx` (full rewrite, +13 LOC net), `viz/src/ui/constants.ts` (deleted 3 now-unused pill tokens, -4 LOC), `README.md` + `docs/MIGRATION.md` (one-line wording fixes from "chip group" → "project selector" matching CLAUDE-snippet phrasing). Tests: 117/117 pass; typecheck clean; visual confirmation accepted. Pill tokens removed cleanly — segmented-control style stays inline (View-mode pattern unchanged, no scope creep into adjacent header controls).

**Archived:** 2026-05-16
