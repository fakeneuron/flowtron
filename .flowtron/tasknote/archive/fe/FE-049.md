---
title: deflake-wikilink-nav-test
status: completed
tags: []
created: 2026-06-01
due:
related-tasks: []
---

# FE-049 | deflake-wikilink-nav-test

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Eliminate the 1-in-3 intermittent timeout in the wikilink-navigation test under the full parallel vitest run, and correct the stale comment that claims real timers make the gate deterministic.

## ⚡ Notes

**Relevance:** Proceed — single-test flakiness fix + comment correction; no design tradeoffs.

**Drift check:** Confirmed: `findByRole` at line 42 (`viz/src/ui/App.test.tsx:42`) has no explicit timeout (uses @testing-library/dom default 1000ms). Comment at lines 23–29 still says "make the gate deterministic" — inaccurate since 1/3 runs still fail under parallel load.

**Archive skim:** No prior FE tasknotes (archive dir missing/empty).

**Pattern survey:** Other `findByRole` calls in this file use defaults; the `waitFor` at lines 45–47 and 51–53 use the same default. The raised timeout at line 42 is the earliest async point after the heavy rAF+setTimeout chain fires — raising it here is the correct insertion point.

**Implementation:** Added `{ timeout: 4000 }` to `findByRole` at line 42. Updated the comment at lines 23–29 to accurately state that real timers reduce (not eliminate) flakiness, and that the raised `findByRole` timeout is the complementary mitigation.

**Docs touched:** no change

## ✅ Recap

Raised `findByRole` timeout at line 42 from the 1000ms default to 4000ms — the tight default was the intermittent failure point under jsdom contention from the parallel test run. Corrected the comment at lines 23–29 to drop the false "deterministic" claim and describe both mitigations accurately.

**Archived:** 2026-06-01
