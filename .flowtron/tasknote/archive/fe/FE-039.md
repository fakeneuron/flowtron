---
title: board-critical-positioning
status: completed
tags: []
created: 2026-05-18
due:
related-tasks: [[FE-044]]
---

# FE-039 | board-critical-positioning

[← PLAN.md](../PLAN.md) · ✅ Completed

## 🎯 Goal

Render Critical as the leftmost board column when it has tasks, and omit it from the board entirely when empty — matching the existing empty-when-empty behavior of the other board sections.

## ✅ Acceptance

- [x] In board view, when any task has `priority: Critical`, the Critical column renders as the **leftmost** column (left of High).
- [x] In board view, when no tasks have `priority: Critical`, no Critical column is rendered (no empty placeholder).
- [x] List view ordering is unchanged (Critical remains above High via the existing `SECTIONS` order).
- [x] Targeted vitest covers both board-mode cases (Critical-present and Critical-empty).
- [x] Lint + type-check clean on changed files; full vitest suite green.

## 🧩 Subtasks

- [x] Edit `viz/src/ui/App.tsx`: move `'Critical'` from `BELOW_BOARD_SECTIONS` to the front of `BOARD_SECTIONS`.
- [x] Add two board-view tests to `viz/src/ui/App.test.tsx`: (a) Critical-present → renders as first board column; (b) Critical-empty → no Critical column in board.
- [x] Run targeted vitest (`viz/src/ui/App.test.tsx`) plus full viz suite.
- [x] Run `npm --prefix viz run lint` and `npm --prefix viz run typecheck` on changed files.
- [x] 👁️ Visual confirmation in browser (board view, with and without Critical tasks).
- [x] Phase 4 doc-drift sweep + closure.

## 🔗 Related

- [[FE-044]] — follow-up: drop `'Critical'` from `Priority` in favor of a per-task critical flag (supersedes the column-ordering approach).

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Direction is unambiguous, surface area is small (one constants edit + tests), and the existing `BoardView.tsx:38` `visibleSections` filter gives empty-when-empty for free. Pure positioning fix.

- [x] Read relevant source files
- [x] **Archive skim** — no prior FE tasknotes (`_project/tasknote/archive/fe/` does not exist yet); no archive risk.
- [x] **Drift check** — minor line drift: PLAN.md cites `App.tsx:42-43` but actual is `App.tsx:41-42` (the comment-stripped lines). `BoardView.tsx:38 visibleSections` filter matches exactly. Constant names and contents (`BOARD_SECTIONS`, `BELOW_BOARD_SECTIONS`) match the description verbatim. Hypothesis confirmed.
- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  **No clarifications needed.** Assumptions:
  - Critical column gets its existing `SECTION_TINT['Critical']` styling automatically via `PrioritySection` — no special handling needed.
  - Adding `'Critical'` to `BOARD_SECTIONS` does not change list-view (`SECTIONS` is separate and untouched).
  - Keyboard nav order (`visibleIds`) iterates `SECTIONS` which already has Critical first — unchanged.
  - The empty-when-empty behavior is delegated to `BoardView`'s existing filter; no new filter logic needed.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- Source: `viz/src/ui/App.tsx:41-42` defines the two section arrays; line 413 renders `<BoardView sections={BOARD_SECTIONS} ... />` and lines 428-449 render `BELOW_BOARD_SECTIONS` as stacked `PrioritySection`s below the board.
- `BoardView.tsx:38` already filters empty sections — so dropping Critical into `BOARD_SECTIONS` covers the "empty → don't render" requirement without new logic.
- `PrioritySection` styles via `palette.SECTION_TINT[priority]` — Critical's red tint already exists in all palettes (Default/Linear/GitHub).
- List view (line 454) filters `SECTIONS` by non-empty and is unaffected.
- Tests: `viz/src/ui/App.test.tsx` exists with `renderApp` fixture; view mode persisted via localStorage key `flowtron-viz-active-view-mode` (from `viewMode.ts`). New test will seed `'board'` before render.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — `BoardView.tsx:38` already filters empty sections via `visibleSections = sections.filter((p) => (bySection[p] ?? []).length > 0)`. `PrioritySection` styles each priority via `palette.SECTION_TINT[priority]`. Both are priority-agnostic — no new shape needed; just reorder the constants.
- [x] Implemented the minimal solution
- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

- `viz/src/ui/App.tsx:41-42` — moved `'Critical'` from `BELOW_BOARD_SECTIONS` to the front of `BOARD_SECTIONS`. Net diff: 2 lines.
- `viz/src/ui/App.test.tsx` — added a new `describe('App — board view Critical positioning (FE-039)')` block with two tests: leftmost-when-present (asserts column order `['Critical','High','Medium']`) and absent-when-empty (asserts `Critical` not present in the board container). Both seed `'flowtron-viz-view': 'board'` in localStorage before render.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `App.test.tsx` 26/26 pass.
- [x] Ran lint/type-check on changed code — `npm --prefix viz run lint` and `npm --prefix viz run typecheck` both clean.
- [x] Full viz suite — 148/148 passing across 10 files.
- [ ] 👁️ (frontend) Asked the user for visual confirmation

**Testing Notes:**

- Live dev-server verification on `http://localhost:5174` (`flowtron` project):
  - **Critical-present case** (temporarily injected `FE-999` under `## Critical` in `_project/PLAN.md`, then reverted): board column order via SSE refresh = `['Critical', 'Medium', 'Low']` (High empty → filtered). Critical sits in the leftmost slot. Screenshot: `/tmp/playwright-mcp/fe-039-critical-present.png`.
  - **Critical-empty case** (after revert): board column order = `['Medium', 'Low']`. No Critical column rendered. Screenshot: `/tmp/playwright-mcp/fe-039-critical-empty.png`.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — `README.md` / `SPEC.md` / `docs/MIGRATION.md` / `claude/CLAUDE-snippet.md` / `docs/CONVENTIONS.md` / `CONTRIBUTING.md` — all "no change". None mention board-section ordering or the `BOARD_SECTIONS`/`BELOW_BOARD_SECTIONS` constants.
- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-05-18.` and tasknote moved to `_project/tasknote/archive/frontend/`.
- [x] Recap drafted.

**Final Summary:**

Board view now renders Critical as the leftmost column when populated and omits it entirely when empty — same shape as the other priority columns, no special-casing. Implementation was a 2-line constant reorder in `viz/src/ui/App.tsx:41-42`; the existing `BoardView.tsx:38` `visibleSections` filter gives empty-when-empty for free, and `PrioritySection`'s `palette.SECTION_TINT[priority]` lookup already styles Critical's red tint across all palettes.

Files: `viz/src/ui/App.tsx` (2 lines), `viz/src/ui/App.test.tsx` (+ ~50 lines, new `describe` block with two tests). Vitest 148/148, lint + typecheck clean. Verified live via the dev server with a temporary `## Critical` injection (reverted).

Follow-up filed as **FE-044** — user decided mid-task to drop `'Critical'` as a `Priority` value entirely in favor of a per-task critical flag. FE-039 lands as a small, correct improvement that remains valid until that redesign happens.

**Archived:** 2026-05-18
