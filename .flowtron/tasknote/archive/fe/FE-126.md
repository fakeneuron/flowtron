---
title: vite-config-comment-trim
status: completed
tags: [viz, comments]
created: 2026-09-23
due:
related-tasks: [FE-EPIC-125, FE-125.2]
touches:
  - viz/vite.config.ts
---

# FE-126 | vite-config-comment-trim

[← PLAN.md](../PLAN.md) · ✅ Completed

## 🎯 Goal

Strip task-ID provenance from `viz/vite.config.ts` comments per the FE-125.2 pilot rule, keeping the rationale and backticking the CVE reference.

## ⚡ Notes

**Relevance:** Proceed — `vite.config.ts` sat outside FE-EPIC-125's `viz/src` scope but carries the same provenance pattern (bare task-ID tags, history narration) the pilot rule targets; a sidequest stub from FE-125.N's audit already scoped the exact lines.
**Best Practices Review:** Comment-only edit; no responsibility, dependency, or public-surface change.
**Drift check:** Sidequest stub's line numbers (L88, L96, L149, L163, L170-171) confirmed current against the live file.
**Archive skim:** `archive/fe/` (confirmed against README table: `FE-*` → `archive/fe/`). FE-125.2 defines the pilot rule this task applies: provenance = bare task-ID tags and "before X / is now" narration; keep = present-tense rationale, SPEC/Contract pointers, and backticked syntax-example IDs. FE-125.3/FE-125.N applied the same rule to the rest of `viz/src`; `vite.config.ts` was their one out-of-scope miss.
**Declared scope:** `viz/vite.config.ts`
**Pattern survey:** Extends the FE-125.2 pilot rule verbatim — no new shape.
**Implementation:** 5 comment edits in `viz/vite.config.ts`: dropped bare tags `(CORE-222)` L88 and rewrote the history narrative `was the cost CORE-431.2 removes` (L96) to present tense ("would waste CPU at this scale"); backticked `` `CVE-2025` `` (L149); dropped trailing `See FE-053.` (L163) and, on the `maxWorkers` comment, rewrote `(FE-053/FE-089.2 exhausted that lever)` → `(already maxed out)` and dropped `See FE-95.` (L170-171). Verification receipt:
- gate grep (`grep -nE '^\s*(//|/\*|\*)' viz/vite.config.ts | sed -E 's/\`[^\`]*\`//g' | grep -E '\b[A-Z]{2,}(-EPIC)?-[0-9]+'`) → exit 1 (no output)
- comment-only diff (`git diff -U0 viz/vite.config.ts | grep -E '^[+-][^+-]' | grep -vE '^[+-]\s*(//|/\*|\*|$)'`) → exit 1 (comment-only)
- `npm --prefix viz run typecheck` → 0 · `npm --prefix viz run lint` → 0 · `npm --prefix viz test` → 0 (587 passed)
- Structural: no code touched; no duplication, dead code, or public-surface change.
**Docs touched:** no change — no entry in `.flowtron/tasknote/README.md` §"AI-referenced docs" quotes `vite.config.ts` comment text.

## ✅ Recap

Applied the FE-125.2 pilot rule to `viz/vite.config.ts`, the one file FE-EPIC-125's `viz/src` scope missed. Five comment-only edits: stripped bare task-ID tags (`CORE-222`, `See FE-053.`, `See FE-95.`), rewrote two history narratives into present-tense rationale (`CORE-431.2 removes` → "would waste CPU at this scale"; `FE-053/FE-089.2 exhausted that lever` → "already maxed out"), and backticked the CVE reference (`` `CVE-2025` ``). Gate grep and comment-only-diff checks both pass (exit 1, no output); `npm --prefix viz test`/`typecheck`/`lint` all green. `touches:` reconciliation: declared `viz/vite.config.ts`; diff also carries `.flowtron/PLAN.md`, this tasknote, and the deleted `.flowtron/sidequest/FE-126.md` stub (workflow files) — no undeclared deliverable paths.

**Archived:** 2026-09-23
