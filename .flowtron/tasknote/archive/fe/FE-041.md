---
title: phase-dot-size-and-contrast
status: completed
tags: []
created: 2026-05-19
due:
related-tasks: []
---

# FE-041 | phase-dot-size-and-contrast

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Bump PhaseDots from 8px to 10px, fix the inactive-dot color so it clears ≥3:1 non-text contrast in both light and dark mode, and document the N/4 tail decision.

## ✅ Acceptance

- [ ] `h-2 w-2` → `h-2.5 w-2.5` in `PhaseDots.tsx`
- [ ] Inactive token passes ≥3:1 in all three palettes, both light and dark
- [ ] N/4 tail decision documented in tasknote
- [ ] `tsc --noEmit` + ESLint clean
- [ ] Visual confirmation from user

## 🧩 Subtasks

- [ ] Update dot size in `PhaseDots.tsx`
- [ ] Update inactive token in all three palettes in `constants.ts`
- [ ] Type-check + lint
- [ ] Request visual confirmation

## 🔗 Related

- [[FE-019]] — viz-a11y-perf-pass (established the ≥3:1 non-text contrast bar)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed  
  **Rationale:** Task description matches code exactly — `h-2 w-2` at `PhaseDots.tsx:22`, title attr confirmed, inactive token identical across all 3 palettes. No drift.

- [x] Read relevant source files
- [x] **Archive skim** — `_project/tasknote/archive/fe/` absent; no prior tasknotes.
- [x] **Drift check** — all file paths, class names, and palette token values in the task description match the current code exactly.
- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

**Contrast analysis** (all row backgrounds verified from `App.tsx:265` and `TaskRow.tsx:37`):
- Light mode row bg: `bg-white` (#ffffff, lum=1.0)
- Dark mode row bg: `bg-slate-900` (#0f172a, lum≈0.016)

| Token | Light contrast | Dark contrast | Pass 3:1? |
|---|---|---|---|
| `bg-slate-200 dark:bg-slate-700` (current) | 1.22:1 | 1.81:1 | ❌ both fail |
| `bg-slate-500 dark:bg-slate-400` (proposed) | 4.36:1 | 6.59:1 | ✅ both pass |

Fix: replace `inactive: 'bg-slate-200 dark:bg-slate-700'` with `inactive: 'bg-slate-500 dark:bg-slate-400'` in all three palettes (DEFAULT, LINEAR, GITHUB all share the same inactive token).

**N/4 tail decision:** Skip. The dots now pass contrast. The `aria-label` covers screen-reader users; `title` covers hover. Adding visible text ("2/4") would clutter the compact chip row and require restructuring the flex container. The size bump + contrast fix makes the dots readable without a label tail. Decision is final; no follow-up ticket needed.

No clarifications needed. Assumptions: "fix" is implied when contrast fails; "consider" tail was evaluated and rejected.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — pure token substitution in existing palette objects and class string; no new shape needed
- [x] Implemented the minimal solution
- [x] Updated/added tests for non-trivial behavior — no test changes needed (visual-only; palette token values are not unit-tested)

**Implementation Notes:**

- `PhaseDots.tsx:22`: `h-2 w-2` → `h-2.5 w-2.5`
- `constants.ts`: `inactive: 'bg-slate-200 dark:bg-slate-700'` → `inactive: 'bg-slate-500 dark:bg-slate-400'` (all 3 palettes via `replace_all`)
- N/4 tail: skipped (documented in Discovery Notes)

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — no test changes; visual-only diff
- [x] Ran lint/type-check on changed code — both clean
- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:**

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — all 6 AI-referenced docs: no change
- [x] Closed — PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` (see §"`## Completed` archive convention") and tasknote moved to `_project/tasknote/archive/<area>/`
- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Bumped PhaseDots from 8px to 10px and fixed the inactive-dot token (`bg-slate-200 dark:bg-slate-700` → `bg-slate-500 dark:bg-slate-400`) across all three palettes, bringing light-mode contrast from 1.22:1 to 4.36:1 and dark-mode from 1.81:1 to 6.59:1 — both now clear the ≥3:1 FE-019 bar. N/4 tail skipped (documented). Two files changed: `PhaseDots.tsx` (1 line), `constants.ts` (3 lines via replace_all).

**Archived:** 2026-05-19
