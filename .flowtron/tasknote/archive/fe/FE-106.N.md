---
title: viz-tier-guardrail audit
status: completed
tags: []
created: 2026-09-09
due:
related-tasks: [FE-EPIC-106, FE-106.2, FE-106.3, FE-106.4]
---

# FE-106.N | viz-tier-guardrail audit

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-106]]

## 🎯 Goal

Verify the completed FE-EPIC-106 (`viz-tier-guardrail`) cohort sits coherently in the codebase: cumulative doc-drift sweep across `.flowtron/tasknote/README.md` §"AI-referenced docs", naming/style consistency across the cohort's deliverables, and follow-up filings for any miss.

## ✅ Acceptance

- [x] **Doc-drift sweep (fixed line, per SPEC/epic.md §"Audit acceptance — fixed doc-drift line")** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the specific update. Always present; surfaces cumulative slice-local staleness that per-task Phase 4 closures can miss.
- [x] Cohort coherence inventory: each implementation child's deliverables read against the others (naming consistency, style parity, no contradictory cross-refs)
- [x] No regressions surfaced in earlier-shipped cohort children's surfaces
- [x] Audit findings recorded in Implementation Notes; misses cited as candidates for `/ft-file-followup <NEW-ID>` filing (filed AFTER audit closure to preserve `/ft-file-followup`'s filing-discipline gate)
- [x] Single `feat: FE-106.N — audit FE-EPIC-106` (or `chore: ...` if no code edits land) commit lands
- [x] PLAN.md line for `FE-106.N` flipped to stub form `Completed YYYY-MM-DD.`
- [x] Tasknote moved to `.flowtron/tasknote/archive/fe/FE-106.N.md`
- [x] Parent-flip prompt surfaced after audit closure (skill Step 8) — user confirms or declines flipping `FE-EPIC-106` to `Completed` and moving the cohort to `## Completed`

## 🧩 Subtasks

- [x] Inventory cohort children's archived tasknotes — read each implementation child's Final Summary + Implementation Notes; capture deliverables in Discovery Notes
- [x] Walk `.flowtron/tasknote/README.md` §"AI-referenced docs" entries — fixed doc-drift sweep
- [x] Cohort coherence pass — naming consistency, style parity, no contradictory cross-refs across the cohort's deliverables
- [x] Surface audit findings in Implementation Notes; cite each miss as a `/ft-file-followup <NEW-ID>` candidate
- [x] Phase 4: flip `FE-106.N` PLAN line to stub form + archive tasknote
- [x] Parent-flip: skill Step 8 prompts user; on confirm, atomic flip parent line + move cohort to `## Completed`

## 🔗 Related

- [[FE-EPIC-106]] — parent epic (viz-tier-guardrail)
- [[FE-106.2]] — eslint-node-tier-list (cohort child)
- [[FE-106.3]] — js-yaml-merge-cve (cohort child)
- [[FE-106.4]] — types-node-engines (cohort child)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** All three implementation children (`.2` `.3` `.4`) are `[x]` and archived, the parent `FE-EPIC-106` is still active in `## Medium`, and `.N` is the reserved terminal audit child. No early-audit gate fired (zero open siblings). Discovery was supplied by audit-repo at filing — there is no `.1` tasknote, which every child already recorded. Closed 2026-09-09, same day as the children.

- [x] Read relevant source files — the three archived cohort tasknotes in full, then each claimed deliverable against HEAD: `viz/eslint.config.js`, `viz/package.json`, `viz/package-lock.json`, `viz/src/tasknote.test.ts`, `viz/src/tasknote-parse.ts`, `viz/README.md` §"Hard rule", `viz/src/ui/App.test.tsx`, `viz/src/ui/useProjectData.test.ts`. Narrow known set; no probe.

- [x] **Best Practices Review** — N/A as a code-design review: the audit's deliverable is verification, not a new code surface. No inline fix landed.

- [x] **Archive skim** — self-referential by construction (cohort children are the archive entries under review). Beyond the cohort, the children's own skims plus a targeted grep of `archive/fe/` for `eslint.config` / `tasknoteRead` / `js-yaml` / `@types/node` pointed at [[FE-97]] (original eight-name denylist, extensionless only), [[FE-104]] (added `tasknoteRead` + README list, did not edit eslint), [[FE-085]] (direct `js-yaml` 4.3.1 + CORE_SCHEMA + `!!omap` test), and [[FE-090.N]] (accepted `@types/node` 20-vs-engines asymmetry; deferred `js-yaml` 5 / `@types/node` 26). No ⚠️ `Superseded by` pointer on the cohort; FE-090.N's asymmetry is the historical residual `.4` closed, not a false-at-the-time claim.

- [x] **Drift check** — every cohort deliverable still matches HEAD (verified by grep, not recall): eslint `src/ui/**` group lists all nine README-named modules in README order, each with extensionless + `.ts` twins (`eslint.config.js:45-63`); `js-yaml` is `^4.3.2` with lockfile `4.3.2` and the GHSA-2883 merge-key test at `tasknote.test.ts:825`; `@types/node` is `^24.13.4` with lockfile `24.13.4` and no remaining `.at(-1)` in `viz/src`. Re-read PLAN lines at `.flowtron/PLAN.md:18-22`; they match as filed. No SPEC contradiction: `.N` is the reserved audit suffix, children stayed nested, no `.1` is the audit-repo-supplied-Discovery shape the children already documented.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  No clarifications needed. Explicit assumptions: (a) audit scope is the full three-child cohort, none deferred; (b) items each child already deferred (gray-matter nested `js-yaml@3.15.1`, `js-yaml` 5, `@types/node` 26, tsconfig `lib` ES2020, permanent lint-rule unit test) stay deferred unless HEAD shows they became in-scope; (c) no `.1` is not a miss — PLAN says "Discovery supplied by audit-repo".

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

**Cohort inventory** (one line per child, deliverable verified against HEAD):

| Child | Deliverable | Verified at HEAD |
|---|---|---|
| [[FE-106.2]] `eslint-node-tier-list` | ui-tier denylist closed: `tasknoteRead` plus `.ts` twins for all nine README Node-only modules (8 → 18 patterns) | `eslint.config.js:45-63` matches README §"Hard rule" names and order |
| [[FE-106.3]] `js-yaml-merge-cve` | direct `js-yaml` `^4.3.1` → `^4.3.2` (GHSA-2883) + CORE_SCHEMA merge-key regression next to `!!omap` | `package.json:19`, lockfile `4.3.2`, `tasknote.test.ts:825`, `tasknote-parse.ts:24` |
| [[FE-106.4]] `types-node-engines` | `@types/node` `^20.14.10` → `^24.13.4` so typecheck matches engines + CI Node 24; two test `.at(-1)` rewrites | `package.json:32`, lockfile `24.13.4`, `App.test.tsx:1158`, `useProjectData.test.ts:19` |

**Suite state at audit time** — `npm --prefix viz test` **28 files / 531 tests passing**; `run typecheck` and `run lint` clean. Matches `.4`'s recorded 531; `.3` added the 57th `tasknote.test.ts` case (57/57 still).

**Recorded so the next audit does not re-raise them:**

- **A. No `.1` Discovery tasknote.** Filing used audit-repo Discovery; PLAN parent line says so; every child omitted `blocked-by` / `parallel-safe-with` for that reason. Not a miss.
- **B. gray-matter nested `js-yaml@3.15.1` still in the lockfile.** `.3` deferred it: parse path overrides gray-matter's YAML engine with `CORE_SCHEMA` (`tasknote-parse.ts:26-27`). Still true; GHSA-2883's 3.15.2 patch is unused-path residual, not a new gap.
- **C. Permanent lint-rule unit test still absent.** FE-97 and `.2` both verified with a temp probe and deferred a fixture. Established pattern, not a cohort miss.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — N/A for the verification body of the audit (no new code surface; no inline fix).

- [x] **Minimal refactor gate** — no refactor. No code or archived-record correction landed.

- [x] Implemented the minimal solution — verification pass over the cohort coherence checklist; findings recorded below. Nothing in scope to patch inline.

- [x] Updated/added tests for non-trivial behavior — N/A: no code changed.

**Implementation Notes:**

### Cohort children inventoried

- [[FE-106.2]] — closed the FE-97/FE-104 denylist gap. README already named nine modules including `tasknoteRead`; eslint now restricts all nine in both import forms. Probe-verified then deleted.
- [[FE-106.3]] — patched GHSA-2883 on the declared parser dep and pinned the CORE_SCHEMA non-merge with a sibling of the `!!omap` test. Comment on `tasknote-parse.ts` names the advisory. Independent of `.2`.
- [[FE-106.4]] — moved types onto the CI Node 24 line (closes the FE-090.N accepted asymmetry) and adapted two tests that lost `Array.at` under `lib: ES2020`. Shares `package.json` / lockfile with `.3` but a different key; no overwrite.

### Coherence findings

**No inconsistencies surfaced.** Three independent slices of one viz-toolchain theme (eslint denylist · parser CVE pin · types/engines alignment). Naming, style, and `[[…]]` chains agree: each child points at the parent, the other two siblings, and `.N`; `.2` additionally cites the FE-97/FE-104 predecessors; `.4` cites FE-090.N as the residual it closes. No child contradicts another on versions, module lists, or schema.

**No regressions in earlier-shipped surfaces.** `.2`'s 18-pattern denylist is intact after `.3`/`.4` (neither touches `eslint.config.js`). `.3`'s `js-yaml@4.3.2` and merge-key test are intact after `.4` (`.4` bumped a different `package.json` key). `.4`'s 531-test suite still passes, including `.3`'s 57 `tasknote.test.ts` cases and the two rewritten last-element lookups.

### Inline fixes

None.

### Follow-up candidates (file after audit closure)

**None.** Deferred residuals listed in Discovery Notes B–C stay with the children that recorded them; HEAD does not make them in-scope.

### Checked and cleared (not findings)

- **`.2` `related-tasks` lists FE-97/FE-104 instead of siblings** — optional key; the Related prose still names `.3`/`.4`/`.N`. Legal.
- **`.3` `touches:` omits `tasknote-parse.ts`** despite a one-line GHSA-2883 comment — optional planning key; omission means undeclared.
- **`viz/README.md` not in the sweep set** — already listed `tasknoteRead`; denylist now matches; no edit needed.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — no code changed, so no targeted suite for the audit diff. Ran the full viz suite as the audit's regression check over the cohort's surface: `npm --prefix viz test` → **28 files, 531 tests, all passing**.

- [x] Ran lint/type-check on changed code — `npm --prefix viz run typecheck` exit 0; `npm --prefix viz run lint` exit 0. Audit diff is markdown-only; these are the cohort verification.

- [x] **Quality assertions** — N/A for the diff: markdown audit record, no code. No duplication, dead code, complexity, or public-surface growth.

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line) — N/A. No UI/visual surface. The denylist is a lint rule; the other two children are dep pins + tests.

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

- Full suite 531/531 matches `.4`'s recorded count; `tasknote.test.ts` still 57/57 (omap + GHSA-2883 merge-key both present).
- No `.at(-1)` remains under `viz/src`; the two `.4` rewrites (`arr[arr.length - 1]`) are the live form.
- Lint clean with the 18-pattern denylist in place (no leftover probe file).

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep (the fixed audit line)** — walked all 18 entries. **No change** on every one: `README.md` · `AGENTS.md` · `SPEC.md` · `docs/MIGRATION.md` · `claude/AGENTS-snippet.md` · `codex/AGENTS-snippet.md` · `cursor/AGENTS-snippet.md` · `grok/AGENTS-snippet.md` · `docs/CONVENTIONS.md` · `CONTRIBUTING.md` · `SECURITY.md` · `docs/AGENT-NEUTRALITY.md` · `docs/PLATFORMS.md` · `claude/CAPABILITIES.md` · `docs/AGENT-COMPAT.md` · `docs/EXTERNAL-AGENTS.md` · `docs/WORKTREES.md` · `docs/VISION.md`.

  Swept as a **cumulative cohort sweep**: grepped the whole set in one pass for `js-yaml` / `4.3.1` / `GHSA-2883` / `CORE_SCHEMA` / `@types/node` / `20.14.10` / `no-restricted-imports` / `tasknoteRead`. **Exactly one region matched**, then checked by hand:

  - **`SECURITY.md`:249** — names `readTasknoteDir` (`viz/src/tasknoteRead.ts`) as the shared containment helper for `readArchive` and `/api/active`. `.2` only added that module to the ui-tier eslint denylist; it did not change the helper or the containment claim. Still accurate. **No change.**
  - **`docs/CONVENTIONS.md`:54** — "on Node 24 with npm caching." Matches CI `node-version: 24` and `.4`'s `@types/node` `^24.13.4`. No edit. **No change.**
  - **`AGENTS.md`** — viz `test` / `typecheck` / `lint` commands; all three exist and all three passed this session. **No change.**
  - **`README.md`** — describes `viz/` at a level (what it is, how to run it) that no cohort change reaches. **No change.**

  `viz/README.md` is not a sweep-set member; consulted because `.2`'s whole point is matching it. §"Hard rule" still lists the same nine modules in the same order the eslint group now restricts. Accurate as written.

- [x] Closed — every `## ✅ Acceptance` criterion ticked (none annotated `N/A` or not-met), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form, then tasknote moved to `.flowtron/tasknote/archive/fe/`. Operator confirmed parent-flip: `FE-EPIC-106` stubbed and the nested cohort moved atomically to the top of `## Completed`.

- [x] **Evidence-based recap** drafted — see Final Summary

**Final Summary:**

The `FE-EPIC-106` (`viz-tier-guardrail`) cohort is coherent and all three deliverables are live at HEAD. The audit surfaced **no inconsistencies and no follow-ups to file**. Operator confirmed parent-flip: parent stubbed and the nested cohort (`.2` `.3` `.4` `.N`) moved to the top of `## Completed`.

- **Files changed:** this audit tasknote + PLAN.md (`.N` stub, parent stub, cohort moved to `## Completed`). Workflow-only. No code, no archived-record correction.
- **Verification:** `npm --prefix viz test` 28 files / **531 tests** passing; typecheck and lint clean. Deliverables re-confirmed at HEAD by grep: 18 eslint patterns including `tasknoteRead` + `.ts` twins; `js-yaml` 4.3.2 + merge-key test; `@types/node` 24.13.4 with index lookups in the two tests that lost `Array.at`.
- **Refactors:** none.
- **Documentation:** no drift across all 18 AI-referenced docs. The one sweep-set hit (`SECURITY.md`'s `tasknoteRead` containment helper) is unchanged by the denylist close. `docs/CONVENTIONS.md` already documents CI Node 24.
- **Follow-ups — none.** gray-matter nested `js-yaml@3.15.1` and the missing lint-rule fixture remain the children's recorded deferrals; HEAD does not make them in-scope.
- **Maintainability:** the three children bound different axes of the same viz toolchain (what UI may import, which YAML engine is patched, which Node types `tsc` sees) without overlapping edits except `.3`/`.4` sharing `package.json` on distinct keys. FE-97's denylist, FE-104's `tasknoteRead`, FE-085's CORE_SCHEMA, and FE-090.N's types-vs-engines residual are all closed or still-valid history.

**Archived:** 2026-09-09
