---
title: types-node-engines
status: completed
tags: []
created: 2026-09-09
due:
related-tasks: [FE-EPIC-106, FE-106.2, FE-106.3, FE-106.N, FE-090.N]
touches:
  - viz/package.json
  - viz/package-lock.json
  - viz/src/ui/App.test.tsx
  - viz/src/ui/useProjectData.test.ts
---

# FE-106.4 | types-node-engines

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-106]] [[FE-106.2]] [[FE-106.3]] [[FE-106.N]] [[FE-090.N]]

## 🎯 Goal

Move viz's `@types/node` off the v20 line onto the v24 line so typecheck matches `engines.node` and CI Node 24.

## ✅ Acceptance

- [x] `viz/package.json` (and lockfile) pin `@types/node` to `^24.13.4`; installed `node_modules/@types/node` is 24.x (not 20.x or 26.x)
- [x] `npm --prefix viz run typecheck` stays clean
- [x] Targeted viz tests and lint stay clean

## 🧩 Subtasks

- [x] Bump `@types/node` to `^24.13.4` in `viz/package.json` and refresh the lockfile
- [x] Confirm the resolved version is 24.x
- [x] Run typecheck + lint + targeted tests

## 🔗 Related

- [[FE-EPIC-106]] — parent epic (viz-tier-guardrail)
- [[FE-106.2]] — sibling (eslint-node-tier-list); independent, already closed
- [[FE-106.3]] — sibling (js-yaml bump); independent, already closed
- [[FE-106.N]] — epic audit
- [[FE-090.N]] — recorded the v20-types vs engines-floor asymmetry this child closes

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Direct `@types/node` is still `^20.14.10` (lockfile 20.19.39) against `engines.node` `^22.22.2 || ^24.15.0 || >=26.0.0` and CI `node-version: 24`; FE-090.N recorded this as an accepted asymmetry for a later deliberate bump.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; when the grep returns more than a handful of notes (~3 is a fair line), prefer handing the reading to a **probe**; log relevant findings in Discovery Notes before re-interpreting the task

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- PLAN line (Medium, `[light]`): move `@types/node` off the v20 line onto a 22/24 line that matches viz engines and CI Node 24. Matches HEAD: `viz/package.json:32` is `"@types/node": "^20.14.10"`; lockfile `node_modules/@types/node` is 20.19.39; `engines.node` is `^22.22.2 || ^24.15.0 || >=26.0.0`; `.github/workflows/ci.yml` `node-version: 24`; `docs/CONVENTIONS.md` documents Node 24.
- No `.1` Discovery tasknote and no `## 🌳 Fan-out` — epic Discovery was supplied by audit-repo; omit `blocked-by` / `parallel-safe-with`.
- npm registry (2026-09-09): `@types/node` latest 22.20.2; latest 24 is 24.13.4; latest 26 is 26.5.1. Vite optional peer is `^20.19.0 || >=22.12.0`, so 24.x is in range.
- Pin choice: `^24.13.4` (CI Node 24). Not `^22` (engines floor / npm `latest` tag) because the PLAN names CI Node 24; not `^26` because FE-090 children deferred that major. A dual `^22 || ^24` range would still lock one version; skip it.
- `viz/tsconfig.json` `"types": ["node", "vitest/globals"]` — no version pin there; the package bump is sufficient.
- [[FE-090.N]] accepted asymmetry: types still on 20 after `.2` raised the runtime floor; latent (typecheck green, no Node 22+ API used). This child is that deliberate bump. No ⚠️ pointer — the claim was true at FE-090.N's archive time (historical residual, not a false-at-the-time fact).
- [[FE-090.2]] / [[FE-090.3]] / [[FE-090.4]] listed `@types/node` 26 among unclaimed majors — stay off 26.
- [[FE-001]] first added `@types/node` to tsconfig for the middleware + tests.
- Best Practices: bump the declared direct devDep and lockfile only. No tsconfig/engines/CI edit (already Node 24). No new helper. Deferred: `@types/node` 26, `globals` 17, `typescript` 7.
- Assumptions: (1) pin `^24.13.4`, not 22 and not 26; (2) no engines/CI/docs edits; (3) no source change unless typecheck fails on 24 types. No clarifications needed.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

- Pattern: same as FE-106.3 — bump the declared direct dep and lockfile. No new wrapper.
- Pin: `@types/node` `^20.14.10` → `^24.13.4`; lockfile `node_modules/@types/node` 20.19.39 → 24.13.4 (`undici-types` 6.21 → 7.18 as its dependency).
- Typecheck fallout: `@types/node` 24 no longer leaks ES2022 `Array.at` onto tsconfig `lib: ES2020`. Two test sites used `.at(-1)` (`App.test.tsx` SSE registry; `useProjectData.test.ts` `latestES`). Replaced with `arr[arr.length - 1]` rather than bumping `lib` to ES2022 (out of PLAN scope).
- Minimal refactor: N/A beyond that typecheck adaptation. Deferred: `@types/node` 26; tsconfig `lib`/`target` still ES2020 against a Node 22+ engines floor.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Quality assertions** — for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

- First typecheck after the bump failed on the two `.at(-1)` sites; after the index rewrite, `npm --prefix viz run typecheck` — exit 0.
- `npm --prefix viz run lint` — exit 0.
- `npm --prefix viz test` — 28 files, 531 tests passed.
- Quality: no duplication (same last-element lookup, no shared helper for two call sites); no public-surface growth; lockfile peer ranges on vite/vitest left as-is (optional `@types/node`).
- 👁️ **CONFIRM**: N/A — no UI/visual surface.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

  - `README.md`, `AGENTS.md`, `SPEC.md`, `docs/MIGRATION.md`, `claude/AGENTS-snippet.md`, `codex/AGENTS-snippet.md`, `cursor/AGENTS-snippet.md`, `grok/AGENTS-snippet.md`, `docs/CONVENTIONS.md`, `CONTRIBUTING.md`, `SECURITY.md`, `docs/AGENT-NEUTRALITY.md`, `docs/PLATFORMS.md`, `claude/CAPABILITIES.md`, `docs/AGENT-COMPAT.md`, `docs/EXTERNAL-AGENTS.md`, `docs/WORKTREES.md`, `docs/VISION.md` — no change. Grep of the sweep set for `@types/node` returned no hits. `docs/CONVENTIONS.md` already documents CI Node 24.

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed per SPEC/tasknote-selection.md §"`## Completed` archive convention" (standalone → top of `## Completed`; epic child → kept nested beneath its active parent), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Moved viz's `@types/node` off the v20 line onto `^24.13.4` so typecheck matches `engines.node` and CI Node 24, and rewrote two test `.at(-1)` lookups that the v24 types no longer provide under `lib: ES2020`.

- **Files changed:** `viz/package.json` / `viz/package-lock.json` (`@types/node` 20.19.39 → 24.13.4); `viz/src/ui/App.test.tsx` and `viz/src/ui/useProjectData.test.ts` (index instead of `Array.at`).
- **Verification:** typecheck and lint exit 0; `npm --prefix viz test` 531/531.
- **Refactors:** none. Deferred: `@types/node` 26; tsconfig `lib`/`target` ES2020 vs Node 22+ floor.
- **Documentation:** sweep-set no change.
- **Maintainability:** `tsc` now type-checks Node APIs against the CI runtime line instead of a Node 20 the project no longer supports.

**Archived:** 2026-09-09
