---
title: viz containment & visible staleness audit
status: completed
tags: []
created: 2026-08-22
due:
related-tasks: [FE-EPIC-088, FE-088.2, FE-088.3, FE-088.4]
---

# FE-088.N | viz containment & visible staleness audit

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-088]]

## 🎯 Goal

Verify the completed `FE-EPIC-088` (`viz containment & visible staleness`)
cohort sits coherently in the codebase: cumulative doc-drift sweep across
`.flowtron/tasknote/README.md` §"AI-referenced docs", naming/style consistency
across the cohort's deliverables, and follow-up filings for any miss.

## ✅ Acceptance

- [x] **Doc-drift sweep (fixed line, per SPEC/epic.md §"Audit acceptance — fixed doc-drift line")** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the specific update. Always present; surfaces cumulative slice-local staleness that per-task Phase 4 closures can miss.
- [x] Cohort coherence inventory: each implementation child's deliverables read against the others (naming consistency, style parity, no contradictory cross-refs)
- [x] No regressions surfaced in earlier-shipped cohort children's surfaces
- [x] Audit findings recorded in Implementation Notes; misses cited as candidates for `/ft-file-followup <NEW-ID>` filing (filed AFTER audit closure to preserve `/ft-file-followup`'s filing-discipline gate)
- [x] Single `feat: FE-088.N — audit FE-EPIC-088` (or `chore: ...` if no code edits land) commit lands
- [x] PLAN.md line for `FE-088.N` flipped to stub form `Completed YYYY-MM-DD.`
- [x] Tasknote moved to `.flowtron/tasknote/archive/fe/FE-088.N.md`
- [x] Parent-flip prompt surfaced after audit closure (skill Step 8) — user confirms or declines flipping `FE-EPIC-088` to `Completed` and moving the cohort to `## Completed`

## 🧩 Subtasks

- [x] Inventory cohort children's archived tasknotes — read each implementation child's Final Summary + Implementation Notes; capture deliverables in Discovery Notes
- [x] Walk `.flowtron/tasknote/README.md` §"AI-referenced docs" entries — fixed doc-drift sweep
- [x] Cohort coherence pass — naming consistency, style parity, no contradictory cross-refs across the cohort's deliverables
- [x] Surface audit findings in Implementation Notes; cite each miss as a `/ft-file-followup <NEW-ID>` candidate
- [x] Phase 4: flip `FE-088.N` PLAN line to stub form + archive tasknote
- [x] Parent-flip: skill Step 8 prompts user; on confirm, atomic flip parent line + move cohort to `## Completed`

## 🔗 Related

- [[FE-EPIC-088]] — parent epic: viz containment & visible staleness
- [[FE-088.2]] — realpath containment for project reads
- [[FE-088.3]] — SSE disconnect visibility + polling fallback
- [[FE-088.4]] — debounce max-wait flush + glob-metachar watch escape

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** User invoked `/ft-close-epic FE-088.N`. Parent `FE-EPIC-088`
  active under `## Medium`; implementation children `.2`–`.4` all closed
  2026-08-21/22; no open siblings; early-audit gate N/A. No `.1` — Discovery
  supplied by audit-repo 2026-08-20.

- [x] Read relevant source files — archived `FE-088.2`–`.4`; live cohort
  surfaces: `viz/src/fsSafe.ts`, `viz/src/workspace.ts`, `viz/src/archiveCache.ts`,
  `viz/src/devApi.ts`, `viz/src/flowtronWatch.ts`, `viz/src/watchSet.ts`,
  `viz/src/ui/useProjectData.ts`, `viz/src/ui/App.tsx`, `SECURITY.md`

- [x] **Best Practices Review** — N/A for the audit pass. Applied *to* the
  cohort: containment primitives in `fsSafe.ts` (leaf), read gates in
  workspace/archiveCache/devApi (callers), SSE orchestration in
  `flowtronWatch.ts`, client state in `useProjectData` — correct dependency
  direction preserved across all three children.

- [x] **Archive skim** — cohort archives are the primary inventory. Non-cohort
  context cited by children: [[CORE-222]] (symlink-root polling), [[CORE-431.3]]
  (attributed debounce), [[FE-062]] (503 capacity), [[FE-064]] (drop reconcile),
  [[FE-011]] (superseded silent-UX decision). No additional hits changed the
  picture.

- [x] **Drift check** — HEAD matches each child's shipped shape at audit time:
  - `.2`: `realpathWithin` on discover/archive/active reads; SECURITY.md
    enforced-bound bullet; symlink-root tests green
  - `.3`: `liveDisconnected` + 5 s poll/reconnect in `useProjectData`; amber
    chip + banner in `App.tsx`; `MockEventSource.CLOSED` static
  - `.4`: `SSE_MAX_WAIT_MS = 1000` dual-timer debounce; `escapeGlobLiteral` /
    `watchGlob` in `watchSets`
  - Full suite **382/382** at audit time

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  No clarifications needed. Assumptions: (1) full-cohort audit (all three
  implementation children closed); (2) `/api/plan` read-without-per-request
  containment is the accepted `.2` residual (planPath gated at discovery);
  (3) misses file via `/ft-file-followup` after closure, not inline.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

### Cohort inventory

| Child | Closed | Deliverable |
|---|---|---|
| **FE-088.2** | 2026-08-21 | `safeRealpath` + `realpathWithin` in `fsSafe.ts`; containment gates in `discoverProjects`, `readArchive`, `createActiveHandler`; SECURITY.md read-scope bullet: assumed → enforced; +7 tests |
| **FE-088.3** | 2026-08-22 | `liveDisconnected` state + 5 s poll/reconnect in `useProjectData.ts`; amber chip + banner in `App.tsx`; `MockEventSource` `readyState` statics; supersedes FE-011 silent-UX decision; +4 tests |
| **FE-088.4** | 2026-08-22 | `SSE_MAX_WAIT_MS` max-wait on `createChangeBroadcaster`; `escapeGlobLiteral` / `watchGlob` in `watchSet.ts`; +2 tests |

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — N/A (verification pass)

- [x] Implemented the minimal solution — audit verification only; no code edits

- [x] Updated/added tests — N/A (no inline fixes)

**Implementation Notes:**

### Cohort coherence

**Theme alignment.** Parent epic: replace viz's silent wrong-states with path
containment and user-visible indicators. Each child maps cleanly:
- `.2` — silent symlink escape → enforced realpath containment
- `.3` — silent SSE death → visible disconnect + self-healing poll
- `.4` — silent stale board during write bursts → max-wait flush

**Naming/style parity.** Amber warning surfaces in `App.tsx` follow one
pattern (chip in header row + banner below header): unparsed, duplicate-epic,
near-miss, and `liveDisconnected` all share the same Tailwind classes and
structure. Server-side constants co-locate in their owning modules
(`SSE_DEBOUNCE_MS` / `SSE_MAX_WAIT_MS` in `flowtronWatch.ts`; escape helpers
in `watchSet.ts`; containment in `fsSafe.ts`).

**Cross-child invariants preserved.**
- [[CORE-222]] symlinked project roots still discovered and polled (hot watcher
  unchanged in spirit; `.4` only escapes metachars in watch path strings)
- [[CORE-431.3]] attributed debounce + `{}` fail-open intact after `.4` max-wait
- [[FE-062]] 503 newest-rejection unchanged; `.3` makes its client consequence visible
- [[FE-064]] `droppedSinceOpen` reconcile on `open` still runs before clearing
  `liveDisconnected`

**Known accepted asymmetries (not misses).**
- `/api/plan` reads `project.planPath` without a per-request `realpathWithin`
  gate — same residual `.2` recorded: planPath validated at discovery; TOCTOU
  accepted for loopback dev tool
- `SECURITY.md` names containment + 503 capacity but not live-update UX or
  debounce intervals — correct scope split (security vs dev-tool behaviour)
- No `.1` Discovery tasknote — audit-repo supplied child list (established
  local convention per [[CORE-425.N]])

**Regressions.** None surfaced on earlier-shipped surfaces. Prior CORE-431 cohort
behaviours (split watch, attributed SSE, integration pins) remain green in the
382-test suite.

### Follow-up candidates

None. Cohort sits coherently; no `/ft-file-followup` filings warranted.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — N/A (audit-only); full viz suite
  **382/382** as cohort regression gate

- [x] Ran lint/type-check on changed code — N/A (no code edits)

- [x] **Quality assertions** — N/A (verification pass)

- [x] (frontend) N/A — audit did not change UI surfaces

**Testing Notes:**

Full `npm --prefix viz test` at audit time: 382/382 pass.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep (fixed line)** — all 17 entries in
  `.flowtron/tasknote/README.md` §"AI-referenced docs":
  - `README.md` — no change (viz overview unchanged; containment detail in SECURITY.md)
  - `AGENTS.md` — no change
  - `SPEC.md` — no change (no viz contract)
  - `docs/MIGRATION.md` — no change
  - `claude/AGENTS-snippet.md` · `codex/` · `cursor/` · `grok/` — no change
  - `docs/CONVENTIONS.md` · `CONTRIBUTING.md` — no change
  - `SECURITY.md` — no change. `.2` updated the read-scope bullet to "enforced";
    cumulative audit confirms it still matches HEAD (`discoverProjects`,
    `readArchive`, `/api/active` trinity). Live-update UX and debounce are
    dev-tool behaviour, not security claims — correctly absent.
  - `docs/AGENT-NEUTRALITY.md` · `docs/PLATFORMS.md` · `claude/CAPABILITIES.md` ·
    `docs/AGENT-COMPAT.md` · `docs/EXTERNAL-AGENTS.md` · `docs/WORKTREES.md` — no change

- [x] Closed

- [x] **Evidence-based recap** drafted

**Final Summary:**

Fleet-scale viz containment audit found no inconsistencies. The FE-088.2/.3/.4
cohort coherently closes the audit-repo "viz trusts the workspace and fails
silent" theme: enforced read containment, visible SSE failure, and bounded
debounce coalescing. Full viz verification green at HEAD (382 tests). Parent
flip pending operator confirm at 📦 gate.

**Parent flip:** confirmed Yes — parent `FE-EPIC-088` flipped to Completed 2026-08-22; cohort moved to top of `## Completed`.

**Archived:** 2026-08-22
