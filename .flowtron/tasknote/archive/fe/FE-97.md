---
title: ui-tier-eslint-rule
status: completed
tags: []
created: 2026-08-26
due:
related-tasks: []
---

# FE-97 | ui-tier-eslint-rule

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Mechanize the no-Node-under-`src/ui/` rule as an eslint `no-restricted-imports` rule scoped to `src/ui/**`, and update `viz/README.md` §Hard rule wording to match.

## ✅ Acceptance

- [x] `viz/eslint.config.js` enforces no Node imports under `src/ui/**` via `no-restricted-imports` (or equivalent), scoped correctly (not applied repo-wide)
- [x] Lint passes with zero code changes (rule is a codification of an already-true invariant, not a new constraint)
- [x] `viz/README.md` §Hard rule wording updated to reflect the rule is enforced, not just documented

## 🧩 Subtasks

- [x] Add a `no-restricted-imports` block to `eslint.config.js`, scoped to `files: ['src/ui/**/*.{ts,tsx}']`, restricting `node:*` and the eight Node-only tier modules
- [x] Run `npm run lint` to confirm zero code changes are required
- [x] Update `viz/README.md` §Hard rule wording to say the rule is enforced, not just a convention

## 🔗 Related

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Task is a direct mechanization of an already-documented, already-true convention; no ambiguity or missing prerequisite.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; log relevant findings in Discovery Notes before re-interpreting the task

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- `viz/README.md` §"Hard rule: no Node imports under `src/ui/`" (lines 35-44) names the eight Node-only tier modules: `devApi`, `workspace`, `fsSafe`, `archiveCache`, `flowtronWatch`, `watchSet`, `originGuard`, `apiResponse` — all confirmed present at `viz/src/*.ts`, matching the README exactly (no drift).
- `viz/eslint.config.js` currently has one flat-config block for `src/**/*.{ts,tsx}` (react-hooks rules only); no existing `no-restricted-imports` usage to extend — new block is a clean addition, not a rewrite.
- Confirmed `src/ui/` is flat (no subdirectories), and all existing `src/ui/*` relative imports of shared/Node-only modules are extensionless (`'../parser'`, not `'../parser.ts'`) — despite `tsconfig.json` having `allowImportingTsExtensions: true`. Scoped the `no-restricted-imports` `patterns` glob groups to match only the extensionless form actually in use (`**/devApi`, etc.), matching current convention rather than speculatively covering an unused import style.
- Archive skim of `.flowtron/tasknote/archive/fe/` found no prior tasknote that added or discussed a `no-restricted-imports` rule — no precedent to extend, no superseded claim to flag.
- No clarifications needed — task description and README precisely matched observed code; assumption: restrict only `node:*` builtins per the README's literal wording (not bare builtin names like `fs`/`path`), since the codebase already uses `node:` prefixes exclusively (verified via grep — zero bare-builtin imports across `src/`).

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — flat-config style already established in `eslint.config.js` (one block per scope, `files:` + `rules:`); extended it with a second block scoped to `src/ui/**/*.{ts,tsx}` rather than mutating the shared block, keeping the ui-tier restriction cleanly separated from the general React-hooks rules.

- [x] **Minimal refactor gate** — no refactor; pure additive config + doc-wording change. N/A.

- [x] Implemented the minimal solution — added `no-restricted-imports` block to `viz/eslint.config.js` (two `patterns` groups: `node:*` builtins, and the eight named Node-only tier modules); updated `viz/README.md` §Hard rule wording to state the rule is enforced.

- [x] Updated/added tests for non-trivial behavior — N/A; this is a lint-config change with no runtime behavior to unit-test. Verified via a temporary probe file exercising both restricted-import patterns (see Testing Notes), then removed.

**Implementation Notes:**

- `viz/eslint.config.js`: added one new flat-config block after the existing `src/**/*.{ts,tsx}` block, scoped to `files: ['src/ui/**/*.{ts,tsx}']`. Two `no-restricted-imports` `patterns` groups: one for `node:*`, one for the eight tier modules (`devApi`, `workspace`, `fsSafe`, `archiveCache`, `flowtronWatch`, `watchSet`, `originGuard`, `apiResponse`), each with a message pointing back to the README §Hard rule.
- `viz/README.md`: replaced the "convention held by the import graph today, not yet an eslint rule" sentence with a statement that it's enforced by the new `no-restricted-imports` rule.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — N/A (no test file references `eslint.config.js`; verified via grep). Config correctness verified directly via `eslint`.

- [x] Ran lint/type-check on changed code — `npm run lint` (exit 0, zero findings — confirms the new rule requires zero code changes) and `npm run typecheck` (exit 0).

- [x] **Quality assertions** — for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason). No duplication (new block is additive, distinct scope from the existing one); no dead code; README wording now matches enforced reality.

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line) — N/A; lint-config + doc-wording change, no UI/visual surface touched.

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

- `npm run lint` → exit 0, zero findings on the full `src` tree (rule addition required zero code changes, confirming the invariant was already true).
- `npm run typecheck` → exit 0.
- Sanity-checked the rule is not a no-op: wrote a temporary `src/ui/__lint_probe.tsx` importing `node:fs/promises` and `../devApi`, ran `npm run lint` — both restricted-import patterns fired with the expected messages (`node:fs/promises` and `../devApi` each flagged by `no-restricted-imports`). Deleted the probe file immediately after; `git status --porcelain` confirmed no residue.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

  - `README.md`, `AGENTS.md`, `SPEC.md`, `docs/MIGRATION.md`, `claude/AGENTS-snippet.md`, `codex/AGENTS-snippet.md`, `cursor/AGENTS-snippet.md`, `grok/AGENTS-snippet.md`, `docs/CONVENTIONS.md`, `CONTRIBUTING.md`, `SECURITY.md`, `docs/AGENT-NEUTRALITY.md`, `docs/PLATFORMS.md`, `claude/CAPABILITIES.md`, `docs/AGENT-COMPAT.md`, `docs/EXTERNAL-AGENTS.md`, `docs/WORKTREES.md` — no change. Grepped all for `no-Node`/`no-restricted-imports`/`ui-tier`/`src/ui`; only `AGENTS.md:39` matched, and it's a plain pointer to `viz/README.md` with no claim about enforcement mechanism — nothing to update.
  - `viz/README.md` (not in the AI-referenced set, but the doc named by the task's own acceptance criteria) — updated in Phase 2 (§Hard rule wording, "convention... not yet an eslint rule" → "enforced by an eslint `no-restricted-imports` rule").

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed per SPEC/tasknote-selection.md §"`## Completed` archive convention" (standalone → top of `## Completed`; epic child → kept nested beneath its active parent), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Mechanized the `no-Node-under-src/ui/` convention as an eslint `no-restricted-imports` rule, scoped to `src/ui/**/*.{ts,tsx}`, restricting `node:*` builtins and the eight named Node-only tier modules (`devApi`, `workspace`, `fsSafe`, `archiveCache`, `flowtronWatch`, `watchSet`, `originGuard`, `apiResponse`). Confirmed via `npm run lint` that the rule requires zero code changes (the invariant was already true) and, via a temporary probe file, that it actually fires on both restricted-import shapes. Updated `viz/README.md` §Hard rule wording from "not yet an eslint rule" to "enforced by".

- **Files changed:** `viz/eslint.config.js` (+30 lines, one new flat-config block), `viz/README.md` (1 sentence reworded).
- **Verification:** `npm run lint` exit 0 (zero findings), `npm run typecheck` exit 0, manual probe-file sanity check (both patterns fire, then removed — confirmed clean via `git status --porcelain`).
- **Refactors:** none — additive config block only; minimal refactor gate is N/A.
- **Documentation:** `viz/README.md` updated in-scope (per Acceptance); doc-drift sweep of the AI-referenced set found no other doc claiming the pre-enforcement state.
- **Maintainability effect:** future violations of the ui-tier boundary now fail lint immediately with a message pointing back to the README rationale, instead of relying on audits/reviews to catch the drift after the fact.

**Archived:** 2026-08-26
