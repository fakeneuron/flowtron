---
title: sse-backpressure
status: completed
tags: []
created: 2026-06-14
due:
related-tasks: []
---

# FE-062 | sse-backpressure

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Add a `MAX_SSE_CLIENTS` cap to `createEventsHandler` so unbounded SSE connections can't exhaust server resources.

## ⚡ Notes

**Relevance:** Proceed — `createEventsHandler` exists at `viz/src/devApi.ts:129`, `sseClients.add(res)` at line 137; paths match exactly.
**Drift check:** No drift — function signature and sseClients usage unchanged from PLAN.md description.
**Archive skim:** No prior tasknotes touch devApi.ts SSE paths.
**Pattern survey:** Existing guards (originGuard) use early-return with no body; cap check follows the same shape with a 503 status + end().
**Implementation:** Added `const MAX_SSE_CLIENTS = 10;` at module level; added size-check guard before `sseClients.add(res)` returning 503 when at capacity.
**Docs touched:** no change

## ✅ Recap

Added a `MAX_SSE_CLIENTS = 10` constant and a capacity guard in `createEventsHandler` (`devApi.ts:134-137`) — connections beyond the cap receive a 503 and are never added to the set, preventing unbounded growth under concurrent browser tabs or misbehaving clients.

**Archived:** 2026-06-14
