---
title: fence-mask-shared-module
status: completed
tags: []
created: 2026-09-08
due:
related-tasks: ['CORE-421.2', 'CORE-423', 'FE-99']
touches:
  - viz/src/parser.ts
  - viz/src/tasknote.ts
  - viz/src/fence.ts
supersedes:
  - CORE-421.2
  - CORE-423
# Optional planning keys — omit when absent (SPEC.md §Tasknote frontmatter).
# Omitted means undeclared, not "touches nothing" / "safe with everyone".
# blocked-by:
#   - TASK-ID
# parallel-safe-with:
#   - TASK-ID
---

# FE-103 | fence-mask-shared-module

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[CORE-421.2]] [[CORE-423]] [[FE-99]]

## 🎯 Goal

Extract the byte-identical `FENCE_DELIMITER` + `fenceMask` from `parser.ts` and `tasknote.ts` into a shared-pure `fence.ts`, imported by both.

## ✅ Acceptance

- [x] New `viz/src/fence.ts` (shared-pure tier, no `node:` imports) exports `FENCE_DELIMITER` regex + `fenceMask(lines: string[]): boolean[]`, carrying the existing CommonMark-fence-rules implementation verbatim (comment reworded from single-consumer to multi-consumer framing, content unchanged)
- [x] `parser.ts` imports `fenceMask` from `./fence.ts`; its local `FENCE_DELIMITER`/`fenceMask` copy and the "ported from tasknote.ts... kept private" comment are removed
- [x] `tasknote.ts` imports `fenceMask` from `./fence.ts`; its local `FENCE_DELIMITER`/`fenceMask` copy is removed
- [x] All four existing `tasknote.ts` call sites and the one `parser.ts` call site behave identically to before — confirmed by the full unchanged test suite passing (154/154 targeted, 530/530 full)
- [x] Existing fence regression tests in `parser.test.ts` and `tasknote.test.ts` pass unchanged; no test asserted on the old private-per-module shape (none did — tests exercise behavior through public functions, not module internals)
- [x] `npm --prefix viz test`, `npm --prefix viz run typecheck`, `npm --prefix viz run lint` all pass
- [x] Tasknote records the precedent override: `supersedes: [CORE-421.2, CORE-423]` in YAML, and Discovery/Recap document that the operator explicitly chose to override the twice-made no-export precedent FE-99 (2026-08-26) upheld

## 🧩 Subtasks

- [ ] Create `viz/src/fence.ts`: move `FENCE_DELIMITER` + `fenceMask` (and its CommonMark-rules comment) out of `tasknote.ts` verbatim, exported this time
- [ ] Update `viz/src/parser.ts`: remove the local `FENCE_DELIMITER`/`fenceMask`/comment block, add `import { fenceMask } from './fence';`, verify the single call site is unchanged
- [ ] Update `viz/src/tasknote.ts`: remove the local `FENCE_DELIMITER`/`fenceMask` block, add `import { fenceMask } from './fence';`, verify all four call sites are unchanged
- [ ] Run targeted tests (`parser.test.ts`, `tasknote.test.ts`) plus typecheck/lint on changed files
- [ ] Run full `npm --prefix viz test` suite (cross-cutting: two existing shared-pure modules changed, one added)
- [ ] Confirm `viz/README.md`'s architecture table needs no edit (new module fits the existing "Shared pure" tier row) or add `fence.ts` to its module list if warranted

## 🔗 Related

- [[CORE-421.2]] — supersedes: introduced `fenceMask`/`FENCE_DELIMITER` in `tasknote.ts`, deliberately unexported (public-surface-growth cost)
- [[CORE-423]] — supersedes: ported the same helper into `parser.ts` by duplication, not import, specifically to avoid a `parser.ts`↔`tasknote.ts` coupling
- [[FE-99]] — related-decision: an earlier filing with this exact premise, De-scoped 2026-08-26 after surfacing the CORE-421.2/CORE-423 precedent; this task's operator was shown that precedent and chose to override it (see Relevance Assessment)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** The Archive skim (below) surfaced that this task's premise is identical to [[FE-99]] (2026-08-26), which was De-scoped because it directly reverses a twice-made architectural decision ([[CORE-421.2]] kept the helper unexported; [[CORE-423]] duplicated rather than imported it to avoid a new inter-module edge). This is a real fork, not a judgment call — surfaced to the operator via AskUserQuestion (De-scope and respect precedent / Proceed and override it / Re-scope to hardened comments instead). Operator selected **Proceed anyway**, explicitly accepting the public-surface-growth cost CORE-421.2 named. The shape chosen (a third shared-pure module `fence.ts` that both `parser.ts` and `tasknote.ts` import) avoids the specific `parser.ts`↔`tasknote.ts` coupling CORE-423 avoided — per `viz/README.md`'s architecture table, "shared pure → other shared pure only" is an already-permitted dependency direction, so this doesn't cross a tier boundary, only accepts the export-surface cost.

- [x] Read relevant source files — `viz/src/parser.ts` (fence helper + comment, L142-176; call site L449), `viz/src/tasknote.ts` (fence helper, L138-159; four call sites at L169, L196, L236, and via `structuralChecklistLines`), `viz/README.md` (architecture tiers, confirms "Shared pure" tier and its dependency rules). Read set was narrow and known from the PLAN.md line's file citations — no probe needed.

- [x] **Best Practices Review** — The two `fenceMask`/`FENCE_DELIMITER` copies are byte-identical (confirmed by direct read of both files). Touched responsibility: "which lines are fenced (document-structure-inert) content" — currently answered twice, independently, by design (per CORE-421.2/CORE-423). This task's in-scope refactor is exactly the extraction PLAN.md names: a new shared-pure module, both existing modules importing from it instead of holding a private copy. `viz/README.md`'s tier table already has a "Shared pure" row covering exactly this shape (`parser`, `tasknote`, `sseChange`, `viewMode`, etc. — "usable from the browser bundle and, where relevant, from the Node plugin"; dependency rule "shared pure → other shared pure only"), so `fence.ts` fits the existing architecture without inventing a new tier or crossing a dependency boundary. No other deferred cleanup identified.

- [x] **Archive skim** — `grep -rl "fenceMask\|FENCE_DELIMITER"` over `.flowtron/tasknote/archive/` returns many hits (broad terms), but the load-bearing ones were already named by the code comment in `parser.ts` ("Ported from `tasknote.ts`'s `fenceMask` (CORE-421.2) — kept private and per-module... mirroring that note's no-export precedent") and confirmed by direct reads:
  - [[CORE-421.2]] introduced the helper in `tasknote.ts`, Implementation Notes: "Deliberately not done. No export of either helper — they are consumed only inside this module... public-surface growth as a cost."
  - [[CORE-423]] ported it into `parser.ts` by duplication; Best Practices Review: "the established shape... is pure predicate + module-level regex constant, private to the consuming module — not a shared cross-module import... `parser.ts` has no existing import from/to `tasknote.ts` today... this preserves that boundary." Quality Assertions: "the two per-module `fenceMask` copies are the established, deliberate pattern."
  - [[FE-99]] (2026-08-26) is the load-bearing find: an earlier task with this exact goal line ("Extract the verbatim-duplicated fenceMask/FENCE_DELIMITER... into one shared-pure module"), De-scoped in its own Phase 1 for reversing the above precedent without engaging it. Its Final Summary explicitly predicts a future re-filing and records itself as the thing a future archive skim should find — which is exactly what happened here.
  This finding was surfaced to the operator (see Relevance Assessment) rather than silently overridden or silently honored.

- [x] **Drift check** — Code matches: `FENCE_DELIMITER`/`fenceMask` still byte-identical in both files (`parser.ts` L155/157, `tasknote.ts` L138/140 — close to FE-99's cited line numbers, minor drift from unrelated intervening edits, not material). PLAN.md line ("Extract the byte-identical FENCE_DELIMITER + fenceMask from parser.ts and tasknote.ts into a shared-pure fence.ts") matches the code and matches FE-99's premise. No SPEC contract governs this specifically; `viz/README.md`'s architecture tiers (read above) support rather than contradict the plan — `fence.ts` as a third shared-pure module is compliant with the documented tier rules, distinguishing this from a naive `parser.ts`↔`tasknote.ts` import (which CORE-423 rejected and which this plan does not propose).

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions — Asked via AskUserQuestion: De-scope (respect precedent) / Proceed anyway (override it) / Re-scope to hardened comments. Operator chose **Proceed anyway**. Explicit assumption carried forward: "shared-pure module" means a new `viz/src/fence.ts` (not `parser.ts` importing from `tasknote.ts` or vice versa), matching the PLAN.md line's own phrasing and avoiding the specific coupling CORE-423 rejected.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

This task is a direct re-run of [[FE-99]]'s fork, with the opposite operator answer. FE-99 recorded (2026-08-26): "gives the next audit sweep... a tasknote to find via archive skim before re-flagging the same duplication" — the 2026-09-08 audit that filed FE-103 apparently re-flagged it anyway without checking archive history, exactly as FE-99 predicted could happen. Rather than silently defer to either the newer audit finding or the older precedent, this Discovery surfaced the conflict to the operator, who chose to proceed. The `supersedes: [CORE-421.2, CORE-423]` YAML key (added to this note's frontmatter) records that this later decision replaces those two — per `SPEC/superseded-claims.md` §"A superseded decision", written only on the later note, not as a `⚠️ Superseded by` pointer on the old ones (this isn't a factual correction, it's a design decision changing).

**Exit-gate judgment:** Discovery surfaced a real fork (override vs. respect the CORE-421.2/CORE-423/FE-99 precedent), but it was already resolved through a direct AskUserQuestion approval during Discovery itself — the operator has already seen and approved exactly what Phase 2 is about to execute, and the resulting plan matches the original PLAN.md line's stated approach verbatim (extract to `fence.ts`, both modules import it; no file-to-edit change, no subtask restructuring beyond what PLAN.md already named, no different root cause). Verdict is Proceed, not Re-scope/De-scope. → skip 🛠️ (a second banner asking the same question the AskUserQuestion just resolved would be redundant friction).

✅ Phase 1 Discovery complete; entering Phase 2 Execution.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — `fence.ts` matches the existing shared-pure module shape exactly (`sseChange.ts`'s "stays Node-free so multiple tiers share one codec" pattern, per `viz/README.md`): one module-level regex + one pure exported function, no class, no state. Both `parser.ts` and `tasknote.ts` now import from it rather than each holding a private copy — removes the duplication rather than adding a new one, and introduces no `parser.ts`↔`tasknote.ts` edge (both stay siblings importing a shared-pure module, permitted by the "shared pure → other shared pure only" rule).

- [x] **Minimal refactor gate** — the only structural change is moving `FENCE_DELIMITER`/`fenceMask` to their own file and exporting them; no other logic in either consuming file was touched. Reason: this *is* Acceptance (the extraction is the task). No unrelated cleanup performed.

- [x] Implemented the minimal solution — created `viz/src/fence.ts` (exported `FENCE_DELIMITER` + `fenceMask`, comment updated to describe both consumers instead of one); `parser.ts` and `tasknote.ts` each replaced their private copy with `import { fenceMask } from './fence';`. `viz/README.md`'s architecture table and layout sketch updated to list the new module (it declares the module architecture structural audits judge against, so a new shared-pure module needs a row).

- [x] Updated/added tests for non-trivial behavior — no new tests needed: this is a pure move with no behavior change, and `fenceMask`'s behavior is already covered by the existing fence regression tests in `parser.test.ts` (added by CORE-423) and `tasknote.test.ts` (added by CORE-421.2), which exercise it indirectly through each module's public functions. Phase 3 confirms those still pass unchanged.

**Implementation Notes:**

`viz/src/fence.ts` (new, +26 lines): `FENCE_DELIMITER` regex + `fenceMask(lines: string[]): boolean[]`, moved verbatim from `tasknote.ts` with the explanatory comment reworded from "the note" / "below" to describe both consumers generically. `viz/src/parser.ts` (+1/-30): added `import { fenceMask } from './fence';`, removed the local `FENCE_DELIMITER`/`fenceMask`/comment block (which had explicitly named itself as "ported... kept private... mirroring that note's no-export precedent" — that comment is now obsolete and removed along with the code it described). `viz/src/tasknote.ts` (+1/-30): same shape — import added, local copy removed. `viz/README.md` (+2/-1): `fence.ts` added to the "Shared pure" tier row and the layout sketch.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npm --prefix viz test -- parser.test.ts tasknote.test.ts run` → 154/154 passed. First run surfaced a Vite `configLoader: 'native'` warning on the extensionless `./fence` import; fixed by matching the codebase's established `.ts`-extension convention for `.ts`→`.ts` imports (used throughout the Node-only tier — `archiveCache.ts`, `devApi.ts`, `workspace.ts`, etc.) — `import { fenceMask } from './fence.ts';` in both files. Re-run clean, no warning.

- [x] Ran lint/type-check on changed code — `npm --prefix viz run typecheck` clean; `npm --prefix viz run lint` clean.

- [x] **Quality assertions** — no avoidable duplication (the change *removes* the two-way duplication this task exists to remove); no dead code (both consumers use the imported `fenceMask`); no unexplained complexity (pure move, zero logic change); public-surface growth is real and acknowledged, not accidental — `fenceMask`/`FENCE_DELIMITER` go from module-private in two files to exported from one, which is the specific cost CORE-421.2 named and the operator explicitly accepted overriding (see Relevance Assessment); no stale code-facing documentation (the obsolete "kept private... no-export precedent" comment in `parser.ts` was removed along with the code it described; `viz/README.md`'s architecture table updated — confirmed in the Phase 4 doc-drift sweep below).

- [x] N/A — no frontend/UI surface changed; `parser.ts`/`tasknote.ts` are shared-pure parsing modules with no render path, matching CORE-423's precedent for the same kind of change.

Full suite also run for cross-cutting confidence (two existing shared-pure modules changed, one new one added): `npm --prefix viz test` → 530/530 passed.

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — 17/17 no change. `grep -in "fence"` across all 17 entries (`README.md`, `AGENTS.md`, `SPEC.md`, `docs/MIGRATION.md`, `claude/AGENTS-snippet.md`, `codex/AGENTS-snippet.md`, `cursor/AGENTS-snippet.md`, `grok/AGENTS-snippet.md`, `docs/CONVENTIONS.md`, `CONTRIBUTING.md`, `SECURITY.md`, `docs/AGENT-NEUTRALITY.md`, `docs/PLATFORMS.md`, `claude/CAPABILITIES.md`, `docs/AGENT-COMPAT.md`, `docs/EXTERNAL-AGENTS.md`, `docs/WORKTREES.md`) returns three hits, all unrelated to the `viz/src` fenceMask helper — generic Markdown-fenced-code-block mentions (`docs/CONVENTIONS.md` §"Markdown flavor" GFM feature list; `claude/AGENTS-snippet.md`'s comment about pasted-block fences; `docs/AGENT-NEUTRALITY.md`'s citation of a fenced `sh` check) — confirming, not drifting, same disposition CORE-421.2/CORE-423 recorded. None of the 17 describes `viz/src` internal module or function names. `docs/VISION.md` (lazy-loaded, swept per README) — no change, unrelated to viz internals. `viz/README.md` is **not** on this list (it's a `viz/`-local architecture doc, not a repo-wide AI-referenced doc) but was updated in Phase 2 regardless, since it explicitly states it declares the module architecture — see Implementation Notes.

- [x] Closed — every `## ✅ Acceptance` box ticked above. `status:` flipped to `completed` below (first of the three closure writes). PLAN.md line to be flipped to stub form and placed at the top of `## Completed` in the same commit. Tasknote to be moved to `.flowtron/tasknote/archive/fe/`. No factual claim in any archived note was falsified (this is a documented *decision reversal*, handled via YAML `supersedes:` per `SPEC/superseded-claims.md` §"A superseded decision" — no ⚠️ pointer needed on CORE-421.2 or CORE-423, which remain accurate records of what was decided then).

- [x] **Evidence-based recap** drafted — see Final Summary below.

**Final Summary:**

Extracted the byte-identical `FENCE_DELIMITER`/`fenceMask` fence-detection helper, previously duplicated in `viz/src/parser.ts` and `viz/src/tasknote.ts`, into a new shared-pure `viz/src/fence.ts` that both modules now import.

**Context.** This is the second time this exact extraction was proposed. The first, [[FE-99]] (2026-08-26), was De-scoped after Discovery found the duplication was a deliberate, twice-made architectural decision — [[CORE-421.2]] kept the helper unexported to avoid public-surface growth, and [[CORE-423]] duplicated it into `parser.ts` rather than importing it, specifically to avoid a `parser.ts`↔`tasknote.ts` coupling. This task's Discovery surfaced the same precedent and put the same fork to the operator, who this time chose to proceed. `supersedes: [CORE-421.2, CORE-423]` is recorded in this note's YAML per `SPEC/superseded-claims.md`.

**Changed files:**
- `viz/src/fence.ts` (new, +26 lines) — `FENCE_DELIMITER` + `fenceMask`, moved verbatim from `tasknote.ts`, now exported.
- `viz/src/parser.ts` (+1/-30) — local fence helper + its now-obsolete "kept private... no-export precedent" comment removed; replaced with `import { fenceMask } from './fence.ts';`.
- `viz/src/tasknote.ts` (+1/-30) — same shape: local copy removed, import added.
- `viz/README.md` (+2/-1) — `fence.ts` added to the "Shared pure" architecture-table row and the layout sketch (this file declares the module architecture structural audits judge against, so a new shared-pure module needed a row).

**Verification:** `npm --prefix viz test -- parser.test.ts tasknote.test.ts run` → 154/154 passed. `npm --prefix viz run typecheck` clean. `npm --prefix viz run lint` clean. Full suite `npm --prefix viz test` → 530/530 passed.

**Refactors.** Only the extraction itself — no unrelated cleanup. One incidental fix: the extensionless `import { fenceMask } from './fence'` initially triggered a Vite `configLoader: 'native'` future-compat warning; corrected to `'./fence.ts'` to match the codebase's established `.ts`-extension convention for same-tier `.ts`→`.ts` imports (used throughout the Node-only tier).

**Documentation verdict.** 17/17 repo-wide AI-referenced docs unchanged (no doc describes `viz/src` internals). `viz/README.md` (viz-local architecture doc, not on the AI-referenced-docs list but declares the module map audits judge against) updated to list the new module.

**Maintainability effect.** Removes a duplication three separate tasknotes ([[CORE-421.2]], [[CORE-423]], [[FE-99]]) had each independently re-justified keeping. The two per-module copies are gone; one shared-pure module now owns "which lines are fenced content" for both `parser.ts` and `tasknote.ts`. Cost accepted (per the operator's explicit choice): `fenceMask`/`FENCE_DELIMITER` are now public exports rather than module-private, and `fence.ts` is a new file both modules depend on — the specific trade-off CORE-421.2 and CORE-423 each declined to make.

**Archived:** 2026-09-08
