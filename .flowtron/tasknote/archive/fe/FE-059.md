---
title: model-chip-fable-row-gate
status: completed
tags: []
created: 2026-06-13
due:
related-tasks: []
---

# FE-059 | model-chip-fable-row-gate

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Align the row-level model-chip gate with `ModelChip`'s predicate so `fable` tasks show the 🧠 heavy-model chip in rows.

## ✅ Acceptance

- [ ] A `fable` task renders the 🧠 chip in its row when `rowChips.model` is on
- [ ] An `opus` task still renders the 🧠 chip (no regression)
- [ ] A non-heavy task (e.g. `sonnet`) still renders no chip
- [ ] Predicate is single-sourced (no duplicated `opus`/`fable` list)

## 🧩 Subtasks

- [ ] Change `TaskRowInner.tsx:42` gate to `rowChips.model` and let `ModelChip` own the model predicate
- [ ] Add a row-level regression test asserting a `fable` task shows 🧠
- [ ] Run targeted vitest + lint/type-check on changed files

## 🔗 Related

- [[CORE-303]] — added `fable` to ModelChip; introduced the gate drift

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Real, localized drift. `TaskRowInner.tsx:42` gates `showModel` on `task.model === 'opus'` only; `ModelChip.tsx:5` renders for `opus`|`fable`. A `fable` row never satisfies the gate, so the chip never appears.

- [x] Read relevant source files

- [x] **Archive skim** — `grep -l 'TaskRowInner\|ModelChip' .flowtron/tasknote/archive/fe/*.md` returned no hits. No prior FE tasknote touched these paths.

- [x] **Drift check** — `TaskRowInner.tsx:42` and `ModelChip.tsx:5` match the description exactly; line numbers and predicates current. No drift.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

Two fix options offered by the audit: (a) align the gate to also allow `fable`, or (b) let `ModelChip` own the predicate. Choosing (b) — `ModelChip.tsx:5` already returns `null` for any non-`opus`/`fable` model, and a unit test (`ModelChip.test.tsx`) already covers opus/fable/sonnet. Setting `showModel = rowChips.model` removes the duplicated model list and makes `ModelChip` the single source of truth. `showModel` is only consumed at line 97 guarding the `ModelChip` render, so the looser boolean is safe.

Coverage gap: the existing row-level test (`App.test.tsx:348`) uses an `[opus]` task only; the `fable` path has no row-level assertion — adding one closes the regression.

No clarifications needed. Assumption: option (b) is the intended "let ModelChip own the predicate" path the audit named.

Discovery surfaced no significant deviation → skip 🛠️.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — sibling gates (`showRelated`, `showDue`) stay thin and let the child component own its render logic; `ModelChip` already returns `null` for non-`opus`/`fable`, so making the gate `rowChips.model` fits the existing shape.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

`TaskRowInner.tsx:42`: `showModel = rowChips.model && task.model === 'opus'` → `showModel = rowChips.model`. The model predicate is now single-sourced in `ModelChip.tsx:5` (`opus`|`fable`). Line 97's `{showModel && task.model && <ModelChip .../>}` stays; `ModelChip` returns `null` for non-heavy tokens, so a `sonnet` row renders no chip.

Added 2 row-level regression tests in `App.test.tsx` (new `describe('App — model chip row gate (FE-059)')`): a `[fable]` task shows 🧠; a `[sonnet]` task shows none. The pre-existing `[opus]` row test (`App.test.tsx`, settings-modal block) covers the opus no-regression case.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:**

`vitest run src/ui/App.test.tsx src/ui/ModelChip.test.tsx` → 33 passed. `tsc --noEmit` clean; `eslint src` clean. Behavior fully covered at component (ModelChip.test) + row (App.test) levels; 👁️ visual confirmation offered at the 📦 gate.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — all 12 AI-referenced docs: no change (viz-internal UI fix; none describe the row-chip gate).

- [x] Closed — PLAN.md line flipped to stub form and tasknote moved to `.flowtron/tasknote/archive/fe/`

- [x] Recap drafted

**Final Summary:**

Aligned the row-level model-chip gate (`TaskRowInner.tsx:42`) with `ModelChip`'s `opus`|`fable` predicate so `fable` tasks now show the 🧠 heavy-model chip in rows. Single-sourced the predicate in `ModelChip`; added 2 row-level regression tests.

**Archived:** 2026-06-13
