---
title: handoff-chip
status: completed
tags: []
created: 2026-09-21
due:
related-tasks: [CORE-598.3]
touches:
  - viz/src/ui/HandoffChip.tsx
  - viz/src/ui/TaskRowInner.tsx
  - viz/src/ui/TaskRow.test.tsx
---

# FE-122 | handoff-chip

[← PLAN.md](../PLAN.md) · ✅ Completed · 🔗 [[CORE-598.3]]

## 🎯 Goal

Render `Task.handoff` on the board with a `HandoffChip` (✋) in `TaskRowInner`, suppressing the `UnattendedChip` (🤖) when both tokens are present.

## ✅ Acceptance

- [x] `HandoffChip` renders ✋ with an accessible label when `task.handoff` is true — `npm --prefix viz test` → 0
- [x] `TaskRowInner` shows `UnattendedChip` only when `task.unattended && !task.handoff` (handoff wins per SPEC §"Task-line format") — `npm --prefix viz test` → 0
- [x] New/updated tests in `TaskRow.test.tsx` cover: handoff-only, unattended-only, both-present (handoff shown, unattended suppressed), neither — `npm --prefix viz test` → 0
- [x] Typecheck and lint clean on changed files — `npm --prefix viz run typecheck && npm --prefix viz run lint` → 0

## 🧩 Subtasks

- [x] Add `viz/src/ui/HandoffChip.tsx` mirroring `UnattendedChip.tsx` (✋ glyph, same accessible-label shape)
- [x] Wire `HandoffChip` into `TaskRowInner.tsx`; suppress `UnattendedChip` when `task.handoff` is also true
- [x] Extend `viz/src/ui/TaskRow.test.tsx` with a `[handoff]` chip describe block mirroring the `[unattended]` one, plus a both-tokens suppression case
- [x] Run targeted tests, typecheck, lint

## 🔗 Related

- [[CORE-598.3]] — parent: added `Task.handoff` capture and deferred the viz chip to this task (Discovery §E)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** PLAN.md line, current code, and CORE-598.3's Discovery §E deferral all agree: `Task.handoff` is parsed and typed but no chip consumes it yet. Nothing to re-scope or de-scope.

- [x] Read relevant source files — read `viz/src/ui/UnattendedChip.tsx` (glyph-only chip pattern), `viz/src/ui/TaskRowInner.tsx` (chip cluster wiring + render order), `viz/src/ui/TaskRow.tsx` and `EpicRow.tsx` (both route through `TaskRowInner`, so one wiring point covers both row surfaces), `viz/src/parser.ts` (`Task.handoff: boolean` already captured), `SPEC.md` §"Task-line format" (`[handoff]` wins over `[unattended]` on a mis-authored row), and `viz/src/ui/TaskRow.test.tsx` (existing `[unattended]` chip test shape to mirror).

- [x] **Best Practices Review** — one code boundary: `viz/src/ui/*Chip.tsx` components are presentational, each owning one glyph; `TaskRowInner` composes them. Extends the existing pattern (`UnattendedChip`, `CriticalChip`) with a second glyph-only chip — no new abstraction. The only conditional logic is the suppression rule in `TaskRowInner`, which mirrors how `showModel`/`showTags` gate other chips.

- [x] **Archive skim** — `archive/fe/` (FE-118 added `UnattendedChip`) and `archive/core/CORE-598.3.md` (added `Task.handoff` capture, explicitly deferred the chip in §E). No other tasknote in `archive/fe/` or `archive/core/` touches these chip files per `grep -l 'TaskRowInner\|UnattendedChip' archive/fe/*.md archive/core/*.md`.

- [x] **Drift check** — PLAN.md cites `HandoffChip`, `UnattendedChip`, `TaskRowInner`, and the `[handoff]` SPEC wording verbatim; all match current code and SPEC.md §"Task-line format" (`:91`). No drift.

- [x] Asked clarifying questions — No clarifications needed. Glyph (✋) and suppression behavior are both stated explicitly in the PLAN.md line; the chip's visual position (right-hand chip cluster, alongside the other status chips) follows the existing `UnattendedChip` slot.

- [x] Subtasks above populated with concrete, ordered steps; `touches:` declared.

**Discovery Notes:** Straightforward, fully-scoped UI addition — parser support already lands from CORE-598.3. No new abstraction, no refactor.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended the existing glyph-only chip pattern (`UnattendedChip`); `HandoffChip` is a direct structural mirror, same shape, same slot in the chip cluster. No new abstraction.
- [x] **Minimal refactor gate** — `N/A` — no refactor; one new component + a two-line conditional in `TaskRowInner`.
- [x] Implemented the minimal solution — `HandoffChip.tsx` added; `TaskRowInner.tsx` renders it when `task.handoff`, and gates `UnattendedChip` on `task.unattended && !task.handoff`.
- [x] Updated/added tests for non-trivial behavior — three new `it` blocks in `TaskRow.test.tsx` mirroring the `[unattended]` set: chip renders, chip absent, and both-tokens suppression.

**Implementation Notes:** `EpicRow.tsx` and `TaskRow.tsx` both route through `TaskRowInner`, so the single wiring point covers both row surfaces — no separate epic-row change needed.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code
- [x] Ran lint/type-check on changed code
- [x] **Verification receipt** — see Testing Notes. Structural: no duplication (`HandoffChip` mirrors `UnattendedChip`'s exact shape rather than adding an abstraction over both), no dead code, no unnecessary public-surface growth beyond the one new exported component, no stale code-facing docs to update.
- [x] (frontend) Asked the user for visual confirmation

  👁️ **CONFIRM** — the ✋ handoff chip renders in the row's chip cluster (mirroring the 🤖 unattended chip's position) and replaces 🤖 when a row carries both markers. No live PLAN.md row currently carries `[handoff]` to eyeball directly; confirmed via the new automated tests below plus reading the rendered DOM assertions. Flag if the visual placement should differ once you see a real `[handoff]` row on the board.

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**
- `npm --prefix viz test` → 0 (578 tests passed, 29 files; includes 3 new `[handoff]` chip tests in `TaskRow.test.tsx`)
- `npm --prefix viz run typecheck` → 0
- `npm --prefix viz run lint` → 0
- Acceptance verify commands: all four map to the commands above; all → 0.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — walked `.flowtron/tasknote/README.md` §"AI-referenced docs". This task is a pure viz-UI consumer of `Task.handoff`, a field CORE-598.3 already shipped into the contract (SPEC.md, docs/EXTERNAL-AGENTS.md, docs/GLOSSARY.md); it adds no new grammar, contract clause, or agent-facing surface. `grep -rln 'UnattendedChip' --include='*.md'` (excluding archive) → only `.flowtron/PLAN.md` (this task's own row). No change to any swept doc.

- [x] Closed — every Acceptance criterion ticked (verified below); `status:` flipped to `completed`; PLAN.md line to be flipped to stub form and moved to top of `## Completed`; tasknote to move to `archive/fe/`.

- [x] **Evidence-based recap** drafted (see Final Summary).

**Final Summary:**

Added `viz/src/ui/HandoffChip.tsx` (11 lines, mirrors `UnattendedChip.tsx` exactly: glyph-only span, `title`/`aria-label` from one `LABEL` constant, ✋ glyph). Wired it into `viz/src/ui/TaskRowInner.tsx` (2-line change: import + `{task.handoff && <HandoffChip />}` before the existing `UnattendedChip` render, which now gates on `task.unattended && !task.handoff` so `[handoff]` wins per SPEC.md §"Task-line format"). Both `TaskRow.tsx` and `EpicRow.tsx` route through `TaskRowInner`, so this one wiring point covers both board surfaces — no separate `EpicRow` change needed. Extended `viz/src/ui/TaskRow.test.tsx` with 3 new tests (chip renders, chip absent, suppression when both markers present), mirroring the existing `[unattended]` chip test block.

Verification: `npm --prefix viz test` → 0 (578/578, 29 files). `npm --prefix viz run typecheck` → 0. `npm --prefix viz run lint` → 0. All four Acceptance criteria map to these commands; all pass.

Refactors: none — `N/A`, no refactor triggered (see Phase 2 Minimal refactor gate).

Documentation: no drift — this task consumes an already-contracted field (`Task.handoff`, shipped by CORE-598.3); no swept doc describes the chip set at a level this task would touch.

`touches:` scope reconciliation — declared `viz/src/ui/HandoffChip.tsx`, `viz/src/ui/TaskRowInner.tsx`, `viz/src/ui/TaskRow.test.tsx`; `git diff --name-only` will match exactly (plus this tasknote and the PLAN.md flip) once staged.

Maintainability effect: closes the gap CORE-598.3 Discovery §E deferred — `[handoff]` rows are now visible on the board and correctly take priority over `[unattended]` in the chip cluster, matching the SPEC contract's "handoff wins" rule visually as well as in dispatch logic.

**Archived:** 2026-09-21
