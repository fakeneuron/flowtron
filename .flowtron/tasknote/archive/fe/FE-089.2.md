---
title: deflake wikilink-nav waits
status: completed
tags: []
created: 2026-08-22
due:
related-tasks: [FE-EPIC-089, FE-089.N]
touches:
  - viz/src/ui/App.test.tsx
---

# FE-089.2 | deflake wikilink-nav waits

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-089]]

## 🎯 Goal

Make the two 4s `findByRole` wikilink-navigation waits in `viz/src/ui/App.test.tsx` (lines 42, 1227) survive cold-start / loaded-machine runs so the `/ft-release` viz gate stops flaking.

## ✅ Acceptance

- [x] The two wikilink `findByRole` calls (`App.test.tsx:42`, `:1227`) carry no explicit `timeout` option — they inherit the shared `asyncUtilTimeout` (5000ms, FE-053)
- [x] No per-test timeout overrides remain in `App.test.tsx` (restores FE-053's "fix lives in shared config" acceptance)
- [x] The stale comment block (lines 24–29) no longer references per-call/per-test timeouts that were removed
- [x] 5 consecutive cold runs (`node_modules/.vite* caches cleared each time`) pass 382/382
- [x] `typecheck` + `lint` clean

## 🧩 Subtasks

- [x] Remove `{ timeout: 4000 }` at `App.test.tsx:42` and `:1227`, `{ timeout: 2500 }` at `:60`, and the `10_000` per-test caps at `:62` and `:1234`
- [x] Rewrite the lines 24–29 comment to reference the shared-config globals (FE-053) instead of the removed overrides
- [x] Run 5 consecutive cold viz suite runs + typecheck + lint
- [x] If any cold run flakes: escalate `asyncUtilTimeout` in `setup.ts` (shared-config lever) and re-verify

## 🔗 Related

- [[FE-EPIC-089]] — parent epic (release-gate flake hardening); Discovery supplied by audit-repo 2026-08-22
- [[FE-089.N]] — terminal audit child (follow-up)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Flake reproduced today (cold run: 1/382 fail at `App.test.tsx:42`, findByRole expiry; two warm reruns green). Root cause confirmed in history: FE-049 set explicit `{ timeout: 4000 }` when the Testing Library default was 1000ms; FE-053 later raised the global `asyncUtilTimeout` to 5000ms, so the explicit option now *lowers* the wait below the shared config. Same relic pattern: `10_000` per-test caps vs the 15s global `testTimeout`.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; log relevant findings in Discovery Notes before re-interpreting the task

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- Read: `App.test.tsx` (both flake sites + comment block 24–29), `test/setup.ts` (FE-053 `asyncUtilTimeout: 5000`), `vite.config.ts` (`testTimeout: 15_000`). Explicit-timeout survey found exactly five overrides, all in `App.test.tsx`: `:42`/`:1227` (`timeout: 4000`), `:60` (`timeout: 2500`, same test/failure class — highlight-clear wait, resolves ~1500ms when healthy so inheriting 5000 costs nothing), `:62`/`:1234` (`10_000` per-test caps).
- Best practices: fix belongs in shared config per FE-053's explicit acceptance ("No per-test timeout overrides introduced"); removing the per-call/per-test relics restores that invariant rather than adding a third timing knob. No refactor needed.
- Archive skim: [[FE-053]] (raised `testTimeout` 15s + `asyncUtilTimeout` 5s; documented the load-driven flake characterization method), FE-049 via `git log -L` (added the 4000ms option pre-FE-053), FE-052 (Node-26 slowness root cause). The in-file comment cites FE-045 for the fake-timers→real-timers move.
- Drift check: PLAN line's cited sites `:42`/`:1227` match current code exactly. PLAN offers "widen/restructure … or amortize cold-start env cost" — chosen lever is widen-by-inheritance (minimal); cold-start amortization (pool/isolate tuning) deferred unless verification fails.
- No clarifications needed. Assumptions: (1) inheriting the 5000ms global is the FE-053-aligned minimal fix; escalation of the *global* (not per-call values) is the fallback if a cold run still flakes; (2) "cold run" = vite/vitest transform caches cleared before each run; (3) comment block 24–29 rewrite is in-scope (it names the removed overrides).

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

- Removed all five timing overrides in `App.test.tsx`: `{ timeout: 4000 }` at the two wikilink `findByRole` calls, `{ timeout: 2500 }` at the highlight-clear `waitFor`, and the two `10_000` per-test caps — every wait now inherits the FE-053 shared config (`asyncUtilTimeout` 5000ms / `testTimeout` 15s), a strict widening at each site.
- Rewrote the FE-045 comment block: it now points at the shared-config globals and states the no-local-overrides invariant (they can only undercut the globals).
- Pattern survey: extends FE-053's established shape (timing lives in shared config); no new tests needed — the change *is* test infra. Minimal refactor gate: nothing beyond the acceptance-required edits; escalation subtask stayed conditional and did not fire.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Quality assertions** — for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

- Targeted: `vitest run src/ui/App.test.tsx` — the **first** post-change run failed 1/54 (25.5s duration, machine under load; output regrettably filtered before capture, failing test unidentified), then 5/5 consecutive isolated runs green (logs in session scratchpad `run1..5.log`).
- Acceptance gate: 5 consecutive **cold** full-suite runs (`rm -rf viz/node_modules/.vite` before each) — **382/382 green all five** (`cold1..5.log`), durations 12–14s.
- `typecheck` + `lint` clean.
- Honest bound: this widens margin (4s→5s per-wait, 10s→15s per-test); it cannot prove zero flake probability under arbitrary load. If the gate flakes again, the documented escalation is raising the *global* `asyncUtilTimeout` in `setup.ts` — flagged for [[FE-089.N]] to note.
- 👁️ visual confirmation: N/A — test-infra timing only, no UI surface changed (same rationale as FE-053).

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed per SPEC/tasknote-selection.md §"`## Completed` archive convention" (standalone → top of `## Completed`; epic child → kept nested beneath its active parent), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Deleted the five stale per-call/per-test timing overrides in `viz/src/ui/App.test.tsx` (+11/−10 lines, one file) so every wait inherits FE-053's shared-config globals — the explicit `{ timeout: 4000 }` values FE-049 added against a 1000ms default had become *downgrades* once FE-053 raised the global to 5000ms. Verified with 5 consecutive cold full-suite runs (382/382 each), 5 isolated file runs, typecheck + lint clean. Docs: no change (17/17 AI-referenced docs unaffected). Maintainability: `App.test.tsx` now holds zero local timing knobs, so future global bumps take effect everywhere without hunting for undercutting overrides.

**Archived:** 2026-08-22
