---
title: viz-comment-provenance-trim audit
status: completed
tags: [viz, comments, epic-audit]
created: 2026-09-23
due:
related-tasks: [FE-EPIC-125, FE-125.2, FE-125.3]
---

# FE-125.N | viz-comment-provenance-trim audit

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-125]]

## 🎯 Goal

Verify the completed `FE-EPIC-125` (`viz-comment-provenance-trim`) cohort sits coherently in the codebase: cumulative doc-drift sweep across `.flowtron/tasknote/README.md` §"AI-referenced docs", naming/style consistency across the cohort's deliverables, and follow-up filings for any miss.

## ✅ Acceptance

- [x] **Doc-drift sweep (fixed line, per SPEC/epic.md §"Audit acceptance — fixed doc-drift line")** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the specific update. Always present; surfaces cumulative slice-local staleness that per-task Phase 4 closures can miss.
- [x] Cohort coherence inventory: each implementation child's deliverables read against the others (naming consistency, style parity, no contradictory cross-refs)
- [x] No regressions surfaced in earlier-shipped cohort children's surfaces
- [x] Audit findings recorded in Implementation Notes; misses cited as candidates for `/ft-file-followup <NEW-ID>` filing (filed AFTER audit closure to preserve `/ft-file-followup`'s filing-discipline gate)
- [x] Single `chore: FE-125.N — audit FE-EPIC-125` commit lands (no code edits)
- [x] PLAN.md line for `FE-125.N` flipped to stub form `Completed 2026-09-23.`
- [x] Tasknote moved to `.flowtron/tasknote/archive/fe/FE-125.N.md`
- [x] Parent-flip prompt surfaced after audit closure (skill Step 8) — user confirms or declines flipping `FE-EPIC-125` to `Completed` and moving the cohort to `## Completed`

## 🧩 Subtasks

- [x] Inventory cohort children's archived tasknotes — read each implementation child's Final Summary + Implementation Notes; capture deliverables in Discovery Notes
- [x] Walk `.flowtron/tasknote/README.md` §"AI-referenced docs" entries — fixed doc-drift sweep
- [x] Cohort coherence pass — naming consistency, style parity, no contradictory cross-refs across the cohort's deliverables
- [x] Surface audit findings in Implementation Notes; cite each miss as a `/ft-file-followup <NEW-ID>` candidate
- [x] Phase 4: flip `FE-125.N` PLAN line to stub form + archive tasknote
- [x] Parent-flip: skill Step 8 prompts user; on confirm, atomic flip parent line + move cohort to `## Completed`

## 🔗 Related

- [[FE-EPIC-125]] — parent epic
- [[FE-125.2]] — pilot: `parser.ts` trim + §"Pilot rule"
- [[FE-125.3]] — rollout across the rest of `viz/src`

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Operator invoked `/ft-close-epic FE-125.N`; both implementation children (`FE-125.2`, `FE-125.3`) closed 2026-09-22, no open siblings. The epic has no `.1` child — Discovery was supplied by `/ft-audit-repo` 2026-09-22, as the parent line records.

- [x] Read relevant source files — both cohort archives (`archive/fe/FE-125.2.md`, `archive/fe/FE-125.3.md`) read; gates re-run at HEAD over `viz/src` and the non-`src` viz files.

- [x] **Best Practices Review** — N/A: verification pass, no code edits.

- [x] **Archive skim** — self-referential (cohort children are the archive entries; `FE-*` → `archive/fe/` confirmed against the README table). No non-cohort history needed: the epic touched only comment text.

- [x] **Drift check** — `.3` is the most recent commit (`5745fd29`); nothing has landed in `viz/src` since, so cited paths hold. The plan matches the PLAN line and contradicts no SPEC contract.

- [x] No clarifications needed. Assumption: epic scope is `viz/src` as the parent line states, so provenance outside it is a follow-up, not an audit fix.

- [x] Subtasks above populated with concrete, ordered steps (no file deliverable; `touches:` omitted)

**Discovery Notes:**

- `FE-125.2` (parser-comment-trim) — `viz/src/parser.ts` comment-only trim; wrote §"Pilot rule" (provenance = task-ID tags + history narration; keep present-tense *why*, SPEC pointers, backticked syntax-example IDs; gated per-file grep; comment ratio reported, not gated).
- `FE-125.3` (viz-comment-trim-rest) — applied the rule to the other 33 `viz/src` files (+161/−165); by operator decision widened the gate to `it` / `describe` titles; backticked GHSA/CVE advisories, fixture IDs, and syntax examples.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — N/A: audit only; no new code surface.

- [x] **Minimal refactor gate** — N/A: no refactor.

- [x] Implemented the minimal solution — verification only; no inline fixes.

- [x] Updated/added tests for non-trivial behavior — N/A: no code change.

**Implementation Notes:**

- **Gates at HEAD:** the comment gate (pilot rule, every `git ls-files viz/src` file) and `.3`'s test-title gate both print nothing.
- **Coherence:** no inconsistencies. `.3` applies `.2`'s rule unchanged; its one extension (test titles) is an operator decision recorded in `.3`'s own Acceptance, not a contradiction of `.2`. Both notes use the same backtick convention for kept IDs. Neither cites the other stale.
- **Residue review:** five comments still read as history (`used to` / `no longer` in `watchSet.ts:16`, `tasknote-parse.ts:29`, `useProjectData.test.ts:9`, `App.test.tsx:857`, `useKeyboardNav.test.ts:101`). Each states the *why* (what the code prevents, or the regression it guards) with no task ID, so each is kept under the pilot rule.
- **Miss — `/ft-file-followup` candidate:** `viz/vite.config.ts` sits outside the epic's `viz/src` scope and still carries provenance: `(CORE-222)` L88, `CORE-431.2 removes` L96, `See FE-053.` L163, `(FE-053/FE-089.2 …)` L170, `See FE-95.` L171, plus unbackticked `CVE-2025` L149. `viz/eslint.config.js` and `viz/public/theme-init.js` are clean. Small, mechanical, same rule.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — regression check only: `npm --prefix viz test` → 0 (29 files, 587 passed)

- [x] Ran lint/type-check on changed code — `npm --prefix viz run typecheck` → 0 · `npm --prefix viz run lint` → 0

- [x] **Verification receipt** — gate loops recorded in Implementation Notes (both → no output); no changed code.

- [x] **External review** — N/A: no code diff to grade; the deliverable is this note.

- [x] (frontend) Asked the user for visual confirmation — N/A: nothing renders differently.

**Testing Notes:**

- Comment gate loop over `git ls-files viz/src` → no output
- `.3` title gate (`rg … | sed … | rg …`) → exit 1, no output
- `npm --prefix viz test` → 0 (587 passed) · typecheck → 0 · lint → 0

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — no change for every entry (`README.md`, `AGENTS.md`, `SPEC.md`, `docs/MIGRATION.md`, the four `*/AGENTS-snippet.md`, `docs/CONVENTIONS.md`, `CONTRIBUTING.md`, `SECURITY.md`, `docs/AGENT-NEUTRALITY.md`, `docs/PLATFORMS.md`, `claude/CAPABILITIES.md`, `docs/AGENT-COMPAT.md`, `docs/EXTERNAL-AGENTS.md`, `docs/WORKTREES.md`, `docs/VISION.md`). None quotes `viz/src` comment text; `git grep` finds no live `viz/src/<file>:<line>` citation in any markdown. The only "provenance" hits (`README.md:221`, `docs/MIGRATION.md:176`) are unrelated uses of the word.

- [x] Closed — Acceptance ticked, YAML `status:` flipped, PLAN.md line stubbed nested under the active parent, tasknote moved to `archive/fe/`

- [x] **Evidence-based recap** drafted — no files changed beyond PLAN.md + this note; gates and viz suite green; one follow-up candidate

- [x] **Learnings** — N/A: the pilot rule is tasknote-local by operator choice (`FE-125.2`).

**Final Summary:**

Audit of `FE-EPIC-125`: the cohort is coherent. Every `viz/src` comment and test title passes the pilot gate at HEAD, the viz suite is green, and no swept doc drifted. One miss outside the epic's scope: `viz/vite.config.ts` still carries six provenance IDs. It is a `/ft-file-followup` candidate.

Parent flip: operator confirmed 2026-09-23. `FE-EPIC-125` is stubbed and the cohort moved to the top of `## Completed`, so the `vite.config.ts` miss is a standalone follow-up rather than a new `.4` child.

**Archived:** 2026-09-23
