---
title: viz-architecture-doc
status: completed
tags: []
created: 2026-08-24
due:
related-tasks:
  - FE-092
  - FE-EPIC-94
touches:
  - viz/README.md
  - AGENTS.md
---

# FE-093 | viz-architecture-doc

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-092]] · [[FE-EPIC-94]]

## 🎯 Goal

Declare `viz/`'s three tiers (browser UI `src/ui/` · shared pure parsing · Node-only dev API) and the no-Node-imports-under-`src/ui/` rule, so structural audits of the repo's only code surface have a declared boundary to judge against.

## ✅ Acceptance

- [x] `viz/README.md` exists and declares the three tiers by name, with a concrete module inventory and the dependency direction (UI → shared; Node → shared; never UI → Node)
- [x] The no-Node-imports-under-`src/ui/` rule is stated as a hard boundary (convention today; not yet eslint-enforced)
- [x] `AGENTS.md` §"Repo Layout" `viz/` bullet points at `viz/README.md` so agents find the boundary without a prior audit
- [x] No runtime/code change — documentation only; existing import graph already holds the rule

## 🧩 Subtasks

- [x] Write `viz/README.md`: purpose + commands + Architecture (three tiers, rule, dependency direction, entrypoints)
- [x] Add a one-line pointer on the `AGENTS.md` `viz/` layout bullet
- [x] Spot-check: `rg` confirms zero `node:` imports under `viz/src/ui/` and zero UI imports of Node-tier modules
- [x] Phase 3: N/A for tests (markdown-only); no lint/typecheck delta expected

## 🔗 Related

- [[FE-092]] — sibling Low finding from the same audit-structure pass (row-dedup); parallel-safe surface inside `viz/src/ui/`
- [[FE-EPIC-94]] — completed viz App decompose epic that repeatedly deferred to this architecture-doc declaration

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** The three-tier split already exists in the import graph, but no doc declares it — FE-EPIC-94 children deferred to this task, and structural audits still have nothing to judge against. Still the right fix for Finding #5.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; log relevant findings in Discovery Notes before re-interpreting the task

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

**Read set.** `viz/src/` layout (all non-test `*.ts` / `*.tsx`), `viz/vite.config.ts` (Node plugin host), `viz/eslint.config.js` (no boundary rule today), `viz/src/main.tsx` (browser entry), `viz/src/sseChange.ts` (explicit Node-free shared encoder comment), `README.md` §Visualizer (operator runbook — stays), `AGENTS.md` §Repo Layout + Validation, `SECURITY.md` §Visualizer (threat model — stays), PLAN.md FE-093 line + FE-94.3 / FE-94.5 / FE-94.6 archive notes. No probe — read set was a directory inventory + known docs.

**Tier inventory (from `node:` import scan).**

| Tier | Location | Modules (non-test) |
|---|---|---|
| Browser UI | `src/ui/` | React components, hooks, view selectors (`App.tsx`, `taskView.ts`, …). Zero `node:` imports; imports down into shared only. |
| Shared pure | `src/*.ts` without `node:` | Parsing: `parser.ts`, `tasknote.ts`, `tasknote-parse.ts`. Also Node-free shared: `sseChange.ts` (client + plugin wire format), `viewMode.ts`, `visibilityPrefs.ts`, `projectStorage.ts` (browser `localStorage` helpers — not Node, not under `ui/`). |
| Node-only | `src/*.ts` with `node:` + `vite.config.ts` | `devApi.ts`, `workspace.ts`, `fsSafe.ts`, `archiveCache.ts`, `flowtronWatch.ts`, `watchSet.ts`, `originGuard.ts`, `apiResponse.ts`. UI never imports these. |

**Best Practices Review.** This is a module-boundary documentation task, not a code move. Touched responsibility: declare the existing dependency direction so audits can cite it. Existing abstraction: the import graph already enforces the rule by convention; `sseChange.ts` even documents why it stays Node-free. Nearby duplication: none — `viz/README.md` is absent (FE-94.5/94.6 assumed it existed for "commands + directory-level layout"; that claim was stale). Required in-scope: write the declaration. Deferred: eslint `no-restricted-imports` enforcement of the rule (declare ≠ enforce; out of [light] scope).

**Archive skim.** Hits naming FE-093 / tier boundary: FE-94.3 (selectors sit under `src/ui/`; "no Node imports (the rule FE-093 will declare)"), FE-94.5 / FE-94.6 (deferred architecture-doc; incorrectly assumed `viz/README.md` already covered commands/layout). FE-026 mentioned in architecture grep — not load-bearing for this boundary. No ⚠️ superseded pointers to follow. Load-bearing: the rule and three-tier names are already oral contract in the epic; this task writes them down.

**Drift check.** PLAN line still matches: three tiers named exactly as the epic used them; `src/ui/` exists; Node modules still use `node:`; UI still has zero `node:` imports and zero imports of Node-tier modules. No SPEC contract governs viz layout. Soft drift: archives claimed `viz/README.md` exists — it does not (never in git history). Creating it fulfills both this task and that assumed home. Middle-tier short label "shared pure parsing" is PLAN/epic vocabulary; inventory honestly includes the non-parse Node-free helpers under the same tier rather than inventing a fourth.

**No clarifications needed.** Assumptions: (1) deliverable is new `viz/README.md` (architecture + the commands/layout the epic already expected there); (2) thin `AGENTS.md` pointer for discoverability; (3) no eslint enforcement in this task; (4) root `README.md` §Visualizer and `SECURITY.md` §Visualizer stay as operator/threat surfaces — no duplication of the tier table there; (5) `src/main.tsx` is the browser entry that mounts `ui/App` and is called out as entrypoint, not a fourth tier.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

**Pattern survey.** Extended the existing dual-doc pattern: root `README.md` / `SECURITY.md` keep operator + threat surfaces; a local `viz/README.md` owns package-local architecture (what FE-94.5/94.6 already assumed). `AGENTS.md` gains a one-line pointer — same shape as other Repo Layout bullets that deep-link when detail lives elsewhere. No new doc genre.

**Minimal refactor gate.** Docs only. Deferred eslint enforcement of the boundary (declare ≠ enforce). No code moves.

**Implemented.** New `viz/README.md` (commands + three-tier table + hard rule + dependency direction + entrypoints + layout sketch). `AGENTS.md` `viz/` bullet now links to it.

**Tests.** N/A — no non-trivial runtime behavior; boundary spot-check via `rg` only.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — **N/A** (markdown-only; no runtime behavior)

- [x] Ran lint/type-check on changed code — **N/A** (no TS/JS changed)

- [x] **Quality assertions** — for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation — **N/A** (documentation only; no UI surface change)

**Testing Notes:**

Boundary spot-check (not a suite): zero `node:` imports under `viz/src/ui/`; zero UI imports of Node-tier modules; shared pure does not import `ui/`. Doc quality: single architecture home; root README / SECURITY left alone to avoid duplicating the tier table; middle-tier inventory names parsing first and lists the other Node-free helpers honestly.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed per SPEC/tasknote-selection.md §"`## Completed` archive convention" (standalone → top of `## Completed`; epic child → kept nested beneath its active parent), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Declared `viz/`'s three-tier module boundary in a new `viz/README.md` and pointed `AGENTS.md` at it, so structural audits finally have a named rule to judge the repo's only code surface against.

**Changed files.** `viz/README.md` (new) · `AGENTS.md` (one-line pointer on the `viz/` layout bullet).

**Verification.** `rg` spot-check: no `node:` under `src/ui/`, no UI→Node-tier imports, no shared→`ui/` imports. No test/lint/typecheck delta (markdown-only).

**Refactors.** None made. Deferred: eslint `no-restricted-imports` enforcement of the no-Node-under-`src/ui/` rule.

**Documentation verdict.** AI-referenced sweep below. `viz/README.md` is outside that set (package-local); creating it does not require adding it to the sweep list.

**Maintainability.** Audits and FE work can cite a stable tier table + hard rule instead of rediscovering the import graph; FE-EPIC-94's deferred "see FE-093" pointers now resolve.

**Doc-drift sweep (17 AI-referenced docs):**
- `README.md` — no change (operator §Visualizer + layout one-liner unchanged; architecture lives under `viz/`)
- `AGENTS.md` — **updated**: `viz/` Repo Layout bullet now links to `viz/README.md`
- `SPEC.md` — no change
- `docs/MIGRATION.md` — no change
- `claude/AGENTS-snippet.md` — no change (adopter runbook only)
- `codex/AGENTS-snippet.md` — no change
- `cursor/AGENTS-snippet.md` — no change
- `grok/AGENTS-snippet.md` — no change
- `docs/CONVENTIONS.md` — no change
- `CONTRIBUTING.md` — no change
- `SECURITY.md` — no change (threat model stays; no tier table duplication)
- `docs/AGENT-NEUTRALITY.md` — no change
- `docs/PLATFORMS.md` — no change
- `claude/CAPABILITIES.md` — no change
- `docs/AGENT-COMPAT.md` — no change
- `docs/EXTERNAL-AGENTS.md` — no change
- `docs/WORKTREES.md` — no change

No archived tasknote factual claim falsified (FE-94.5/94.6 assumed `viz/README.md` already existed — creating it aligns reality with that assumption rather than falsifying a claim) — no ⚠️ superseded pointer.

**Archived:** 2026-08-24
