---
title: model-chip-category-tokens
status: completed
tags: []
created: 2026-08-04
due:
related-tasks: []
---

# FE-078 | model-chip-category-tokens

[← PLAN.md](../PLAN.md) · ✅ Completed

## 🎯 Goal

Align viz model rendering with SPEC's primary-label vocabulary — `ModelChip` recognizes the `heavy` category token, and the `TaskModel` comment states category labels as the recommended set with concrete tokens as precision tokens.

## ⚡ Notes

**Relevance:** Proceed — both target files/lines unchanged since the audit; no scope drift.
**Best Practices Review:** Single-responsibility change, no dependency-direction shift. `ModelChip`'s `HEAVY_MODELS` set is the existing extension point (added to, not restructured) — matches FE-037's asymmetric-chip design (sonnet/medium/light stay glyph-free). No refactor needed; no cleanup deferred.
**Drift check:** `ModelChip.tsx`'s `HEAVY_MODELS` set (`viz/src/ui/ModelChip.tsx:5`) and `parser.ts`'s `TaskModel` comment (`viz/src/parser.ts:8-13`) both matched PLAN.md's description exactly — no drift.
**Archive skim:** `FE-037.md` (archive/frontend) — established the asymmetric chip design (bare 🧠 for opus-tier only, null for sonnet); this task extends that set rather than changing its shape. `FE-066.md` — TASK_LINE tolerances for stacked `[model]` tokens and trailing glyphs; confirms bracket tokens are captured bracket-free (`[heavy]` → `model: "heavy"`), which is why adding the literal string `'heavy'` to `HEAVY_MODELS` is correct.
**Pattern survey:** Extended `ModelChip`'s existing `Set` literal (matches its established shape) and its test file's existing `it`/`it.each` structure — no new shape introduced.
**Implementation:** `ModelChip.tsx` — added `'heavy'` to `HEAVY_MODELS` alongside the existing concrete heavy-tier tokens (`opus`, `fable`, `mythos`), with a comment clarifying it covers both the concrete tokens and the category token itself. `ModelChip.test.tsx` — added a `renders 🧠 for the heavy category token` case, and extended the negative `it.each` to also cover `'medium'`/`'light'` category tokens (previously only concrete non-heavy models were tested). `parser.ts` — reworded the `TaskModel` doc comment to lead with `'heavy' | 'medium' | 'light'` as the recommended set (mirroring SPEC.md §"Task-line format"'s "Recommended primary labels"), with concrete model names now described as precision tokens rather than the primary recommendation.
**Docs touched:** No change — both edits are inline code comments/tests, not `.flowtron/tasknote/README.md`-referenced docs.

## ✅ Recap

`ModelChip.tsx`'s `HEAVY_MODELS` set now includes the `'heavy'` category token (in addition to concrete tokens `opus`/`fable`/`mythos`), so a `[heavy]`-tagged PLAN.md task renders the 🧠 chip the same as a concrete heavy-tier model — closing the gap where category-tagged tasks silently got no glyph. `[medium]`/`[light]` remain glyph-free per FE-037's asymmetric design (now explicitly tested). `parser.ts`'s `TaskModel` doc comment now states the category labels (`heavy`/`medium`/`light`) as flowtron's recommended set, with concrete model names reframed as precision tokens — matching SPEC.md's canonical phrasing instead of implying concrete names are primary. 3 files changed (`ModelChip.tsx`, `ModelChip.test.tsx`, `parser.ts`); full suite 245/245 passing, typecheck clean, lint clean.

**Archived:** 2026-08-04
