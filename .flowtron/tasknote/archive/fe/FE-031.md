---
title: viz-chip-system-trim
status: completed
tags: []
created: 2026-05-14
due:
related-tasks: []
---

# FE-031 | viz-chip-system-trim

[← PLAN.md](../../PLAN.md) · ✅ Completed

## 🎯 Goal

Strip the viz row's chip system back to ProjectSelector + StatusChip, removing surplus chips and the "no tasknote" placeholder, and re-evaluate PhaseDots / SubtaskProgress.

## ✅ Acceptance

- [x] Per-row chip surface contains only ProjectSelector + StatusChip (plus PhaseDots / SubtaskProgress if retained after re-evaluation)
- [x] ModelChip, frontmatter tag chips, due chip, BlockerChip, RelatedChip, BackRefChip, and "no tasknote" placeholder are removed
- [x] PhaseDots / SubtaskProgress decision documented in Discovery (keep / drop / defer) — both kept; sit in their own grid column, dense + informative, not the visual clutter source
- [x] Dead code removed (components, helpers, types, styles, imports made unused by this change)
- [x] Tests updated; `npm test` passes in `viz/` (no `lint` script — `npm run typecheck` clean)

## 🧩 Subtasks

- [x] Strip surplus chip blocks from `viz/src/ui/TaskRowInner.tsx` (ModelChip, frontmatter tag chips, due chip, BlockerChip cluster, RelatedChip cluster, BackRefChip, "no tasknote" placeholder) and prune imports / unused props / unused helpers (`formatDue`, `inboundCount`, `dueLabel`)
- [x] Swap the inline status badge in `TaskRowInner` (lines 126-138) for the existing `StatusChip` component
- [x] Delete now-orphaned chip files: `viz/src/ui/ModelChip.tsx`, `BlockerChip.tsx`, `RelatedChip.tsx`, `BackRefChip.tsx`
- [x] Remove the header Tags filter row from `viz/src/ui/App.tsx` (the `<div>` at lines 349-367) plus `allTags` memo, `tagFilter` state, `setTagFilter` resets in `handleSelectProject`, the tag branch in `matchesFilter`, and the `fm.tags.join(' ')` token in the search-haystack (also updated search placeholder to drop "tags")
- [x] Audit downstream removals: `useKeyboardNav` props (`tagFilter`, `setTagFilter`), `buildInboundRefs` helper + `inboundRefs` plumbing through `PrioritySection` → `TaskRow` → `TaskRowInner`, `navigateToTask` from `TaskRowInner` props (only used by removed Blocker/Related chips), `PILL_DEFAULT_SLATE` import from App
- [x] Update `viz/src/ui/App.test.tsx` — drop assertions for removed chips and the Tags filter row, rework `navigateToTask` test to click a wikilink in TaskDetail instead of RelatedChip, drop the entire "inbound back-refs" describe, add coverage that `StatusChip` renders in rows
- [x] Run `npm test` and `npm run typecheck` in `viz/`; both clean
- [x] Visual confirmation (👁️) — confirmed by user: row stripped to ID + title + PhaseDots + SubtaskProgress + StatusChip; header has no Tags filter row; keyboard nav + status filter still work

## 🔗 Related

- [[FE-008]] — viz row-density redesign (introduced the per-row chip system being trimmed)
- [[FE-012]] — viz inbound wikilink back-refs (introduced BackRefChip + `buildInboundRefs`)
- [[FE-024]] — viz-app-decomposition (split App.tsx into the current component fanout)
- [[FE-015]] — filter chips include no-tasknote rows (Status filter row stays; this trim drops Tags filter row only)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed (scoped)
  **Rationale:** PLAN intent — strip surplus per-row chips — is sound and well-localized in `viz/src/ui/TaskRowInner.tsx` + `App.tsx`. Re-scoped from a literal reading of the PLAN line on three points after Discovery + user discussion: (1) "ProjectSelector" in the PLAN line is a misnomer — ProjectSelector is the header-level project switcher (App.tsx:344), not a per-row chip; left untouched. (2) PhaseDots and SubtaskProgress are kept — they sit in a separate row-grid column, don't visually contribute to the clutter the user is targeting, and carry genuine signal (active phase + epic completion). (3) Header Tags filter row added to removal scope — per-row tag chips were what drove it, and tags are dead weight in flowtron-self anyway (15/104 archived tasknotes have non-empty tags; SPEC.md doesn't define them).

- [x] Read relevant source files
- [x] **Archive skim** — see Discovery Notes
- [x] **Drift check** — see Discovery Notes
- [x] Asked clarifying questions (3 questions; user redirected PhaseDots/SubtaskProgress re-eval into a tag-purpose investigation)
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- **Component inventory (TaskRowInner.tsx).** Per-row right cluster is two grid columns:
  - **Left column (phase/progress/blockers):** `PhaseDots` (when tasknote present and not starter), `SubtaskProgress` (when subtasks > 0), `BlockerChip` per blocker.
  - **Right column (metadata/status):** "no tasknote" placeholder, `ModelChip`, frontmatter tag chips, `RelatedChip` per related task, `BackRefChip` (inbound count), due chip, inline status badge.
  - Per the scoped plan: keep the left column entirely; right column collapses to just `StatusChip`.
- **StatusChip drift.** `TaskRowInner` inlines the same status-badge mapping that `StatusChip.tsx` already encapsulates (used by `TaskDetail.tsx:39`). This trim swaps the inline span for the component — matches the PLAN line's "StatusChip" wording literally and removes a duplicate branch.
- **Tags investigation.** 15/104 archived tasknotes have non-empty `tags:` (~14%); 89 ship `tags: []`. SPEC.md never defines tags' purpose; templates default to `[]`. For an AI coding assistant the task-ID prefix + PLAN description carry the same signal more precisely. UI surface (per-row chips + header filter) removed; `tags:` frontmatter field, parser, and `Tasknote` type left intact (cheap to keep; schema change is a separate task).
- **Archive skim findings:**
  - `FE-008` (viz row-density redesign) — introduced the per-row chip system; this task partially reverses that.
  - `FE-012` (viz inbound wikilink back-refs) — introduced `BackRefChip` + `buildInboundRefs` helper. Audit whether `inboundRefs` plumbing has any other consumer after the chip goes.
  - `FE-024` (viz-app-decomposition) — split App.tsx; the row-cluster structure being edited dates to this split.
  - `FE-015` (filter chips include no-tasknote rows) — Status filter row in header stays; only the Tags filter row goes.
- **Drift check vs PLAN description.**
  - "ProjectSelector + StatusChip" — `ProjectSelector` is the header switcher (App.tsx:344), not a per-row chip. Interpreted as "header `ProjectSelector` untouched; per-row chip surface collapses to `StatusChip` only."
  - All other named chips (ModelChip / BlockerChip / RelatedChip / BackRefChip / due / tags / "no tasknote") match current code in `viz/src/ui/`.
- **Downstream plumbing to audit (Phase 2).** `buildInboundRefs` + `inboundRefs` prop drilled through `PrioritySection` → `TaskRow` → `TaskRowInner`; `useKeyboardNav` props `tagFilter` / `setTagFilter`; `handleSelectProject`'s `setTagFilter(new Set())` reset. Remove only what the chip removal makes unused.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — `TaskDetail.tsx:39` already uses `<StatusChip status={status} />` with `effectiveStatus(task, tasknote)`. Extended the same pattern to `TaskRowInner`. No new shape introduced.
- [x] Implemented the minimal solution
- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

- **`TaskRowInner.tsx`** rewritten end-to-end: imports collapsed to React + parser/tasknote types + `effectiveStatus` + `PhaseDots` + `SubtaskProgress` + `StatusChip`. Props dropped: `inboundRefs`, `navigateToTask`. Right grid column collapsed to `{status && <StatusChip status={status} />}` followed by `{extraRightSlot}`. Left grid column (PhaseDots, SubtaskProgress) kept verbatim. `formatDue`, `inboundCount`, `dueLabel`, `relatedTasks`, `showNoTasknote` — all gone. The degenerate "tasknote present but no parseable frontmatter status" fallback dropped (matches TaskDetail's behavior of rendering nothing in that case).
- **`App.tsx`** edits: dropped `buildInboundRefs` from imports + `inboundRefs` useMemo + `inboundRefs` prop on PrioritySection; dropped `tagFilter` useToggleSet + `allTags` useMemo + tag branch in `matchesFilter` + `fm.tags.join(' ')` from search haystack + `setTagFilter(new Set())` in `handleSelectProject` + `tagFilter`/`setTagFilter` from `useKeyboardNav` call; dropped the Tags filter `<div>` block; dropped `PILL_DEFAULT_SLATE` import (Tags row was its only App-side consumer; ProjectSelector still uses it); updated search placeholder from "Search id, description, tags, status" to "Search id, description, status".
- **`useKeyboardNav.ts`**: dropped `tagFilter`/`setTagFilter` from `UseKeyboardNavParams`, destructuring, Escape-clear logic, and effect deps.
- **`utils.ts`**: deleted `buildInboundRefs` (no other consumers).
- **`PrioritySection.tsx`, `TaskRow.tsx`, `EpicRow.tsx`**: dropped `inboundRefs` prop type + destructuring + pass-down through the chain; dropped `navigateToTask` from `TaskRowInner` call sites (only kept where TaskRow/EpicRow themselves need it — for TaskDetail).
- **Deleted files**: `viz/src/ui/ModelChip.tsx`, `BlockerChip.tsx`, `RelatedChip.tsx`, `BackRefChip.tsx`.
- **Constants kept**: `STATUS_LABEL` and `STATUS_BADGE` still used by App's Status filter row; `STATUS_CHIP_LABEL` still used by `StatusChip` (now in the row too).
- **Frontmatter `tags:` field, parser, and Tasknote type left intact** — schema change deferred (cheap to keep; UI surface removal is the user's actual complaint).

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npm test` in `viz/`: 84/84 pass
- [x] Ran lint/type-check on changed code — no `lint` script; `npm run typecheck` (tsc --noEmit) clean
- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask) — user replied "go" after seeing the trimmed flowtron view

**Testing Notes:**

- All 6 test files pass, 84/84 tests. Rewrote `App.test.tsx` substantially: dropped the tag-filter dimension from the intersection test, reworked `navigateToTask` to click a `[[CORE-1.1]]` wikilink inside TaskDetail (WikilinkMarkdown path) instead of the now-removed RelatedChip, deleted the BackRefChip-specific "inbound back-refs" describe block, swapped the "filters reset on project switch" assertion from tag-chip click to query-input value, and added a new "row StatusChip" describe verifying `🟢 In progress` renders in the row.
- Typecheck clean — confirms removed props (`inboundRefs`, `navigateToTask` from `TaskRowInner`, `tagFilter`/`setTagFilter` from `useKeyboardNav`) and removed imports (`buildInboundRefs`, `PILL_DEFAULT_SLATE` in App) leave no dangling references.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — per-entry verdicts:
  - `README.md` — **no change.** Line 31 "project-selector chip group" refers to header `ProjectSelector` (untouched).
  - `SPEC.md` — **no change.** Lines 206-215 describe the tasknote markdown nav-header status chip glyph vocabulary (🟢 / ✅ / ⏸ / ⚪ / 🌱). After FE-031, viz still renders exactly that vocabulary via `StatusChip` + `STATUS_CHIP_LABEL`.
  - `docs/MIGRATION.md` — **no change.** Line 366 "header-rail chip group swaps the active project" refers to `ProjectSelector` (untouched).
  - `claude/CLAUDE-snippet.md` — **no change.** No viz/UI references at all.
- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-05-14.` and tasknote moved to `_project/tasknote/archive/frontend/`
- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate)

**Final Summary:**

Trimmed the viz per-row chip surface to just `StatusChip` (emoji-prefixed, matching TaskDetail), kept PhaseDots + SubtaskProgress in their own grid column, and dropped the header Tags filter row. Removes the visual clutter the user flagged without re-evaluating the dense progress indicators that carry genuine signal. Files: `viz/src/ui/TaskRowInner.tsx` (rewrite), `App.tsx`, `useKeyboardNav.ts`, `utils.ts`, `PrioritySection.tsx`, `TaskRow.tsx`, `EpicRow.tsx`, `App.test.tsx` (substantial rewrite); deleted `ModelChip.tsx`, `BlockerChip.tsx`, `RelatedChip.tsx`, `BackRefChip.tsx`. Net ~140 LOC removed across the viz UI. Tests: 84/84 pass; typecheck clean; visual confirmation accepted.

**Archived:** 2026-05-14
