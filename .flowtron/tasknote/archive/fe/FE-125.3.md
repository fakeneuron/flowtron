---
title: viz-comment-trim-rest
status: completed
tags: [viz, comments, epic-child]
created: 2026-09-22
due:
related-tasks: [FE-EPIC-125, FE-125.2, FE-125.N]
touches:
  - viz/src/**/*.ts
  - viz/src/**/*.tsx
  - viz/src/styles.css
---

# FE-125.3 | viz-comment-trim-rest

[← PLAN.md](../PLAN.md) · ✅ Completed · 🔗 [[FE-EPIC-125]]

## 🎯 Goal

Apply the FE-125.2 §"Pilot rule" to every remaining `viz/src` file: strip task-ID provenance from comments, keep the rationale as present-tense prose, and backtick the IDs that are references rather than history (security advisories, test-fixture IDs, syntax examples).

## ✅ Acceptance

- [x] Pilot done-signal passes on every `viz/src` file — `for f in $(git ls-files viz/src); do grep -nE '^\s*(//|/\*|\*)' "$f" | sed -E 's/`[^`]*`//g' | grep -E '\b[A-Z]{2,}(-EPIC)?-[0-9]+' | sed "s|^|$f:|"; done` prints nothing
- [x] Rationale kept: every comment that loses an ID keeps its *why* in present tense; `SPEC` / `Contract:` pointers untouched; advisory and fixture IDs kept inside backticks — `judgment` (checked by the external review against the diff)
- [x] No task-ID provenance left in `it` / `describe` titles (operator widening 2026-09-22; syntax examples and advisories backticked) — `rg -n "(it|test|describe)(\.\w+)?\(\s*['\"\`][^'\"]*" viz/src | sed -E 's/`[^`]*`//g' | rg "(it|test|describe)(\.\w+)?\(\s*['\"][^'\"]*\b[A-Z]{2,}(-EPIC)?-[0-9]+"` prints nothing
- [x] Comment + title-only diff: no code token changed — `git diff -U0 -- viz/src | grep -E '^[+-][^+-]' | grep -vE '^[+-]\s*(//|/\*|\*|$|(it|test|describe)\()'` prints nothing
- [x] Viz still green — `npm --prefix viz test` → 0, `npm --prefix viz run typecheck` → 0, `npm --prefix viz run lint` → 0
- [x] Comment-line ratio across `viz/src` reported before/after in Testing Notes — reported, not gated (pilot rule)

## 🧩 Subtasks

- [x] Record the baseline: hit count (104 lines / 30 files) and comment-line ratio
- [x] Non-UI modules: `archiveCache.ts`, `devApi.ts`, `flowtronWatch.ts`, `fsSafe.ts`, `originGuard.ts`, `sseChange.ts`, `tasknote.ts`, `tasknote-parse.ts`, `tasknoteRead.ts`, `watchSet.ts`, `workspace.ts`, `styles.css`
- [x] UI modules: `ui/constants.ts`, `ui/taskView.ts`, `ui/useProjectData.ts`, `ui/utils.ts`, `ui/TaskDetailPanel.tsx`
- [x] Tests: `parser.test.ts`, `devApi.test.ts`, `flowtronWatch.test.ts`, `watchSet.test.ts`, `test/setup.ts`, and the `ui/*.test.ts(x)` files
- [x] Backtick advisory IDs (GHSA/CVE) and test-fixture IDs rather than removing them
- [x] Strip provenance IDs from `it` / `describe` titles (backtick syntax-example / advisory IDs)
- [x] Run the gate loop + comment-only-diff check; record the after ratio
- [x] Run viz test / typecheck / lint

## 🔗 Related

- [[FE-EPIC-125]] — parent epic
- [[FE-125.2]] — predecessor pilot; source of §"Pilot rule"
- [[FE-125.N]] — epic audit

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** 104 comment lines across 30 non-parser `viz/src` files still fail the pilot's done-signal; the pilot left the rule and gate ready to apply.

- [x] Read relevant source files — ran the pilot gate grep over all 98 tracked `viz/src` files and read every hit line; trailing/JSX comments and `it`/`describe` titles carry no IDs (both greps empty), so full-line comments are the whole surface. No doc cites `viz/src/*:<line>`.

- [x] **Best Practices Review** — N/A: comment-only edit; no responsibility, dependency, or public-surface change. The risk is losing *why* while cutting *who/when*; the pilot rule separates the two.

- [x] **Archive skim** — `archive/fe/` (confirmed against README table: `FE-*` → `archive/fe/`); 129 of 140 FE notes touch `viz/src`, so the per-path grep is useless as a filter. Grepped instead for comment-provenance precedent: FE-125.2 (the rule, loaded in full) and CORE-652.2/.N (the same trim over repo docs, with a labelled-retention convention for pointers that must stay). Nothing contradicts the pilot rule.

- [x] **Drift check** — the PLAN line ("apply the pilot's rule to the remaining `viz/src` files") matches the pilot's §"Pilot rule" and current code; `parser.ts` already passes. No SPEC contract governs in-code comment citations.

- [x] Asked clarifying questions (AskUserQuestion 2026-09-22): security advisory IDs (`GHSA-…`, `CVE-…`) → **backtick them**; test-fixture IDs in comments (`CORE-101`, `CORE-1.1`, …) → **backtick them**. Both keep the pilot gate unchanged.

- [x] Subtasks above populated with concrete, ordered steps, and YAML `touches:` declared with the paths this task expects to edit

**Discovery Notes:**

- Hit classes: (a) bare provenance tags `(FE-xxx)` / `FE-xxx:` prefixes, the bulk; (b) history narratives ("FE-072 had one", "until CORE-425.2", "FE-101.4 replaced …", "post-CORE-098.2", `tasknote.ts` CORE-393 landing-time note); (c) references to keep inside backticks: `GHSA-*` / `CVE-*` advisories (`originGuard.ts:14`, `tasknote-parse.ts:13-14`) and test-fixture IDs (`App.test.tsx`, `useBoardSelection.test.ts`).
- `tasknote.ts:222-226` is a narrative whose timestamps back code: the comparison excludes the day the `**Archived:**` stamp rule landed. Keep the dates and times and restate them as "the day the stamp rule landed", with no ID.
- Regression-test comments ("The CORE-425.2 regression: …") → "Regression: …", keeping the failure description.
- **Scope widening (Phase 2, operator 2026-09-22):** the first title/trailing-comment greps used `\b` under `git grep -E`, which macOS ERE does not support, so they came back empty wrongly. Re-run with `rg`: trailing comments are still clean, but 27 `it`/`describe` titles carry IDs (25 provenance, plus `FE-310.3a` / `FE-067.2.1` syntax examples and `GHSA-2883`). Operator chose to include them, which widens the diff criterion to comments + title strings. No snapshots are keyed on titles (`*.snap` / `toMatchSnapshot` absent). The effect stays inside this task, so no downstream reconciliation is needed.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — N/A: comment + test-title diff, no behavior change

**Implementation Notes:**

- Pattern survey / refactor gate: no new shape. This applies the FE-125.2 rule as-is; no code was refactored.
- 33 files, +159/−164. Edits were applied as exact-match replacements, each asserted to match once (zero misses).
- Bare `(ID)` tags and `ID:` prefixes dropped, most of the diff.
- Narratives rewritten to present tense:
  - `flowtronWatch.ts`: the archive-reach / hot-set / native-watch docblocks no longer mention retired globs.
  - `watchSet.ts`: the chokidar literal-path note.
  - `tasknote.ts`: the tick-through cutoff note keeps its 23:32 / 21:24 timestamps without naming the tasks.
  - `constants.ts`: the palette invariants. `.3` (Linear) / `.4` (GitHub) was epic-child provenance the gate misses, reworded to "Linear and GitHub".
  - `useProjectData(.test).ts`: the per-scope counter rationale.
  - `PrioritySection.test.tsx`: the collapse rationale.
  - `flowtronWatch.test.ts`: the one-shot-iterator rationale.
  - `useKeyboardNav.test.ts`: the regression framing.
- Backticked, not removed: `GHSA-*` / `CVE-*` in `originGuard.ts`, `tasknote-parse.ts`, and the `tasknote.test.ts` title; fixture IDs in `App.test.tsx` and `useBoardSelection.test.ts`; syntax-example titles `FE-310.3a` / `FE-067.2.1`.
- `viz/src/parser.ts` untouched; it already passed after FE-125.2.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Verification receipt** — recorded each Acceptance verify command in Testing Notes as `command → exit code`, with the first failure line when non-zero; and, for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] **External review** — a context that did not write the diff graded it against `## ✅ Acceptance`, and every finding is recorded below with its disposition (**blocker** → back to Phase 2; **note** → fixed or filed). `N/A` with a one-line reason when the diff is too small to grade

- [x] (frontend) Asked the user for visual confirmation — N/A: comments and test titles only; nothing renders differently

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**

Verification receipt (re-run from the top after the review fixes):

- Comment gate (pilot done-signal loop over `git ls-files viz/src`) → prints nothing (baseline: 104 lines / 30 files)
- Title gate (`rg` over `it`/`test`/`describe` titles, code spans stripped) → exit 1, no output (baseline: 27 titles)
- `git diff -U0 -- viz/src | grep -E '^[+-][^+-]' | grep -vE '^[+-]\s*(//|/\*|\*|$|(it|test|describe)\()'` → exit 1 (comments + titles only)
- `npm --prefix viz test` → 0 (29 files, 587 passed) · `npm --prefix viz run typecheck` → 0 · `npm --prefix viz run lint` → 0
- Ratio (reported, not gated): 866/14021 (6.18%) → 862/14017 (6.15%), across `viz/src` `.ts`/`.tsx`/`.css`
- Structural: no code touched; no snapshots keyed on titles; no duplication, dead code, or public-surface change.

External review (general-purpose subagent, graded against Acceptance, scoped to `git diff -- viz/src`): **no blockers**; criteria 1–4 pass.

- **note** `tasknote.test.ts:671` dropping `(CORE-098.1 shape)` lost a fixture reference → **fixed**: backticked `` (`CORE-098.1` shape) `` (the fixture's own heading).
- **note** `flowtronWatch.test.ts:248` "on its own the suite is unable…" read awkwardly → **fixed**: "array-fed tests alone can't express this failure".
- **note** `flowtronWatch.ts:42` ~90-col line, `App.test.tsx:1297` dangling `// —` → **fixed** (reflowed).
- **note** `tasknote.ts:222,226` "this date" unanchored, "(one that day closed…)" narration → **fixed**: names `TICK_THROUGH_EFFECTIVE`; "notes that day closed as early as 21:24".
- **note** ID-free "used to" narration on untouched lines (`watchSet.ts:16`, `useKeyboardNav.test.ts`), ragged reflow after shortened prefixes (`parser.test.ts`, `TaskRow.test.tsx`), pre-existing typo "staleds" (`useProjectData.test.ts:352`) → **left**: outside the ID-bearing scope the pilot rule gates / cosmetic only; handed to FE-125.N's audit sweep rather than filed.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed (standalone → top of `## Completed`; epic child → kept nested beneath its active parent — see SPEC/plan-filing.md §"`## Completed` archive convention" if unclear), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, the `touches:` scope reconciliation (`git diff --name-only` vs declared; name undeclared paths), and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

- [x] **Learnings** — did this task teach something the always-loaded layer (AGENTS.md / README §AI-referenced docs) should carry? `N/A` or the line

Doc-drift sweep — no change for every entry (`README.md`, `AGENTS.md`, `SPEC.md`, `SPEC/*.md`, `docs/MIGRATION.md`, the four `*/AGENTS-snippet.md`, `docs/CONVENTIONS.md`, `CONTRIBUTING.md`, `SECURITY.md`, `docs/AGENT-NEUTRALITY.md`, `docs/PLATFORMS.md`, `claude/CAPABILITIES.md`, `claude/skills/*/SKILL.md`, `docs/AGENT-COMPAT.md`, `docs/EXTERNAL-AGENTS.md`, `docs/WORKTREES.md`, `docs/VISION.md`, `docs/GLOSSARY.md`, `docs/PHILOSOPHY.md`, `docs/DOGFOOD.md`, `docs/CONTEXT-BUDGET.md`, `docs/VERSION-HISTORY.md`): none quotes `viz/src` comment text or a renamed test title (checked with `rg` outside `viz/src` and the archive).

Learnings — the obvious greps missed two kinds of provenance: `it`/`describe` title strings, and `.3`/`.4`-style bare child suffixes, which have no area prefix for the regex to catch. For FE-125.N: its audit should re-run the title gate. `\b` does not work under macOS `grep -E` / `git grep -E`, so use `rg` for any gate that needs word boundaries. Tasknote-local, same as the pilot rule. N/A for the always-loaded layer.

**Final Summary:**

Rolled the FE-125.2 rule out to the rest of `viz/src`. The pilot's gate still holds unchanged, but the gated surface now covers `it`/`describe` titles as well as comments, by operator decision. Changes: 33 files, +161/−165.

- Every comment and test title passes the gate.
- History narratives are restated as present-tense rationale.
- Advisory, fixture, and syntax-example IDs are backticked.
- Tests, typecheck, and lint are green.

`touches:` reconciliation: declared `viz/src/**/*.ts`, `viz/src/**/*.tsx`, `viz/src/styles.css`, and the diff matches them. The diff also carries `.flowtron/PLAN.md` and this tasknote, which are workflow files. There are no undeclared deliverable paths.

**Archived:** 2026-09-22
