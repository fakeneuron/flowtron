---
title: local-storage-helper
status: completed
tags: []
created: 2026-09-14
due:
related-tasks: []
touches:
  - viz/src/storage.ts
  - viz/src/projectStorage.ts
  - viz/src/viewMode.ts
  - viz/src/visibilityPrefs.ts
  - viz/src/ui/theme.ts
---

# FE-121 | local-storage-helper

[← PLAN.md](../PLAN.md) · ✅ Completed

## 🎯 Goal

Extract a shared, pure `viz/src/storage.ts` (`readLocal`/`writeLocal`) and route `projectStorage`, `viewMode`, `visibilityPrefs`, and `ui/theme` through it, keeping theme's legacy-key fallback.

## ⚡ Notes

**Relevance:** Proceed — a small, well-scoped extraction; four call sites duplicate the same try/catch localStorage pattern (`projectStorage.ts`, `viewMode.ts`, `visibilityPrefs.ts`, `ui/theme.ts`), surfaced by the 2026-09-14 audit (Finding #2, Low).
**Best Practices Review:** All four files repeat `try { window.localStorage.get/setItem } catch { ... }` with only the fallback-on-failure value differing (`null`, a typed default, a parsed object, silently swallowed). A pure `readLocal(key): string | null` / `writeLocal(key, value): void` pair in `viz/src/storage.ts` gives each caller one shared try/catch, with domain-specific parsing/defaulting staying in the caller. No existing shared storage module to extend — this creates the first one. `ui/theme.ts`'s two-key read (`STORAGE_KEY` then `LEGACY_STORAGE_KEY`) stays a caller-side concern: `readLocal` takes one key, so `theme.ts` calls it twice and keeps its own fallback-chaining logic.
**Drift check:** PLAN.md line paths (`projectStorage`, `viewMode`, `visibilityPrefs`, `ui/theme`) match current file names and contents — no drift. No SPEC contract touched.
**Archive skim:** No prior tasknote created a shared storage helper. FE-116 (`ui/theme.ts` rename) established the legacy-key (`theme`) read fallback this task must preserve. FE-98 (`visibilityPrefs.ts` vocabulary dedup) confirms `visibilityPrefs.ts` owns its own pref vocabulary/parsing — only its raw get/set should route through the new helper, not `parsePrefs`.
**Declared scope:** see YAML `touches:` above.
**Pattern survey:** No existing `viz/src/storage.ts` or equivalent; new module. Each of the four call sites keeps its own exported read/write function signatures (`readStoredProject`, `readStoredViewMode`, `readVisibilityPrefs`, `readPreference`, etc.) — only their internal `window.localStorage.getItem/setItem` calls route through the new `readLocal`/`writeLocal`.
**Implementation:** Added `viz/src/storage.ts` exporting pure `readLocal(key): string | null` / `writeLocal(key, value): void`, each with its own try/catch (matches the per-call-site swallow-and-fallback behavior being replaced). Routed the four call sites' raw `window.localStorage`/`localStorage` access through it: `projectStorage.ts` (`readStoredProject`/`writeStoredProject`), `viewMode.ts` (`readStoredViewMode`/`writeStoredViewMode`), `visibilityPrefs.ts` (`readVisibilityPrefs`/`writeVisibilityPrefs` — `parsePrefs` unchanged, still owns vocabulary parsing/defaulting), `ui/theme.ts` (`readPreference`/`writePreference` — legacy `theme` key fallback preserved as two sequential `readLocal` calls chained with `??`). No public API of any of the four modules changed. Verification receipt: `npm --prefix viz test -- --run` → 0 (29 files, 561 tests); `npm --prefix viz run typecheck` → 0; `npm --prefix viz run lint` → 0. Structural quality: no duplication introduced (the four call sites' shared try/catch boilerplate is exactly what was removed); dependency direction intact (all five files are `src/*.ts` shared-pure tier, no Node/`src/ui/` boundary crossed).
**Docs touched:** No `.flowtron/tasknote/README.md` §"AI-referenced docs" entry covers viz internals — no change there. `viz/README.md` (not in that sweep set, but directly describes the touched module-tier table) updated: added `storage.ts` to the "Shared pure" tier row and the layout sketch, since it's now part of the same shared-pure module set as `viewMode`/`visibilityPrefs`/`projectStorage`.

## ✅ Recap

Extracted `viz/src/storage.ts` (`readLocal`/`writeLocal`) as the shared localStorage read/write primitive and routed `projectStorage.ts`, `viewMode.ts`, `visibilityPrefs.ts`, and `ui/theme.ts` through it, removing four duplicated try/catch blocks. `ui/theme.ts` keeps its legacy `theme` → `flowtron-viz-theme` key fallback, now expressed as two `readLocal` calls. No behavior change: full suite (561 tests), typecheck, and lint all pass. `touches:` reconciliation — `git diff --name-only` shows `viz/src/storage.ts` (new), `viz/src/projectStorage.ts`, `viz/src/viewMode.ts`, `viz/src/visibilityPrefs.ts`, `viz/src/ui/theme.ts` (all declared) plus `viz/README.md` (undeclared — a small doc-accuracy addition to the module-tier table this task's new module belongs in). Nothing for `/ft-audit` to flag.

**Archived:** 2026-09-14
