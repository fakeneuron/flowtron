---
title: plan-parse-near-miss
status: completed
tags: []
created: 2026-08-13
due:
related-tasks: [FE-066, FE-063.2, FE-067, CORE-333]
---

# FE-087 | plan-parse-near-miss

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-066]] [[FE-063.2]] [[FE-067]] [[CORE-333]]

## 🎯 Goal

Tighten the viz PLAN parser so adopter near-misses — lettered subtasks, nested decimals, `[!critical]` after the model glyph, and bare checkbox bullets — parse or stay silent instead of flagging as failed tasks.

## ✅ Acceptance

- [x] Lettered subtask IDs (`FE-310.3a`) parse as tasks, not unparsed diagnostics; they nest under the matching epic when present
- [x] Nested-decimal IDs (`FE-067.2.1`) parse as tasks, not unparsed diagnostics; they nest under the matching epic when present
- [x] `[!critical]` after `[model]` still sets `critical: true` (canonical order unchanged)
- [x] Bare checkbox bullets (`- [ ]` with no ID emphasis) are silent — neither tasks nor unparsed diagnostics; genuine ID near-misses (`*FE-064*`, `**fe-065**`) still flag
- [x] Parser tests cover each case; `WIKILINK_PATTERN` / `groupTasks` stay in lockstep with the widened ID body (CORE-333 precedent)
- [x] SPEC §"Task-line format" documents the new tolerances; canonical Task ID convention is not rewritten

## 🧩 Subtasks

- [x] Extract a shared `TASK_ID_BODY` fragment and widen it: repeating decimal segments, optional lowercase letter suffix on a numeric segment
- [x] Thread the same body through `TASK_ID`, `WIKILINK_PATTERN`, `BLOCKED_BY_BLOCK`, `ID_SHAPE_CASE_INSENSITIVE`, and `SUBTASK_ID`
- [x] Add a trailing optional `[!critical]` capture after stacked model tokens so swapped flag order still sets `critical`
- [x] Gate `unparsed` on attempted ID markup (`*`) so bare checkbox bullets stay silent
- [x] Add parser fixtures for all four near-misses plus the still-flagged FE-063.2 cases
- [x] Document the tolerances in SPEC.md §"Task-line format"

## 🔗 Related

- [[FE-066]] — decorative glyph / stacked-model / leading-status tolerances on `TASK_LINE`
- [[FE-063.2]] — unparsed-line diagnostics that currently flag these near-misses as failed tasks
- [[FE-067]] — silent exclusion of completed legacy-label lines (bare-checkbox exclusion follows this, not TASK_LINE)
- [[CORE-333]] — when widening ID grammar, update all five regex slots in `parser.ts`

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** All four named near-misses fail `TASK_LINE` / land in `unparsed` at HEAD; the fleet-viz diagnostic is a real parser gap, not a stale filing.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope; log relevant findings in Discovery Notes before re-interpreting the task

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read them, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- Read set (narrow, no probe): `viz/src/parser.ts`, `viz/src/parser.test.ts`, SPEC.md §"Task ID convention" + §"Task-line format". UI copy in `App.tsx` ("look like tasks but failed to parse") is the banner these near-misses currently trip; no App.tsx edit planned.
- **Why they fail today.** `TASK_ID` is `\*\*([A-Z]+(?:-EPIC)?-\d+(?:\.(?:\d+|N))?)\*\*` — one optional decimal, digits-or-`N` only — so `FE-310.3a` and `FE-067.2.1` miss. `CRITICAL_FLAG` sits before `MODEL_TOKEN`, so `[opus] [!critical]` cannot match `STACKED_MODEL_TOKENS` (`!` is not `[a-z]`) and the line fails. `CHECKBOX_BULLET` (`^\s*-\s+\[[ xX]\]`) then pushes any leftover checkbox line into `unparsed`, including `- [ ] buy milk`.
- **Archive skim.** FE-063.2 introduced the unparsed banner (flag checkbox lines that fail `TASK_LINE`). FE-066 added three *decorative, not captured* `TASK_LINE` tolerances. FE-067 silently excludes completed legacy-label lines *outside* `TASK_LINE`. FE-084 composed `TASK_LINE` from named fragments (extend that shape). CORE-333 is the ID-widening precedent: when the ID body changes, update `TASK_LINE`, `WIKILINK_PATTERN`, `BLOCKED_BY_BLOCK`, `SUBTASK_ID`, and `ID_SHAPE_CASE_INSENSITIVE` together. FE-022 made `parser.ts` the single source for ID grammar. FE-044 locked canonical `[!critical]` *before* `[model]` — this task tolerates the reverse, does not rewrite the authoring grammar.
- **Best Practices.** Touched responsibility is PLAN-line ID/flag grammar in `parser.ts` (FE-022 single source). Dependency direction: parser owns ID shape; `WikilinkMarkdown.tsx` consumes exported `WIKILINK_PATTERN`. Nearby duplication is the five restated ID regexes — extract `TASK_ID_BODY` rather than widening them independently. No unrelated cleanup.
- **Drift check.** Code matches the filing: `TASK_ID` at parser.ts:67, `CRITICAL_FLAG` then `MODEL_TOKEN` at :68-69, `CHECKBOX_BULLET` + unparsed push at :278/:342-350. SPEC §"Task ID convention" still authorizes only `\d+` or `N` in the subtask slot, one decimal; §"Task-line format" still requires `[!critical]` before `[model]`. Plan is viz *tolerance*, not a contract rewrite — same pattern as FE-066's "Parser tolerances" note. No contradiction; PLAN.md line matches the four cases exactly.
- **No clarifications needed.** Assumptions: (1) lettered/nested IDs and swapped `[!critical]` are viz-only tolerances — canonical authoring in SPEC §"Task ID convention" / §"Task-line format" stays as-is, documented like FE-066. (2) "Bare checkbox bullets" means checkbox lines with no `*`/`**` ID emphasis; FE-063.2 still flags `*FE-064*` and `**fe-065**`. (3) `groupTasks` / wikilinks follow the widened ID body so a parsed `FE-310.3a` nests under `FE-EPIC-310` and `[[FE-067.2.1]]` extracts — CORE-333 lockstep, not a scope add. (4) Letter suffix is `\d+[a-z]?` (digits then optional lowercase letter), not a free letter segment (`.a` alone stays unparsed).

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

- **Pattern survey.** Extended FE-084 named fragments + FE-066 optional-group tolerances for the swapped `[!critical]`; followed CORE-333 for the ID-body widening (all five regex slots together) and FE-067 for bare-checkbox *exclusion* outside `TASK_LINE` rather than forcing checklist prose into the grammar. Extracted `TASK_ID_BODY` so `TASK_ID` / `WIKILINK_PATTERN` / `BLOCKED_BY_BLOCK` / `ID_SHAPE_CASE_INSENSITIVE` cannot drift; `SUBTASK_ID` widened in parallel with the same decimal grammar (different capture shape, so not the same fragment).
- **Minimal refactor.** `TASK_ID_BODY` extraction is in-scope DRY for the ID lockstep; `CRITICAL_FLAG_AFTER = CRITICAL_FLAG` reuses the same pattern as a second capturing group. No unrelated cleanup.
- **`viz/src/parser.ts`.** `TASK_ID_BODY` = `[A-Z]+(?:-EPIC)?-\d+(?:\.(?:\d+[a-z]?|N))*`. Trailing `CRITICAL_FLAG_AFTER` after the suggestion glyph so `[model] [!critical]` and `[model]🧩 [!critical]` both set `critical`. Unparsed gate now requires `*` so `- [ ] follow up` is silent; `*FE-064*` / `**fe-065**` still flag. Destructure gained `criticalAfter` (group 5).
- **`viz/src/parser.test.ts`.** Fixtures for lettered + nested IDs (parse, unparsed-clean, wikilink extract, epic grouping), swapped `[!critical]` (plain, stacked models, after glyph), bare checkbox silence, and `FE-310.a` still unparsed (letter without leading digits).
- **`SPEC.md`.** New "Parser tolerances (adopter near-misses)" + "Bare checkbox bullets (excluded, not tolerated)" under §"Task-line format". Canonical Task ID convention and flag order unchanged.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Quality assertions** — for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line) — N/A: parser-only, no `App.tsx`/UI delta. Local `.flowtron/PLAN.md` already parses 753/0 unparsed; nothing new to look at on flowtron's board. Fleet-viz confirmation is post-landing.

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

- `npm --prefix viz test -- --run src/parser.test.ts` — 79/79 (12 new FE-087 fixtures).
- `npm --prefix viz test -- --run` — 354/354 across 22 files.
- `npm --prefix viz run typecheck` + `lint` — clean.
- Live `.flowtron/PLAN.md`: 753 tasks, 0 unparsed, 0 near-miss headings.
- Quality: `TASK_ID_BODY` removes the four-way ID restatement; no new exports; header comment + SPEC parser-tolerance notes updated; FE-063.2 malformed-ID tests still flag.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

  - `README.md` — no change (does not document PLAN-line grammar)
  - `AGENTS.md` — no change
  - `SPEC.md` — **updated**: §"Task-line format" gained adopter-near-miss parser tolerances + bare-checkbox exclusion
  - `docs/MIGRATION.md` — no change (points at SPEC for task-line grammar; canonical authoring unchanged)
  - `claude/AGENTS-snippet.md` — no change
  - `codex/AGENTS-snippet.md` — no change
  - `cursor/AGENTS-snippet.md` — no change
  - `docs/CONVENTIONS.md` — no change
  - `CONTRIBUTING.md` — no change
  - `SECURITY.md` — no change
  - `docs/AGENT-NEUTRALITY.md` — no change
  - `docs/PLATFORMS.md` — no change
  - `claude/CAPABILITIES.md` — no change
  - `docs/AGENT-COMPAT.md` — no change
  - `docs/EXTERNAL-AGENTS.md` — no change
  - `docs/WORKTREES.md` — no change

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed per SPEC/tasknote-selection.md §"`## Completed` archive convention" (standalone → top of `## Completed`; epic child → kept nested beneath its active parent), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Adopter PLAN.md near-misses that previously tripped the viz "failed to parse" banner now parse or stay silent: lettered subtasks (`FE-310.3a`) and nested decimals (`FE-067.2.1`) are tasks, `[!critical]` after `[model]` still flags critical, and bare checkbox bullets are not failed tasks. Canonical Task ID convention and flag order are unchanged — viz tolerances only, documented in SPEC §"Task-line format". Shared `TASK_ID_BODY` keeps the five ID regex slots in lockstep (CORE-333). 79 parser tests, 354/354 viz suite, typecheck + lint clean; live PLAN.md still 753/0 unparsed.

**Archived:** 2026-08-13
