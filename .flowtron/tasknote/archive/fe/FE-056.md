---
title: viz-version-single-source
status: completed
tags: []
created: 2026-06-06
due:
related-tasks: []
---

# FE-056 | viz-version-single-source

[← PLAN.md](../PLAN.md) · ✅ Completed

## 🎯 Goal

Eliminate the version disagreement between `viz/src/ui/constants.ts` (`VIZ_VERSION = 'v5.2.0'`) and `viz/package.json` (`"version": "0.1.0"`) by establishing `VIZ_VERSION` in constants.ts as the single documented source of truth and wiring a matching bump of `viz/package.json` into the `/ft-release` recipe.

## ✅ Acceptance

- [x] `viz/package.json` `"version"` matches `VIZ_VERSION` in constants.ts (`"5.2.0"`)
- [x] `claude/skills/ft-release/SKILL.md` bumps `viz/package.json` version alongside `VIZ_VERSION`
- [x] A comment above `VIZ_VERSION` in constants.ts documents the decision (constants.ts = runtime SOT; package.json mirrors via release recipe)
- [x] `npm --prefix viz run typecheck` and `npm --prefix viz run lint` pass clean

## 🧩 Subtasks

- [x] Bump `viz/package.json` `"version"` from `"0.1.0"` to `"5.2.0"`
- [x] Add a comment above `VIZ_VERSION` in `viz/src/ui/constants.ts` documenting it as the runtime SOT
- [x] Add `viz/package.json` version bump step to `claude/skills/ft-release/SKILL.md` (in the execution section alongside the VIZ_VERSION bump)
- [x] Run `npm --prefix viz run typecheck` and `npm --prefix viz run lint`

## 🔗 Related

- [[CORE-221]] — original version-sync step wiring (added VIZ_VERSION bump to release recipe)
- [[CORE-262]] — renamed FLOWTRON_VERSION → VIZ_VERSION in the release recipe

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Confirmed drift is live: `viz/src/ui/constants.ts` has `VIZ_VERSION = 'v5.2.0'` and `viz/package.json` has `"version": "0.1.0"`. The release recipe (ft-release/SKILL.md) bumps `VIZ_VERSION` explicitly but has no `viz/package.json` bump step. Fixing this is still the right work.

- [x] Read relevant source files

- [x] **Archive skim** — CORE-221 added `VIZ_VERSION` bump to the release recipe; CORE-262 updated the constant name from `FLOWTRON_VERSION`. Neither touched `viz/package.json`. No prior FE tasknote covers this gap.

- [x] **Drift check** — `viz/src/ui/constants.ts:39` has `VIZ_VERSION = 'v5.2.0'`; `viz/package.json:3` has `"version": "0.1.0"`. Both match the task description exactly. ft-release/SKILL.md line ~151 has the VIZ_VERSION bump step (confirmed). All paths current.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  No clarifications needed. Assumptions: (1) `VIZ_VERSION` in constants.ts is the runtime source of truth — App.tsx uses it, the release recipe already bumps it; (2) we keep constants.ts as SOT and add package.json to the release recipe (vs. Vite `define` rewrite, which is more invasive); (3) `viz/package.json` is private and never published — its version is informational/tooling only; (4) we bump it now to `"5.2.0"` to match the current `VIZ_VERSION`.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- `VIZ_VERSION` is used in App.tsx as a fallback in `projectVersions[activeProject ?? ''] ?? VIZ_VERSION` (footer display). It is the runtime constant — the natural source of truth.
- `viz/package.json` `"private": true` — never published to npm, so its `"version"` is informational.
- CORE-221 chose to wire a constants.ts bump into the release recipe; CORE-262 updated the constant name. Extending that same recipe with a package.json bump keeps both in sync with zero runtime change.
- The alternative (Vite `define` to read package.json at build) would require removing `VIZ_VERSION` from constants.ts and updating App.tsx imports — more surface area, no meaningful benefit for a private build tool.
- Discovery surfaced no significant deviation → skip 🛠️.

✅ Phase 1 Discovery complete; entering Phase 2 Execution.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — CORE-221 established the pattern of wiring a version-string bump into the release recipe; CORE-262 updated the constant name. This change extends the same recipe to include `viz/package.json` as a 5th edit. No new pattern required.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — N/A; changes are a code comment, a JSON version field, and prose updates to the release skill. No logic changed.

**Implementation Notes:**

3 files changed:
- `viz/package.json`: `"version": "0.1.0"` → `"5.2.0"` (1 line)
- `viz/src/ui/constants.ts`: added 2-line comment above `VIZ_VERSION` documenting it as runtime SOT
- `claude/skills/ft-release/SKILL.md`: added `viz/package.json` bump as edit #5 to the acceptance checklist, execution steps, verification grep note, Phase 3, §7.4 staging, and updated all "4 doc edits/pins" count references to "5 version edits" (8 lines updated across the file)

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — N/A (no logic changed)

- [x] Ran lint/type-check on changed code — `npm --prefix viz run typecheck` and `npm --prefix viz run lint` both pass clean

- [x] (frontend) Asked the user for visual confirmation — N/A (no UI change; comment and JSON version field only)

**Testing Notes:**

typecheck and lint pass clean. The `flowtron-viz@5.2.0` label in the npm output confirms package.json version is now correct.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — all 11 AI-referenced docs: no change. FE-056 touched `viz/package.json`, `viz/src/ui/constants.ts`, and `claude/skills/ft-release/SKILL.md` — none are in the AI-referenced docs set.

- [x] Closed — PLAN.md line flipped to stub form `Completed 2026-06-06.` and moved to `## Completed`; tasknote moved to `.flowtron/tasknote/archive/frontend/FE-056.md`

- [x] Recap drafted

**Final Summary:**

Established `VIZ_VERSION` in `viz/src/ui/constants.ts` as the documented single source of truth for the footer version display. Synced `viz/package.json` `"version"` from `0.1.0` to `5.2.0` to match the current value, and extended the `/ft-release` recipe with a 5th version edit so both stay in sync on every future release.

**Archived:** 2026-06-06
