---
title: archive-watch-glob-depth
status: completed
tags: []
created: 2026-07-26
due:
related-tasks: []
---

# FE-076 | archive-watch-glob-depth

[← PLAN.md](../PLAN.md) · 🟢 In progress

## 🎯 Goal

Narrow the archive watcher glob in `viz/vite.config.ts` so it matches only what `archiveCache`'s `readArchive` actually reads.

## ⚡ Notes

**Relevance:** Proceed — glob/reader mismatch confirmed live in code; single-line fix, no design tradeoffs.
**Best Practices Review:** N/A — single-line glob narrowing in existing watcher wiring; no new abstraction, no responsibility shift.
**Drift check:** Paths match. `viz/vite.config.ts` still builds `watchPaths` in `configureServer` including `join(p.archiveDir, '**/*.md')` (was line 85); `viz/src/archiveCache.ts`'s `readArchive` still reads exactly one level deep (`archiveDir/<area>/<file>.md`, confirmed at archiveCache.ts:9-14).
**Archive skim:** FE-016 (2026-05-07) originally watched `join(ARCHIVE_DIR, '*', '*.md')` depth 2 — already matched the one-level read shape. FE-028 (2026-05-2x, archive-cache) widened it to `'**/*.md'` while bumping the cache in; the widen wasn't tied to any change in what's read, so the glob outgrew the reader. This fix reverts the glob to the narrow, reader-matching form FE-016 established.
**Pattern survey:** Matches the sibling `join(p.tasknoteDir, '*.md')` glob directly above it in the same `watchPaths` array — both now express "one literal level of nesting" instead of one using an unbounded `**` cap relying on `depth`.
**Implementation:** Changed `join(p.archiveDir, '**/*.md')` → `join(p.archiveDir, '*/*.md')` in `viz/vite.config.ts` (`watchPaths` inside `configureServer`). `depth: 2` left unchanged — it's a shared chokidar option across all watched paths in the array and already sized correctly for `archive/<area>/<file>.md` (2 levels below `archiveDir`); the narrower glob now caps matches to exactly that shape instead of relying on `depth` alone to bound an open-ended `**`. No reader change needed (option B in the PLAN line, "make the reader recurse," was not required since the glob-narrowing path is simpler and matches original FE-016 intent).
**Docs touched:** no change — no AI-referenced doc describes this glob's shape.

## ✅ Recap

Narrowed the archive-directory chokidar watch glob in `viz/vite.config.ts` from `**/*.md` to `*/*.md` so the watcher only fires on the exact `archive/<area>/<file>.md` shape that `archiveCache.ts`'s `readArchive` reads — closing a leniency gap flagged by the 2026-07-26 repo audit (Finding #3, Low). One-line change; `depth: 2` unchanged (already correctly sized for the two-level path). Verified with `tsc --noEmit` and `eslint vite.config.ts` (both clean); no dedicated test exists for this watcher-wiring detail and none was added — behavior is config-level and exercised implicitly by the existing archive-cache invalidation tests.

**Archived:** 2026-07-26
