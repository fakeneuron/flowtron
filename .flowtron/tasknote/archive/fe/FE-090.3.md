---
title: framework pair
status: completed
tags: []
created: 2026-08-22
due:
related-tasks: [FE-EPIC-090, FE-090.2, CORE-457.4]
touches:
  - viz/package.json
  - viz/package-lock.json
  - viz/vite.config.ts
  - viz/src/main.tsx
  - viz/src/ui/
# Optional planning keys — omit when absent (SPEC.md §Tasknote frontmatter).
# Omitted means undeclared, not "touches nothing" / "safe with everyone".
# touches:
#   - path/or/glob
# blocked-by:
#   - TASK-ID
# parallel-safe-with:
#   - TASK-ID
# supersedes:
#   - TASK-ID
---

# FE-090.3 | framework pair

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-090]]

## 🎯 Goal

Bump viz's framework pair — React 19 + its `@types`, and Vite 8 with `@vitejs/plugin-react` 6 — to current majors with every gate green and the dev server's security posture intact.

## ✅ Acceptance

- [x] `viz/package.json` carries `react ^19`, `react-dom ^19`, `@types/react ^19`, `@types/react-dom ^19`, `vite ^8`, `@vitejs/plugin-react ^6`; lockfile regenerated
- [x] Gates green: `npm --prefix viz test`, `run typecheck`, `run lint`, `run build`, plus the `tools/update-adopters` node checks from AGENTS.md §"Validation"
- [x] [[CORE-231]]'s CSP posture holds under plugin-react 6 — `html.cspNonce` still stamps the injected React-refresh preamble, so `script-src` stays free of `'unsafe-inline'`; verified in a real browser with zero CSP violations
- [x] [[CORE-114]]'s dev-server hardening still holds under Vite 8 — `server.allowedHosts` honored, all five `/api/*` middlewares still mount and enforce the Origin guard
- [x] Tailwind 3 + PostCSS still compile under Vite 8's Rolldown/lightningcss-era pipeline (no Tailwind bump — that is [[FE-090.4]])
- [x] SSE live-update path still fires end-to-end under Vite 8 (dev-server smoke: PLAN.md / active tasknote / archive edits each produce a `change` event)
- [x] `npm --prefix viz audit --audit-level=high` introduces no new high-severity findings

## 🧩 Subtasks

- [x] Bump `react` + `react-dom` to ^19 and `@types/react` + `@types/react-dom` to ^19; run the vitest suite and typecheck, resolving React 19 fallout
- [x] Bump `vite` to ^8 and `@vitejs/plugin-react` to ^6 (a required pair — plugin-react 6 peers `vite: ^8.0.0`); confirm the Babel toolchain drops out of the lockfile
- [x] Verify the `flowtronApi()` plugin still works under Vite 8 — `configureServer`, `server.middlewares.use`, `server.httpServer` close hook, and the chokidar watcher wiring
- [x] Verify `html.cspNonce` + `DEV_CSP` under plugin-react 6's oxc-based refresh preamble; adjust the CSP only if the preamble's shape genuinely changed
- [x] Run the full gate set (test / typecheck / lint / build / node checks / audit)
- [x] Live dev-server smoke in a browser: render, zero console + CSP errors, HMR, SSE change events on all three watched surfaces

## 🔗 Related

- [[FE-EPIC-090]] — parent epic (viz major-bump wave)
- [[FE-090.2]] — predecessor pair (ESLint 10 / jsdom 30 / chokidar 5); raised the Node floor this task inherits
- [[FE-090.4]] — successor pair (Tailwind 4); this task must leave Tailwind 3 compiling
- [[CORE-231]] — established the `html.cspNonce` + `DEV_CSP` posture that plugin-react 6 must not break
- [[CORE-114]] — established `allowedHosts` + the `/api/*` Origin guard, and the precedent of bumping Vite to close dev-server CVEs
- [[CORE-324.2]] — deliberately declined the React Compiler preset; unchanged here
- [[CORE-457.4]] — deferral origin for the whole major-bump wave

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** `npm --prefix viz outdated` confirms every named package is still behind — react/react-dom 18.3.1 → 19.2.8, `@types/react` 18.3.28 → 19.2.18, `@types/react-dom` 18.3.7 → 19.2.4, vite 6.4.3 → 8.2.2, `@vitejs/plugin-react` 4.7.0 → 6.1.0. [[CORE-457.4]] deferred these as out-of-scope majors and [[FE-EPIC-090]] filed them as this child; [[FE-090.2]] cleared the toolchain pair and left this set explicitly untouched. Still the right work, and no sibling has taken it.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

  This is a dependency bump, not a module-boundary change: the responsibility split established by [[CORE-431.2]] (`watchSet.ts` path partition → `flowtronWatch.ts` chokidar options + handler → `vite.config.ts` lifecycle) is untouched, and dependency direction stays one-way. Two abstractions are load-bearing against the bump rather than refactored by it — `flowtronApi()`'s use of the `configureServer` plugin hook, and the module-level `DEV_CSP` / `DEV_CSP_NONCE` consts that [[CORE-231]] introduced. No in-scope refactor is anticipated; if Vite 8 changes a plugin-hook signature the edit is a call-site adaptation, not a restructure. Deferred: the unclaimed majors (`@types/node` 26, `globals` 17, `js-yaml` 5, `typescript` 7, `@testing-library/jest-dom` 7) that neither this child nor the epic line names, and [[FE-090.4]]'s Tailwind 4.

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; log relevant findings in Discovery Notes before re-interpreting the task

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  Two asked (both potentially scope-changing), both answered with the recommended option — so the plan is the as-filed one:
  1. **Vite target** → *Vite 8 + plugin-react 6*. Takes the Rolldown/oxc pipeline and drops the `@babel/core` toolchain, rather than the two conservative alternatives (Vite 8 + plugin-react 5.2, or Vite 7 + plugin-react 5.2).
  2. **React 19 scope** → *Bump React + types only*. No opportunistic ref-as-prop / Context-shorthand modernization.

  Standing assumptions: no `.1` Discovery child and no `## 🌳 Fan-out` on [[FE-EPIC-090]] (Discovery was supplied by audit-repo), so `blocked-by:` / `parallel-safe-with:` are omitted as undeclared. `vitest` floats within its existing `^4` range. Unclaimed majors and Tailwind stay put. [[CORE-324.2]]'s React Compiler decline is not revisited.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

**Version evidence (2026-08-22, `npm --prefix viz outdated`).** In scope: `react` / `react-dom` 18.3.1 → 19.2.8, `@types/react` 18.3.28 → 19.2.18, `@types/react-dom` 18.3.7 → 19.2.4, `vite` 6.4.3 → 8.2.2, `@vitejs/plugin-react` 4.7.0 → 6.1.0. Out of scope: `tailwindcss` 3.4.19 → 4.3.3 ([[FE-090.4]]) and the unclaimed majors `@types/node` 26, `globals` 17, `js-yaml` 5, `typescript` 7, `@testing-library/jest-dom` 7.

**The pair is a genuine pair, not a convenience grouping.** `@vitejs/plugin-react@6.1.0` peers `vite: ^8.0.0` — a hard requirement, so plugin-react 6 cannot land without Vite 8. React 19 is separable in principle, but shares the same test surface, so splitting it would mean running the same suite twice.

**Vite 8 is a pipeline swap, not a version bump.** Its direct deps are `rolldown ~1.2.4` + `lightningcss` — Rollup and esbuild are gone. `@vitejs/plugin-react@6` follows suit: zero Babel dependencies (only `@rolldown/pluginutils`), with `oxc-transform-react`, `@rolldown/plugin-babel`, and `babel-plugin-react-compiler` as *optional* peers. plugin-react 5.2 was the conservative alternative — it also peers `^8.0.0` but retains the six-package Babel chain. Operator chose 6.

**Compatibility floor is already satisfied.** `vite@8` and `@vitejs/plugin-react@6` both declare `engines.node: ^20.19.0 || >=22.12.0`; viz's `engines.node` is `^22.22.2 || ^24.15.0 || >=26.0.0` (raised by [[FE-090.2]] for jsdom 30) and CI runs Node 24. No Node-floor movement this time. `vitest@4.1.6` already peers `vite: ^6 || ^7 || ^8`, so the test runner needs no bump — and since `package.json` pins `^4.1.6`, a lockfile regen floats it to 4.1.11 within range.

**React 19 consumers already declare support.** `@testing-library/react@16.3.2` peers `react: ^18.0.0 || ^19.0.0` (and the same for `@types/react`), and `react-markdown@10.1.0` peers `react: >=18`. Nothing in `dependencies` blocks 19.

**React 19 breaking-change sweep over `viz/src` — clean.** Grepped for every removal/change that bites in practice: no argument-less `useRef()` (React 19 requires an initial value), no global `JSX.` namespace references (`@types/react` 19 moves it to `React.JSX`), no `propTypes` / `defaultProps` on function components, no `forwardRef` (0 sites — so the ref-as-prop migration is vacuous here), no legacy `ReactDOM.render` (`main.tsx` already uses `createRoot`), and no string refs. `React.FC` appears at 26 sites and remains valid — its implicit-`children` removal already landed in `@types/react` 18, which viz is on. The one runtime change to watch is React 19's error handling: render errors are no longer re-thrown to the host, they route through `onUncaughtError` / `onCaughtError`, which is the plausible way `ui/ErrorBoundary.test.tsx` could move.

**Archive skim — three prior decisions the bump must not regress:**
- [[CORE-231]] — the sharpest constraint. Vite's dev server injects an *inline* React-refresh preamble via `@vitejs/plugin-react`, and CORE-231 chose a static `html.cspNonce` (`'flowtron-dev'`) over `'unsafe-inline'` so `DEV_CSP`'s `script-src` stays honestly inline-free. plugin-react 6 generates that preamble through a different (oxc/Rolldown) path, so **nonce stamping is the single highest-risk item in this task** and needs a real-browser check, not a unit test.
- [[CORE-114]] — pinned `server.allowedHosts` to loopback and added the `/api/*` Origin guard, and set the precedent that bumping Vite is how viz closes dev-server CVEs. All five middlewares mount inside `configureServer`, so a Vite 8 plugin-hook change would surface as a total API outage, not a subtle one.
- [[CORE-222]] / [[CORE-431.2]] / [[FE-090.2]] — the hot-set polling requirement, the native archive watch, and the chokidar-5 literal-path reach. Those live in `flowtronWatch.ts` / `watchSet.ts`, which this task does not edit; the SSE smoke test exists to prove Vite 8's server lifecycle still drives them.
- [[FE-026]] — cited a "~45 kB gzipped React 18 + ReactDOM floor" while reframing a bundle-size target. React 19's baseline differs, but no test or config encodes a size budget, so nothing enforced moves. Not a falsified factual claim (dependency evolution — an excluded case per SPEC §"Tasknote frontmatter"), so no ⚠️ superseded pointer is planned.
- [[CORE-324.2]] — added `eslint-plugin-react-hooks@7.1.1` with only `rules-of-hooks` + `exhaustive-deps`, deliberately declining v7's React Compiler preset. React 19 does not change that calculus; the config comment stays accurate.

**Tailwind interaction (new risk, no prior note).** Vite 8 pulls `lightningcss`, while viz still runs Tailwind 3 through `postcss` + `autoprefixer`. Vite 8 keeps `postcss ^8.5.26` as a direct dependency so the PostCSS path should survive, but Tailwind 3 under Vite 8 has no precedent in this repo and is an explicit Acceptance line. [[FE-090.4]] owns the Tailwind 4 migration; this task must simply not strand it.

**Baseline captured before any edit** (Node 26.7.0): `npm --prefix viz test` → 22 files / 391 tests passed; `run typecheck` clean; `run lint` clean. Any post-bump delta is attributable to this task.

**Drift.** PLAN.md's `FE-090.3` line reads "React 19 + types, Vite latest + plugin-react" — all five packages are still behind and the line still describes the work; "Vite latest" resolves to 8.2.2 (`npm view vite dist-tags`). The parent [[FE-EPIC-090]] line lists the same staged pairs. No SPEC contract is touched (viz is the visualizer, not the workflow contract). Description is 8 words, well under the 70-word cap. Docs mention the stack only generically (`AGENTS.md:37` and `README.md:270` both say "Vite/React" with no version), so no version-pinned prose is at risk.

**Known unfiled follow-up (not this task's scope).** [[FE-090.2]] surfaced a pre-existing bug — `vite.config.ts:79` passes `projects.values()`, a one-shot `Map` iterator, into `createOnWatchEvent`, which `archiveCache.invalidate()` exhausts before `projectForPath()` reads it — and recommended filing it as an [[FE-EPIC-090]] child. It is still unfiled in PLAN.md. Flagging it here so it is not lost; it remains out of scope for the framework bump.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

  No new shape. The [[CORE-431.2]] module split (`watchSet.ts` path partition → `flowtronWatch.ts` options + handler → `vite.config.ts` lifecycle) and the [[CORE-231]] `DEV_CSP` / `DEV_CSP_NONCE` module-level consts were both carried through untouched — the bump validated them rather than reshaping them. The one convention *extended* is the repo's explain-why comment style, applied to the new `allowImportingTsExtensions` tsconfig option so the asymmetry it creates (config-graph modules carry `.ts`, UI modules do not) is documented at the point of decision rather than inferred.

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

  One change beyond the version bump: explicit `.ts` extensions across `vite.config.ts`'s module graph (8 files) plus `allowImportingTsExtensions` in tsconfig. Not required by a failing gate — it clears a *new* 25-line Vite 8 forward-compat warning that this task would otherwise have introduced on every `test` / `build` / `dev` run. Surfaced to the operator with the trial diff and measured cost; operator chose to keep it. Deferred, unchanged: the unclaimed majors (`@types/node` 26, `globals` 17, `js-yaml` 5, `typescript` 7, `@testing-library/jest-dom` 7), Tailwind 4 ([[FE-090.4]]), and [[FE-090.2]]'s still-unfiled `Map.values()` one-shot-iterator bug.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

  N/A — no behavior changed. The bump is version-only plus import-specifier form; the existing 391 tests are the regression net, and the load-bearing checks (CSP nonce, Origin/Host guards, SSE reach) are integration surfaces verified live in Phase 3 rather than unit-testable.

**Implementation Notes:**

**Dep bumps** (`viz/package.json` + lockfile): `react` / `react-dom` 18.3.1 → 19.2.8, `@types/react` 18.3.28 → 19.2.18, `@types/react-dom` 18.3.7 → 19.2.4, `vite` 6.4.3 → 8.2.2, `@vitejs/plugin-react` 4.7.0 → 6.1.0.

**React 19 was a pure version bump — zero source changes.** The Phase 1 breaking-change sweep predicted this and it held: typecheck clean and 391/391 tests green immediately after the bump, with no edit to any `src/` file. The 26 `React.FC` sites, `main.tsx`'s `createRoot`, and `ui/ErrorBoundary.tsx` all carried over untouched; React 19's `onUncaughtError` reroute never surfaced because the boundary's tests assert on rendered fallback, not on a re-thrown host error.

**Vite 8 dropped 13 packages and added 6.** The Babel toolchain (`@babel/core`, the two JSX transform plugins, `@types/babel__core`, `react-refresh`) and `esbuild` are gone — `@vitejs/plugin-react@6` transforms via Vite 8's built-in oxc, and its three Babel-adjacent peers are all optional. Confirmed indirectly by npm's install-scripts warning, which listed `esbuild@0.25.12` before the bump and only `fsevents` after. The `flowtronApi()` plugin needed **no** call-site changes: `configureServer`, `server.middlewares.use`, `server.httpServer` close-hook, and `html.cspNonce` all carry identical signatures under Vite 8.

**The one real change: config-loader forward-compat.** Vite 8 began warning that `configLoader: 'native'` (Node type-stripping) is the planned default and cannot load a config whose module graph uses extensionless relative imports — 25 lines on every `test` / `build` / `dev` invocation. `configLoader` is a CLI/`resolveConfig` option, not a config-file field, so it cannot be pinned to `'bundle'` from `vite.config.ts`; the alternatives were an env-var suppression (silences the signal without acting on it) or the extensions themselves. Added `.ts` to relative imports in `vite.config.ts`, `src/archiveCache.ts`, `src/devApi.ts`, `src/flowtronWatch.ts`, `src/originGuard.ts`, `src/tasknote-parse.ts`, `src/watchSet.ts`, `src/workspace.ts`, and set `allowImportingTsExtensions: true` (legal because `noEmit` is already set). Scope is deliberately the config graph only — `src/ui/*` is bundled, never loaded natively, so its bare specifiers stay. The comment in `tsconfig.json` records that asymmetry.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Quality assertions** — for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

  No duplication and no dead code — the diff is version fields, import specifiers, and one commented compiler option. Public surface is unchanged: no export added or removed. Complexity is unchanged. Code-facing docs: the `allowImportingTsExtensions` comment is new and explains the non-obvious asymmetry; the [[CORE-231]] CSP comment block in `vite.config.ts` was re-read against observed behavior and remains accurate word-for-word under plugin-react 6 (it describes an injected inline preamble carrying a nonce, which is exactly what Vite 8 still emits).

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

  Screenshot of the running dev server surfaced to the operator: header, project selector, priority sections, and the `FE-EPIC-090` epic row at "1/4 done" all render correctly with Tailwind 3 styling intact under React 19 + Vite 8.

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

**Gate set** (AGENTS.md §"Validation"), all green on Node 26.7.0 — identical to the pre-bump baseline of 391 tests:
- `npm --prefix viz test` — 22 files / **391 tests** passed (baseline 391; no test added or removed)
- `npm --prefix viz run typecheck` — clean
- `npm --prefix viz run lint` — clean
- `npm --prefix viz run build` — 306 modules, built in 738ms, **warning-free**
- `node --test tools/update-adopters.test.mjs` — 37/37 pass; both `node --check`s clean
- `npm --prefix viz audit --audit-level=high` and `--omit=dev` — 0 vulnerabilities each

**Live dev-server verification** (the decisive checks — the risks in this task are integration surfaces no unit test covers):

| # | Check | Expected | Observed |
|---|---|---|---|
| 1 | `Content-Security-Policy` response header | `script-src 'self' 'nonce-flowtron-dev'`, no `'unsafe-inline'` | ✅ exact match |
| 2 | plugin-react 6's injected refresh preamble | inline `<script>` carrying `nonce="flowtron-dev"` | ✅ stamped ([[CORE-231]] holds) |
| 3 | `/@vite/client`, `/theme-init.js`, `/src/main.tsx` tags | all nonce-stamped | ✅ all three |
| 4 | `/api/projects`, `/api/plan`, `/api/active`, `/api/archive` | 200 | ✅ all 200 |
| 5 | `Origin: http://evil.com` on `/api/projects` | 403 ([[CORE-114]]) | ✅ 403 |
| 6 | `Host: evil.com` on `/api/projects` | 403 (`allowedHosts`) | ✅ 403 |
| 7 | SSE `/api/events` — touch `PLAN.md` | `change` event | ✅ fired |
| 8 | SSE — touch active tasknote | `change` event | ✅ fired |
| 9 | SSE — touch archive file | `change` event | ✅ fired |
| 10 | Browser render + console | page renders, 0 errors, 0 CSP violations | ✅ 0 errors, 0 warnings |

Check 2 was the highest-risk item identified in Discovery — plugin-react 6 generates the preamble through oxc rather than Babel, so nonce stamping could plausibly have regressed silently into a CSP violation. It did not.

**Tailwind 3 under Vite 8** compiles cleanly: `dist/assets/index-*.css` at 29.91 kB (5.88 kB gzip), and the browser render confirms styling is applied. [[FE-090.4]] is unblocked.

**Pre-existing bug re-confirmed, still not fixed here.** Every SSE `change` event arrived as `data: {}` — unattributed — exactly as [[FE-090.2]] documented (`vite.config.ts` passes `projects.values()`, a one-shot `Map` iterator, into `createOnWatchEvent`; `archiveCache.invalidate()` exhausts it before `projectForPath()` reads it). Behavior is identical to the pre-bump baseline, so this task neither introduced nor worsened it. It remains **unfiled** in PLAN.md.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

  Swept the doc set for two surfaces this task could drift: stack-version claims and the dev-server CSP description. Neither moved — the docs describe the stack generically ("Vite/React", no version) and the CSP prose was re-verified against observed Vite 8 behavior. No Node-floor movement this time ([[FE-090.2]] already raised it; `vite@8` and `plugin-react@6` both fit under the existing range).

  - `README.md` — no change (`:270` says "Vite/React visualizer", version-free)
  - `AGENTS.md` — no change (`:37` says "Vite/React/TypeScript visualizer"; §"Validation" names commands, not versions)
  - `SPEC.md` — no change
  - `docs/MIGRATION.md` — no change
  - `claude/AGENTS-snippet.md` — no change
  - `codex/AGENTS-snippet.md` — no change
  - `cursor/AGENTS-snippet.md` — no change
  - `grok/AGENTS-snippet.md` — no change
  - `docs/CONVENTIONS.md` — no change (`:54` states Node 24 + the six validation commands; both still exact)
  - `CONTRIBUTING.md` — no change
  - `SECURITY.md` — no change, **and actively re-verified**: `:163` (spoofed `Host` rejected) confirmed by test 6, `:185-186` ("`script-src` with no `'unsafe-inline'`", "Vite's injected dev scripts carry a static nonce", `style-src` keeps `'unsafe-inline'` for Vite/Tailwind) confirmed verbatim by tests 1-3
  - `docs/AGENT-NEUTRALITY.md` — no change
  - `docs/PLATFORMS.md` — no change
  - `claude/CAPABILITIES.md` — no change (last-verified stamp tracks flowtron version bumps, not viz devDeps)
  - `docs/AGENT-COMPAT.md` — no change
  - `docs/EXTERNAL-AGENTS.md` — no change
  - `docs/WORKTREES.md` — no change

  No ⚠️ superseded-claim pointer written. The nearest candidate, [[FE-026]]'s "~50 KB sits below the React 18 + ReactDOM floor (~45 kB gzipped alone)", was accurate when written; React 19 shifting that floor is dependency evolution, not a falsified factual claim — an excluded case per SPEC §"Tasknote frontmatter". No test or config encodes a size budget, so nothing enforced moved.

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed per SPEC/tasknote-selection.md §"`## Completed` archive convention" (standalone → top of `## Completed`; epic child → kept nested beneath its active parent), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Bumped viz's framework pair — React 18.3.1 → 19.2.8 (with `@types/react` / `@types/react-dom` → 19) and Vite 6.4.3 → 8.2.2 with `@vitejs/plugin-react` 4.7.0 → 6.1.0 — with every gate green and no behavior change.

React 19 was a pure version bump: **zero source edits**, typecheck and all 391 tests green on the first run. The Phase 1 sweep is why — grepping for the patterns that actually bite (argument-less `useRef()`, global `JSX.` namespace, `propTypes`/`defaultProps`, `forwardRef`, legacy `ReactDOM.render`) found none, and the 26 `React.FC` sites were never at risk since their implicit-`children` removal landed back in `@types/react` 18.

Vite 8 is a pipeline swap rather than a version bump — Rolldown and oxc replace Rollup and esbuild — and it removed 13 packages while adding 6, taking the entire `@babel/core` toolchain with it, since plugin-react 6 transforms through Vite's built-in oxc. The `flowtronApi()` plugin needed no call-site changes at all.

The one substantive addition was operator-approved: Vite 8 began emitting a 25-line forward-compat warning on every `test` / `build` / `dev` run, because the future default `configLoader: 'native'` cannot load a config graph with extensionless relative imports. Since `configLoader` is a CLI option and not settable from the config file, the choices were to suppress the signal or act on it. I added `.ts` extensions across the config's 8-file module graph plus `allowImportingTsExtensions`, deliberately leaving `src/ui/*` alone — it is bundled, never loaded natively — and documented that asymmetry in `tsconfig.json`.

**Changed:** 11 files — `viz/package.json` + lockfile, `viz/vite.config.ts`, six `viz/src/*.ts` modules, `viz/tsconfig.json`; ~50 non-lockfile lines, all version fields, import specifiers, or comments.

**Verified:** 391 tests (unchanged from baseline), typecheck, lint, a now warning-free 306-module build, 37 tools tests, and `audit` clean on both full and `--omit=dev`. Decisively, ten live dev-server checks covered the three integration surfaces no unit test reaches: [[CORE-231]]'s CSP nonce (still stamped on plugin-react 6's oxc-generated refresh preamble — the highest-risk item in the task), [[CORE-114]]'s Origin and `allowedHosts` guards (both still 403), and the SSE watcher firing on all three watched surfaces. Browser render clean at 0 console errors and 0 CSP violations.

**Refactors:** the config-graph extension fix (operator-approved, scoped and documented). Deferred: the unclaimed majors this epic never named, Tailwind 4 ([[FE-090.4]], confirmed unblocked — Tailwind 3 still compiles to 29.91 kB under Vite 8), and [[FE-090.2]]'s `Map.values()` one-shot-iterator bug, re-confirmed present and identical to baseline but **still unfiled in PLAN.md**.

**Docs:** no updates needed across all 17 AI-referenced docs. `SECURITY.md`'s three dev-server security claims were not merely skimmed but re-verified against observed Vite 8 behavior and hold verbatim.

**Maintainability:** viz now sits on current React and current Vite with a materially smaller toolchain — the Babel transform chain is gone — and the config loader is pre-migrated to the form the next Vite major will require, so that break is already paid for rather than waiting.

**Archived:** 2026-08-22
