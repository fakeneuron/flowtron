---
title: tasknote-parse-node-only-reclass
status: completed
tags: []
created: 2026-09-12
due:
related-tasks: []
touches:
  - viz/README.md
  - viz/eslint.config.js
---

# FE-109 | tasknote-parse-node-only-reclass

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[]]

## 🎯 Goal

Reclassify `viz/src/tasknote-parse.ts` as a Node-only tier module in `viz/README.md` and lock it into the `src/ui/**` restricted-imports eslint group, so the browser tier can never accidentally import it.

## ✅ Acceptance

- [x] `viz/README.md`'s tier table lists `tasknote-parse` under **Node-only dev API**, not **Shared pure** — `grep -n "tasknote-parse" viz/README.md`
- [x] `viz/README.md`'s layout sketch shows `tasknote-parse.ts` under the Node-only block, not the Shared-pure block — `grep -n "tasknote-parse" viz/README.md`
- [x] `viz/README.md`'s dependency-direction ASCII block no longer cites `tasknote-parse` as a "Node-only → shared pure" example — `grep -n "tasknote-parse" viz/README.md`
- [x] `viz/README.md`'s hard-rule Node-only module list (mirrors the eslint group 1:1) includes `tasknote-parse` — `grep -n "tasknote-parse" viz/README.md`
- [x] `viz/eslint.config.js`'s `src/ui/**` `no-restricted-imports` group includes `tasknote-parse` / `tasknote-parse.ts` — `grep -n "tasknote-parse" viz/eslint.config.js`
- [x] No behavior change — full suite still green — `npm --prefix viz test`, `npm --prefix viz run typecheck`, `npm --prefix viz run lint`

## 🧩 Subtasks

- [x] Move `tasknote-parse` from the Shared-pure row to the Node-only dev API row in the tier table (`viz/README.md`)
- [x] Move `tasknote-parse.ts` from the Shared-pure block to the Node-only block in the layout sketch (`viz/README.md`)
- [x] Drop `tasknote-parse` from the "Node-only → shared pure" example list in the dependency-direction block (`viz/README.md`)
- [x] Add `tasknote-parse` to the hard-rule Node-only module name list so it stays 1:1 with the eslint group (`viz/README.md`)
- [x] Add `tasknote-parse` / `tasknote-parse.ts` to the `src/ui/**` `no-restricted-imports` `group` array, following the existing pattern/message shape (`viz/eslint.config.js`)
- [x] Run test / typecheck / lint

## 🔗 Related

- (none — standalone audit follow-up, no parent epic)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** PLAN.md line and current `viz/README.md` / `viz/eslint.config.js` state match; the reclassification is still live and not yet done.

- [x] Read relevant source files — `viz/README.md` (tier table, layout sketch, dependency-direction block, hard-rule list), `viz/eslint.config.js` (restricted-imports group), `viz/src/tasknote-parse.ts` (confirmed it has no direct `node:` import — it only wraps `gray-matter` + `js-yaml`), and its importers (`grep -rl tasknote-parse viz/src`: only `tasknoteRead.ts` — a Node-only module — and its own test `tasknote.test.ts`).

- [x] **Best Practices Review** — the three-tier split's stated test for "Shared pure" is literally "no `node:` imports," which `tasknote-parse.ts` technically satisfies. But its only real consumer is `tasknoteRead.ts` (Node-only) plus its own test; nothing in `src/ui/` imports it and nothing makes it reach the browser bundle. Classifying it as "Shared pure" (implying browser-bundle usable) overstates its actual reach and leaves it un-guarded by the `src/ui/**` eslint gate that every other Node-only-only-consumed module gets. Reclassifying it as Node-only tightens the guardrail with no behavior change — it's a documentation + lint-config move, not a refactor of the file itself.

- [x] **Archive skim** — grep across `.flowtron/tasknote/archive/fe/*.md` for "tasknote-parse" returned 12 hits (over the ~3 fair-line), so delegated to a probe (see Discovery Notes) rather than reading all 12 inline.

- [x] **Drift check** — `tasknote-parse.ts` still exists at the cited path with the cited shape; `viz/README.md`'s table (line 40), sketch (line 86), and dependency-direction block (line 59) still list it as Shared pure exactly as the audit finding describes; `viz/eslint.config.js`'s restricted-imports group (lines 44-67) does not yet include it. No drift between the PLAN.md line and current code/docs.

- [x] Asked clarifying questions OR logged "No clarifications needed" — **No clarifications needed (--fast, implied by `[unattended]` row marker).** Explicit assumption: the audit's "Node-only" recommendation is judged on actual-consumer reach (only ever imported by Node-only code), not the tier table's literal "no `node:` import" test — this is a real architectural tightening, not a factual correction to a wrong audit claim.

- [x] Subtasks above populated with concrete, ordered steps; YAML `touches:` declared above.

**Discovery Notes:**

Probe (archive skim, 12-hit grep on "tasknote-parse" across `.flowtron/tasknote/archive/fe/`) found:

- `tasknote-parse.ts`'s origin (FE-026) called it "server-only" in prose/intent but it was never implemented with a `node:` import — it only ever wrapped `gray-matter`. FE-071 (disabling the JS frontmatter engine) and FE-085/FE-106.3 (js-yaml CVE hardening) touched its *content* but never revisited its tier.
- FE-104 created `tasknoteRead.ts` (Node-only, needs `node:fs/promises` + `node:path`) and had it import `tasknote-parse` — consistent with the existing "Node-only → shared pure" edge. No archived tasknote ever proposed moving `tasknote-parse` to Node-only before now, and none flagged a reason it must stay Shared-pure beyond the literal `node:`-import test.
- The `no-restricted-imports` group in `viz/eslint.config.js` is an exact 1:1 match with every module the README's layout sketch labels "Node-only" — a blanket, tier-based rule, not a subset filtered by current import risk. This confirms `tasknote-parse` becoming Node-only means it must join that group, same as every other Node-only module.

Net: no prior decision blocks this reclassification; it closes a real gap (FE-109's own audit-surfaced premise holds) rather than correcting a mistaken audit claim.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — reused the exact list/table/sketch/diagram shapes already in `viz/README.md`, and the exact `group` pattern + `message` shape already used for the other 9 Node-only modules in `viz/eslint.config.js`
- [x] **Minimal refactor gate** — N/A, doc + lint-config only, no code behavior change
- [x] Implemented the minimal solution
- [x] Updated/added tests for non-trivial behavior — N/A, no runtime behavior changed (nothing under `src/ui/` currently imports `tasknote-parse`, so the new lint rule adds a guardrail with no existing violation to fix)

**Implementation Notes:**

Four `viz/README.md` edits: tier table row (moved `tasknote-parse` example from Shared-pure to Node-only dev API), hard-rule module list (added `tasknote-parse`), dependency-direction block (dropped `tasknote-parse` from the "Node-only → shared pure" example), layout sketch (moved the `tasknote-parse.ts` line from the Shared-pure block to the Node-only block). One `viz/eslint.config.js` edit: added `'**/tasknote-parse'` / `'**/tasknote-parse.ts'` to the `src/ui/**` `no-restricted-imports` group, alongside `tasknoteRead`.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code
- [x] Ran lint/type-check on changed code
- [x] **Verification receipt** — recorded below; no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation in the changed lines
- [x] (frontend) N/A — docs + lint config only, no UI-rendering change

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**

```text
npm --prefix viz test        → 0  (536 tests, 28 files, all passed)
npm --prefix viz run typecheck → 0
npm --prefix viz run lint    → 0
grep -n "tasknote-parse" viz/README.md viz/eslint.config.js → 0 (4 README hits: table row, hard-rule list, dependency-direction block absence confirmed by omission, layout sketch; 2 eslint.config.js hits: group patterns)
```

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — no change to any of the 18 entries in `.flowtron/tasknote/README.md` §"AI-referenced docs"; this task only touches `viz/README.md` and `viz/eslint.config.js`, neither of which is on that list (it covers flowtron-self contract docs, not `viz/`'s internal module-tier docs)

- [x] Closed — every `## ✅ Acceptance` criterion ticked; YAML `status:` flipped to `completed`; PLAN.md line flipped to stub form; tasknote moved to `.flowtron/tasknote/archive/fe/`

- [x] **Evidence-based recap** drafted — see Final Summary below

**Final Summary:**

Reclassified `viz/src/tasknote-parse.ts` from "Shared pure" to "Node-only dev API" in the three-tier architecture docs and lint config, since its only real consumers are Node-only code (`tasknoteRead.ts`) and its own test — nothing in `src/ui/` ever imported it, but it also wasn't guarded against doing so.

- **Changed:** `viz/README.md` (4 edits: tier table row, hard-rule module list, dependency-direction ASCII block, layout sketch — net -1/+3 lines), `viz/eslint.config.js` (+2 lines, `no-restricted-imports` group).
- **Verification:** `npm --prefix viz test` → 0 (536/536 passed); `npm --prefix viz run typecheck` → 0; `npm --prefix viz run lint` → 0; `grep -n "tasknote-parse" viz/README.md viz/eslint.config.js` confirms all 4 doc spots + the eslint group are in sync.
- **Refactors:** none — pure doc + lint-config reclassification, no code behavior change.
- **Documentation:** `viz/README.md`'s tier table, layout sketch, dependency-direction block, and hard-rule module list are now internally consistent and 1:1 with the eslint group, per the existing convention.
- **`touches:` reconciliation:** declared `viz/README.md` + `viz/eslint.config.js`; `git diff --name-only` matches exactly (plus this tasknote + PLAN.md, excluded by convention).
- **Maintainability effect:** closes the guardrail gap the audit flagged — `src/ui/**` can no longer accidentally import `tasknote-parse.ts` and pull `gray-matter`/`js-yaml` into the browser bundle.

**Archived:** 2026-09-12
