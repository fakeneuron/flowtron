---
title: theme-key-namespace
status: completed
tags: []
created: 2026-09-12
due:
related-tasks: []
touches:
  - viz/src/ui/theme.ts
  - viz/public/theme-init.js
  - viz/src/ui/theme.test.ts
---

# FE-116 | theme-key-namespace

[← PLAN.md](../PLAN.md) · ✅ Completed

## 🎯 Goal

Rename the theme localStorage key from bare `theme` to `flowtron-viz-theme` with a one-shot legacy-key fallback so existing user preferences survive the rename.

## ✅ Acceptance

- [x] `viz/src/ui/theme.ts` reads/writes `flowtron-viz-theme`, falling back to the legacy `theme` key on read when the new key is absent — covered by `theme.test.ts` (superseded the planned `judgment` annotation once a test file was added)
- [x] `viz/public/theme-init.js` mirrors the same key + fallback so the pre-render dark-class flash-avoidance still matches `theme.ts`'s resolution — `judgment` (code inspection; plain script, no test harness)
- [x] `npm --prefix viz run typecheck` passes — `npm --prefix viz run typecheck` → exit 0
- [x] `npm --prefix viz run lint` passes — `npm --prefix viz run lint` → exit 0
- [x] `npm --prefix viz test` passes — `npm --prefix viz test -- --run` → exit 0 (550/550)

## 🧩 Subtasks

- [ ] Update `viz/src/ui/theme.ts`: rename `STORAGE_KEY` to `flowtron-viz-theme`; in `readPreference`, fall back to reading the legacy `theme` key when the new key has no value
- [ ] Update `viz/public/theme-init.js`: read `flowtron-viz-theme` first, falling back to legacy `theme`, keeping identical dark-resolution logic to `theme.ts`
- [ ] Run typecheck/lint/test on changed files

## 🔗 Related

(none)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** PLAN.md line names exact files, key names, and behavior (rename + legacy fallback); both files exist as described and match the description.

- [x] Read relevant source files — read `viz/src/ui/theme.ts` and `viz/public/theme-init.js` directly (small, self-contained files; no probe needed for source reading).

- [x] **Best Practices Review** — single localStorage key constant in each file; no existing abstraction to extend for "storage key naming" — this is a one-off rename. No module-boundary or dependency-direction concerns; `ThemeToggle.tsx` only calls `readPreference`/`writePreference` and is unaffected by the key rename.

- [x] **Archive skim** — dispatched to a probe (grep for `theme.ts`/`theme-init.js` returned 9 hits across FE-009/014/018/019/025/033.1/033.2/090.3/090.4). Findings:
  1. `theme-init.js` must stay an external, nonce-stamped script — its header comment ties this to CORE-231's CSP (`script-src 'self' 'nonce-flowtron-dev'`, no `'unsafe-inline'`); FE-090.3/090.4 verified it carries the nonce. The key rename must not turn it inline.
  2. `theme-init.js` and `theme.ts`'s `readPreference()` are two independent readers of the same key that must stay logically in sync (both do `stored === 'dark' || (stored !== 'light' && matchMedia(...))`); both must get the rename + legacy fallback together or a flash/strip mismatch reappears.
  3. FE-025 dropped the old 3-state toggle — `ThemePreference` is `'light' | 'dark'` only, no persisted `'system'` value, so the legacy-key fallback only needs to consider `'light'`/`'dark'`.
  4. No tasknote in archive/fe/ documents a prior rename/migration of this key; `ThemeToggle.tsx` is `theme.ts`'s only consumer (FE-018) and doesn't read the storage key directly.

- [x] **Drift check** — PLAN.md's description matches current code exactly (bare `theme` key in both files, functions named as described); no SPEC contract or PLAN.md divergence. No drift.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  No clarifications needed (--fast). Assumptions: (1) the legacy-key fallback is read-only (no explicit rewrite-on-load) — a subsequent `writePreference` call naturally lands on the new key, so the fallback is genuinely one-shot per SPEC's phrasing; (2) `theme-init.js` keeps its external-file + nonce constraint, only the key name and fallback logic change; (3) fallback only checks `'light'`/`'dark'` legacy values, consistent with FE-025's 2-state model.

- [x] Subtasks above populated with concrete, ordered steps, and YAML `touches:` declared with the paths this task expects to edit

**Discovery Notes:**

- Current state: `viz/src/ui/theme.ts` uses `const STORAGE_KEY = 'theme'` in both `readPreference` (read) and `writePreference` (write). `viz/public/theme-init.js` independently hardcodes the literal `'theme'` key in its own inline read (it's a separate pre-render script, not an import of `theme.ts` — per its own header comment, it's served from `/public` specifically so it can run without a CSP inline-script exception, so it cannot import the TS module).
- Legacy-fallback shape: on read, check `flowtron-viz-theme` first; if absent/invalid, fall back to the legacy `theme` key's value (still validated against `'light'`/`'dark'`) so an existing user's stored preference is honored on first load post-rename. Writes always go to the new key only (one-shot migration via read-time fallback, not an explicit rewrite-on-read, per the "[unattended]" scope — keep this minimal, no migration-writeback needed since a subsequent write naturally lands on the new key).
- `[unattended]` marker present in the PLAN.md row and no `--fast`/`--unattended` flag was passed this invocation, so per Step 1, `fast-mode = true` is implied.
  ⚡ --fast implied by the `[unattended]` row marker — same suppressions as `--fast`; the `--unattended` posture is not implied.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — followed the existing single-constant-key shape already in `theme.ts`/`theme-init.js` (added a second `LEGACY_STORAGE_KEY` constant alongside it); no new abstraction needed for a one-off rename.

- [x] **Minimal refactor gate** — no refactor beyond the rename + fallback; `ThemeToggle.tsx` and `applyResolved`/`writePreference` were left untouched since they don't reference the key name directly.

- [x] Implemented the minimal solution — `theme.ts`: renamed `STORAGE_KEY` to `'flowtron-viz-theme'`, added `LEGACY_STORAGE_KEY = 'theme'`, and `readPreference` now does `localStorage.getItem(STORAGE_KEY) ?? localStorage.getItem(LEGACY_STORAGE_KEY)`. `theme-init.js`: mirrored the same precedence with `localStorage.getItem('flowtron-viz-theme') || localStorage.getItem('theme')`, keeping the rest of its dark-resolution logic identical (per the probe's finding that the two files must stay in sync). `writePreference` is unchanged — it only ever writes the new key, so a legacy user's preference migrates naturally to the new key on their first toggle.

- [x] Updated/added tests for non-trivial behavior — added `viz/src/ui/theme.test.ts` (4 cases: reads new key, falls back to legacy key, prefers new key when both set, write only touches the new key), following the existing `visibilityPrefs.test.ts` localStorage-module test pattern. `theme-init.js` has no test harness in this repo (plain script, not a module) — verified by code inspection instead, consistent with the Acceptance criteria's `judgment` annotation.

**Implementation Notes:**

Changed files: `viz/src/ui/theme.ts` (+2/-1 lines), `viz/public/theme-init.js` (+1/-1 line), `viz/src/ui/theme.test.ts` (new, 26 lines).

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — `npx vitest run src/ui/theme.test.ts` → 4/4 passed.

- [x] Ran lint/type-check on changed code — `npm --prefix viz run typecheck` and `npm --prefix viz run lint` both clean.

- [x] **Verification receipt** — see Testing Notes below. No avoidable duplication (the legacy-key constant is used once per file, matching the existing single-constant shape); no dead code; no public-surface growth (`readPreference`/`writePreference` signatures unchanged); no stale docs — `theme-init.js`'s header comment still accurately describes its CSP/nonce role, untouched by this change.

- [x] (frontend) Asked the user for visual confirmation — suppressed per `fast-mode = true` (⚡ `[unattended]`-implied `--fast`); operator owns visual-confirmation responsibility for this run.

**Choosing a test strategy:** see SPEC.md §"🧪 Phase 3: Testing & Linting".

**Testing Notes:**

- `npx vitest run src/ui/theme.test.ts` → exit 0 (4/4 passed)
- `npm --prefix viz run typecheck` → exit 0
- `npm --prefix viz run lint` → exit 0
- `npm --prefix viz test -- --run` (full suite, run since this touches shared mount-time theme resolution) → exit 0 (29 files, 550/550 passed)

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — `README.md`: no change. `AGENTS.md`: no change. `SPEC.md`: no change. `docs/MIGRATION.md`: no change (its one "theme" hit is an unrelated audit-synthesis usage, not the UI theme). `claude/AGENTS-snippet.md`: no change. `codex/AGENTS-snippet.md`: no change. `cursor/AGENTS-snippet.md`: no change. None of the AI-referenced docs name the storage key or theme internals.

- [x] Closed — every `## ✅ Acceptance` criterion ticked; `status:` flipped to `completed` below; PLAN.md line flipped to stub form and moved to `## Completed`; tasknote moved to `.flowtron/tasknote/archive/fe/`.

- [x] **Evidence-based recap** drafted — see Final Summary.

**Final Summary:**

Renamed the theme localStorage key from bare `theme` to `flowtron-viz-theme` in `viz/src/ui/theme.ts` (+2/-1 lines: new `LEGACY_STORAGE_KEY` constant, `readPreference` fallback via `??`) and `viz/public/theme-init.js` (+1/-1 line: same fallback via `||`), so existing users' stored preference survives the rename without a rewrite-on-read migration — a subsequent `writePreference` naturally lands on the new key. Added `viz/src/ui/theme.test.ts` (new, 4 cases) covering the new-key-present, legacy-fallback, new-key-precedence, and write-only-touches-new-key behaviors, following the repo's existing `visibilityPrefs.test.ts` localStorage-module test pattern (no test file previously existed for `theme.ts`).

Verification: `npx vitest run src/ui/theme.test.ts` → exit 0 (4/4); `npm --prefix viz run typecheck` → exit 0; `npm --prefix viz run lint` → exit 0; `npm --prefix viz test -- --run` (full suite, run because this touches shared mount-time theme resolution) → exit 0 (29 files, 550/550).

No refactor beyond the minimal rename — `ThemeToggle.tsx` was left untouched since it consumes `readPreference`/`writePreference` and never references the key name. Documentation verdict: no drift, no AI-referenced doc names the storage key. `touches:` scope reconciliation: declared `viz/src/ui/theme.ts` and `viz/public/theme-init.js`; actual diff also includes the new `viz/src/ui/theme.test.ts` (undeclared at scaffold time, added during Phase 2 per the "tests for non-trivial behavior" step — a same-task addition, not scope creep). Maintainability effect: the FOUC-guard/`theme.ts` sync constraint (surfaced by the archive-skim probe) is now honored explicitly with matching fallback logic in both files, and the new test file closes a test-coverage gap this module previously had.

**Archived:** 2026-09-12
