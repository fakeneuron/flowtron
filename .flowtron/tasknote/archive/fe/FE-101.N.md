---
title: viz-archive-scale audit
status: completed
tags: []
created: 2026-08-29
due:
related-tasks: [FE-EPIC-101, FE-101.2, FE-101.3, FE-101.4, FE-101.5]
---

# FE-101.N | viz-archive-scale audit

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-101]]

## 🎯 Goal

Verify the completed FE-EPIC-101 (`viz-archive-scale`) cohort sits coherently in the codebase: cumulative doc-drift sweep across `.flowtron/tasknote/README.md` §"AI-referenced docs", naming/style consistency across the cohort's deliverables, and follow-up filings for any miss.

## ✅ Acceptance

- [x] **Doc-drift sweep (fixed line, per SPEC/epic.md §"Audit acceptance — fixed doc-drift line")** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the specific update. Always present; surfaces cumulative slice-local staleness that per-task Phase 4 closures can miss.
- [x] Cohort coherence inventory: each implementation child's deliverables read against the others (naming consistency, style parity, no contradictory cross-refs)
- [x] No regressions surfaced in earlier-shipped cohort children's surfaces
- [x] Audit findings recorded in Implementation Notes; misses cited as candidates for `/ft-file-followup <NEW-ID>` filing (filed AFTER audit closure to preserve `/ft-file-followup`'s filing-discipline gate)
- [x] Single `feat: FE-101.N — audit FE-EPIC-101` (or `chore: ...` if no code edits land) commit lands
- [x] PLAN.md line for `FE-101.N` flipped to stub form `Completed YYYY-MM-DD.`
- [x] Tasknote moved to `.flowtron/tasknote/archive/fe/FE-101.N.md`
- [x] Parent-flip prompt surfaced after audit closure (skill Step 8) — user confirms or declines flipping `FE-EPIC-101` to `Completed` and moving the cohort to `## Completed`

## 🧩 Subtasks

- [x] Inventory cohort children's archived tasknotes — read each implementation child's Final Summary + Implementation Notes; capture deliverables in Discovery Notes
- [x] Walk `.flowtron/tasknote/README.md` §"AI-referenced docs" entries — fixed doc-drift sweep
- [x] Cohort coherence pass — naming consistency, style parity, no contradictory cross-refs across the cohort's deliverables
- [x] Surface audit findings in Implementation Notes; cite each miss as a `/ft-file-followup <NEW-ID>` candidate
- [x] Phase 4: flip `FE-101.N` PLAN line to stub form + archive tasknote
- [x] Parent-flip: skill Step 8 prompts user; on confirm, atomic flip parent line + move cohort to `## Completed`

## 🔗 Related

- [[FE-EPIC-101]] — parent epic (viz-archive-scale)
- [[FE-101.2]] — archive-wire-slim (cohort child)
- [[FE-101.3]] — targeted-sse-refetch (cohort child)
- [[FE-101.4]] — collapsed-section-unmount (cohort child)
- [[FE-101.5]] — archive-cache-bound (cohort child)

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** All four implementation children (`.2` `.3` `.4` `.5`) are `[x]` and archived, the parent `FE-EPIC-101` is still active in `## Medium`, and `.N` is the reserved terminal audit child — the canonical close-out position. No early-audit gate fired (zero open siblings). The cohort's deliverables all touch one surface (`viz/src/`), which is exactly the case the cumulative sweep exists for.

- [x] Read relevant source files — read the four archived cohort tasknotes in full, then verified each one's claimed deliverable against HEAD directly. Read set was narrow and known (four notes + ~8 source files), so no probe was warranted.

- [x] **Best Practices Review** — N/A as a code-design review: the audit's deliverable is verification, not a new code surface. The one inline fix applied (Phase 2) is a markdown-record correction inside archived tasknotes, which introduces no module boundary, dependency, or abstraction.

- [x] **Archive skim** — self-referential by construction: the cohort children *are* the archive entries under review. Skimmed beyond the cohort for the surfaces it touched (`archiveCache`, `sseChange`, `PrioritySection`/`TaskRow`/`EpicRow`, the `Tasknote` DTO) via each child's own recorded skim, which collectively cite [[FE-004]], [[FE-010]], [[FE-020]], [[FE-028]], [[FE-064]], [[FE-072]], [[FE-088.3]], [[FE-088.4]], [[FE-091]], [[FE-093]], [[FE-094]], [[FE-97]], [[FE-98]], [[FE-100]], [[CORE-098.8]], [[CORE-098.14]], [[CORE-118]], [[CORE-421.2]], [[CORE-425.2]], [[CORE-431.2]], [[CORE-431.3]]. No ⚠️ `Superseded by` pointer or `supersedes:` edge exists on the cohort.

  **One non-cohort note was read in full, and it changed a finding.** [[CORE-478]] (`viz-version-drop`) surfaced from the `## Completed` list while confirming the parent-flip section, and reading it converted a confidently-drafted follow-up candidate into a withdrawn non-finding (Implementation Notes §"Investigated and withdrawn"). Logged here because it is the archive skim earning its place: the `viz/package.json` evidence was convincing on its own terms, and only the archived decision record showed it was intentional.

- [x] **Drift check** — every cohort deliverable still matches HEAD (verified by grep, not recall): `Tasknote` carries no `body` field and `parseTasknote` keeps it function-local (`.2`); `ChangeScope` / `ALL_CHANGE_SCOPES` / `parseChangeData` are live in `sseChange.ts:9-52` (`.3`); `TaskRow.tsx:14` and `EpicRow.tsx:18` are `React.memo`-wrapped and `PrioritySection.tsx:39` conditionally renders on `!collapsed` (`.4`); `archiveCache.ts:56` declares `MAX_CACHED_PROJECTS = 5` with touch-on-hit and evict-on-insert at `:66-80` (`.5`). Nothing moved during the cohort. Cross-artifact half: read `SPEC.md` §"Acceptance tick-through" and §"`## 🧩 Subtasks` is exempt" (lines 847-866) rather than recalling them — that read is what separated finding 1 from non-finding A below. Re-read the PLAN lines at `.flowtron/PLAN.md:16-21`; all match as filed.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

  No clarifications needed. Explicit assumptions: (a) the audit's scope is the full four-child cohort, none deferred; (b) correcting a cohort note's Acceptance boxes to match what that note's own Phase 4 already asserts — and what HEAD independently confirms — is a record correction, not a re-judgment of the sibling's work, so it belongs inline rather than as a follow-up filing; (c) findings that are *not* cohort-caused are logged as `/ft-file-followup` candidates rather than fixed here.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

**Cohort inventory** (one line per child, deliverable verified against HEAD):

| Child | Deliverable | Verified at HEAD |
|---|---|---|
| [[FE-101.2]] `archive-wire-slim` | Dropped `body` from the shared `Tasknote` DTO — `/api/archive` 11,304,850 → 1,771,547 B (−84.3%) | `tasknote.ts` has no `body` member; `tasknote-parse.ts:38` keeps the local |
| [[FE-101.3]] `targeted-sse-refetch` | SSE `change` carries `scopes: ['plan'\|'active'\|'archive']`; client refetches only that scope — tasknote edit 1,866,363 → 4,821 B (−99.74%) | `sseChange.ts:9-52`, `watchSet`/`flowtronWatch`/`useProjectData` threaded |
| [[FE-101.4]] `collapsed-section-unmount` | Collapsed `PrioritySection` unmounts its subtree; `TaskRow`/`EpicRow` memoized with props stabilized so the bail is real | `PrioritySection.tsx:39` `{!collapsed && …}`; both rows `React.memo` |
| [[FE-101.5]] `archive-cache-bound` | 5-project LRU on `archiveCache` — touch-on-hit, evict-oldest-on-insert | `archiveCache.ts:56,66-80` |

**Suite state at audit time** — `npm --prefix viz test` **28 files / 516 tests passing**; `run typecheck` and `run lint` clean. The count reconciles exactly with the cohort's recorded arithmetic: 481 (pre-epic) → 481 (`.2`, no new cases) → 504 (`.3`, +23) → 514 (`.4`, +10) → 516 (`.5`, +2).

**Two things the contract cleared that a pattern-match would have flagged.** Recorded so the next audit does not re-raise them:

- **A. Unticked `## 🧩 Subtasks` in `.2`, `.4`, `.5`** (0/7, 0/8, 0/6; `.3` is 10/10). `SPEC.md:857` — "**`## 🧩 Subtasks` is exempt.** … Unticked Subtasks boxes at archive time are therefore **correct, not drift**." Not a finding. The cohort is inconsistent in style but uniformly compliant.
- **B. `.2` carries no `touches:` key** while `.3`/`.4`/`.5` do. `touches:` is an optional planning key, and the template states omission means *undeclared*, not "touches nothing". Legal. Not a finding.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — N/A for the verification body of the audit (no new code surface). For the two inline fixes, the precedent surveyed was the template itself (`templates/tasknote-template.md`) plus the three sibling notes, which agree on the Phase 3 ordering and on ticked Acceptance; both fixes move the outlier onto the shape the cohort already uses rather than inventing one.

- [x] **Minimal refactor gate** — no refactor. Both fixes are record corrections inside archived markdown; no code was touched.

- [x] Implemented the minimal solution — two inline fixes applied (below); everything larger logged as a follow-up candidate instead.

- [x] Updated/added tests for non-trivial behavior — N/A: no code changed. The verification for the fixes is `closureDrift` recomputed against the real parser (Testing Notes).

**Implementation Notes:**

### Cohort coherence findings

**Finding 1 — [[FE-101.5]] shipped with all 5 Acceptance boxes unticked, and the viz board was rendering a closure-drift chip for it.** FIXED INLINE.

Its own Phase 4 line asserts "every `## ✅ Acceptance` criterion ticked (none annotated `N/A` or not-met)" — the boxes say otherwise. This is exactly the miss `SPEC.md:847` §"Acceptance tick-through" exists to prevent ("An unticked, unannotated box at archive time is indistinguishable from an unnoticed one"), and it is the one finding with a user-visible surface: running the cohort's four notes through the real parser showed `FE-101.5 closureDrift = {"unticked":5,"total":5}` while `.2`/`.3`/`.4` were `null`. The epic under audit is *about the viz board*, and its final child was the only note on the board flying a drift chip.

Fixed by ticking all five. Each was independently confirmed satisfied at HEAD before ticking — not taken on the note's word:

| Criterion | Evidence |
|---|---|
| LRU evicts past `MAX_CACHED_PROJECTS` (5) | `archiveCache.ts:56` + `:76-80` (`cache.size > MAX_CACHED_PROJECTS` → delete `keys().next().value`) |
| `get()` hit counts as a use | `archiveCache.ts:66-70` (`delete` + `set` re-insert, comment names it MRU) |
| `invalidate()`/`invalidateProject()`/`clear()` + dedup + error-clears unchanged | `archiveCache.ts:71-75, 83-95` — the `.catch` still compares `cache.get(name) === cached` before deleting |
| New tests cover eviction + touch-protects | `archiveCache.test.ts`, 16 cases, in the passing suite |
| test / typecheck / lint clean | re-run this audit: 28 files / 516 tests, `tsc --noEmit` clean, `eslint src` clean |

Re-running the parser after the fix returns `closureDrift = null` for all four children.

**Finding 2 — [[FE-101.2]] carried a duplicate empty `**Testing Notes:**` heading with the Phase 3 guidance blockquote stranded below its own content.** FIXED INLINE.

The note had `**Testing Notes:**` twice: once with the payload-measurement content, then the template's blockquote + a second, empty `**Testing Notes:**` left below it before `## 🚀 Phase 4`. `.3`/`.4`/`.5` all place the blockquote *above* a single Testing Notes heading, as does the template. Fixed by moving the blockquote above the heading and deleting the empty duplicate — a pure structural move, `+8/−10`, no prose added or lost.

### Follow-up candidates (file after audit closure)

**None.** One candidate was raised, investigated, and **withdrawn** — see below. No miss survives to file, so `/ft-file-followup` is not invoked for this audit.

### Investigated and withdrawn — `viz/package.json` version

Raised as a candidate on the evidence that `viz/package.json` reads `5.19.0` against `SPEC.md:3`'s `v5.21.0`, that `git log -- viz/package.json` shows the field bumped *inside* the release commits for both prior cuts (`a88b625` v5.18.0, `fe8e727` → 5.19.0) and then not bumped for v5.20.0 or v5.21.0, and that `viz/package-lock.json` mirrors the stale value twice. That reads exactly like a convention that silently lapsed.

**It is not drift. It is a deliberate, operator-confirmed decoupling, and the file is correct as it stands.** [[CORE-478]] (`viz-version-drop`, 2026-08-26) deleted the dead `VIZ_VERSION` constant and, on an explicit Phase 1 clarifying question, removed the `viz/package.json` + `viz/package-lock.json` bump step from `/ft-release` as well — its Acceptance says so in terms: "`claude/skills/ft-release/SKILL.md` no longer has a bump step for `VIZ_VERSION` or for `viz/package.json`/`viz/package-lock.json`'s version; the 'N version edits' prose is renumbered to 3 everywhere it appears." The justification chain is [[FE-056]] (the field was only ever a mirror of `VIZ_VERSION`, on a private never-published package with no code consumer) and [[CORE-432.2]] (which dropped the last runtime reader, leaving the constant write-only). CORE-478's own Final Summary states the resulting contract: **"git tag remains the sole version authority for viz."**

The timeline corroborates it exactly: CORE-478 closed 2026-08-26, and the two releases that did not bump the field — v5.20.0 ([[CORE-480]], 2026-08-26) and v5.21.0 ([[CORE-484]], 2026-08-27) — are precisely the cuts that followed it. The field stopped moving the moment the step was removed, not before.

Recorded here at length deliberately, because the surface evidence is genuinely convincing in isolation and a future audit sweeping `viz/` will hit it again. **The three-version-edit recipe is intentional; a bare stale `"version"` in `viz/package.json` is the expected steady state, not a miss.**

### Checked and cleared (not findings)

- **Doc-drift**: see Phase 4 sweep — no drift.
- **Naming/style across the cohort**: consistent. All four use the same `viz/src` module vocabulary, all four ran the full suite (`.5` scoped to `archiveCache` with a stated reason), all four recorded a property-based-testing consider-and-skip, and all four kept the epic-child placement invariant.
- **No contradictory cross-refs**: the `[[…]]` chains are consistent and directional — `.2`→`.5` (moot-check handoff), `.3`→`.2`, `.4`→`.2`/`.3`, `.5`→`.2`/`.4`. Every forward promise was kept: `.2` flagged the archiveCache moot-check for `.5`, and `.5` ran it with real numbers; `.4` said "`.5` is unaffected by this change", and `.5`'s Discovery independently confirms no shared surface.
- **No regressions in earlier children's surfaces**: `.3`'s scoped-refetch tests, `.4`'s memo-bail tests, and `.2`'s parser tests all pass in the current 516-test suite, so the later children did not erode the earlier ones.
- **Unticked Subtasks in `.2`/`.4`/`.5`** — exempt by `SPEC.md:857`. Non-finding A in Discovery Notes.
- **`.2` has no `touches:` key** — optional planning key. Non-finding B in Discovery Notes.
- **Minor figure inconsistency, deliberately not filed**: `.2` measured flowtron's archive at 782 notes / 11,304,850 B, while `.5`'s Discovery cites "795-note" and a 10,976,000 B denominator (yielding 83.9% where `.2` measured 84.3%). The archive genuinely grew across the cohort's own day, and the 0.4-point ratio difference does not move `.5`'s conclusion or the operator decision it fed. Archived notes are historical records; not worth a filing.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — no code changed, so there is no targeted suite for the diff. Ran the **full** `viz` suite anyway as the audit's own regression check over the whole cohort's surface: `npm --prefix viz test` → **28 files, 516 tests, all passing** (36.4s).

- [x] Ran lint/type-check on changed code — `npm --prefix viz run typecheck` (`tsc --noEmit`) clean; `npm --prefix viz run lint` (`eslint src`) clean. Both are strictly the audit's verification of the cohort, since the audit's own diff is markdown-only.

- [x] **Quality assertions** — N/A for the diff: two markdown record corrections, no code. Neither adds duplication, dead code, complexity, or public surface. The `.2` fix *removes* a stale duplicated heading; the `.5` fix makes an archived note's checkboxes agree with its own closure assertion.

- [x] (frontend) Asked the user for visual confirmation — N/A. No UI code changed. The one user-visible effect (the closure-drift chip disappearing from `FE-101.5`'s row) was verified programmatically against the real parser rather than by eye, which is the stronger check here: it asserts the derived value the chip renders from, not a screenshot of it.

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

**Closure-drift verified against the real parser, before and after.** Rather than eyeballing checkboxes, ran the cohort's four archived notes through `parseTasknote` from `viz/src/tasknote-parse.ts` — the same function the board uses — and read `closureDrift` / `subtasksProgress` directly:

| Child | `closureDrift` before | after | `subtasksProgress` |
|---|---|---|---|
| FE-101.2 | `null` | `null` | 0/7 |
| FE-101.3 | `null` | `null` | 10/10 |
| FE-101.4 | `null` | `null` | 0/8 |
| FE-101.5 | **`{"unticked":5,"total":5}`** | **`null`** | 0/6 |

This is what turned Finding 1 from a cosmetic checkbox observation into a confirmed user-visible defect: `closureDrift` fires only when `archivedDate > TICK_THROUGH_EFFECTIVE` (`2026-08-01`) and an unticked box carries no `N/A` / `not met` annotation (`tasknote.ts:280-288`) — both true for `.5` at `2026-08-29`. The `subtasksProgress` column is shown to make the non-finding explicit: three of four children sit at 0 done, and per `SPEC.md:857` that is correct rather than drift.

**Suite arithmetic reconciles across the whole cohort** — 481 → 481 (`.2`) → 504 (`.3`, +23) → 514 (`.4`, +10) → 516 (`.5`, +2), matching today's measured 516. No child silently dropped a test.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep (the fixed audit line)** — walked all 18 entries. **No change** on every one: `README.md` · `AGENTS.md` · `SPEC.md` · `docs/MIGRATION.md` · `claude/AGENTS-snippet.md` · `codex/AGENTS-snippet.md` · `cursor/AGENTS-snippet.md` · `grok/AGENTS-snippet.md` · `docs/CONVENTIONS.md` · `CONTRIBUTING.md` · `SECURITY.md` · `docs/AGENT-NEUTRALITY.md` · `docs/PLATFORMS.md` · `claude/CAPABILITIES.md` · `docs/AGENT-COMPAT.md` · `docs/EXTERNAL-AGENTS.md` · `docs/WORKTREES.md` · `docs/VISION.md`.

  Swept as a **cumulative cohort sweep**, not four per-task sweeps replayed: grepped the whole set in one pass for every surface the epic touched — `archiveCache|ArchiveCache|sseChange|useProjectData|watchSet|flowtronWatch|PrioritySection|TaskRow|EpicRow|pruneMatchingNodes|React\.memo|ChangeScope|/api/archive|/api/active|/api/plan|Tasknote interface|MAX_CACHED_PROJECTS|LRU|grid-rows`. **Exactly one region matched**, and four entries were then checked by hand rather than dismissed by category:

  - **`SECURITY.md`:233-234** — the only hit in the entire sweep set, and the only entry naming any file this epic touched. Its symlink-containment list names `discoverProjects`, `archiveCache.readArchive`, and the `/api/plan` · `/api/active` · `/api/plan-archive` handlers. Verified still accurate on both halves the cohort could have falsified: (i) `readArchive` itself is untouched — `.5`'s LRU only adds and reorders `Map` keys *around* calls to it, so containment is unchanged; (ii) the list's omission of `/api/archive` is correct rather than a gap, since that endpoint is served through `archiveCache.readArchive`, which the list already names. Nothing in §"Visualizer (`viz/`) dev-server scope" describes a JSON field set, an SSE payload shape, or a cache retention policy, so `.2`'s field removal and `.3`'s new `scopes` key touch no claim. The scope string also discloses nothing new — any client that can read `/api/events` already receives the project name. **No change.**
  - **`README.md`** — describes `viz/` at a level (what it is, how to run it, where it lives) that no cohort change reaches. Its "**777 tasks** closed … (as of 2026-08-27)" line is a **dated snapshot with a declared owner**, not a live claim: the paragraph itself states the count is "recomputed each release by `/ft-release` §7.1". Today's archive holds 796. That gap is the stamp doing its job, not drift, and pre-empting the release step here would be collateral. **No change.**
  - **`claude/CAPABILITIES.md`** — carries `last-verified: v5.21.0 · 2026-08-27 (dogfooded)`, which matches `SPEC.md`'s current version and is a release-cycle stamp, not a per-task one. Nothing in this epic changed a Claude Code capability. **No change.**
  - **`AGENTS.md`** — the one non-SECURITY entry naming `viz/` commands (`npm --prefix viz test` / `typecheck` / `lint`, §"Validation"). All three still exist and all three pass at HEAD, verified this session. **No change.**

  `viz/README.md` is deliberately **not** a sweep-set member, but was consulted anyway since `.3` placed a new type against its §"Dependency direction" rule: it calls `sseChange.ts` "the canonical shared example" of the Node-free tier and names no payload field, so it stays accurate as written.

- [x] Closed — all 8 `## ✅ Acceptance` criteria ticked (none annotated `N/A` or not-met), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form and kept 2-space nested beneath `FE-EPIC-101` in `## Medium` pending the Step 8 parent-flip decision (epic-child placement invariant, `SPEC/epic.md`), then tasknote moved to `.flowtron/tasknote/archive/fe/`.

  No ⚠️ `Superseded by` pointer written. The two inline fixes corrected `.5`'s and `.2`'s *records* to match what those notes already asserted and what HEAD independently confirms — no factual claim in either note was falsified, so the pointer's trigger condition is not met.

- [x] **Evidence-based recap** drafted — see Final Summary

**Final Summary:**

The `FE-EPIC-101` (`viz-archive-scale`) cohort is coherent and its four deliverables are all live at HEAD. The audit surfaced **two record defects, both fixed inline, and no follow-ups to file** — and the first of those was a real user-visible defect on the very board this epic set out to make faster.

**Headline finding.** [[FE-101.5]] was archived with all five `## ✅ Acceptance` boxes unticked while its own Phase 4 line asserted every one was ticked. Run through `parseTasknote` — the same function the board uses — it returned `closureDrift = {"unticked":5,"total":5}`, meaning `FE-101.5`'s row had been flying a closure-drift chip on the viz board since it closed. Each criterion was independently verified satisfied at HEAD before ticking (LRU cap at `archiveCache.ts:56`, touch-on-hit at `:66-70`, evict-on-insert at `:76-80`, untouched `invalidate`/`clear` semantics, 16 passing cache tests, clean suite), then re-run: all four children now return `closureDrift = null`. This is exactly the miss the fixed doc-drift/audit line exists to catch — a per-task Phase 4 asserted a tick-through it had not performed, and only a cumulative pass over the cohort caught it.

**Changed files** — two archived tasknotes, +13/−15, no code: `.flowtron/tasknote/archive/fe/FE-101.5.md` (five Acceptance boxes ticked to match its own closure assertion and HEAD) and `.flowtron/tasknote/archive/fe/FE-101.2.md` (a duplicate empty `**Testing Notes:**` heading removed and the stranded Phase 3 guidance blockquote moved above the heading, matching the template and all three siblings — a pure structural move, no prose added or lost).

**Verification** — `npm --prefix viz test` 28 files / **516 tests** passing; `run typecheck` and `run lint` clean. Every cohort deliverable re-confirmed at HEAD by grep rather than by the notes' word: no `body` on the `Tasknote` DTO, `ChangeScope`/`parseChangeData` live in `sseChange.ts`, both row components `React.memo`-wrapped with `PrioritySection` conditionally rendering, `MAX_CACHED_PROJECTS = 5`. The suite arithmetic reconciles across the whole cohort (481 → 504 → 514 → 516), so no child silently dropped a test. The fixes' effect was verified against the real parser, before and after, rather than by eye.

**Refactors** — none. Both fixes are markdown record corrections; no code was touched.

**Documentation** — no drift across all 18 AI-referenced docs, swept cumulatively in one grep over every surface the epic touched. Exactly one region matched (`SECURITY.md`:233-234) and was verified accurate on both halves the cohort could have falsified; `README.md`, `claude/CAPABILITIES.md`, and `AGENTS.md` were checked by hand rather than dismissed. `README.md`'s 777-vs-796 task count is a dated, release-owned stamp — correctly left alone.

**Follow-ups — none.** One candidate was raised and withdrawn on evidence. `viz/package.json` reads `5.19.0` against `SPEC.md`'s `v5.21.0`, and the git history makes it look like a lapsed convention (bumped inside the release commits for v5.18.0 and v5.19.0, then not for v5.20.0 or v5.21.0). It is not: [[CORE-478]] deliberately removed that bump step from `/ft-release` on an operator-confirmed clarifying question, because the field only ever mirrored the `VIZ_VERSION` constant ([[FE-056]]) whose last runtime reader had already been dropped ([[CORE-432.2]]) — leaving "git tag remains the sole version authority for viz." The two unbumped releases are exactly the two cuts *after* CORE-478 closed. The stale value is the intended steady state. Recorded at length in Implementation Notes because the surface evidence is convincing in isolation and the next `viz/` sweep will hit it again.

**Maintainability** — the epic's four changes hold together as one story rather than four patches: `.2` cut what the archive payload *contains*, `.3` cut how *often* it is fetched, `.4` cut what is *mounted* from it, `.5` bounded what is *retained* — each bounding a different axis, none overlapping, every forward promise between them kept (`.2` flagged the archiveCache moot-check for `.5`, and `.5` ran it with real numbers; `.4` predicted no shared surface with `.5`, and `.5` independently confirmed it). Two subtleties are now pinned by tests that fail against the old design rather than merely describe the new one (`.3`'s per-scope sequencing, `.4`'s memo-bail via `Component.type`). What the audit adds is that the cohort's own record now matches its work — which matters more than usual here, because this epic's product *is* the tool that reads those records.

**Archived:** 2026-08-29
