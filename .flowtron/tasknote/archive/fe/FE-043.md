---
title: status-legend-icon-consistency
status: completed
tags: []
created: 2026-05-19
due:
related-tasks: []
---

# FE-043 | status-legend-icon-consistency

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-023]] [[FE-033.2]]

## 🎯 Goal

Normalize `STATUS_CHIP_LABEL` to a single glyph style so the shortcuts modal legend renders consistently across operating systems.

## ✅ Acceptance

- [ ] `STATUS_CHIP_LABEL` in `viz/src/ui/constants.ts` uses one consistent glyph style (all mono OR all via StatusChip in the legend)
- [ ] No color emoji mixed with mono unicode glyphs in any rendered status legend or chip
- [ ] `ShortcutsModal.tsx` legend renders visually consistent across platforms
- [ ] Lint and typecheck pass; existing tests pass

## 🧩 Subtasks

- [x] Decide normalization direction (clarifying question — see Discovery Notes)
- [ ] Update `STATUS_CHIP_LABEL` in `viz/src/ui/constants.ts` per chosen direction
- [ ] If "colored dots via StatusChip" direction: update `ShortcutsModal.tsx` legend to use `<StatusChip>` as the icon
- [ ] Run lint/typecheck and confirm tests pass
- [ ] Visual confirmation in browser

## 🔗 Related

- [[FE-023]] — moved STATUS_CHIP_LABEL to constants.ts; emoji values set here
- [[FE-033.2]] — introduced PALETTE / STATUS_BADGE; glyphs untouched

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Confirmed the mixed set: `🌱`/`🟢` are color emoji; `⚪`/`⏸`/`✓` are mono unicode. Both `StatusChip.tsx:16` and `ShortcutsModal.tsx:82` consume `STATUS_CHIP_LABEL`. The problem is real.

- [x] Read relevant source files
- [x] **Archive skim** — FE-023 set the emoji values verbatim and FE-033.2 added PALETTE without touching glyphs. No prior normalization attempt.
- [x] **Drift check** — file paths and line numbers in the PLAN description match HEAD. No drift.
- [x] Asked clarifying questions — direction confirmed: all mono glyphs (`🌱` → `◎`, `🟢` → `●`; `⚪`/`⏸`/`✓` unchanged)
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

Two normalization approaches are available:

**A — All mono glyphs:** Change `STATUS_CHIP_LABEL` values so all five entries use plain unicode symbols (no color emoji). Minimal change: 2 string values in `constants.ts`. `StatusChip` continues to render the glyph on a palette-colored background. Example candidates: `🌱` → `◎`, `🟢` → `●`.

**B — Colored StatusChip in modal legend:** Leave `STATUS_CHIP_LABEL` values as mono glyphs (same as A), but also swap the bare `<span aria-hidden>{STATUS_CHIP_LABEL[s]}</span>` in `ShortcutsModal.tsx` for `<StatusChip status={s} />` — so the legend shows the actual full palette-colored chip rather than a bare icon. Two files touched instead of one, but the legend then matches the in-app chip exactly.

Design question surfaced — see clarifying question below.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — pure data change in one record; no new pattern needed
- [x] Implemented the minimal solution
- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code
- [x] Ran lint/type-check on changed code
- [x] (frontend) Asked the user for visual confirmation (👁️ prefix on the prose ask)

**Testing Notes:** 162/162 tests pass; typecheck and lint clean. Visual: legend shows `◎ ⚪ ● ⏸ ✓` — uniform mono rendering confirmed in browser.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — all 6 AI-referenced docs: no change
- [x] Closed — PLAN.md line flipped to stub form and moved to `## Completed`; tasknote moved to `_project/tasknote/archive/frontend/`
- [x] Recap drafted

**Final Summary:** Replaced color emoji `🌱` and `🟢` in `STATUS_CHIP_LABEL` with mono glyphs `◎` and `●`; updated the one test that asserted `🟢`. All five status icons now use plain unicode — no OS-dependent color rendering. Two files changed, 162 tests pass, lint+typecheck clean, visual confirmed.

**Archived:** 2026-05-19
