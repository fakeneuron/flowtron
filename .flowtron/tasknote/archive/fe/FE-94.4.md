---
title: Collapse App's repeated warning badges and diagnostic strips into components
status: completed
tags: [viz, refactor]
created: 2026-08-23
due:
related-tasks: [FE-EPIC-94, FE-94.3, FE-94.5]
touches:
  - viz/src/ui/App.tsx
  - viz/src/ui/HeaderBadge.tsx
  - viz/src/ui/DiagnosticBanner.tsx
blocked-by:
  - FE-94.3
---

# FE-94.4 | Collapse App's repeated warning badges and diagnostic strips into components

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-94]] [[FE-94.3]] [[FE-94.5]]

## 🎯 Goal

Collapse `App.tsx`'s four duplicated amber header badges and three duplicated amber diagnostic strips into two shared presentational components — `HeaderBadge` and `DiagnosticBanner` — with byte-identical rendered output.

## ✅ Acceptance

- [x] `viz/src/ui/HeaderBadge.tsx` and `viz/src/ui/DiagnosticBanner.tsx` exist as presentational, props-only components (no state, no hooks) following the existing chip-component shape (`CriticalChip.tsx` et al.)
- [x] All four header badge call sites (`unparsed`, `duplicateEpics`, `nearMissHeadings`, `liveDisconnected`) use `HeaderBadge`
- [x] All three diagnostic strip call sites (`unparsed`, `duplicateEpics`, `nearMissHeadings`) use `DiagnosticBanner`
- [x] Rendered DOM is byte-identical to the current markup — every class string preserved verbatim; the existing diagnostics tests (`App.test.tsx`) pass unmodified
- [x] The single-instance error banner (red) and SSE-disconnect banner (amber, `role="status"`) stay inline, untouched
- [x] `App`'s public surface (props-less `React.FC`) unchanged
- [x] Full `viz` suite green before and after, unchanged from the FE-94.3 baseline (431 passed / 23 files)
- [x] `npm --prefix viz test`, `npm --prefix viz run typecheck`, `npm --prefix viz run lint` all green

## 🧩 Subtasks

- [x] Capture the pre-move baseline: `npm --prefix viz test` and record the green count
- [x] Create `viz/src/ui/HeaderBadge.tsx` — `children`-only presentational span owning the shared amber className
- [x] Create `viz/src/ui/DiagnosticBanner.tsx` — takes a `message` node and renders `children` inside its own `<ul>` (each strip keys its `<li>` differently, so `children` over an `items` array)
- [x] Replace the four header badge call sites in `App.tsx` with `<HeaderBadge>`
- [x] Replace the three diagnostic strip call sites in `App.tsx` with `<DiagnosticBanner>`
- [x] Run `npm --prefix viz test`, `run typecheck`, `run lint`; confirm the post-move suite equals the baseline exactly (no new/changed/skipped tests — this is a pure JSX move)
- [x] Visual confirmation ask (badges + strips render unchanged)

## 🔗 Related

- [[FE-94.3]] — predecessor (`blocked-by:`); lifted the derivation logic this task's badges/strips read from
- [[FE-94.5]] — next in sequence; consumes both components when it moves the header
- [[FE-EPIC-94]] — parent epic

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Predecessor FE-94.3 closed 2026-08-24 (431/431 green) and touched only the derivation layer — the badge/strip JSX this task targets is untouched by that move. The starter's captured line numbers drifted (FE-94.3 shifted `App.tsx` from 541 → 486 lines) but the badge/strip markup itself is unchanged in shape and content. No new blocker surfaced.

- [x] Read relevant source files — `viz/src/ui/App.tsx` (current 486 lines: four header badges at 285-304, three diagnostic strips at 391-438, the single-instance error banner at 375-379 and SSE-disconnect banner at 381-389 that stay out of scope), `viz/src/ui/App.test.tsx` (diagnostics tests around lines 1001-1080, asserting on badge/strip text content via `screen.getByText`, not on DOM structure), and the existing `viz/src/ui/` chip components (`CriticalChip.tsx`, `ModelChip.tsx`, `StatusChip.tsx`, `RelatedChip.tsx`) as the established shape for small presentational components. Read set was bounded — no probe needed.

- [x] **Best Practices Review** — `viz/src/ui/` already has a clear precedent for this exact shape: single-purpose presentational chips (`CriticalChip.tsx`: `React.FC<{ density }>` returning one `<span>`, no internal state) living in their own file. `HeaderBadge` and `DiagnosticBanner` extend that pattern rather than inventing a new one. Dependency direction stays one-way: both new components import nothing from `App.tsx` and are imported by it — no cycle. No nearby duplication to fold in beyond the four/three call sites this task itself collapses.

- [x] **Archive skim** — 56 archived `fe/` tasknotes touch `App.tsx`; narrowed by content, not full-read. Load-bearing:
  - [[FE-94.3]] (predecessor) — extracted the derivation logic (`unparsed`, `duplicateEpics`, `nearMissHeadings` are now `taskView.ts` outputs); confirms this task's JSX targets are unaffected by that move and the 431/431 baseline this task must reproduce.
  - [[FE-063.2]] — origin of the `unparsed` badge + strip; its Final Summary calls the amber badge/strip presentation "user-chosen," not an accessibility or design-system contract — safe to refactor freely.
  - `CORE-421.3` (`core/` archive) — origin of the `duplicateEpics` badge/strip, explicitly built by "mirroring" the `unparsed` pattern (copy, not derived) — exactly the duplication this task collapses.
  - `CORE-425.3` (`core/` archive) — origin of the `nearMissHeadings` badge/strip, again explicitly built "mirroring the existing `unparsed`/`duplicateEpics` pattern." All three strips are confirmed independently-authored copies of one shape, not three deliberately-divergent designs.
  - [[FE-088.3]] — origin of the `liveDisconnected` header badge (single-instance; its paired SSE banner stays out of scope per the starter, confirmed still true).

- [x] **Drift check** — starter's line numbers (badges 340-359, strips 446-493) no longer match; current code has badges at 285-304 and strips at 391-438, an offset caused entirely by FE-94.3 shrinking the derivation block above them. Content is unchanged: same four badges, same three strips, same class strings, same `<li>` key shapes (`u.line` / `${d.id}-${i}` / `h.line`) the starter cited. No contradiction with the `FE-EPIC-94` / `FE-94.4` PLAN.md line or any SPEC contract. `HeaderBadge.tsx` / `DiagnosticBanner.tsx` do not yet exist — no conflict with prior work.

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions — **No clarifications needed.** The starter's two "Open at promotion" questions both resolve on direct evidence gathered above, not judgment calls worth pausing on:
  - `DiagnosticBanner` renders `children` (not an `items: ReactNode[]` prop) — confirmed each strip keys its `<li>` differently (`u.line`, `` `${d.id}-${i}` ``, `h.line`), so a generic items array would need its own per-item key function; `children` lets each call site keep its own `.map()`.
  - The badges stay a component, not a `constants.ts` token — the Best Practices Review found `constants.ts` holds semantic color-token strings, not JSX markup; `HeaderBadge` is markup-plus-class, matching the existing `CriticalChip.tsx`-style component precedent instead.

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

**Fidelity check.** The starter's Solution shape (two components: `HeaderBadge` for the inline span, `DiagnosticBanner` for the block strip), Decisions locked (two components not one; class strings copied verbatim; badges-before-header ordering vs FE-94.5), and Files to touch are all reflected in the Goal/Acceptance/Subtasks above. Nothing dropped.

**Starter context (preserved verbatim from filing, 2026-08-23):**

> _Captured during the `/ft-refactor viz/src/ui/App.tsx` depth survey — promote to full tasknote at `/ft-task` checkout._
>
> **Why this exists**
>
> `App.tsx` carries two families of duplicated JSX, ~85 lines between them:
>
> - **Four header badges** (lines 340-359: unparsed, duplicate epics, near-miss
>   headings, live-updates-off) each repeating the same 9-class amber `<span>`
>   verbatim, differing only in the copy.
> - **Three diagnostic strips** (lines 446-493: unparsed, duplicate epics,
>   near-miss headings) each repeating the same amber container, the same
>   singular/plural `<p>`, and the same `<ul className="mt-1 font-mono text-sm">`
>   list, differing only in copy and the key/label of each `<li>`.
>
> Extracting them first shrinks what FE-94.5 has to move by roughly a third and
> gives the header a clean composition boundary to move across. Lowest-risk step
> in the sequence: pure JSX, no state, no hooks.
>
> **Solution shape**
>
> - `viz/src/ui/HeaderBadge.tsx` — presentational, children-only, owning the
>   shared amber className. Four call sites become one-liners.
> - `viz/src/ui/DiagnosticBanner.tsx` — takes the message node and the list
>   items (or renders `children` for the `<ul>`), owning the amber container.
>   Three call sites collapse.
> - Rendered DOM must be byte-identical: the diagnostics tests assert on badge
>   text and strip content (`App.test.tsx:1002`, `:1033`, `:1073`), and the
>   board test at `:917` queries by `.overflow-x-auto` — className drift is
>   a real failure mode here, so keep every class string as-is.
> - The error banner (red, line 430) and the SSE banner (amber, `role="status"`,
>   line 436) are single-instance and shaped differently — leave them inline
>   unless promotion finds the abstraction genuinely fits.
> - Acceptance seeds for Phase 1 to promote into `## ✅ Acceptance`:
>   - the full suite including FE-94.2's additions green **before** and
>     **after**, unchanged;
>   - behavior-preserving, no feature change, no visual change;
>   - `App`'s public surface unchanged;
>   - `npm --prefix viz test`, `run typecheck`, `run lint` green.
>
> **Files to touch (preliminary survey — drift-check at promotion)**
>
> - `viz/src/ui/HeaderBadge.tsx` — new.
> - `viz/src/ui/DiagnosticBanner.tsx` — new.
> - `viz/src/ui/App.tsx` — seven call sites collapse; imports adjusted.
>
> **Explicitly out of scope**
>
> - The header block as a whole — FE-94.5.
> - The error and SSE-disconnect banners (single-instance, different shapes).
> - Any restyling, token migration, or `constants.ts` palette work — this is a
>   move, not a redesign.
> - Data-driving the three strips off a single diagnostics array; that is a
>   behavior-shaped change, not a de-duplication.
>
> **Decisions locked in this conversation**
>
> | Decision | Choice | Rationale |
> |---|---|---|
> | Two components, not one | `HeaderBadge` + `DiagnosticBanner` | The badge is an inline span in a `<p>`; the strip is a block section — different call contexts |
> | Class strings | Copied verbatim | Tests and visual output both depend on them; a "tidy while I'm here" pass would break the behavior-preservation contract |
> | Ordering vs FE-94.5 | Badges first | Shrinks the header move and gives it composed pieces to carry |
>
> **Open at promotion (Phase 1 should resolve)**
>
> - Does `DiagnosticBanner` take `items: ReactNode[]` or render `children`
>   inside its own `<ul>`? Lean: `children`, since each strip keys its `<li>`
>   differently (`u.line`, `${d.id}-${i}`, `h.line`).
> - Do the badges belong in `constants.ts` as a token instead of a component?
>   Lean: no — it is markup plus a class, not a token.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended the existing `viz/src/ui/` single-purpose presentational-component shape (`CriticalChip.tsx`, `ModelChip.tsx`, `StatusChip.tsx`, `RelatedChip.tsx`: a `React.FC` returning one element, no internal state); no new shape introduced.

- [x] **Minimal refactor gate** — no refactor beyond the extraction itself; nothing else in `App.tsx` was touched.

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior — none needed; this is a pure JSX move and the existing diagnostics tests (`App.test.tsx`, text-content assertions) already cover the badges and strips unmodified.

**Implementation Notes:**

New `viz/src/ui/HeaderBadge.tsx` (6 lines) and `viz/src/ui/DiagnosticBanner.tsx` (10 lines), both `React.FC` components with no hooks and no App-specific imports, matching the existing chip-file shape. `App.tsx` 486 → 485 lines: two new imports, four badge call sites collapsed to one-liners (or two-liners where the label pluralizes), three diagnostic-strip call sites collapsed to a `<DiagnosticBanner message={...}>{...}</DiagnosticBanner>` shape. The `message` prop for the two pluralizing/ternary strips is wrapped in a `<>...</>` fragment rather than a plain string — React renders fragment children flattened into the parent `<p>`, so this doesn't change the rendered text node. Every class string was copied verbatim (no restyling). The error banner and SSE-disconnect banner were left untouched, per starter scope.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Quality assertions** — no avoidable duplication (the four badge and three strip call sites were the duplication being collapsed; nothing new was copy-pasted); no dead code; no unexplained complexity (each new component is a single-purpose wrapper matching an established sibling shape); public-surface growth is two new exports, both consumed at all seven call sites and covered by the existing diagnostics tests; no stale code-facing docs (no doc describes `App.tsx` internals — see Phase 4 doc-drift sweep).

- [x] (frontend) Asked the user for visual confirmation — operator replied "go" after being shown the diff, confirming the badges and strips at http://localhost:5120.

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

Pre-move baseline: `npm --prefix viz test -- --run` → **431 passed across 23 files**. Post-move: **431 passed across 23 files** — identical, zero tests changed/added/skipped, confirming the JSX move is behavior-preserving. `npm --prefix viz run typecheck` clean. `npm --prefix viz run lint` clean.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — all 17 AI-referenced docs: **no change**. This task moved JSX between three files inside `viz/src/ui/`; none of the 17 (`README.md`, `AGENTS.md`, `SPEC.md`, `docs/MIGRATION.md`, the four agent-snippet wiring docs, `docs/CONVENTIONS.md`, `CONTRIBUTING.md`, `SECURITY.md`, `docs/AGENT-NEUTRALITY.md`, `docs/PLATFORMS.md`, `claude/CAPABILITIES.md`, `docs/AGENT-COMPAT.md`, `docs/EXTERNAL-AGENTS.md`, `docs/WORKTREES.md`) describes `viz/` module inventories or `App.tsx` internals. No archived tasknote carried a factual claim this task falsified, so no ⚠️ superseded pointer is written.

- [x] Closed — every `## ✅ Acceptance` criterion ticked, YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and kept nested beneath its active parent epic, then tasknote moved to `.flowtron/tasknote/archive/fe/`

- [x] **Evidence-based recap** drafted

**Final Summary:**

Collapsed `App.tsx`'s four duplicated amber header badges and three duplicated amber diagnostic strips into two new presentational components, `HeaderBadge` and `DiagnosticBanner`, matching the codebase's existing single-purpose chip-component shape. Pure JSX move — no logic, no styling, and no behavior changed.

Changed files: `viz/src/ui/HeaderBadge.tsx` (new, 6 lines), `viz/src/ui/DiagnosticBanner.tsx` (new, 10 lines), `viz/src/ui/App.tsx` (486 → 485 lines; seven call sites collapsed, two imports added).

Verification: pre-move baseline `npm --prefix viz test -- --run` → 431 passed / 23 files; post-move → **431 passed / 23 files**, identical — no test changed, added, or skipped, which is the behavior-preservation evidence for a move with no dedicated new tests. `npm --prefix viz run typecheck` and `run lint` both clean. Operator visually confirmed the badges and strips at http://localhost:5120.

Refactors: none beyond the extraction itself — the Minimal Refactor Gate found nothing else in `App.tsx` needing to change. The error banner and SSE-disconnect banner were confirmed still single-instance and left inline, as scoped. Deferred as scoped: the header extraction ([[FE-94.5]]) and the state hooks ([[FE-94.6]]).

Documentation: no drift across all 17 AI-referenced docs.

Maintainability effect: `App.tsx` shed ~85 lines of duplicated markup down to seven one-to-three-line call sites; the amber badge/strip styling now has one definition each instead of seven, so a future style change (or the epic's eventual `constants.ts` token work, explicitly out of scope here) touches two files instead of seven call sites scattered across the component. FE-94.5's header move now has `HeaderBadge` as a composed piece to carry rather than four spans to relocate individually.

**Archived:** 2026-08-24
