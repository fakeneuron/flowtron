---
title: js-yaml-merge-cve
status: completed
tags: []
created: 2026-09-09
due:
related-tasks: [FE-EPIC-106, FE-106.2, FE-106.4, FE-106.N]
touches:
  - viz/package.json
  - viz/package-lock.json
  - viz/src/tasknote.test.ts
---

# FE-106.3 | js-yaml-merge-cve

[← PLAN.md](../PLAN.md) · 🟢 In progress · 🔗 [[FE-EPIC-106]] [[FE-106.2]] [[FE-106.4]] [[FE-106.N]]

## 🎯 Goal

Bump viz's `js-yaml` to `^4.3.2` and add a `CORE_SCHEMA` merge-key rejection test next to the existing `!!omap` case.

## ✅ Acceptance

- [x] `viz/package.json` (and lockfile) pin `js-yaml` to `^4.3.2`; `node_modules/js-yaml` resolves ≥ 4.3.2
- [x] `viz/src/tasknote.test.ts` has a CORE_SCHEMA merge-key case next to the `!!omap` test proving `<<` is not applied as merge
- [x] Targeted viz tests, typecheck, and lint stay clean

## 🧩 Subtasks

- [x] Bump `js-yaml` to `^4.3.2` in `viz/package.json` and refresh the lockfile
- [x] Add a merge-key regression next to the `!!omap` case in `viz/src/tasknote.test.ts`
- [x] Run targeted `tasknote.test.ts` + typecheck + lint

## 🔗 Related

- [[FE-EPIC-106]] — parent epic (viz-tier-guardrail)
- [[FE-106.2]] — sibling (eslint-node-tier-list); independent predecessor, already closed
- [[FE-106.4]] — sibling (`@types/node` engines); independent
- [[FE-106.N]] — epic audit

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md

- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** Direct `js-yaml` is still `^4.3.1` (GHSA-2883 / CVE-2026-84375, patched in 4.3.2); the `!!omap` CORE_SCHEMA test exists and has no merge-key sibling.

- [x] Read relevant source files — when the read set is broad or its shape is unknown, consider isolating the search in a **probe** (`templates/subagent-probe-template.md`) and recording only its distilled return in Discovery Notes

- [x] **Best Practices Review** — for code or module-boundary work, identified touched responsibilities, dependency direction, existing abstractions, nearby duplication, and any required in-scope refactor or deferred cleanup (otherwise `N/A` with reason)

- [x] **Archive skim** — skim `.flowtron/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope (prefer YAML `touches:` when set); also follow Related / `supersedes` / ⚠️ pointers; when the grep returns more than a handful of notes (~3 is a fair line), prefer handing the reading to a **probe**; log relevant findings in Discovery Notes before re-interpreting the task

- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code, **and** the plan this tasknote is forming neither contradicts a SPEC contract nor diverges from its `PLAN.md` line (read both, don't recall them); flag any drift before re-interpreting the task

- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions

- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

- PLAN line (Medium, `[light]`): bump viz's `js-yaml` to `^4.3.2` and add a CORE_SCHEMA merge-key rejection test next to the existing `!!omap` case. Matches HEAD: `viz/package.json:19` is `"js-yaml": "^4.3.1"`; lockfile `node_modules/js-yaml` is 4.3.1; `tasknote.test.ts:810` is the omap throw; `tasknote-parse.ts` loads with `{ schema: CORE_SCHEMA }`.
- No `.1` Discovery tasknote and no `## 🌳 Fan-out` — epic Discovery was supplied by audit-repo; omit `blocked-by` / `parallel-safe-with`.
- GHSA-2883 / CVE-2026-84375: `maxTotalMergeKeys` does not count empty merge-source mappings; patched in 4.3.2 / 3.15.2. Merge is on by default in 3.x/4.x (`DEFAULT_SCHEMA` registers `lib/type/merge.js`); `CORE_SCHEMA` is JSON schema and does not.
- Live probe of 4.3.1: CORE `<<: { b: 2 }` parses as a literal `"<<"` key; DEFAULT actually merges. Explicit `!!merge` is an unknown-tag throw under CORE. FE-085's "merge `<<` throw" claim is slightly loose — implicit `<<` does not throw; it is simply not merge-processed. The PLAN names a test, not a parser change, so the regression asserts merge is not applied (title kept; merge-source `tags` not copied), not a throw.
- [[FE-085]] (2026-08-12) added the direct 4.3.1 dep, CORE_SCHEMA engine, and omap test. Protection against `!!omap` is the schema restriction, not the 4.x bump. Nested `gray-matter/node_modules/js-yaml@3.15.1` was deferred as unused for our parse path — still true, still out of scope (PLAN names viz's js-yaml).
- [[FE-090.4]] / [[FE-090.N]] deferred the `js-yaml` 5 major; stay on `^4.3.2`.
- [[FE-071]] engines-override shape; [[FE-026]] server-only `tasknote-parse.ts`; [[FE-029]] catch-and-skip on throw.
- Best Practices: bump the declared direct dep; add one test next to the omap case. No new helper, no schema change, no gray-matter nested bump, no js-yaml 5. Optional comment tweak in `tasknote-parse.ts` if the omap-only GHSA note would otherwise go stale — keep only if it stays one sentence.
- Assumptions: (1) caret range `^4.3.2`, not a 5.x jump; (2) do not retarget gray-matter's nested 3.15.1; (3) test asserts CORE_SCHEMA does not apply `<<` merge, matching current engine behavior; (4) no parser-side `<<` throw unless the bump itself starts throwing. No clarifications needed.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — extended an established pattern or justified a new shape; checked DRY and single-responsibility (SRP) boundaries; preferred composition when it reduced coupling

- [x] **Minimal refactor gate** — refactored only for Acceptance or to prevent duplication, obscured responsibility, or a dependency-boundary violation in the touched path; recorded the reason and deferred unrelated cleanup

- [x] Implemented the minimal solution

- [x] Updated/added tests for non-trivial behavior

**Implementation Notes:**

- Pattern: FE-085's existing CORE_SCHEMA engine + omap rejection test. Bump the declared direct dep; add one sibling test. No new wrapper.
- Minimal refactor: N/A — version pin + one test. One-sentence comment on `tasknote-parse.ts` so the omap-only GHSA note also names GHSA-2883. Deferred: gray-matter nested `js-yaml@3.15.1` (unused parse path, same as FE-085); js-yaml 5 major (FE-090).
- `js-yaml` `^4.3.1` → `^4.3.2`; lockfile `node_modules/js-yaml` 4.3.2.
- Test `does not apply YAML merge keys under CORE_SCHEMA (GHSA-2883)`: `<<:` source would copy `tags: [pwned]` under DEFAULT_SCHEMA; CORE_SCHEMA keeps `title: kept` and empty tags.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code

- [x] Ran lint/type-check on changed code

- [x] **Quality assertions** — for changed code, confirmed no avoidable duplication, dead code, unexplained complexity, unnecessary public-surface growth, or stale code-facing documentation (otherwise `N/A` with reason)

- [x] (frontend) Asked the user for visual confirmation (emphasized `👁️ **CONFIRM**` ask on its own line)

> **Choosing a test strategy (guidance, not a gate).** Default to targeted
> tests on the changed behavior. Where the input space is wide — parsers,
> encoders, round-trips, invariants that must hold across many inputs — a
> property-based test earns its keep; reach for one when example tests would
> leave large gaps. Visual confirmation covers UI surfaces that assertions
> can't. This is engineering judgment folded into Phase 3, never a new
> lifecycle phase or a schema/validator.

**Testing Notes:**

- `npm --prefix viz test -- src/tasknote.test.ts` — 57/57 passed (1 new).
- `npm --prefix viz run typecheck` — exit 0.
- `npm --prefix viz run lint` — exit 0.
- Quality: no duplication (one test next to omap); gray-matter nested 3.15.1 left on purpose; no public-surface growth.
- 👁️ **CONFIRM**: N/A — no UI/visual surface.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — for each entry in `.flowtron/tasknote/README.md` §"AI-referenced docs", state "no change" or the update

  - `README.md`, `AGENTS.md`, `SPEC.md`, `docs/MIGRATION.md`, `claude/AGENTS-snippet.md`, `codex/AGENTS-snippet.md`, `cursor/AGENTS-snippet.md`, `grok/AGENTS-snippet.md`, `docs/CONVENTIONS.md`, `CONTRIBUTING.md`, `SECURITY.md`, `docs/AGENT-NEUTRALITY.md`, `docs/PLATFORMS.md`, `claude/CAPABILITIES.md`, `docs/AGENT-COMPAT.md`, `docs/EXTERNAL-AGENTS.md`, `docs/WORKTREES.md`, `docs/VISION.md` — no change. Grep of the sweep set for `js-yaml` / `4.3.1` / `GHSA-2883` / `CORE_SCHEMA` returned no hits.

- [x] Closed — every `## ✅ Acceptance` criterion ticked or explicitly annotated (`N/A` / not-met with a one-line reason), YAML `status:` flipped to `completed`, PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and placed per SPEC/tasknote-selection.md §"`## Completed` archive convention" (standalone → top of `## Completed`; epic child → kept nested beneath its active parent), then tasknote moved to `.flowtron/tasknote/archive/<area>/`

- [x] **Evidence-based recap** drafted — changed files/LOC where meaningful, verification commands/results, refactors made or deferred with rationale, documentation verdict, and concrete maintainability effect (surfaces at the 📦 ready-to-commit gate, or inline on conditional skip)

**Final Summary:**

Bumped viz's direct `js-yaml` to `^4.3.2` (GHSA-2883 / CVE-2026-84375) and added a CORE_SCHEMA merge-key regression next to the existing `!!omap` case, proving `<<` is not applied as merge on the parse path.

- **Files changed:** `viz/package.json` / `viz/package-lock.json` (`js-yaml` 4.3.1 → 4.3.2); `viz/src/tasknote.test.ts` (one test); `viz/src/tasknote-parse.ts` (one comment line naming GHSA-2883).
- **Verification:** `npm --prefix viz test -- src/tasknote.test.ts` 57/57; typecheck and lint exit 0.
- **Refactors:** none. Deferred: gray-matter nested `js-yaml@3.15.1`; js-yaml 5 major.
- **Documentation:** sweep-set no change.
- **Maintainability:** the declared parser dependency is on the patched 4.3.2 line, and a regression guards CORE_SCHEMA's non-merge so a later schema widening would fail the test.

**Archived:** 2026-09-09
