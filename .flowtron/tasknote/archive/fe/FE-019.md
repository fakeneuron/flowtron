---
title: viz-a11y-perf-pass
status: completed
tags: []
created: 2026-05-10
due:
related-tasks: [FE-010, FE-014]
---

# FE-019 | viz-a11y-perf-pass

[← PLAN.md](../PLAN.md) · ✅ Completed · 🔗 [[FE-010]] · 🔗 [[FE-014]]

## 🎯 Goal

Establish a baseline a11y + perf posture for `viz/` via Lighthouse + axe and a manual sweep, then file each actionable finding as its own PLAN.md follow-up. Audit-only — no fixes in viz/ during this task.

## ✅ Acceptance

- [x] Lighthouse run captured (Performance / Accessibility / Best-Practices / SEO scores + Core Web Vitals) against the prod build via `npm run preview`. (axe coverage rolled into Lighthouse — `@axe-core/cli` v5 requires Node ≥22 via chromedriver, current env is Node 20; Lighthouse runs ~50 axe rules natively, sufficient for this baseline.)
- [x] Bundle-size baseline recorded: per-chunk JS / CSS sizes, raw + gzipped, from `npm run build`.
- [x] Manual a11y sweep: ARIA semantics review, landmark/heading inventory, contrast analysis of flagged classes in both themes, native-semantics audit (zero `tabIndex` / `role=` overrides confirmed).
- [x] FE-010 keyboard-nav regression sweep clean (`useKeyboardNav.ts` re-read post-FE-024 decomposition; all 6 keys + guards intact).
- [x] Findings categorized by severity (serious / minor) and actionability.
- [x] Each actionable finding filed as a new PLAN.md entry with suggested fix + severity tag; non-actionable findings logged in the tasknote only.
- [x] No code changes under `viz/`; `package.json` untouched (npx one-shots only).

## 🧩 Subtasks

- [x] Run `npm run build` from `viz/`; record per-chunk raw + gzipped sizes in Implementation Notes.
- [x] Start `npm run preview` (prod-build serve) on port 4173.
- [x] ~~`npx @axe-core/cli <url>`~~ — chromedriver ≥Node 22 incompat with Node 20 env. Lighthouse a11y category provides axe-core coverage (~50 rules); used as the axe surrogate.
- [x] `npx lighthouse <url> --only-categories=performance,accessibility,best-practices,seo --form-factor=desktop --output=json` — recorded the four scores + LCP / CLS / TBT / FCP / TTFB.
- [x] ARIA semantics review: walked `App.tsx` + each sub-component, confirmed landmarks, heading hierarchy, button-vs-link semantics, aria-* correctness.
- [x] Re-walk FE-010 shortcuts (`/`, `j`, `k`, `Enter`, `r`, `Esc`) against current code — `useKeyboardNav.ts` intact, no regression.
- [x] Compile the findings table (rule / location / severity / suggested fix / file-as-followup? Y/N).
- [x] File each actionable finding as a new PLAN.md task line (FE-025 contrast, FE-026 code-split).
- [x] Write the audit summary into Implementation Notes — this tasknote carries the canonical baseline post-archival.

## 🔗 Related

- [[FE-010]] — keyboard-nav predecessor; this task verifies its coverage holds up under axe + manual review.
- [[FE-014]] — react-testing-library bring-up; existing tests already query by `aria-pressed` / `aria-expanded`, anchoring some of the a11y posture.

---

## 📝 Phase 1: Discovery

- [x] Reviewed the task entry in PLAN.md
- [x] **Relevance Assessment**

  **Verdict:** Proceed
  **Rationale:** No prior a11y/perf tasknote exists for viz/; FE-010 (keyboard nav) and FE-014 (test bring-up) shipped, FE-024 just decomposed App.tsx — a stable moment to lay down a baseline. PLAN line is intact and unblocked.

- [x] Read relevant source files
- [x] **Archive skim** — skim `_project/tasknote/archive/<area>/` for prior tasknotes that touched the source paths in scope; log relevant findings in Discovery Notes before re-interpreting the task
- [x] **Drift check** — file paths, line numbers, function names, and root-cause hypotheses cited in the task description still match current code; flag any drift before re-interpreting the task
- [x] Asked clarifying questions OR logged "No clarifications needed" with explicit assumptions
- [x] Subtasks above populated with concrete, ordered steps

**Discovery Notes:**

**Source files in scope (audit-only — read, don't modify):**
- `viz/index.html` — `<html lang="en">`, theme-bootstrap inline script, single root div.
- `viz/src/main.tsx` — React 18 entry; renders `<App />` into `#root`.
- `viz/src/styles.css` — Tailwind base + a small custom layer.
- `viz/src/ui/*.tsx` — 21 components: `App.tsx` (state + layout), `PrioritySection.tsx`, `TaskRow.tsx` / `EpicRow.tsx` / `SubtaskRow.tsx` / `TaskRowInner.tsx`, `TaskDetail.tsx`, `DetailSection.tsx`, `Chevron.tsx`, chip set (`StatusChip` / `BlockerChip` / `BackRefChip` / `RelatedChip` / `ModelChip`), `PhaseDots.tsx`, `SubtaskProgress.tsx`, `ProjectSelector.tsx`, `ThemeToggle.tsx`, `WikilinkMarkdown.tsx`, plus `useKeyboardNav.ts` / `useToggleSet.ts` / `theme.ts` / `utils.ts` / `constants.ts`.
- Build pipeline: Vite 5 + React 18 + Tailwind 3; `tsconfig.json` strict-ish. No linter configured (per FE-010 archive — `tsc --noEmit` covers static analysis).

**Archive skim:** 5 prior tasknotes touched these paths.
- `FE-002` — project selector / cross-project scaffolding; introduced the chip-pattern + `aria-pressed` semantics shared across filter chips.
- `FE-011` — SSE wiring; no a11y/perf concerns recorded.
- `FE-014` — react-testing-library bring-up; tests query rows by role + `aria-expanded`/`aria-pressed`, so existing a11y semantics are already anchored by tests (regressions would be caught).
- `FE-018` — code-token audit; cleanup pass, not a11y.
- `FE-022` — viz id-prefix-helpers; refactor.
- **No prior a11y or perf tasknote.** This is a fresh baseline.

**Pre-audit a11y posture (read-only inventory, not a verdict):**
- `aria-label` present on: search input, ⓘ shortcut chip, ThemeToggle, ProjectSelector chips, SubtaskProgress, EpicRow chevron.
- `aria-pressed` on: filter chips, theme toggle, project chips.
- `aria-expanded` on: priority section toggles, row toggle (TaskRowInner), epic chevron.
- `aria-hidden` on: decorative Chevron + SubtaskRow's leading marker.
- `lang="en"` on `<html>`; viewport meta present.
- Visible focus: search input has `focus:ring-2 focus:ring-slate-300` (light) / `focus:ring-slate-600` (dark). Selection cursor uses `ring-2 ring-sky-400 dark:ring-sky-600` on top-level rows, `ring-1` on subtask rows.

**Drift check:** PLAN line cites no concrete file paths or function names — only behavior / tooling. The "FE-010 coverage" reference is current (FE-010 archived 2026-05-07; `useKeyboardNav.ts` lives at `viz/src/ui/useKeyboardNav.ts`). The **"focus-trap"** mention in the PLAN line is likely vestigial — viz has no modal dialogs, popovers, or overlays today; the detail panel is inline and the project selector is a chip group, not a menu. Will explicitly note in the audit ("no focus-trap needed; no trapping surfaces exist").

**Pre-build bundle baseline (existing dist/, May 10 build):**
- `dist/assets/index-CZ8bJXte.js` — **415,286 B** raw.
- `dist/assets/index-D4B8deAJ.css` — **22,406 B** raw.
- Gzipped sizes will be (re)captured during the fresh `npm run build` step in Phase 2.

**Decisions confirmed via clarifying questions:**
1. **Tooling:** one-shot `npx @axe-core/cli` + `npx lighthouse`; no devDependencies added to `viz/package.json`.
2. **Deliverable shape:** audit-only — no fixes to viz/ during this task. Each actionable finding is filed as a new PLAN.md follow-up entry with severity + suggested fix; the tasknote carries the canonical baseline.
3. **Audit weight:** all three lanes — (a) axe + manual a11y sweep; (b) Lighthouse perf + bundle-size baseline; (c) manual UX/keyboard regression sweep against FE-010's shortcuts.

**Assumptions logged:**
- "Run Lighthouse" = run against the prod build (`vite build` + `vite preview`), not the dev server, so scores reflect what real users would see.
- Desktop form-factor for Lighthouse (the viz/ tool is desktop-only by design — no mobile target).
- Severity bands follow axe conventions: critical / serious / moderate / minor. Lighthouse a11y issues are rolled into the same bands using its weight metadata.
- "File specific fixes as follow-ups" = one PLAN.md line per actionable finding; trivial cosmetic notes that aren't worth a task stay in the tasknote audit log.

## 🛠️ Phase 2: Execution

- [x] **Pattern survey** — audit-only task; no new code patterns introduced. Tooling (Lighthouse, axe-core) sourced via npx one-shots per Discovery decision; no `viz/package.json` changes. The "follow-up filing" pattern matches prior audit-style tasknotes (e.g., FE-018 viz-code-token-audit) — surface findings, file PLAN.md entries, archive the tasknote as the canonical baseline.
- [x] Implemented the minimal solution — ran the audit, compiled findings, filed two actionable follow-ups in PLAN.md.
- [x] Updated/added tests for non-trivial behavior — N/A (no behavior changed in viz/).

**Implementation Notes:**

### Bundle-size baseline (`npm run build`, 2026-05-10)

| Asset | Raw | Gzipped |
|---|---|---|
| `dist/index.html` | 0.93 kB | 0.53 kB |
| `dist/assets/index-*.css` | 22.41 kB | 4.48 kB |
| `dist/assets/index-*.js` | 415.23 kB | **123.72 kB** |

Single-chunk JS (no code-splitting). 406 modules transformed in 2.78 s. Vite warned that `gray-matter`'s `engines.js` uses `eval` — known transitive issue, not actionable in viz/.

### Lighthouse scores (desktop, no throttling, prod build via `vite preview`)

| Category | Score |
|---|---|
| Performance | **0.84** (84) |
| Accessibility | **0.94** (94) |
| Best Practices | **0.96** (96) |
| SEO | **0.82** (82) |

**Core Web Vitals / load metrics:**
- LCP: 1881 ms (good — under 2.5 s threshold)
- FCP: 1558 ms (needs-improvement band, threshold 1.8 s)
- Speed Index: 1.8 s (needs-improvement)
- CLS: 0 (perfect)
- TBT: 0 (perfect)
- TTFB: 151 ms (good)
- TTI: 1.9 s (good)

Raw report: `/tmp/flowtron-lighthouse.json` (transient — not checked in).

### Findings table

| # | Severity | Category | Rule | Location | Filed as |
|---|---|---|---|---|---|
| 1 | Serious | a11y | `color-contrast` (3.83–3.91:1, expected 4.5:1) | `text-slate-400 dark:text-slate-500` on `text-xs` / `text-[10px]` — `PrioritySection.tsx:65` ("No tasks" placeholder) and `SubtaskRow.tsx:48` (completed-date badge) | **FE-025** |
| 2 | Moderate | perf | `unused-javascript` — ~80 KiB unused at first paint (~65% of gzipped JS) | Single-chunk `dist/assets/index-*.js` ships `react-markdown` + `remark-gfm` + `gray-matter` upfront despite being detail-panel-only / parser-only | **FE-026** |
| 3 | Minor | perf | `render-blocking-resources` — 150 ms savings on 4.5 kB CSS | `<link rel="stylesheet" href="...index-*.css">` in built HTML | Not filed — fix complexity (Vite plugin or manual preload swap) outweighs 150 ms saving on a personal/local-only tool |
| 4 | Minor | seo | `meta-description` missing | `viz/index.html` `<head>` | Not filed — viz is local-only, not indexed |
| 5 | Minor | seo | `robots.txt` invalid (404 → SPA index.html parsed as 27 errors) | No `viz/public/robots.txt` | Not filed — viz is local-only, not indexed |
| 6 | Info | best-prac | `errors-in-console` — 1× 404 on a network resource | Preview-only artifact: `vite preview` runs without the SSE/HTTP backend, so `/api/*` fetches 404. Not a real production bug. | Not filed — environment-specific, not a viz/ issue |

### Manual a11y sweep — passing posture

- **Landmarks:** `<header>` (App.tsx:306) + `<main>` (App.tsx:395) + `<section>` per priority block (PrioritySection.tsx:51). Lighthouse marked `landmark-one-main` and `bypass` as `notApplicable` because the page already passes both rules.
- **Heading hierarchy:** single `<h1>` (App.tsx:310, "Flowtron — PLAN.md"). No skipped levels. Lighthouse `heading-order` clean.
- **Native semantics — strong baseline:** zero `tabIndex` and zero `role=` overrides across all 21 components in `viz/src/ui/`. Every interactive element is a real `<button>` or `<input>`, so the keyboard/AT story is mostly free.
- **ARIA inventory:** `aria-label` on 7 controls (search, ⓘ chip, ThemeToggle, ProjectSelector chips, SubtaskProgress, EpicRow chevron, PhaseDots), `aria-pressed` on 3 chip-button families (filter chips, theme toggle, project chips), `aria-expanded` on 3 toggle types (priority sections, row toggle, epic chevron), `aria-hidden` on decorative chevrons + SubtaskRow markers. All idiomatic.
- **`<html lang="en">`** present. **Viewport meta** present. **`<title>`** present and meaningful.
- **Touch-target sizes:** Lighthouse `target-size` clean (no failing elements).
- **Focus rings:** search input uses `focus:ring-2 focus:ring-slate-300 dark:focus:ring-slate-600`; selection cursor uses sky ring (FE-010 work). No global `outline-none` reset.
- **"Focus-trap" PLAN-line mention is vestigial** — viz has no modals, dialogs, popovers, or overlays. The detail panel is inline in the document flow; the project selector is a chip group, not a menu. Lighthouse `focus-traps`, `managed-focus`, `aria-dialog-name` all `notApplicable`. Confirmed: nothing to trap; not a real concern.

### FE-010 keyboard-nav regression sweep

Re-read `viz/src/ui/useKeyboardNav.ts` against FE-010's archived spec (subtasks list at `archive/frontend/FE-010.md:33-46`):

| Key | Behavior | Status |
|---|---|---|
| `/` | Focus search; bypassed when typing | ✅ intact (lines 72-77) |
| `j` / `k` | Walk `visibleIds`, clamp at bounds, `scrollIntoView` | ✅ intact (lines 81-97) |
| `Enter` | Smart epic toggle (children-then-detail), detail toggle on rows | ✅ intact (lines 99-108) |
| `r` | Re-fire `load()` | ✅ intact (lines 110-114) |
| `Esc` | Cascade: detail → filters → blur search | ✅ intact (lines 56-70) |
| Modifier-key bypass | Cmd/Ctrl/Alt/Meta combos not intercepted | ✅ intact (line 50) |
| Typing-context guard | `j/k/r/Enter` no-op while typing | ✅ intact (line 79) |

No regression after FE-024's App.tsx decomposition — the hook is self-contained and survived the split untouched.

### Audit-tooling notes (for future re-runs)

- `@axe-core/cli` v5 pulls in `chromedriver@148` which requires Node ≥22; current dev env is Node 20. Either upgrade Node or pin axe CLI to v4.x for re-runs. Lighthouse's a11y category includes ~50 axe rules out of the box and was sufficient as the axe surrogate for this baseline.
- Lighthouse runs against `vite preview` (port 4173), not `vite dev`, so scores reflect prod-shipped assets. Default theme renders dark (the inline pre-React script defaults to dark when no `prefers-color-scheme` preference exists in headless Chrome).
- For a re-run, the canonical command bundle is in this tasknote's "Subtasks" section above.

## 🧪 Phase 3: Testing & Linting

- [x] Ran targeted test suite for changed code — N/A; no viz/ source changed.
- [x] Ran lint/type-check on changed code — `tsc --noEmit` ran cleanly as part of `npm run build` (build exits 0). No other source touched.
- [x] (frontend) Asked the user for visual confirmation — N/A; audit-only, no UI change to confirm. The audit *findings* themselves are surfaced in the closure recap for the user to validate.

**Testing Notes:**

- Build is green: `npm run build` (which runs `tsc --noEmit && vite build`) completed in 2.78 s with no TS errors and no Vite errors.
- The two PLAN.md edits (flipping FE-019 to stub-form + adding FE-025 / FE-026 follow-ups) are the only file changes shipping with this task.

## 🚀 Phase 4: Closure

- [x] **Doc-drift sweep** — see §"Doc-drift verdicts" below.
- [x] Closed — PLAN.md line flipped to stub form `Completed YYYY-MM-DD.` and tasknote moved to `_project/tasknote/archive/frontend/`.
- [x] Recap drafted (surfaces at the 📦 ready-to-commit gate).

**Doc-drift verdicts** (per `_project/tasknote/README.md` §"AI-referenced docs"):

- `README.md` — **no change.** Public-facing flowtron overview; an audit-only viz/ task doesn't shift any flowtron-as-a-product surface.
- `SPEC.md` — **no change.** Workflow contract is unchanged; this task didn't touch the tasknote lifecycle.
- `docs/MIGRATION.md` — **no change.** Adoption / bump procedures unaffected.
- `claude/CLAUDE-snippet.md` — **no change.** Adopters' assistant-facing surface unaffected.

**Final Summary:**

Established viz/'s first a11y + perf baseline. Lighthouse scored 84/94/96/82 (perf/a11y/best-prac/seo); bundle is 415 kB raw / 124 kB gzipped JS in a single chunk. Two actionable findings filed as follow-ups: **FE-025** (Medium, contrast — `text-slate-400 dark:text-slate-500` on small text fails 4.5:1 in `PrioritySection.tsx:65` and `SubtaskRow.tsx:48`) and **FE-026** (Low, code-split — ~80 KiB unused JS at first paint, driven by upfront-loaded `react-markdown`/`remark-gfm`/`gray-matter`). Manual sweep confirmed strong native-semantics baseline (zero `tabIndex` / `role=` overrides, real `<button>`s throughout, full landmark + heading + ARIA coverage). FE-010 keyboard nav intact post-FE-024 decomposition. The PLAN-line "focus-trap" mention is vestigial — viz has no modal/overlay surfaces.

**Archived:** 2026-05-10
