services:
  db:
    image: postgres:16-alpine
    container_name: flowtron-db
    ports:
      - "55432:5432"
    environment:
      POSTGRES_USER: flowtron
      POSTGRES_PASSWORD: flowtron
      POSTGRES_DB: flowtron
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data: {}


