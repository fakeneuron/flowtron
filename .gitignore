.DS_Store
node_modules/
dist/
.env
.env.local
*.log
*.tsbuildinfo
.playwright-mcp/
SCRATCH*.md

# Personal Claude Code / Codex / Cursor wiring for this checkout.
# Repo-scoped `ln -s ../../claude/...` and `ln -s ../../codex/...` symlinks are
# the canonical install (see docs/MIGRATION.md §1.2.2); the agent home carries
# only the global-only utilities. Never commit per-machine state.
.claude/
.agents/
